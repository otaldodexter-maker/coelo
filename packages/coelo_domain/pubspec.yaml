name: coelo_domain
description: Pure Dart domain contracts and business rules shared by Coelo applications.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
