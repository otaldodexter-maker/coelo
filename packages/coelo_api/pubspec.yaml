name: coelo_api
description: Typed DTOs, codecs, cursor envelopes, and API contracts shared by Coelo clients.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  coelo_domain:
    path: ../coelo_domain

dev_dependencies:
  lints: ^6.0.0
  test: ^1.26.3
