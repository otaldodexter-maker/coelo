# Pendentes (lista única — ADR 0045 §7 / CLAUDE.md "Modo de construção")

Uma linha por item: tela · o que falta · quem pediu · data. Apague a linha quando entregar.

- Estrutura · spec 066 entregue para instituições, unidades e turmas (ativar/inativar/excluir com motivo, exclusão real só sem dependentes, PT409, auditoria; lotes 94 e 97); faltam atividades pelo mesmo predicado, filtro "Arquivados" e pessoas/suspensão por período (no Principal a instituição inativa já some dos contextos: `list_my_principal_contexts` exige `institution.status='active'`; os diretórios do Superadmin seguem listando os filhos de propósito, para o operador reativar) · Sessão ETAPA-3 · 20/09
- Locais · spec 067: marcadores (pontos e áreas, vínculo ao local, visibilidade por público) em Locais da instituição/unidade entregues (lote 96) sobre grade neutra; falta a imagem da planta como mídia de entidade (image_kind `floor_plan` em entity_image_assets + Edge entity-media, pedido à MÍDIA-E2E) e o `LocationMapPanel.background` passar a exibi-la; fotos/vídeos com visibilidade todos/seguidores/ninguém e bloco/andar/tipo do local ficam para a Etapa 4 · Sessão ETAPA-3 · 20/09
- Avisos · spec 069: CTA por tipo no Principal hospedado abre circular (leitura), aviso (diálogo), formulário (visão geral do Superadmin) e convite (detalhe do Superadmin); rotas próprias de resposta/convite para famílias ficam para o Principal da Etapa 4 · Sessão ETAPA-3 · 20/09
- Principal · spec 068: catálogo `official_profiles` (só `coelo`, obrigatório), follow automático por pg_cron e publicação assinada no Para você entregues (lote 95); faltam a lista final de 3–7 perfis (decisão do Owner, vira carga), posts do perfil oficial no feed Acontece (posts hoje exigem instituição) e a tela do perfil oficial no Principal · Sessão ETAPA-3 · 20/09
- Acessos · acesso por perfil: conta de pessoa real (`qa-e3-educadora`, realm people-based) não abre o shell do Superadmin (trigger de realms separados) — prova visual do popup usou o espelho interno de qa-r06-principal herdando o perfil "QA E3 Educador"; a educadora real está provada pela API (`19-api-educadora-real-*.json`); prova visual com pessoa real só no Principal próprio (Etapa 4) · Sessão ACESSO-PERFIL · 19/09
- Acessos · acesso por perfil: sessão já aberta recebe o motivo (PT403/STAFF_ACCESS_DENIED → popup + volta ao seletor) via `StaffAccessDeniedHttpClient` no cliente do Supabase; provado por teste de widget e pela API; na rota real o popup apareceu pelo seletor (contextos recarregados ao trocar de tela), não pelo PT403 — o caminho PT403 na UI depende de uma ação de equipe na mesma tela · Sessão ACESSO-PERFIL · 19/09
- Acessos · acesso por perfil: `staff_access_assert` não entra em `assert_agenda/plan/people/support_permission` (realm interno, sem membership de instituição); herança usa a atribuição ativa mais antiga com regra de perfil (vínculo com dois perfis com horário: vale o primeiro); regra própria idêntica à do perfil conta como "Padrão do perfil" · Sessão ACESSO-PERFIL · 19/09
- Acessos · acesso por perfil: varredura dos leitores legados — 8 funções passaram a excluir vínculo bloqueado (destinatários de cuidado/medicação/segurança/avisos, classe do leitor do Agora, feed Para você, ticket de mídia do Agora); 49 restantes (contagens/diretórios administrativos, `form_*` audiência/monitor, `superadmin_chat_create_group_v2`, `access_profile_*`, `superadmin_*_payload`) mantidas de propósito: não são notificação nem leitura de contexto · Sessão ACESSO-PERFIL · 19/09
- Acessos · acesso por perfil: `x-coelo-surface` agora recalcula ao redimensionar (debounce 300 ms), mas o servidor continua sem verificar a superfície declarada; app instalado só na Etapa 4 · Sessão ACESSO-PERFIL · 19/09
- Acessos · dados QA: perfil "QA E3 Educador", pessoa "QA E3 Educadora Perfil" (login em Coelo-backups/qa-e3-educadora.env), vínculos em QA R04 Cuidado (herdando o perfil) e na Escola R04 Estrutura (instituição `draft`, por isso não aparece nos contextos do Principal), e atribuição do perfil ao espelho de qa-r06-principal (571fb282) ficaram em produção; regras de horário zeradas ao fim da prova (inclusive a da "QA R15 Educadora Turma") · Sessão ACESSO-PERFIL · 19/09
- Tour · textos dos 42 roteiros são propostas (rascunho `tour-telas-rascunho-20260918.md`, fonte executável em `apps/superadmin/lib/app/tour/screens/`): Owner revisa olhando a tela · Owner · 19/09
- Tour · balão "Mensagens" (launcher do chat) fica acima do escurecimento do tour em todas as telas; passos de elementos ausentes (estado vazio, paginação de 1 página, painel sem seleção) somem do tour da tela mas seguem contados no "N" do tour completo (282 declarados, 256 mostrados em 1440) · Sessão TOUR-TELAS · 19/09
- Tour · Planos (dev-only) sem tour por decisão; telas de editar/detalhe (`/…/:id`) e Permissões da Agenda não são destinos do menu e ficam sem tour · Sessão TOUR-TELAS · 19/09
- Fim da Etapa 3 · dez nomes para "Para você" + três instituições fictícias · Owner · 18/09
- Etapa 4 · publicação (host, Auth, CORS, SMTP), apps/admin, apps/principal + spec 064, push preparado, analytics, IA · Owner · ADR 0045
- Imagens · diretórios/cards e cabeçalhos de detalhe ainda mostram placeholder (has_logo/has_cover); mostrar a foto real (RPC em lote `superadmin_entity_images_list_v1` + cache) · Sessão IMAGENS · 19/09
- Imagens · Atividade só aceita foto/capa/ícone depois de criada (o id nasce no servidor via `ActivityFormSubmit` sem retorno); ícone é PNG 512 rasterizado (não SVG); Principal ainda não lê `entity_image_assets` · Sessão IMAGENS · 19/09
- Imagens · CORS do bucket R2 `coelo-media-prod` lista só localhost:3014/3000, 127.0.0.1:3014/3016 e superadmin.coelo.me; o token Cloudflare do wrangler não tem R2 (erro 10000) — não bloqueia mais (bytes passam pela Edge), mas Momentos/Agora/Circulares/Chat/Cardápios ainda fazem PUT/GET direto ao R2 e falham em outra porta · Sessão IMAGENS · 19/09
- Imagens · rascunhos `entity_image_assets` sem upload não têm limpeza (expiram em 30 min só logicamente); worker de expiração como no account-media · Sessão IMAGENS · 19/09
- Review @ · backend: `set_activity_canonical_handle` monta `stem.unit.slug.institution.slug` (slugs com hífen) enquanto o @ da unidade/turma usa `structure_handle_segment` (sem hífen): o @ da atividade pode não bater com o @ da unidade que a criou; o stem aceita hífen e os outros @ não. Alinhar no servidor (migration + pgTAP) antes de mostrar prévia exata na tela · Sessão REVIEW-UI r3 · 19/09
- Review @ · turma: a prévia do @ padrão mostra `<@ da unidade>` porque `GroupDirectoryFilterOption` não traz o handle da unidade; atividade só mostra o stem (opção de instituição sem slug). Expor `handle`/`slug` nas opções dos formulários para prévia completa · Sessão REVIEW-UI r3 · 19/09
- Review UI · `shell_profile_summary` (avatar) e o hue wheel do seletor de cor (institution_form_sections) mantêm `Colors.white/black` por serem cor livre do usuário/mídia; `superadmin_chat_emoji_picker` fontSize 24 do emoji · Sessão REVIEW-UI r3 · 19/09
