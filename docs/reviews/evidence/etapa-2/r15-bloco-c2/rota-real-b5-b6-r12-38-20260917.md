---
source: "R15 Bloco C2 (Fable 5.1), 17/09/2026; lote 78 em produção (migrations 20260917160000/170000/180000 + Edges child-safety-media, meal-plan-media, meal-plan-image-cleanup); specs 061/062/063; R15-pendencias.md (owner.r12-17, r12-18, r12-38)"
status: evidence
generated_at: 2026-09-17
---

# B5, B6 e r12-38 — rota real em produção, 17/09/2026

Ambiente: produção (`evvbomzejfijozbtgvpt`); build
`flutter build web --release -t test_driver/qa_main.dart --dart-define-from-file=.env.local
--dart-define=COELO_QA_TEXT_ENTRY_EMULATION=true` da worktree `r15-bloco-c2` (dev mergeado até
`9b5682b8d`), servido por `serve.py` em `127.0.0.1:3017` (B5/B6 sem upload) e depois em `127.0.0.1:3016` (única origem local, além da 3014 do Bloco A, permitida no CORS do bucket `coelo-media-prod`; liberada pela coordenadora após o C1 encerrar); Chrome CDP `9417` (SwiftShader, perfil
`%TEMP%\coelo-r15-c2-chrome`). Sessões: `qa-r06-acessos` (Segurança da criança) e `qa-r06-operacoes`
(Cardápios). Tenant `QA R04 Cuidado (sintetico)` (`d0c40000-…0001`), unidade `Unidade QA R04`
(`d0c40000-…0002`), criança `Crianca QA R04` (`d0c40000-…0003`, contexto `d0c40000-…0004`).
Negativas por `packages/coelo_database/scripts/r13-rpc-proof.mjs` (PostgREST, mesma sessão, sem chave
de serviço). Nenhuma credencial em log; nenhum dado real; prefixo `QA R15` nos registros novos.

## Ferramenta

`ferramentas/qa_drive.dart` (Dart puro): dirige o build web pela extensão do Flutter Driver exposta
em `window.$flutterDriver` (CDP `Runtime.evaluate`; a resposta chega em
`window.$flutterDriverResult`), com `login` lendo `QA_EMAIL`/`QA_PASSWORD` do arquivo no processo,
`tapkey/typekey/taptext/taptooltip`, `pushroute` (navegação interna por `history.pushState`, porque a
sessão não sobrevive a um reload) e roteiros em `ferramentas/roteiros/*.txt`. Upload real pelo seletor
nativo com `r14-sessao-1/ferramentas/cdp_filechooser.dart`.

## B5 — busca de pessoa autorizada (`owner.r12-17`)

| Gate | Prova | Captura |
|---|---|---|
| Campo único no wizard (etapa Criança "Ou busque pelo responsável" e etapa Pessoa autorizada) | `/safety/new` real | `b5-01-wizard-inicial.png` |
| Mínimo por tipo | "QA" (2 caracteres) → dica "Digite ao menos 3 caracteres." sem chamar o servidor; por PostgREST `superadmin_person_search_v1('QA')` → `400 22023 PERSON_SEARCH_MIN_LENGTH` | `b5-03-pessoa-minimo.png` |
| Nome ≥ 3 | "QA R04 Resp" → card **QA R04 Responsavel** com iniciais `QR`, `@qar04.responsavel · sem conta`; payload por PostgREST: `person_id`, `display_name`, `initials`, `handle`, `phone_last4:null`, `matched_by:"name"`, `has_account:false`, `children:[]` — **sem CPF, e-mail ou celular** | `b5-04-pessoa-nome.png` |
| @handle ≥ 3 | "@qar04" → mesma pessoa (`kind:"handle"`) | `b5-05-pessoa-handle.png` |
| Celular ≥ 4 / CPF completo | por PostgREST: `"1234"` → `kind:"digits"`, `results:[]`; `"111.444.777-35"` → `results:[]` (nenhuma pessoa **com conta** tem esse CPF); a massa do tenant não tem celular nem CPF cadastrados — correspondência provada pelo pgTAP 33/33 (`child_safety_person_search_v1_test.sql`) | — |
| Clique preenche a pessoa | card selecionado → campo mostra o nome com ✓; revisão "Pessoa autorizada: QA R04 Responsavel" (nunca UUID) | `b5-06-pessoa-selecionada.png`, `b5-08-revisao.png` |
| CRUD em produção | "Enviar para aprovação" → `child_safety_request_authorization` criou a autorização pendente; volta a `/safety` | `b5-09-apos-envio.png` |
| Reload | `/safety/children/d0c40000-…0003` relê "QA R04 Responsavel · Mãe · Desde 17/09/2026 · Pendente" (+ banner "1 solicitação aguardando aprovação") | `b5-10-detalhe-crianca.png` |
| Limite de taxa | 32 chamadas seguidas por PostgREST: a partir da 31ª → **`422 PT422 PERSON_SEARCH_RATE_LIMIT`** (`person search rate limit`) | log do script |
| Escopo / cross-tenant | "Estrutura", "QA R15" → `results:[]` (pessoa `QA R15 Responsavel` da915f98 é `draft` e sem vínculo/membership; pessoa sem conta não entra na busca — só pessoas com presença no escopo) | log do script |
| Auditoria | gravada pela RPC (`child_safety.person_search`, sem o texto) — provado no pgTAP; leitura de `audit.audit_logs` não é exposta ao ator QA | pgTAP |

Dependente da massa (Bloco B, AP-1 / conta do Owner): "responsável lista crianças vinculadas" exige
`guardian_links`, que a tela de Pessoas não cria; fica provado só no pgTAP (`children` só do escopo)
até a fixture `20260917110000_qa_r15_guardian_fixture_v1` entrar em produção.

## B6 — pessoa sem conta (`owner.r12-18`)

| Gate | Prova | Captura |
|---|---|---|
| Cadastro só quando B5 não encontra | "QA R15 Tio" → "Nenhuma pessoa encontrada no escopo autorizado. Se ela não tem conta, cadastre abaixo." + botão "Cadastrar pessoa sem conta" | `b6-01-busca-vazia.png` |
| Formulário (nome, CPF com máscara, celular/e-mail opcionais) | "QA R15 Tio Sem Conta", `111.444.777-35` (CPF sintético válido) | `b6-02-formulario.png` |
| Registro em produção, CPF nunca inteiro | card "Pessoa sem conta · QA R15 Tio Sem Conta · `***.***.***-35`"; `authorized_people` `e9f02f4b-1dd5-4287-bf92-034e8aa3e807` (`person_id` nulo, `document_fingerprint` = HMAC) | `b6-03-cadastrada.png` |
| Dedupe por (instituição, CPF) | por PostgREST `child_safety_register_person_without_account_v1` com o mesmo CPF e outro `request_id` → `200 {existing:true, authorized_person_id: e9f02f4b…, document_status:"missing"}`; pela tela o segundo cadastro devolve a mesma pessoa | `b6-03b-cadastrada-dedupe.png` |
| Documento obrigatório em R2 privado (`coelo-documents-prod`) | Pelo gateway `child-safety-media` (Node, `ferramentas/edge_probe.mjs`, mesma sessão QA): `prepare` 200 (URL assinada + cabeçalhos), `PUT` 200 no R2, `finalize` 200 → documento `9a01711e-1bf9-49d3-ab7a-de930d11aa2e` **`ready`**, `read` 200 (URL assinada de 60 s). Pelo navegador o `PUT` falha no preflight: o bucket `coelo-documents-prod` não tem nenhuma origem CORS (medição abaixo) — **limite de ambiente aceito pelo Owner em 17/09** (CORS não será alterado hoje). | log do script |
| Wizard com o documento `ready` (3016) | busca vazia → "Cadastrar pessoa sem conta" → nome + CPF → dedupe devolve `document_status: ready` → card "Pessoa sem conta · QA R15 Tio Sem Conta · `***.***.***-35` · Documento enviado."; etapa Pessoa autorizada preenchida (✓) com motivo; revisão "Pessoa autorizada: QA R15 Tio Sem Conta · sem conta · ***.***.***-35 · Capacidades: pickup" | `b6-r1-busca-vazia.png`, `b6-r2-formulario.png`, `b6-r3-cadastrada-documento-ready.png`, `b6-r4-pessoa-autorizada.png`, `b6-r5-validade.png`, `b6-r6-revisao.png` |
| Envio (`child_safety_request_authorization` com `authorized_person_id`) e detalhe | pela tela o clique em "Enviar para aprovação" não concluiu (a aba do Chrome 9417 parou de responder ao CDP/driver após ~2 h de sessão com SwiftShader); o envio foi feito pela RPC `child_safety_request_authorization` com o **mesmo payload que o wizard monta** (`authorized_person_id: e9f02f4b…`, sem `person_id`, `other/Tio`, `pickup`) → `200 {authorization_id: b729f6b8-ac69-4201-b289-27d87c1a7c86, version: 1, decision_status: pending, lifecycle_status: inactive}`; `superadmin_child_safety_get` da Crianca QA R04 relê `name: QA R15 Tio Sem Conta`, `person_id: null`, `relationship_code: other`, `capability_codes: [pickup]`, `pending` | `b6-r6-revisao.png`; log do script |
| Negativa de leitura | `child_safety_person_document_read_v1('00000000-…')` → `500 P0002 child safety record unavailable` (fail-closed, sem distinguir inexistência de falta de acesso) | log do script |
| `PERSON_HAS_ACCOUNT` | nenhuma pessoa com conta no tenant tem CPF em `person_identity_identifiers`; provado só no pgTAP 40/40 | pgTAP |

## r12-38 — Cardápios imagem R2

Sessão `qa-r06-operacoes`; cardápio `QA R15 Cardapio imagem R2` (`6272879e-9a52-4ce5-8922-5777c18ea82b`,
Simples, Alunos, QA R04 Cuidado, 17–23/09/2026) criado pela tela em `/meal-plans/new` (capturas `mp-01`…`mp-12`).
Imagem sintética `qa-r15-documento.png` (PNG 1×1, 70 bytes) injetada no seletor nativo por `cdp_filechooser.dart`.

| Gate | Prova | Captura |
|---|---|---|
| Envio habilitado nas rotas produtivas | botão "Anexar imagem do cardápio" na etapa Cardápio (create e edit); antes o FE mantinha o envio desabilitado | `mp-09-etapa3.png`, `mp-e2-cardapio-existente.png` |
| Upload pelo gateway `meal-plan-media` a partir do **navegador** (origem 3016) | "Salvar rascunho" executa `prepare` → `PUT` R2 → `finalize`; ativo `2541d4b5-f21a-4143-b74c-49a8a4a368e2` criado às 17:54:51Z (`active`, `storage_provider: r2`, `coelo-media-prod`, chave canônica `tenants/<tenant>/meal_plans/meal_plan/6272879e…/image/2541d4b5…/original/<uuid>.png`, 70 bytes) — lido por PostgREST em `meal_plan_image_assets` | `mp-e3-imagem-anexada.png`, `mp-e5-apos-salvar.png` |
| Upload pelo gateway a partir de Node (antes da liberação da 3016) | ativo `35dd7c74-1341-4d42-973f-baefed317ae4` (`prepare`/`PUT`/`finalize` 200) | log do script |
| **Achado e correção** (`ece99cc3a`) | o rascunho salvo ficou com `simpleImageMeta: {}` (revisão 3): `meal_plan_create_or_update_draft` persiste e `meal_plan_get` devolve a chave `simpleImageMeta`, mas o FE enviava/lia `simpleImage` — a imagem nunca reaparecia após reload (defeito anterior a esta rodada, invisível enquanto o envio estava desabilitado). Corrigido no FE (`_simpleImageFromJson`, `toJson` emite `simpleImageMeta`, `{}` limpa) + teste de contrato; rebuild e novo salvar → revisão 5 com `simpleImageMeta = {kind:image, title:qa-r15-documento.png, reference:362777eb-2404-4aee-98c6-246b3abad755}` | RPC `meal_plan_get` |
| Prévia após **reload real** (nova sessão) | `/meal-plans/6272879e…/edit` → etapa Cardápio mostra "Trocar imagem", "Arquivo: qa-r15-documento.png" e a imagem renderizada pela URL assinada do gateway (`read`); widget `meal-plan-image-preview-362777eb…` presente (`waitFor` ok) | `mp-e11-previa-apos-reload.png` |
| Publicar | "Enviar e publicar" → `status: published`, revisão 8, `simpleImageMeta` preservado; volta ao diretório | `mp-e12-revisao.png`, `mp-e12-apos-publicar.png` |
| model-edit sem regressão | `/meal-plans/models/bef18a8c-c208-4eea-97bf-a2051e400789/edit` (modelo da R14) abre, 3 etapas até "Revisão e publicação" (3 refeições) | `mp-e13-model-edit-etapa1.png`, `mp-e13-model-edit-conteudo.png` |
| Escopo / negativas | `meal_plan_image_read_descriptor_v2` exige `meal_plans.read` + `meal_plan_scope_allowed` (pgTAP 26/26 cobre outro tenant); em produção todos os atores QA (`qa-r06-*`, `qa-r03`) pertencem ao tenant QA R04 com `meal_plans.read` → 200 (em escopo); ativo inexistente → `P0002 meal plan image not found`. Ator de outro tenant não existe na massa QA — só pgTAP | log do script |
| 409 legado | não ocorre: os três ativos do cardápio são `storage_provider: r2`; não há ativo `supabase_mvp` em produção (envio estava desabilitado) | PostgREST |
| Limpeza | `meal_plan_request_image_delete` nos dois ativos órfãos (`35dd7c74`, `2541d4b5`, revisão 1) → `22023 meal plan image parent is immutable` (cardápio publicado; regra de `20260910190100` exige revisão aberta). Ficam `active` sem referência até o Owner abrir revisão ou rodar o cleanup — registrado como massa residual | log do script |

Correções colaterais nesta prova: `4c89d3065` (validação "Anexe a imagem" bloqueava com arquivo pendente) e
`5ea5984c6` (Edge `child-safety-media` `finalize` 422 `media_descriptor_invalid`; v2 implantada pela coordenadora).

## Ambiente — CORS dos buckets R2 (medição 17/09)

`OPTIONS` na URL assinada de `prepare` (`ferramentas/edge_probe.mjs`, `ORIGINS`): `coelo-media-prod` responde
`204` só para `http://127.0.0.1:3014`, `http://127.0.0.1:3016` e `https://superadmin.coelo.me`;
`coelo-documents-prod` responde `403` para todas as origens. Consequência: upload de imagem pelo navegador só na
3016 (feito); upload do documento (B6) pelo navegador impossível hoje — decisão do Owner (17/09): CORS não muda
nesta rodada; prova do documento pelo gateway em Node + wizard com o documento `ready`.
