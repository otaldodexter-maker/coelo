---
title: "D01-UI — detalhe read-only de Unidades e Grupos"
source: "specs 043/045; reserva E2E2-D01-UI e autorização visual do Coordenador; testes locais"
status: "local-green; logout-desktop-open; not-verified-e2e"
generated_at: "2026-09-07"
---

# Entrega incremental

Branch `codex/e2e-estruturas-pessoas-locais`; base deste pacote
`e78caaf5a6f860448f40d2cfe99563c0e635d5c7`.
Recorte: detalhes/reload de Unidades e Grupos, somente Superadmin; sem escrita,
listagem legada, cutover, migration, acesso remoto ou alteração de autorização.

## Implementação e limites

- Controllers limpam payload antes do reload e ignoram respostas de geração
  antiga ou depois de dispose. Troca de ID/repository reinicia a leitura.
- Páginas somente leitura usam shell, tokens, `CoeloStatePanel` e footer
  canônicos, com Voltar e Recarregar; botão de reload desabilitado na carga.
  Não inventam branding, membros, permissões efetivas ou relações ausentes.
- Composition root configurado injeta os dois repositories internos v2;
  defaults indisponíveis, listas e gate de mutação anteriores preservados.
- Rotas aditivas `/units/:unitId` e `/groups/:groupId` ficam depois das rotas
  estáticas de criação/edição. Voltar leva ao diretório, ainda indisponível na
  composição produtiva atual; nenhum diretório foi declarado concluído.
- Sem configuração, sem sessão, erro, negação e revogação não ganham fallback
  de dados. Autorização fina continua nas RPCs, não no cliente.

## Evidência executada

Windows local; Flutter SDK `C:\src\flutter`. Nenhum Supabase/Cloudflare remoto.

| Verificação | Resultado |
| --- | --- |
| Repositories DTO/HTTP mock | 73 GREEN (commit anterior reexecutado) |
| Controllers | 16 GREEN |
| Páginas: reload/negação, troca de ID, 375/768/1024/1440, claro/escuro, texto 200% | 20 GREEN |
| Router: com/sem sessão, larguras 800/1440, logout somente em 800 | 8 GREEN |
| Goldens + teclado Enter/reload | 4 GREEN, 12 imagens novas |
| Composition root sem/configurado | 2 GREEN |
| Suíte D01 combinada após ajustes | **123 GREEN**, exit 0 |
| Analyzer: páginas, controllers, composição e testes alterados | zero issues, exit 0 |
| Validador visual do catálogo | exit 0 |
| Review independente read-only | sem novos bloqueadores atribuíveis a D01 |
| Gate coelo-knowledge: base e cenários | PASS; no-op de projeção |

Comando da suíte em `apps/superadmin` (prefixo operacional `rtk proxy
C:\src\flutter\bin\flutter.bat`): `test --no-pub` seguido de
`test/features/groups/data/supabase_group_detail_repository_test.dart`,
`test/features/units/data/supabase_unit_detail_repository_test.dart`,
os quatro testes `group_detail_controller_test`, `unit_detail_controller_test`,
`group_detail_page_test`, `unit_detail_page_test` nos diretórios de presentation,
`test/app/router/structure_detail_routes_test.dart`,
`test/app/router/structure_detail_golden_test.dart` e
`test/core/config/structure_detail_composition_test.dart`.

Referência declarada: Criar/Editar Instituição. Novos baselines autorizados
em `apps/superadmin/test/app/router/goldens/`: cada entidade ×375 claro/1440
escuro ×ready/focus/reload. Todas as 12 imagens foram abertas e inspecionadas
visualmente; nenhum baseline existente foi sobrescrito. Foco visível,
footer acessível e launcher acima do footer. Captura de reload aguarda dois
quadros para não registrar apenas o instante inicial do spinner/transição.

## Gates abertos, sem promoção indevida

1. **Logout desktop compartilhado:** Home existente também trava no primeiro
   layout após `session.signOut()` em1440×900, sem páginas D01. Em800×600
   passa. A chamada signOut e o pump restrito a build retornam; layout trava.
   Teste original de logout da Home passou na viewport original. Encaminhado
   ao Coordenador/Engenheiro2; shell e guards não foram alterados.
   Reprodução opt-in preservada em
   `test/app/router/desktop_logout_regression_probe_test.dart`:

   ```text
   rtk proxy C:\src\flutter\bin\flutter.bat test --no-pub --dart-define=RUN_DESKTOP_LOGOUT_PROBE=true test/app/router/desktop_logout_regression_probe_test.dart --reporter expanded
   ```

   Rodar isoladamente; o travamento pode impedir timeout fake-async, sendo
   necessário interromper somente esse processo. O probe fica fora da suíte
   normal e não é contado como GREEN.

2. **Regressão ampliada não verde:** dois testes em
   `unit_fail_closed_composition_source_test.dart` e dois em
   `composition_root_sanitization_test.dart` falharam (5 passaram).
   As expectativas proíbem Unit backend commands, adapters de Activities e
   páginas Access já presentes em `git show HEAD:<arquivo>` na base
   `e78caaf5`; D01 não toca esses trechos. Evidência por leitura da base,
   não reexecução de checkout antigo. Testes antigos preservados.

3. Produção real, A/B, sessão revogada real, reload/nova sessão e retorno do
   diretório produtivo não comprovados. Front-end não é `verified`; Back-end
   não é `done`; E2E não é `verified-e2e`.

## Memória e ownership

Conhecimento: no-op, pois o pacote implementa contrato aprovado e registra
evidência local, sem nova regra de produto durável ou capacidade concedida.
Validador exigiu `-Root` explícito no PowerShell; base e cenários passaram.
Arquivos compartilhados alterados apenas nos hunks reservados: router, routes,
auth_scope, app e main. Trackers/ledger/main branch são do Coordenador.
