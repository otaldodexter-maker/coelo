---
source:
  - "Coordenador Etapa2: preparação A01 local HTTP e decisão taxonomy.manage, 2026-09-08"
  - "docs/reviews/evidence/etapa-2/engenheiro-1/a01-local-http-window-proposal-2026-09-08.md"
  - "f5b0e5bff1a9b9ba5a11f7c01f2fe8c2d91652b0 e f0be734ac3210c84523a61700f73feaba5150368: cliente nominal"
  - "A01DirectoryAuditGreen/profile.json e fixture97 ee212cb56e9dc18400a8d105aeeba3a5f77bbbf4"
status: preparacao_local_para_revisao_sem_execucao_sql_http_docker
generated_at: "2026-09-08"
scope: "Helper A01 específico, Pester com mocks e pins; wrapper/cliente/SQL inalterados"
---

O helper específico Test-LocalA01Runtime.ps1 está preparado para o ponto posterior ao reset55 e anterior ao finally já existente do wrapper. Esta entrega prepara código e testes; não integra a flag no wrapper e não inicia SQL, Docker, Auth, PostgREST ou Flutter HTTP. A conclusão fica limitada às três RPCs do roteiro e aos comandos de UI que o teste pinado exerce. A concessão histórica activities.taxonomy.manage a Operations é preservada por decisão central; não se afirma autorização exclusivamente de leitura em todo o domínio.

O contrato tem somente ProjectRoot, ProjectId e ClientRoot. Não expõe endpoint, seed, RPC, SQL, callback, comando, token, retry ou timeout livres. Dot-source importa as funções para mocks Pester; a invocação normal executa o caminho fechado. Não há módulo ou framework novo. O provisionamento Git do checkout, flutter pub get, seleção de serviços e desmontagem pertencem ao operador/wrapper e ficam fora deste helper.

| Âncora | Pin |
| --- | --- |
| Perfil | A01DirectoryAuditGreen:53 canônicas +2 preflights =55 |
| Alvo único | 20260907222911 |
| Descriptor SHA256 UTF-8/CRLF | bbd606be64e366502d84389f4a66e9e32ba500f29b043c5cf21cb332a14424ba |
| Resolver SHA256 UTF-8/CRLF | 6b055f0c09b0a918ce5c027e2b59504d7662646535bdcf065e24cdcecde863f2 |
| Corretiva A01Green SHA256 UTF-8/CRLF | e72e11c5d0f8fd8d49bfe098a230530edb3d4070d80ca5b4ae04b61b72eb196f |
| Seed SHA256 UTF-8/CRLF | 758e6b4b3c8ad237ec709acd17c0fe59c9d554f6c789c76bbb81ab13a67adf99 |
| Commit cliente exato | f0be734ac3210c84523a61700f73feaba5150368 |
| Árvore apps/superadmin | 8fc63de9f4862b05ceea4b3d233c6e1977443cde |
| Árvore packages | cb6baa89f821a4a775bdc8bb1055668d053ec35e |
| Teste Flutter blob | 0ec4960cd332fa1479dce4b1154ae9ffd1287faa |
| Parser/allowlist cliente blob | b1837273ea13424d06d6516b740d6f75a2357733 |
| Adapter Activities blob | 9224edb857392448e8cd940710e8f877ee4ceb2a |
| Gateway bootstrap blob | aad98edf35500bd9717e88937a11eedf0cde0317 |
| pubspec.lock cliente blob | 3ca4f74725acc111cd7a9dad1dac5b6ee625413f |

O checkout preparado pelo root é C:/Users/adrie/Documents/Coelo/.worktrees/e1-a01-client; o diretório do processo Flutter é apps/superadmin nesse checkout. O helper não descobre ou troca checkout. Exige HEAD exato, árvore inteira sem mudanças/untracked, árvores app/packages pinadas, ausência de flags de índice que ocultem mudanças, arquivos locais sem reparse e sete dependências coelo_* dirigidas aos diretórios locais esperados em package_config.json. Arquivos externos do cache Pub e SDK Flutter não são reconstituídos nem baixados pelo helper; suas versões/dependências vêm do pubspec/lock pinados e do provisionamento prévio do operador. Não há claim de atestação criptográfica de todo SDK/cache.

Antes do seed, o caminho deve ser exatamente TEMP/ProjectId, com ProjectId coelo_safe_ seguido de29 hexadecimais, marker próprio e config com uma única identidade correspondente; arquivo e ancestrais são checados contra reparse. O resolver fixo valida as55 fontes. As duas árvores de staging — validated-migrations e supabase/migrations — devem ter somente as55 entradas e hashes correspondentes às fontes. A tabela supabase_migrations.schema_migrations deve fornecer as55 versões exatas, únicas e ordenadas, não apenas count/max. AuditRed55, outra corretiva ou mesmo alvo com origem diferente não satisfazem os pins.

Antes de qualquer inspect/SQL, o helper rejeita overrides DOCKER_HOST/CONTEXT/CONFIG/TLS/TLS_VERIFY/CERT_PATH e SUPABASE_* sem imprimir valores, exige endpoint npipe local do contexto atual e conserva o valor validado. Cada chamada Docker usa --host com esse pipe literal, sem resolver novamente um nome de contexto mutável. Antes do statusCLI verifica novamente o nome e o endpoint: mudança de qualquer um, ou endpoint TCP/SSH, interrompe. O statusCLI recebe ambiente exclusivo do filho, formado pela allowlist do sistema e DOCKER_HOST com o mesmo pipe; DOCKER_CONTEXT, configuração/TLS e demais overrides não são herdados. O ambiente do processo pai não é alterado. A identidade operacional exige os containers próprios DB/Auth/REST/Kong running/healthy quando o healthcheck existir, label com.supabase.cli.project correto, uma rede própria comum e volume PostgreSQL próprio. PGRST_DB_URI e GOTRUE_DB_DATABASE_URL devem apontar ao alias do DB nessa rede, porta5432/banco postgres. API_URL deve ser http://127.0.0.1 com porta explícita1024–65535 e corresponder à porta publicada8000/tcp do Kong. O helper não escolhe um endpoint remoto, localhost alternativo, redirect, query ou fragmento.

StatusCLI supabase@2.116.0 e inspect são capturados em memória, com stdout/stderr sem eco. ANON_KEY precisa ter role anon e assinatura HMAC compatível com JWT_SECRET local; Auth e REST devem usar o mesmo signer. O arquivo declarativo nominal /home/kong/kong.yml deve associar exatamente os caminhos gerais /auth/v1 e /rest/v1 aos aliases próprios Auth/REST, sem aceitar uma URL correta encontrada em bloco sem relação com a rota. Em seguida, GET local de /auth/v1/health e /rest/v1/ confirma startup, sem redirects/proxy, antes da escrita sintética. Esses dois probes operacionais não são novas RPCs do produto.

O layout efetivo da CLI2.116 ainda não foi observado numa stack reservada nesta preparação. Nomes, labels, aliases, env, mount, porta ou formato declarativo divergentes provocam falha antes do seed; não há fallback. O primeiro gate operacional deve capturar somente metadata estrutural redigida e revisar qualquer ajuste nominal necessário. Não se presume que mocks comprovem endpoint→DB55 real.

O seed fixo recebe coelo.local_replay='a01-http-green55' na mesma sessão psql, somente depois de identidade, ledger e startup. Seu guard55000 antecede qualquer escrita. Contém Auth/users/sessions reais sintéticos e constraints imediatas antesCOMMIT, conforme a nota de seed; as sessions herdadas continuam com uma hora. Não é idempotente e só se destina à stack própria recém-resetada. Nenhuma senha, JWT ou signing secret é gravada no seed.

O helper gera na memória HS256 para102/202,104/204 e106/206, role authenticated, aal2, iat corrente e exp=iat+600. O filho recebe apenas os seis nomes COELO_A01_LOCAL_RUNTIME, COELO_A01_LOCAL_URL, COELO_A01_LOCAL_ANON_KEY, COELO_A01_READER_JWT, COELO_A01_REVOKED_JWT e COELO_A01_DENIED_JWT, além de uma allowlist de ambiente do sistema/toolchain. O signer e demais credenciais herdadas não entram no filho. Não há token em argumento, arquivo ou output.

O comando é fixo: flutter test --no-pub test/features/activities/a01_local_runtime_test.dart. A janela de180s usa um Job Object Windows privado, sem breakaway, criado com KILL_ON_JOB_CLOSE. O processo nasce suspenso e só é retomado depois de associado ao job, impedindo a corrida de fork anterior ao vínculo. Um único relógio inclui escrita assíncrona de stdin, término do pai e drains stdout/stderr; retornar do pai não basta. O finally termina o job mesmo após saída do pai, confere ActiveProcesses=0 por até5s de graça e fecha seu handle. Nenhum taskkill livre permanece. A stack continua pertencendo ao finally do wrapper. A implementação segue as APIs primárias [Job Objects](https://learn.microsoft.com/en-us/windows/win32/procthread/job-objects), [CreateProcessW](https://learn.microsoft.com/en-us/windows/win32/api/processthreadsapi/nf-processthreadsapi-createprocessw) e [redirecionamento de pipes](https://learn.microsoft.com/en-us/windows/win32/procthread/creating-a-child-process-with-redirected-input-and-output). Pester simulou stdin bloqueado e descendente retendo pipe com pai encerrado; compilou C#, sem criar processos com essas APIs e sem demonstrar teardown Docker real. O smoke sintético posterior do root encontrou ERROR_INVALID_PARAMETER87 em CreateProcessW antes da criação do filho: PowerShell converteu o argumento nulo de ambiente em string vazia e a implementação alocou um bloco inválido. A condição do construtor agora trata nulo e vazio como ambiente herdado (ponteiro zero); blocos explícitos continuam separados. A mesma decisão pura usada pelo construtor é exercitada em Pester com $null, string vazia e bloco fictício não vazio. O root confirmou constructor/Wait/exit0/Dispose numa cópia corrigida somente em memória; o snapshot em arquivo ainda exige seu smoke real completo de stdin, drain, descendentes e timeout.

Exit0 sem conclusão e relatório nominal não passa. O helper exige All tests passed!, exatamente um relatório não vazio/bounded, somente RPCs bootstrap/directory/filter_options, status200, correlações UUID únicas para Activities e resultados positivos/negativos dos dois readers. O cliente pinado só imprime o relatório após suas expectativas de UI/escopo/revogação e rejeita qualquer POST fora da allowlist. Raw stdout/stderr do Flutter é retido; a evidência final não reproduz tokens nem payloads.

As correlações de Activities viram apenas UUIDs validados na consulta de audit. Cada resposta precisa de exatamente uma linha, action/outcome/permission corretos, ator/link/membership302/402/502 ou304/404/504 ou306/406/506, hash de sessão exato, aal2 e digest íntegro. Sucessos exigem instituição010 e after_json somente row_count (diretório0/2, opções8); negações exigem contexto global minimizado e motivo de revogação/permissão. Nenhum sucesso de consulta audit substitui validação de ator/escopo. Bootstrap é comprovado pelo roteiro e pelo seu resultado; seu relatório não exige correlação e não é contado entre as linhas Activities verificadas.

O snapshot antes/depois compara o conteúdo completo das23 tabelas locais enumeradas no helper: entidades/links/configuração/recibos Activities, instituições/unidades/grupos, autorizações e identidades internas pertinentes. Audit fica fora da igualdade por ser append-only. A igualdade não afirma matriz global sem escrita, não autoriza revogar taxonomy.manage e não cobre comandos não exercitados. Nenhum dado bruto dos snapshots/audit é emitido.

Validação realizada nesta preparação: RED17/17 com helper ausente; casos adicionais de timeout e parser Kong também passaram por RED antes da implementação. Após review independente, RED4 adicionais do daemon local e RED3 adicionais da janela completa precederam as correções. A regressão do construtor e a fixação do endpoint produziram RED43 (40 PASS,3 FAIL esperados) antes da correção; o caso adicional de mudança do pipe sob o mesmo contexto verifica o novo guard. GREEN44/44 Pester3.4, zero skip,3,32s; houve correção de enumeração de arrays JSON específica do Windows PowerShell5 encontrada pelo positivo. Parse do helper e do teste sem erros. O gate read-only real do checkout f0be734a e do resolver55 passou antes da revisão do launcher; as funções de pins/closure não mudaram e o launcher em arquivo foi compilado/simulado pelo autor; o smoke sintético do root e sua limitação estão registrados acima. Nenhuma execução SQL/Docker/HTTP/Flutter ou token real da stack foi feita nesta preparação. Os tokens dos testes usam apenas signer fictício em memória.

Próximo gate: revisão independente do helper/tests/pins → integração nominal da flag somente no wrapper pelo root, preservando mutex/finally e rejeitando combinações → autorização/reserva central → snapshot estrutural redigido de startup da identidade55 → seed/HTTP/audit/snapshot e teardown independente. O wrapper precisa validar helper/seed/cliente antes de staging/Docker e chamar o helper dentro de seu try após reset55; esta entrega não fez essa integração. Falhas reais não podem ser contornadas com callback, retry livre, seletor alternativo ou claim de E2E. Nenhuma regra de produto ou projeção de conhecimento foi alterada.

Revisão independente identificou P1 (daemon herdado sem vínculo local) e P2 (stdin/drain fora do timeout e árvore dependente do pai vivo). Ambos receberam testes RED e correções específicas acima; novo snapshot inclui também a correção confirmada do ambiente vazio e o pipe Docker literal, e requer re-review. O primeiro smoke do root identificou uma falha real antes do filho; não se declara a janela completa comprovada pela correção em memória ou por compilação. A emissão de tokens sintéticos reais e a primeira execução do launcher contra os serviços continuam dependendo do gate operacional do root.

Atualização do root: integração31/31 PASS e smoke Windows do helper final ab894730 PASS (stdin65.536, stdout/stderr, timeout com descendente e cleanup próprio em2,0485704s; cliente f0 limpo). Detalhes e limites em [a01-local-http-window-proposal-2026-09-08.md](a01-local-http-window-proposal-2026-09-08.md). A regressão compartilhada e o gate de serviços permanecem separados.
