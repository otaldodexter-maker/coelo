---
title: "R15 Bloco C1 — Momentos (view/publish/remove/create) na rota real"
source: "R15-prompts.md (Bloco A fatia 5, liberada para C1 pela coordenadora em 17/09); R14-handoff-sessao-7.md; r14-sessao-7/momentos-bloqueado-20260916.md; ADR 0037; ADR 0032"
status: "verified"
lifecycle: "current"
generated_at: "2026-09-17"
updated_at: "2026-09-17"
audience: "team"
---

# Momentos (`momentos.create`, `momentos.publish`, `momentos.view`, `momentos.remove`) — 17/09/2026

Ambiente: produção Supabase (`evvbomzejfijozbtgvpt`); build da worktree `r15-bloco-c1` em
`6e9234c87` servido por `serve.py` em `127.0.0.1:3016`; Chrome CDP `9416` (SwiftShader); sessões
`qa-r06-publicacoes` (autor, perfil `%TEMP%\coelo-r15-c1-chrome`) e `qa-r06-principal` (leitor,
perfil `…-principal`), ambas Owner no tenant `QA R04 Cuidado (sintetico)` `d0c40000…0001`.
Mídia: PNG 64×64 de cor sólida (179 B), sem dados pessoais, pelo seletor nativo interceptado
(`Page.setInterceptFileChooserDialog` + `DOM.setFileInputFiles`). Roteiro seguido:
`r14-sessao-7/momentos-bloqueado-20260916.md`.

## Rota real

| Passo | Resultado observado | Captura |
|---|---|---|
| `/principal-moments/publish` (autor) | publicador "Publicar em Momentos" com Mídia, Prévia, Legenda, Público e contexto | `capturas/momentos-40-moments-publish.png` |
| Adicionar mídia (`momentos.create`) | seletor nativo (`selectMultiple`) → "1 mídia selecionada. O envio acontece ao publicar." — sem preflight recusado na origem `3016` (CORS liberado em 16/09); o bloqueio histórico `blocked-environment` não se reproduz | `…-41-moments-media.png` |
| Legenda + público | "QA R15 Momento (sintetico) - R15 C1 17/09" (41/220); chips Famílias + Equipe escolar selecionados; contexto "QA R04 Cuidado (sintetico)" | `…-43-moments-filled.png` |
| Publicar agora (`momentos.publish`) | rede: `save_moments_draft` 200 → `moments-media` (prepare) 200 → `PUT` R2 (`coelo-media-prod`) 200 → `moments-media` (finalize) 200 → `publish_moment` 200; snackbar "Momento publicado." e volta ao feed Acontece. O duplo clique de proteção da ferramenta gerou **duas** publicações (`74c56d7c` 14:12:22 UTC e `7a5865ac` 14:12:27 UTC), cada uma com seu asset — o publicador não bloqueia o segundo toque durante o envio (observação de UX, sem defeito de contrato) | `…-44-moments-published.png` |
| `/principal-moments` (autor) | feed com o momento e a mídia real do R2 (`moments-media read` + GET assinado 200), ícone "Retirar" (⊖) presente para o autor | `…-45-moments-feed-author.png` |
| Retirar momento (`momentos.remove`) | ⊖ → "Retirar este momento? … A mídia não é apagada e a retirada fica registrada." → Retirar → `withdraw_moment` 200, "Momento retirado.", feed avança para o outro momento; reload → só `74c56d7c` continua | `…-47-moments-withdraw.png`, `…-48-moments-withdrawn.png`, `…-49-moments-reload.png` |
| `/principal-moments` (leitor `qa-r06-principal`, `momentos.view`) | momento `74c56d7c` visível com a mídia real (`list_visible_moments` 200, `moments-media` 200, GET `original` 200); **sem** ícone Retirar (não autor, `can_withdraw=false`); reload relê | `…-50-view-principal.png`, `…-51-view-reload.png` |

Observação de UX (pré-existente): a linha "Curtido por Maria e outras 531 pessoas" e os contadores
da prévia (128/14) são texto de demonstração no feed/publicador real; não pertence a esta fatia.

## Backend por PostgREST (`qa-r06-principal`, sem chave de serviço)

| Chamada | Resposta |
|---|---|
| `list_visible_moments(d0c40000…0001)` antes da retirada | 2 itens (`7a5865ac`, `74c56d7c`), `can_withdraw=false`, `author_name` "Operador interno db9ebfa5", 1 mídia cada |
| `list_visible_moments(d0c40000…0099)` | `403 42501 moments_permission_denied` |
| `withdraw_moment(7a5865ac)` por não autor | `403 42501 publication_not_authorized`, sem mutação |
| `withdraw_moment(id inexistente)` | `403 42501 publication_not_authorized` (não enumerável) |
| `list_visible_moments(d0c40000…0001)` após a retirada pela tela | 1 item (`74c56d7c`) |

Nenhum SQL novo; contratos `save_moments_draft`/`publish_moment`/`withdraw_moment`/
`list_visible_moments` e a Edge `moments-media` já estavam em produção (BE `local-green`/`done`
no corte) e foram exercitados de ponta a ponta.

## Resultado

- `momentos.create`: FE verified, BE done, integrado **verified-e2e** (sai de `blocked-environment`:
  upload real pela origem `3016`).
- `momentos.publish`: FE verified, BE done, integrado **verified-e2e**.
- `momentos.view`: integrado **verified-e2e** (FE/BE já terminais).
- `momentos.remove`: FE verified, BE done, integrado **verified-e2e**.
- Massa residual (sem PII): momento `74c56d7c` publicado e `7a5865ac` retirado, ambos com mídia
  sintética em R2, instituição `d0c40000…0001`.
