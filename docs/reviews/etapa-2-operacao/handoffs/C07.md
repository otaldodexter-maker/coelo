---
title: "Handoff R01 C07 — Validação visual e fluxos"
source: "assignments/C07.md canônica C00 (R01-C07-I002, I003 de 18:51:29 e decisão R01-VISUAL-1835 de 18:33:32); PROTOCOLO.md R01 vivo na C00; mensagens operacionais da C06; specs/050 e 036; docs/superpowers/specs e plans; referência visual aprovada do Owner de 2026-08-31; .agents/skills/coelo-ui/references (C00)"
status: "final"
generated_at: "2026-09-09T05:31:00-03:00"
timezone: "America/Sao_Paulo"
---

# E2 R01 C07 — Validação visual e fluxos

## Revisão 12 — HANDOFF FINAL da rodada R01

Entrega final no marco. O conteúdo substantivo está na revisão 11, escrita às 03:32 justamente para
que este momento fosse publicar e conferir, não redigir sob relógio. Esta revisão fecha, confirma o
que mudou desde então — **nada** — e registra o encerramento.

- rodada: R01; revisão: 12; **final**
- conversa: `E2 R01 C07 — Validação visual e fluxos`; ID real
  `local_3b1f7fb1-731c-4ba9-bae5-1657903ca22f`; ferramenta Claude Code, modelo `claude-opus-5`.
- última instrução processada: **`R01-C07-I004`** (C00, 2026-09-08T21:10:41−03:00). Conferido às
  05:28 de 09/09: **nenhuma instrução nova** desde então, na assignment viva nem no PROTOCOLO.
- timestamp: 2026-09-09T05:31:00-03:00, relógio da máquina.
- branch `claude/e2-r01-c07-validacao-visual`; baseline `4af42925`.

### 1. SHA e ponta remota, conferidos no fechamento

- HEAD local no momento desta redação: **`750d6d1f`**.
- `git ls-remote origin claude/e2-r01-c07-validacao-visual`: **`750d6d1f`** — idêntico.
- Commits pendentes de push: **zero**.
- O SHA desta revisão fica registrado no commit que a publica, para não gerar SHA circular.

### 2. WIP: **zero**, declarado nominalmente

Não há trabalho em andamento a preservar. Árvore limpa, nenhum arquivo não rastreado — incluindo
busca por ignorados —, nenhum resíduo das cópias temporárias usadas nas execuções fora da árvore.

### 3. Medição final

Regressão executada às **05:29**, no baseline `4af42925`, comando
`flutter test test/visual_review test/acceptance` em `apps/superadmin`:

**169 casos, 144 verdes, 25 vermelhos.** Idêntico à medição das 03:30 — nada mudou no intervalo.

**Correção de número, para o registro:** a C06 pediu "a lista dos 11 vermelhos". Onze era o número do
lote de sete arquivos, antes da instrução I004. Com os quatro aceites concedidos, o conjunto tem
**onze arquivos** e **25 vermelhos**. A lista nominal completa dos 25, agrupada por dono e por
expectativa, está na **revisão 11, seção 2**, e não a repito aqui para não duplicar registro.

Resumo por família: Publicar no Agora 7, Pessoas 4, Instituições 3, Acompanhamento 3, Comunicados 3,
Acontece 3, Turmas 1, Locais 1.

**Nenhum é regressão minha.** Dezenove têm correção anunciada e devem virar verdes no baseline
conjunto; **seis** — Comunicados e Acontece — não têm correção anunciada e **espero que continuem
vermelhos**. Nenhum golden foi regenerado, criado ou removido em nenhum momento da rodada.

### 4. Dependência aberta, datada

**A materialização do baseline conjunto `aba29322` na minha worktree, que é da C00.** Conferido às
05:28: não materializado. Declarada por mim desde a **revisão 10, às 01:02**, e reafirmada em todos
os limites de lote desde então.

Consequência honesta: **a reverificação independente combinada não foi executada.** O roteiro está
pronto em `C07-evidence/2026-09-08-roteiro-reverificacao.md`, com as previsões registradas **antes**
da janela — 25 vermelhos caindo para 6 — para que a comparação futura seja um diff e não uma
suposição. Fica documentado como **não executado**, com a dependência nomeada.

Registro o que a C06 pediu que ficasse escrito, porque é fato verificável no Git e protege a leitura
dos números: **entreguei os quatro aceites concedidos e fiquei sem trabalho autorizado por
dependência de integração não atendida, não por falta de iniciativa.** Entre 23:10 e 05:30 mantive
manutenção declarada, com a dependência escrita, em vez de abrir frente sem autorização — que é o
erro de escopo que cometi uma vez nesta rodada e não repeti.

### 5. O que fica entregue

**Onze arquivos de teste**, todos reservados nominalmente, revisados e executados em série por mim
antes de cada commit. **Dez documentos de evidência** e **sete reproduções**, em
`docs/reviews/etapa-2-operacao/handoffs/C07-evidence/`.

O documento para a C00 usar na consolidação é
`C07-evidence/2026-09-08-registro-defeitos-c07.md`: **24 defeitos** com dono, arquivo, linha, prova e
estado, separando **medido** de **lido** e **verificado por mim** de **declarado por quem corrigiu**.

### 6. Encerramento dos mecanismos

- `CronDelete` do job **`d5b22e3a`** (`27,57 * * * *`) executado como último passo da entrega, após
  o commit e o push desta revisão. Antes disso ele permaneceu vivo, conferido às 01:00 a pedido da
  C06.
- Nenhum outro mecanismo de retomada foi configurado por mim.

### 7. O que **não** removo, por não ser meu

- **Worktree de observação** `C:/Users/adrie/Documents/Coelo.worktrees/e2-r01-c07-c04ro`: limpa,
  restaurada em `479d1bd1`. Remoção de worktree é da C00. Comando, se ela quiser:
  `git worktree remove C:/Users/adrie/Documents/Coelo.worktrees/e2-r01-c07-c04ro`.
- **Branch residual** `claude/e2-r01-c07-validacao`, em `cea29b1f`, sem commits únicos: preservada,
  como a I002 determina.

### 8. Comandos exatos de retomada

```
cd C:\Users\adrie\Documents\Coelo.worktrees\e2-r01-c07
git status --short
git log --oneline -5
cd apps\superadmin
flutter test test\visual_review test\acceptance
```

Para a reverificação, quando o baseline chegar: seguir
`C07-evidence/2026-09-08-roteiro-reverificacao.md`, que traz as sete reproduções com o método de
cópia temporária, execução em série e remoção conferida.

### 9. Nada foi declarado concluído além do que foi medido

Nenhuma ação foi promovida a FE, BE ou E2E por mim. Um verde meu certifica que o defeito que medi
deixou de reproduzir no baseline em que medi — não que a ação está concluída. Essa distinção está em
cada documento que produzi, e é a coisa que peço que sobreviva à rodada, junto com as três
advertências de método da revisão 11.

---

## Revisão 11 — conteúdo do fechamento, escrito antes do marco

Escrita às 03:32, **antes** do marco das 05:30, para que a entrega final seja publicar e conferir
push, não redigir sob relógio. Se o baseline conjunto chegar antes, esta revisão ganha a
reverificação; se não chegar, ela **é** o fechamento, com a dependência registrada como aberta.

- rodada: R01; revisão: 11
- última instrução processada: `R01-C07-I004` (C00, 21:10:41 de 08/09). **Nenhuma instrução nova** na
  assignment viva nem no PROTOCOLO — último commit deles `dcbcebc1`, de 21:21 de 08/09, conferido às
  03:09 e às 03:30 de 09/09.
- timestamp: 2026-09-09T03:32:00-03:00, relógio da máquina.
- conversa: `E2 R01 C07 — Validação visual e fluxos`; ID `local_3b1f7fb1-731c-4ba9-bae5-1657903ca22f`;
  modelo `claude-opus-5`.
- branch `claude/e2-r01-c07-validacao-visual`; baseline `4af42925`; ponta `2feba954`.
- **árvore limpa, zero commits pendentes de push, WIP zero.**

### 1. O que foi entregue

**Onze arquivos de teste**, todos reservados nominalmente e executados em série por mim antes de cada
commit:

| Arquivo | Casos | Verdes | Vermelhos | Instrução |
|---|---|---|---|---|
| `visual_review/c07_moments_acceptance_test.dart` | 18 | 18 | 0 | I002 |
| `visual_review/c07_profile_acceptance_test.dart` | 12 | 12 | 0 | I002 |
| `acceptance/c07_institutions_directory_test.dart` | 24 | 21 | 3 | I002 |
| `acceptance/c07_units_directory_test.dart` | 12 | 12 | 0 | I002 |
| `acceptance/c07_groups_directory_test.dart` | 25 | 24 | 1 | I003 |
| `acceptance/c07_people_directory_test.dart` | 21 | 17 | 4 | I003 |
| `acceptance/c07_students_directory_test.dart` | 11 | 8 | 3 | I003 |
| `visual_review/c07_now_publication_acceptance_test.dart` | 17 | 10 | 7 | I004 |
| `acceptance/c07_locations_directory_test.dart` | 12 | 11 | 1 | I004 |
| `visual_review/c07_happens_acceptance_test.dart` | 12 | 9 | 3 | I004 |
| `acceptance/c07_notices_directory_test.dart` | 5 | 2 | 3 | I004 |

**Regressão do conjunto, medida às 03:30 no baseline `4af42925`: 169 casos, 144 verdes, 25
vermelhos.**

**Dez documentos de evidência** e **sete reproduções** em `C07-evidence/`, entre elas o laudo das 61
divergências com duas erratas, o dossiê do Publicar no Agora, as duas varreduras e o registro
consolidado.

### 2. Os 25 vermelhos, nominalmente

Contagem sem lista não se compara com nada. A lista abaixo é para a próxima medição ser um **diff**,
não uma suposição.

**Publicar no Agora — 7.** Alvo aprovado que o código ainda não implementa; devem virar verdes no
baseline conjunto.
1. `the approved composer is headed by Sua publicacao at 375`
2. `... at 768`
3. `... at 1440`
4. `the desktop keeps the lateral preview beside the media stage at 1440`
5. `no segmented progress bar at 375`
6. `... at 768`
7. `... at 1440`

**Pessoas — 4.** Dono C04; correções anunciadas.
8. `people.reload: retry lê exatamente uma vez preservando busca, filtro e página`
9. `people.list: "sem resultados" difere de falha e limpar restaura o diretório`
10. `people.list: o indicador de status responde ao teclado como responde ao toque`
11. `people.list: o indicador de status oferece alvo de toque de 48`

**Instituições — 3.** Dono C04 e C00; correções anunciadas.
12. `institutions.detail: o card abre o detalhe com o id da instituição por toque e por Enter`
13. `institutions.list: o banner Criar da tabela ativa no primeiro ponto de foco`
14. `institutions.list: o indicador de status ativa por teclado como se anuncia`

**Acompanhamento — 3.** Dono C04; correção anunciada em `7d499cf2`.
15. `a leitura de alunos que lança Error conclui em falha e o retry recupera`
16. `a leitura do snapshot que lança Error conclui em falha e o retry recupera`
17. `a carga que lança Error sai do esqueleto e oferece retry`

**Comunicados — 3.** Sem correção anunciada; **espero que continuem vermelhos**.
18. `comunicados.card@375: o título do card compacto ocupa as mesmas linhas que o de Instituições com o mesmo texto`
19. `comunicados.card@375: o indicador de status divide a linha do título, como no card de Instituições`
20. `comunicados.card@375: o alvo do indicador não vira largura de layout além da que a referência ocupa`

**Acontece — 3.** Sem correção anunciada; **espero que continuem vermelhos**.
21. `hover reforca algum sinal alem de cor no cartao Publicar agora`
22. `foco reforca algum sinal alem de cor no cartao Publicar agora`
23. `pressionado reforca algum sinal alem de cor no cartao Publicar agora`

**Turmas — 1.** Dono C00, banner compartilhado; correção anunciada.
24. `groups.create: o banner Criar da tabela aciona no primeiro ponto de foco`

**Locais — 1.** Dono C04; corrigido em `15dc4727`, não medido por mim.
25. `locations.access-denied o detalhe negado não deve oferecer um Recarregar ativo`

**Nenhum é regressão minha. Nenhum golden foi regenerado, criado ou removido.**

### 3. Medido por mim versus declarado por quem corrigiu

O documento que sustenta isso é `C07-evidence/2026-09-08-registro-defeitos-c07.md`, com **24
defeitos** e essa distinção em cada linha.

**Verificado por mim, não por relato:** o diff de `302d4e90` cobre os dois defeitos do toggle que
medi; a imagem aprovada está em git com blob idêntico ao original byte a byte; as duas correções do
toggle medidas em branches disjuntas, cada uma servindo de controle da outra; e o diff de `15dc4727`
desabilita o retry no negado.

**Declarado, com confirmação apenas de que o commit existe e toca o arquivo certo:** as correções de
Acompanhamento, do retry e dos dois botões de Pessoas, do salvar incompleto, dos focos de
Instituições e do cabeçalho do shell.

**Nenhuma correção foi medida por mim**, porque vivem fora do meu baseline. É a diferença entre
relatar e certificar, e ela está escrita.

### 4. Erratas que publiquei contra mim

1. **Cabeçalho mobile administrativo** — afirmei que não havia aprovação; a fonte existia em
   `docs/superpowers/specs/`, caminho que minha busca não cobriu. Cinco casos de Chat saíram de
   indeterminado para master desatualizado.
2. **Escopo do fio de Locais** — afirmei que os painéis não têm consumidor **sem qualificar o
   escopo**. É verdade na minha árvore e em `dev`, falso na da C04. A leitura correta muda a ação de
   quem integra: custa uma integração, não escrever a tela.
3. **Contagem do lote I003** — publiquei "nove defeitos" quando eram oito.
4. **Seção corrompida** — gravei uma tabela dentro de um comando de shell e as crases viraram
   substituição de comando, apagando os identificadores.
5. **Atribuição de dono** — atribuí Segurança da criança à C04 por proximidade de domínio; é da C02,
   conferido no mapa de donos.

### 5. Duas advertências de método que peço que sobrevivam à rodada

1. **Medir só por timeout dá falso verde.** A carga é assíncrona e **completa com erro**; o
   travamento é da tela. A prova tem de afirmar o estado final e a ausência do esqueleto.
2. **Não concluir pela existência de um `finally` — ler o que ele restaura.** Nos compositores ele só
   limpa uma bandeira; na busca de Segurança da criança ele desliga o indicador e **converte falha em
   "nada encontrado"**.

E uma terceira, que aprendi corrigindo meu próprio instrumento: **falso vermelho é tão nocivo quanto
falso verde**, porque acusa o produto por limitação do teste. Corrigi quatro no aceite do Acontece
antes de commitar.

### 6. Dependência aberta e o que não foi feito

**A materialização do baseline conjunto `aba29322`, que é da C00.** Não faço merge nem cherry-pick.
Sem ela, a reverificação independente combinada **não foi executada**.

O roteiro está pronto em `C07-evidence/2026-09-08-roteiro-reverificacao.md`, com as previsões
registradas **antes** da janela: espera os 25 vermelhos caírem para **6**, e que os 6 remanescentes
sejam exatamente Comunicados e Acontece. Qualquer outro número exige investigação nominal.

### 7. Estado final

- **WIP: zero.** Árvore limpa, nada não rastreado, tudo empurrado.
- **Worktree de observação** `e2-r01-c07-c04ro`: limpa, restaurada em `479d1bd1`. Não a removo —
  remoção de worktree é da C00. Comando, se ela quiser:
  `git worktree remove C:/Users/adrie/Documents/Coelo.worktrees/e2-r01-c07-c04ro`.
- **Branch residual** `claude/e2-r01-c07-validacao`, em `cea29b1f`, sem commits únicos: preservada,
  como a I002 manda.
- **Continuidade:** `CronCreate` `d5b22e3a`, `27,57 * * * *`, vivo e verificado às 01:00. **Será
  removido com `CronDelete` na entrega final**, não antes.

### 8. Comandos exatos de retomada

```
cd C:\Users\adrie\Documents\Coelo.worktrees\e2-r01-c07
git status --short
git log --oneline -5
cd apps\superadmin
flutter test test\visual_review test\acceptance
```

---

## Revisão 10 — manutenção declarada, mecanismo verificado, e o que farei nos marcos

Revisão curta e deliberada: **quase duas horas sem commit precisam aparecer como estado declarado,
não como silêncio.** Silêncio às 05:30 parece abandono; dependência declarada é entrega completa.

- rodada: R01; revisão: 10
- última instrução processada: `R01-C07-I004`. **Nenhuma instrução nova** na assignment viva nem no
  PROTOCOLO, conferidos às 00:39 e novamente à 01:00.
- timestamp: 2026-09-09T01:02:00-03:00, relógio da máquina.
- branch `claude/e2-r01-c07-validacao-visual`; ponta `133133ed`; **árvore limpa**; zero commits
  pendentes de push.

### 1. Estado: manutenção autorizada, não ociosidade

Desde as 23:10 não produzo commit **porque não há o que produzir dentro do que me foi concedido**. O
lote `R01-C07-I004` está completo, os quatro aceites entregues, e a C06 autorizou explicitamente a
manutenção como estado legítimo. Não vou inventar frente para preencher o tempo nem usar diretório
temporário como atalho — foi exatamente esse o erro de escopo que já cometi uma vez nesta rodada.

### 2. Dependência exata, única

**A materialização do baseline conjunto `aba29322` na minha worktree, que é da C00.** Não faço merge
nem cherry-pick. Enquanto ela não chega, a reverificação combinada não pode ser executada.

### 3. Verificação de integridade, feita às 23:40

- commits pendentes de push: **zero**;
- arquivos não rastreados na árvore, incluindo ignorados: **zero** — nenhum resíduo das cópias
  temporárias usadas nas execuções;
- worktree de observação `e2-r01-c07-c04ro`: **limpa**, restaurada em `479d1bd1`;
- entregue e versionado: **11 arquivos de teste, 10 documentos de evidência, 7 reproduções**.

Observação sem consequência, para não confundir quem ler no fechamento: o handoff da C05 aparece na
minha árvore porque meu baseline descende da branch dela. É herança de baseline, não escrita minha, e
está congelado no ponto em que a base foi tirada.

### 4. Mecanismo de continuidade — verificado, não presumido

A C06 pediu verificação porque o mecanismo de outra frente estava morto. `CronList` à 01:00 devolve
**um** job, o meu:

```
d5b22e3a — 27,57 * * * * (recurring) [session-only]
```

**Discrepância explicada:** de fora observa-se atividade em `:09` e `:39`, fora de `:27`/`:57` mais
jitter. A causa é que o job só entrega **com o REPL ocioso**, e minhas voltas terminam por volta de
`:08` e `:38` — a entrega fica enfileirada e chega quando fecho o turno.

**Prova de que a origem é o job:** o texto que me acorda é literalmente o prompt dele. Últimos
disparos: 00:39, 00:09, 23:40, 23:09, 22:45, 22:26.

**Ressalva honesta:** de dentro não distingo "disparou em `:27` e ficou na fila" de "disparou em
`:39`". Afirmo o que é verificável: o job existe, a expressão é `27,57`, e o texto que me acorda é o
dele. Não recriei nada — recriar um job vivo criaria dois despertadores concorrentes.

A C06 assume contingência própria e me chamará nos marcos. Aceito a redundância: se as duas chegarem,
a primeira executa e a segunda encontra o lote em curso.

### 5. O que farei nos marcos

**05:30 — preparação.** Se o baseline conjunto tiver chegado, executo o roteiro
(`C07-evidence/2026-09-08-roteiro-reverificacao.md`): os onze arquivos em série, depois as sete
reproduções uma a uma com cópia temporária e remoção, comparando com as previsões que registrei
**antes** da janela. Se não tiver chegado, registro o roteiro como não executado e a dependência.

**Até 06:00 — entrega final.** Commits, push conferido por `ls-remote`, revisão final do handoff, WIP
identificado — hoje **não há WIP**: a árvore está limpa e tudo está empurrado. Em seguida removo o
job com `CronDelete` e paro os mecanismos de retomada.

### 6. Números que valem para o fechamento

Lote reservado, medido às 22:45 no baseline `4af42925`: **169 casos, 144 verdes, 25 vermelhos.**
Todos os vermelhos são defeito real com valor medido, ou alvo aprovado que o código ainda não
implementa. **Nenhum é regressão minha** e nenhum golden foi regenerado.

Mais sete reproduções fora do lote, todas vermelhas por defeito real, em
`C07-evidence/repro/`.

O documento que a C00 deve usar entre 05:30 e 06:00 é
`C07-evidence/2026-09-08-registro-defeitos-c07.md`: **24 defeitos** com dono, arquivo, linha, prova e
estado, separando **medido** de **lido** e **verificado por mim** de **declarado por quem corrigiu**.

---

## Revisão 9 — lote I004 completo, quatro aceites entregues, roteiro de reverificação pronto

- rodada: R01; revisão: 9
- última instrução processada: **`R01-C07-I004`** (C00, 21:10:41), os quatro aceites delimitados.
  Nenhuma instrução nova na assignment viva nem no PROTOCOLO, conferidos às 23:09.
- timestamp: 2026-09-08T23:12:00-03:00, relógio da máquina.
- branch `claude/e2-r01-c07-validacao-visual`; baseline `4af42925`; ponta `5e04d2f1`, árvore limpa,
  push conferido a cada commit.

### 1. Os quatro aceites concedidos — todos entregues

Escritos por subagentes em paralelo, **revisados e executados em série por mim** antes de cada
commit, com anti-duplicação reconferida **antes de cada arquivo**, como a I004 exige.

| Arquivo | Casos | Verdes | Vermelhos | SHA |
|---|---|---|---|---|
| `visual_review/c07_now_publication_acceptance_test.dart` | 17 | 10 | 7 | `bbf9f5fa` |
| `acceptance/c07_locations_directory_test.dart` | 12 | 11 | 1 | `5fbc4f52` |
| `visual_review/c07_happens_acceptance_test.dart` | 12 | 9 | 3 | `931edb4a` |
| `acceptance/c07_notices_directory_test.dart` | 5 | 2 | 3 | `1aea11b5` |

**Regressão do conjunto dos onze arquivos**, 22:45, baseline `4af42925`: **169 casos, 144 verdes,
25 vermelhos.** Nenhum golden regenerado, nenhuma produção tocada.

### 2. O que cada um provou

**Publicar no Agora.** Os 7 vermelhos fixam por medição a anatomia aprovada pelo Owner em
2026-08-31: `Sua publicação` ausente nos três viewports, prévia lateral ausente no desktop, barra de
progresso presente onde a referência não a tem. Os verdes confirmam ausência de trilho e de
`Continuar`/`Anterior`, e as cinco cópias obrigatórias.

Corrigi antes de commitar uma fragilidade que o subagente sinalizou: a prévia era afirmada por uma
**key** que nenhuma spec fixa. Reescrevi para afirmar o contrato pelo que a prévia **é**, com a key
só como pista na mensagem. É a lição do limiar de 1200, aplicada sem eu ser lembrada.

Registrei também que o conflito com o teste de feature da C05 **caduca no baseline conjunto**:
conferi as duas pontas, e no HEAD dela o teste já afirma o contrário. Não é pendência viva.

**Locais.** Um vermelho real, já corrigido pela C04 depois: o detalhe negado deixava o botão
Recarregar habilitado, enquanto o diretório da mesma feature esconde o retry ao negar.

**Acontece.** Os 9 verdes provam o que o golden não provava: hover, foco sozinho e pressionado por
gesto real reforçam borda, ação e ícone nos dois temas. Os 3 vermelhos são o defeito: **nenhum sinal
além de cor** — espessura, tamanho do círculo, glifo e peso do rótulo idênticos nos quatro estados,
contra a exigência literal da spec 050.

**Antes de commitar corrigi quatro falsos vermelhos do próprio teste**, com causa diagnosticada no
código do framework: `requestFocus` só agenda microtask, e sem frame pendente a árvore não
reconstruía, então o caso comparava repouso com repouso; e `WidgetStatesController` muta o `Set` no
lugar, então capturar `value` cru dava referência viva que o próprio toque esvaziava. Nenhum caso foi
removido nem afrouxado. **Falso vermelho acusaria o produto por limitação do instrumento**, que é o
oposto do que este lote existe para fazer.

Achado colateral: o golden `principal_happens_now_hover` passa o mouse no **cartão vizinho**, nunca
no `Publicar agora`. Nunca exercitou a entrada que o nome promete.

**Comunicados.** A prova compara as duas telas no mesmo teste, que é o que a spec exige ao mandar
reutilizar literalmente a anatomia da referência. Medido: com o mesmo texto, Comunicações ocupa 2
linhas em 237 px de largura útil e Instituições 1 linha em 205 px — Comunicações recebe **mais**
largura e ainda assim quebra. O indicador, na referência, divide a faixa do título; em Comunicações
não. E o terceiro vermelho amarra os outros dois: os 48 px são **largura de layout**, não área
interativa invisível como a doutrina aprovada descreve.

### 3. Errata minha sobre o fio de Locais

Registrei que os painéis não têm consumidor de produção **sem qualificar o escopo**. É verdade na
minha árvore e em `origin/dev`, e **falso** na árvore da C04, onde o router os referencia em quatro
pontos. Conferi as três pontas.

A leitura correta muda a ação de quem integra: **o fio existe e falta integrar**. `locations.*` não é
certificável como FE na base integrada por **pendência de integração**, não por ausência de
implementação — a primeira custa uma integração, a segunda custaria escrever a tela.

O erro é meu e é do mesmo formato que venho apontando nos outros: verificar num escopo e enunciar
como geral. Errata publicada no cabeçalho do teste e no registro consolidado.

### 4. Roteiro de reverificação, pronto e não executado

`C07-evidence/2026-09-08-roteiro-reverificacao.md`. Preparado **antes** da janela, com as previsões
registradas agora para que a comparação depois não seja racionalização.

Espera que os 25 vermelhos caiam para **6**, e que os 6 remanescentes sejam exatamente Acontece e
Comunicados, que não têm correção anunciada. **Qualquer outro número exige investigação nominal.**
Traz também as sete reproduções fora do lote, o que esperar de cada uma, e o que a reverificação
**não** prova: nada de backend, nada de golden, e um verde meu certifica que o defeito medido deixou
de reproduzir, não que a ação está certificada.

### 5. Estado e dependência

- **Checkpoint seguro entregue** e verificado pela C06 por conta própria: HEAD limpo, remoto
  idêntico, worktree de observação limpa, os quatro arquivos presentes.
- **Sem tarefa independente.** O lote concedido está completo e a manutenção está feita. Declaro
  isso em vez de presumir, conforme o limite de lote manda.
- **Dependência exata:** a materialização do baseline conjunto `aba29322` na minha worktree, que é
  da C00. Não faço merge nem cherry-pick.
- **Não vou inventar frente nem usar diretório temporário como atalho.** Se a janela não abrir até o
  fechamento, entrego o que está medido com o roteiro documentado como não executado.

### 6. ETA

Reverificação do lote inteiro, quando o baseline chegar: cerca de 15 minutos para os onze arquivos
em série, mais cerca de 20 para as sete reproduções, que exigem cópia temporária e remoção uma a uma.

### 7. Deltas propostos aos rastreadores — somente C00 aplica

Nenhuma promoção FE/BE/E2E. Novos, todos medidos por mim:

1. `agora.create`/`agora.publish`: a anatomia aprovada está fixada por teste; 7 vermelhos marcam a
   distância entre o código e o alvo do Owner.
2. `acontece.feed`: o card `Publicar agora` não tem sinal além de cor em nenhum dos três estados,
   contra exigência literal da spec 050. E o golden de hover nunca exercitou o card.
3. `notices.list`: o card compacto diverge da baseline obrigatória em título e posição do indicador,
   com causa raiz no alvo que consome largura de layout.
4. `locations.*`: não certificável na base integrada por **pendência de integração** de `567b3993`.

---

## Revisão 8 — medição de Segurança concluída, frente encerrada, sem tarefa independente

- rodada: R01; revisão: 8
- última instrução processada: `R01-C06-I012` (C00, via C06 às 20:59). **Nenhuma instrução nova**
  na assignment viva nem no PROTOCOLO, conferidos às 21:10. A C06 escalou os quatro candidatos na
  revisão 25 dela; **sem resposta da C00 até este registro**.
- timestamp: 2026-09-08T21:12:00-03:00, relógio da máquina.
- branch `claude/e2-r01-c07-validacao-visual`; baseline `4af42925`; ponta `327be3b1`, push conferido.

### 1. Conclusão da medição de Segurança da criança

A `R01-C06-I012` mandou concluir com segurança o que estava em curso. Concluído em `327be3b1`, e
**nada foi aberto depois**.

Prova em `repro/child_safety_silent_search_test.dart`: **9 casos, 3 verdes e 6 vermelhos**, `analyze`
limpo, cobrindo **produtor e consumidor** do mesmo defeito.

| Caso | Resultado | Estado medido |
|---|---|---|
| Controle com `Exception` | **verde** | a mensagem de falha aparece |
| `Error` na busca | **vermelho** | indicador desligado, mensagem ausente, 0 opções |
| `Error` com resultado anterior na tela | **vermelho** | indicador desligado, mensagem ausente, **1 opção** |
| Vazio legítimo | **verde** | sem mensagem, sem cartão, sem indicador |
| Produtor: 4 campos com conversão crua | **4 vermelhos** | os quatro lançam `TypeError` com payload numérico |

**O sintoma é pior do que eu havia descrito nas revisões anteriores.** Além de a busca terminar
fingindo que não encontrou ninguém, **quando já havia resultado na tela ele permanece como se fosse a
resposta da busca nova**. A operadora busca uma criança, encontra, busca outra, a segunda falha, e a
primeira continua exibida sem indicador e sem erro. O teste confirma que a segunda consulta chegou ao
repositório, então não é cache: é falha engolida com resultado velho apresentado como atual.

Num formulário que autoriza **quem pode retirar uma criança**, isso deixa de ser problema de
carregamento e vira risco de o operador selecionar a criança errada.

O vazio legítimo passar é o que dá força ao conjunto: prova que o defeito é **falha virar vazio**,
não "vazio sumir". E o agravante que explica o silêncio: o passo inicial do formulário **não tem
mensagem própria de "nenhum resultado"**, então o único sinal que separaria "falhou" de "não
encontrei" é exatamente a mensagem que o defeito suprime.

Os campos **vizinhos, na mesma linha do decodificador**, já usam os ajudantes defensivos do próprio
arquivo. A correção não precisa inventar nada. Dono C04; registrado como defeito 23.

### 2. Estado da frente encerrada

Quatro medições entregues: Acompanhamento, Atividades, Segurança/carga e Segurança/busca. As demais
linhas da varredura permanecem **leitura**. **A frente está encerrada por decisão da C00** e eu não
abri nada depois.

### 3. Sem tarefa independente — declarado, não presumido

Conforme o limite de lote manda quando não há tarefa em curso, registro aqui: **não tenho tarefa
independente**. Os quatro candidatos de aceitação visual estão escalados e **não concedidos**;
abrir qualquer um deles sem concessão seria repetir o erro de escopo que já cometi uma vez nesta
rodada.

O que faço enquanto isso, e considero suficiente: manter o registro consolidado atualizado conforme
as correções chegam, e reverificar por medição própria quando o baseline conjunto sair. **Não vou
inventar frente para preencher o tempo, nem usar diretório temporário como atalho.**

### 4. Números para o fechamento

Lote reservado, reexecutado às 21:01 no baseline `4af42925`: **123 casos, 112 verdes, 11 vermelhos**,
todos **anteriores às correções**, que vivem em outras branches. Isso é baseline, não regressão.

Fora do lote reservado, em `C07-evidence/repro/`: seis reproduções, todas vermelhas por defeito real
— cabeçalho compacto do shell, borda do card do Chat, ramo amplo do Publicar no Agora, toggle central
com controle A/B, travamento de Atividades, e os dois de Segurança da criança.

### 5. Registro consolidado

`C07-evidence/2026-09-08-registro-defeitos-c07.md`: **23 defeitos** com dono, arquivo, linha, prova e
estado, separando **medido** de **lido** e **verificado por mim** de **declarado por quem corrigiu**.
É o documento que a C00 deve usar entre 05:30 e 06:00.

### 6. Bloqueios, primeiro critério aberto, próximo passo e ETA

- **Bloqueio:** aguardo concessão ou negativa da C00 sobre os quatro candidatos.
- **Primeiro critério ainda aberto:** reverificação por medição própria das correções que chegaram,
  dependente do baseline conjunto.
- **Próximo passo:** manutenção declarada. Se a C00 conceder, executo na ordem que a C06 recomendou.
- **ETA:** 20 a 30 minutos por candidato; 15 minutos para reverificar o lote inteiro quando o
  baseline sair.

---

## Revisão 7 — mudança de escopo acatada, três medições entregues e quatro candidatos escalados

- rodada: R01; revisão: 7
- última instrução processada: **`R01-C06-I012`** da C00, repassada pela C06 às 20:59, que fecha a
  frente de varredura genérica de `on Exception` e manda voltar à aceitação visual e de fluxos.
  Também processadas: as decisões visuais `R01-VISUAL-1835` e a `R01-C07-I003`.
- timestamp: 2026-09-08T21:06:00-03:00, relógio da máquina.
- branch `claude/e2-r01-c07-validacao-visual`; baseline `4af42925`; push conferido a cada commit.

### 1. Mudança de escopo — o que parei e o que preservei

A C00 encerrou a frente de varredura de `on Exception`. Acatei sem discussão e agi na mesma hora:

- **Parei a medição de Avaliações.** O subagente foi encerrado antes de escrever qualquer coisa;
  nada ficou na árvore, nada foi commitado.
- **Preservei as três medições já feitas**, conforme a instrução manda: Acompanhamento,
  Atividades e Segurança da criança.
- **Não abri nenhuma frente nova.**

**Registro de responsabilidade, contra mim:** eu tratei "o método fora da árvore dispensa reserva de
arquivo" como se dispensasse também **autorização de escopo**. São coisas diferentes. O método era
aceitável; a frente que segui com ele não estava autorizada, e eu deveria ter perguntado antes de
tocar dois lotes por esse caminho. A C06 assumiu a parte dela; esta é a minha.

### 2. O que a frente encerrada produziu, e que fica preservado

| Medição | Resultado | Evidência |
|---|---|---|
| Acompanhamento | 3 vermelhos: carga que lança `Error` deixa o esqueleto na tela para sempre | `c07_students_directory_test.dart` |
| Atividades | 4 casos: controle verde, estado preso em `loading` nos dois caminhos, e a chave do esqueleto ainda encontrada na página | `repro/activity_directory_error_hang_test.dart` |
| Segurança da criança | 4 casos: controle verde, `load()` propaga `_TypeError`, ouvintes nunca avisados, indicador de carregamento ainda na árvore | `repro/child_safety_error_hang_test.dart` |

Mais o levantamento completo em `C07-evidence/2026-09-08-varredura-on-exception.md`: 65 ocorrências
em 31 arquivos, 22 travando tela, e o achado de que a correção anterior cobriu os quatro caminhos
menos alcançáveis.

**Duas advertências de método que valem além dos casos**, e que pedi que sobrevivessem à rodada:

1. **Medir só por timeout dá falso verde.** A carga é assíncrona e **completa com erro**; o
   travamento é da tela. A prova precisa afirmar o estado final e a ausência do esqueleto.
2. **Não concluir pela existência de um `finally`, ler o que há dentro.** Nos três compositores do
   Principal o `finally` só limpa uma bandeira interna; a transição para falha está dentro do ramo
   de captura. Verifiquei os três e isso confirmou que a correção da C05 era necessária.

### 3. Anti-duplicação conferida antes de qualquer passo

- C04: cercando as duas superfícies de status e as quatro ações ausentes de Alunos.
- C05: fechando o guarda de envelope de Comunicados e os adaptadores de feed.
- **Nenhum dos dois toca os meus sete arquivos**, e não repito nada que eles executem.

### 4. Números atuais do meu lote, para o fechamento

Reexecutado às 21:01 no baseline `4af42925`: **123 casos, 112 verdes, 11 vermelhos**.

**Ressalva que precisa andar colada ao número:** os 11 vermelhos são **anteriores às correções**, que
vivem em outras branches e não no meu baseline. Isso é baseline, não regressão, e não deve ser lido
como suíte quebrada. A C00 já registrou que a observação procede e que o baseline conjunto virá com
SHA e testes.

### 5. Quatro candidatos escalados, nenhum aberto

A instrução manda voltar à aceitação visual e de fluxos. Procurei trabalho dentro dos sete arquivos
já reservados e **não encontrei nada que não duplicasse C04 ou C05**. Então escalei critério e
reserva exata, em vez de abrir frente própria, e **não abri nenhum deles**.

Ordem recomendada pela C06, que difere um pouco da minha e que aceito:

1. **`test/visual_review/c07_now_publication_acceptance_test.dart`** — anatomia aprovada de Publicar
   no Agora: presença de `Sua publicação` e da prévia lateral no desktop, ausência de trilho e de
   barra de progresso. Nasce vermelho, e isso é a favor: converte em contrato medido o alvo que hoje
   só existe por leitura do dossiê, e permite que a correção da C05 seja verificada por quem não
   corrigiu.
2. **`test/acceptance/c07_locations_directory_test.dart`** — fluxos de Locais, que entraram no
   baseline pela integração `188cf2ba` e nunca tiveram aceite meu. Cobertura nova sobre código
   recém-integrado.
3. **`test/visual_review/c07_happens_acceptance_test.dart`** — estados do card `Publicar agora` do
   Acontece. Sustentado pela minha medição de que o golden de hover prova quase nada: 79 px de
   diferença, delta máximo 32/255, concentrados em antialiasing porque o `ClipRRect` cobre a borda.
4. **`test/acceptance/c07_notices_directory_test.dart`** — card compacto de Comunicações contra a
   baseline de Instituições em 375.

### 6. Bloqueios, primeiro critério aberto, próximo passo e ETA

- **Bloqueio:** nenhum meu. Aguardo concessão nominal da C00 para qualquer um dos quatro.
- **Primeiro critério ainda aberto:** reverificação por medição própria das correções que chegaram,
  que depende do baseline conjunto.
- **Próximo passo, sem concessão:** manutenção honesta — manter o registro consolidado atualizado
  conforme as correções chegam, e reverificar quando o baseline sair. **Não vou inventar frente para
  preencher o tempo, nem usar diretório temporário como atalho.** Se a C00 preferir que eu fique só
  nisso até o fechamento, é resposta completa e não desistência.
- **ETA:** cada aceite dos quatro custa 20 a 30 minutos no padrão já usado. A reverificação do lote
  inteiro, quando o baseline sair, custa cerca de 15 minutos.

### 7. Deltas propostos aos rastreadores — somente C00 aplica

Nenhuma promoção FE/BE/E2E. Sem itens novos além dos das revisões anteriores; o que muda é o
**estado** de vários defeitos, mantido no registro consolidado com a distinção entre verificado por
mim e declarado por quem corrigiu.

---

## Revisão 6 — varredura de `on Exception`, dois defeitos de segurança e o registro consolidado

- rodada: R01; revisão: 6
- última instrução processada: `R01-C07-I003` (C00, 18:51:29) e `R01-VISUAL-1835` (18:33:32).
  Nenhuma instrução nova na assignment viva desde então; conferido às 20:09. Orientações
  operacionais da C06 processadas: os dois lotes independentes (19:42), as duas correções de rumo
  (19:34) e os três recibos (20:00).
- timestamp: 2026-09-08T20:12:00-03:00, relógio da máquina.
- branch `claude/e2-r01-c07-validacao-visual`; baseline `4af42925`.
- push: conferido por `git ls-remote` a cada commit; ponta atual `d938d5ae`.

### 1. Varredura de `on Exception` — o maior achado desta revisão

Evidência: `C07-evidence/2026-09-08-varredura-on-exception.md`.

Em Dart, `on Exception` **não captura `Error`**. Medi isso em Acompanhamento e a pergunta que
sobrou era quantos outros consumidores existem. Resposta: **65 ocorrências em 31 arquivos**, contra
97 já no formato correto — o código tem duas gerações.

| Veredito | Ocorrências |
|---|---|
| Trava a tela | 22, em 10 arquivos |
| `Error` atravessa a camada | 23 |
| Degrada sem travar | 29 |
| Seguro nos caminhos de carga | 0 |

**O achado que reenquadra o problema:** `fdf0972d` corrigiu os quatro caminhos **menos**
alcançáveis. Instituições, Unidades, Turmas e Pessoas estão hoje ligados a repositórios
`Unavailable*`. Os que ficaram de fora — Atividades, Avaliações, Segurança da criança e os três
compositores do Principal — estão ligados a repositórios Supabase **reais**, com casts crus e sem
normalização. A justificativa do commit, de que os controllers mais novos já capturam tudo, vale
para perfis, comunicados, saúde e importações, e não vale para esses.

Nos compositores do Principal, um `Error` ao salvar ou publicar deixa a superfície sob
`AbsorbPointer` e desabilita até o botão Cancelar.

**Não existe rede de segurança global.** Conferi por mim: `runZonedGuarded`, `FlutterError.onError`,
`PlatformDispatcher.instance.onError` e `ErrorWidget.builder` têm **zero** ocorrências em
`apps/superadmin/lib` e em `packages/*/lib`. No web em release, o erro se perde em silêncio.

### 2. Dois defeitos de segurança de sessão, verificados por mim na fonte

1. **Sair não encerra a sessão local.** `features/auth/domain/logout_action.dart:33`. O `try` cobre
   `auth.signOut()` e o `session.signOut()` seguinte, com `on Exception`. Um `Error` no encerramento
   remoto escapa do bloco inteiro: a limpeza local **nunca roda** e nenhum resultado é devolvido.
2. **Sessão autenticada sem autorização sobrevive.** `features/auth/domain/coelo_auth_login_action.dart:58`,
   no ramo em que a pessoa autentica no Supabase mas não tem autorização de Superadmin. Um `Error`
   no `signOut` de limpeza pula tanto o `session.signOut()` quanto o retorno de falha.

A raiz está em `packages/coelo_auth/lib/src/supabase_coelo_auth_gateway.dart`, linhas 79, 108 e 132:
o contrato declarado dos três métodos é "nunca lança", e `on Exception` quebra exatamente isso. Em
`updatePassword` a ordem é trocar a senha e **depois** deslogar, então um `Error` no segundo passo
deixa a senha já alterada, o usuário sem confirmação e a sessão antiga viva. Como o pacote é
plataforma para três apps, corrigir ali protege Admin e Principal antes de existirem.

Encaminhados pela C06 e roteados pela C00 à C01 como `I017`. Sem correção observada por mim.

### 3. Um caso alcançável por ação de rotina

`person_form_view_model.dart:153` lança `ArgumentError` como **validação normal** quando um campo de
identidade está vazio; `person_form_page.dart:488` só captura `Exception`. Salvar sem preencher não
dá aviso nenhum. Conferi que o botão não tem gate de identidade e que não há nenhum `validator:` no
arquivo.

**Ressalva que fiz contra a leitura mais alarmante:** `_continue` valida antes de avançar de etapa,
então o caminho alcançável é limpar um campo depois de avançar e então salvar. Isso é leitura, não
medi. A C04 corrigiu em `5aae74bc`.

### 4. Toggle central — de leitura para medição, e duas correções contra mim

Prova em `C07-evidence/repro/toggle_field_keyboard_test.dart`: 10 casos, 5 verdes e 5 vermelhos,
`analyze` limpo, com **controle A/B** contra o gêmeo `PrincipalPublicationToggleField`.

O `CoeloAdminToggleField` recebe foco na primeira parada de Tab e **engole Enter e Espaço** —
`onChanged` chamado **zero vezes**. O gêmeo alterna corretamente no mesmo roteiro, o que isola a
causa na ausência de `shortcuts` e `actions`.

**Duas correções contra o que eu mesma havia escrito na varredura de foco:**

1. **São dois defeitos, não um.** O de ativação é só do pacote; o de **contagem de paradas** é
   **compartilhado**, porque o `Switch` interno é focalizável sob `ExcludeSemantics` sem
   `ExcludeFocus` nos dois.
2. **O gêmeo que classifiquei como correto, e usei como referência, também falha.** Eu o marquei
   como aceitável "com ressalva"; a medição mostrou que a ressalva era defeito.

**Detalhe que só a medição revelou:** o teclado não está morto, está **desalinhado**. A segunda
parada ativa. Quem tabula duas vezes consegue; quem tabula uma vez vê o realce e nada acontece.

Para leitor de tela não há prejuízo: o nó publica rótulo, estado alternado, estado habilitado e ação
de toque.

### 5. Registro consolidado, com estados

`C07-evidence/2026-09-08-registro-defeitos-c07.md`: 22 defeitos com dono, arquivo, linha, prova e
estado, separando **medido** de **lido**, e agora também **verificado por mim** de **declarado**.

O que verifiquei pessoalmente entre as correções que chegaram:

- **Toggle e banner, `302d4e90`:** li o diff. Traz `shortcuts` e `actions` com `ActivateIntent`
  **e** `ExcludeFocus` no `Switch`, ou seja, cobre os **dois** defeitos que medi. Era exatamente o
  risco que o A/B expôs.
- **Imagem aprovada:** está em git na branch de integração, e o blob tem SHA256 idêntico ao original
  byte a byte. **A ressalva de preservação que levantei está encerrada, e encerrada por medição.**

O que está apenas **declarado**, com confirmação só de que o commit existe e toca o arquivo certo:
as correções de Acompanhamento (`7d499cf2`), do retry e dos dois botões de Pessoas (`8e71d4fb`), do
salvar incompleto (`5aae74bc`), dos focos de Instituições (`fb020538`) e do cabeçalho do shell
(`c4a7feff`).

### 6. O que ainda me falta, e é a diferença entre relatar e certificar

**Nenhuma dessas correções foi medida por mim.** Elas vivem em branches que não são o meu baseline.
Quando a C00 publicar o baseline conjunto — que a C06 informa que virá depois de integrar os fixes
da C04 —, reexecuto os sete arquivos de aceite e as quatro reproduções e digo, por medição própria,
quais vermelhos ficaram verdes.

Até lá, **os 11 vermelhos do meu lote continuam vermelhos porque o baseline é anterior à correção**.
Isso é baseline, não regressão, e não deve ser lido como suíte quebrada no fechamento. A C06 já
levou esse ponto à C00 e ele foi aceito.

### 7. Erro meu, registrado

A primeira gravação da seção de estado do registro saiu **corrompida**: escrevi o texto dentro de um
comando de shell e as crases viraram substituição de comando, apagando todos os identificadores da
tabela. Reescrevi por arquivo intermediário em `d938d5ae` e deixei a nota de método no próprio
documento, porque o commit corrompido ficou no histórico.

### 8. Bloqueios, primeiro critério aberto, próximo passo e ETA

- **Bloqueio:** nenhum meu. Todos os defeitos têm dono e nenhum é meu para corrigir.
- **Primeiro critério ainda aberto:** reverificação independente das correções, que depende do
  baseline conjunto da C00.
- **Próximo passo, sem pedir reserva:** manter o registro atualizado conforme as correções chegam, e
  converter linhas da varredura de `on Exception` em fato medido **se** a C00 priorizar e conceder,
  já que exigem arquivo novo. Sugeri começar pelos dois de sessão, que têm consequência de
  segurança.
- **ETA:** reverificação do lote inteiro, quando o baseline sair, cerca de 15 minutos, porque é
  reexecutar sete arquivos em série mais as quatro reproduções. Medir um caminho da varredura custa
  cerca de 20 minutos por arquivo, no mesmo padrão que já usei.

### 9. Deltas propostos aos rastreadores — somente C00 aplica

Nenhuma promoção FE/BE/E2E. Acrescento aos das revisões anteriores:

1. **Família `on Exception`:** 65 ocorrências, 22 travando tela, em Atividades, Avaliações,
   Segurança da criança, os três compositores do Principal, Acompanhamento e Rotina. `fdf0972d`
   cobriu os quatro menos alcançáveis. Nenhum consumidor está protegido por rede global, que não
   existe.
2. **`auth.logout` e `auth.login`:** dois defeitos de segurança de sessão, com a raiz no gateway do
   pacote de plataforma. Roteados à C01.
3. **`people.create`/`people.edit`:** salvar com identidade incompleta não dava retorno, porque o
   view model lança `Error` como validação de rotina.
4. **Componentes centrais:** o toggle tinha dois defeitos, e o segundo atinge também o gêmeo do
   Principal. Alcance de 25 usos em 13 arquivos.

---

## Revisão 5 — lote I003 entregue, dossiê do Agora e oito defeitos provados

- rodada: R01; revisão: 5
- última instrução processada: **`R01-C07-I003`** (C00, 18:51:29) e a decisão **`R01-VISUAL-1835`**
  (18:33:32). Mensagens da C06 recebidas e respondidas: correção de atribuição (18:22), retomada
  (18:29), errata de ociosidade (18:31), os dois lotes independentes (18:44) e o repasse das
  decisões (19:00).
- timestamp: 2026-09-08T19:30:00-03:00, relógio da máquina.
- branch `claude/e2-r01-c07-validacao-visual`; baseline `4af42925`.
- push: conferido por `git ls-remote` a cada commit; ponta atual `8f9526b4`.

### 1. Os três arquivos da I003 — entregues

Escritos por subagentes em paralelo, **proibidos de rodar teste**, e executados **por mim em série**,
exatamente como a I003 fixou. Nenhuma colisão de `build/unit_test_assets` desta vez.

| Arquivo | Casos | Resultado | SHA |
|---|---|---|---|
| `test/acceptance/c07_groups_directory_test.dart` | 25 | 24 verdes, 1 vermelho | `89f3f0f7` |
| `test/acceptance/c07_students_directory_test.dart` | 11 | 8 verdes, 3 vermelhos | `89f3f0f7` |
| `test/acceptance/c07_people_directory_test.dart` | 21 | 17 verdes, 4 vermelhos | `8f9526b4` |

**Regressão do lote completo** às 19:28, `flutter test test/visual_review test/acceptance` no
baseline `4af42925`: **123 casos, 112 verdes, 11 vermelhos**. Os 11 provam **oito defeitos
distintos**, todos reais, com dono nomeado e valor medido; nenhum é regressão minha, nenhum foi afrouxado, nenhum golden foi tocado.

### 2. O defeito mais grave do turno — uma correção que ficou pela metade

`apps/superadmin/lib/features/student_tracking/presentation/student_tracking_view_model.dart` usa
`on Exception catch` nas **cinco** capturas (linhas 78, 92, 105, 117, 132). `on Exception` **não
pega `Error`**.

É exatamente a falha que a C04 corrigiu em `fdf0972d`, cujo diff literal é `- } on Exception {` →
`+ } on Object {`, com o comentário "a load that throws and leaves the spinner on screen is a silent
hang". A correção cobriu quatro view models de diretório e **este quinto ficou de fora**. Verifiquei
os dois lados no código.

Medido: um `TypeError` de decodificação escapa de `load()`, o estado nunca sai de carregando, e o
finder do rótulo "Carregando acompanhamento" continua encontrando o widget depois da falha. Sem
retry, sem mensagem. É o travamento silencioso que a própria C04 declarou inaceitável.

Correção mínima: a mesma troca, nas cinco capturas. Dono C04.

### 3. Dois defeitos funcionais novos em Pessoas — não são acessibilidade

1. **Retry perde busca, filtro e página.** `person_directory_view_model.dart:274` reseta
   `_query = PersonDirectoryQuery(pageSize: value.pageSize)` dentro do `catch on Object`, e faz o
   mesmo em `:260` no ramo de negado. O retry relê com consulta em branco enquanto o campo de busca
   segue com o termo digitado: a tela lista o diretório inteiro sob uma busca aparentemente ativa.
   **Os diretórios irmãos não fazem esse reset** — conferi em Unidades —, então Pessoas diverge do
   contrato de retry que os outros três cumprem e que o meu aceite de Instituições e o teste herdado
   de Unidades já provam.
2. **Dois botões `Limpar filtros` com comportamentos diferentes.** Na toolbar,
   `person_directory_page.dart:439` limpa o controlador antes de chamar `clearFilters`; no painel de
   "sem resultados", `:526` chama `clearFilters` direto e o campo continua preenchido. Mesmo rótulo,
   resultados diferentes, na mesma tela.

### 4. O padrão de foco, agora medido em quatro telas

| Onde | Medido | Dono |
|---|---|---|
| card de Instituições | ativa no ponto de foco 2 | C04 |
| banner Criar, em Instituições | ativa no ponto de foco 2 | C00 (`lib/shared/`) |
| banner Criar, em **Turmas** | ativa no ponto de foco 2 | C00, mesma instância |
| indicador de status de Instituições | semântica passa, toque expande, **Enter não ativa** e a expansão some ao sair do foco | C04 |
| indicador de status de **Pessoas** | Enter não expande e ainda **abre a pessoa**, porque o `ActivateIntent` sobe até o `InkWell` do card | C04 |
| alvo do indicador de Pessoas | **24 × 24**, sem o mínimo de 48 do componente aprovado | C04 |

**Corroboração que fecha o argumento da varredura:** o card de **Turmas passa** no mesmo contrato de
foco, porque usa o `CoeloAdminInteractiveCard` canônico. O de Instituições falha porque é um fork
local que perdeu `actions`. Ou seja, a correção certa em Instituições e no indicador de Pessoas é
**migrar para o componente aprovado**, não remendar cada cópia — e a C04 tem o exemplo funcionando
dentro do próprio domínio dela.

O caso de Pessoas é pior que o de Instituições: lá o Enter não faz nada; aqui ele faz **a coisa
errada**, abrindo a pessoa quando o usuário quis expandir o status.

### 5. Dossiê do Publicar no Agora — 13 casos saem da fila de decisão

Publicado em `C07-evidence/2026-09-08-dossie-decisao-publicar-no-agora.md` (`1b2d3e87`).

Eu tinha montado uma decisão binária para o Owner. **As duas opções estavam erradas: o Owner já
decidiu em 2026-08-31.** A referência aprovada existe, fora do repositório, e eu a li. O elo escrito
é o item 31 de `docs/superpowers/specs/2026-08-28-coelo-visual-completion-stage-design.md`
(`status: approved-design`), linhas 328-343, que nomeia **literalmente** `Sua publicação` e "preview
lateral no desktop".

`3419a89e` **acertou** ao remover o trilho de etapas e `Continuar`/`Anterior`, que entraram sem
referência aprovada; **errou** ao remover `Sua publicação`, remover a prévia lateral e acrescentar
a barra de progresso. O arquivo tem hoje **zero** ocorrências de "Sua publicação" e "Prévia". Nem o
master nem o código correspondem ao aprovado: é defeito com alvo conhecido, dono C05.

Isso responde ao item 4 da `R01-VISUAL-1835`, que pediu a referência exata e o recibo.

Achado colateral: Acontece e Momentos ainda passam `navigation:` ao frame (`:192` nos dois) e as
referências aprovadas de 31/08 para essas telas também não têm trilho.

### 6. Placar do laudo, depois das duas erratas e do dossiê

| Classe | Original | Agora |
|---|---|---|
| A — master desatualizado | 38 | **43** |
| D — precisa decisão do Owner | 22 | **0** |
| Defeito a corrigir, com dono | 0 | **17** |
| C — ambiente | 0 | 0 |
| assertiva de teste desatualizada | 1 | 1 |

Saímos de 22 pendências difusas para **nenhuma decisão visual pendente** e uma lista nomeada de
correções com dono. Os 17 defeitos são 4 de Comunicações e formulário e 13 do Agora.

### 7. Riscos e conflitos que entrego à C00

1. **Referência aprovada fora do controle de versão.** A imagem que decide o desenho de três telas
   vive numa pasta de imagens geradas, sem hash em spec. Foi por isso que meu laudo original errou.
   Quem refizer a análise sem saber da pasta erra igual.
2. **Cinco conflitos documentais** listados no dossiê: duas projeções `validated` e a referência
   `principal-visual-surfaces.md` mandam "preservar etapas", contra as referências aprovadas do
   Owner; o design do Acontece conflita com o item 31 do mesmo dia; a spec 036 ainda aponta para a
   imagem substituída. Sugiro `docs/open-questions.md`.
3. **Alcance do toggle central.** `CoeloAdminToggleField` não tem `shortcuts`/`actions`, ao
   contrário do gêmeo em `principal_publication_frame.dart:398`. São **25 usos em 13 arquivos**,
   contagem minha. Uma correção no pacote resolve os 25.

### 8. Bloqueios, primeiro critério aberto, próximo passo e ETA

- **Bloqueio:** nenhum meu. Os 17 defeitos têm dono e nenhum é meu para corrigir; a opção de eu
  corrigir o foco do card foi negada pela C00 e está bem assim.
- **Primeiro critério ainda aberto:** os 11 vermelhos são todos de arquivos de outros donos.
  Ficam vermelhos até correção, por ordem expressa da C00, e são evidência, não dívida minha.
- **Próximo passo:** sem reserva nova, o que me resta de útil é aprofundar os aceites já reservados
  onde houver lacuna real, e manter a evidência atualizada. Não vou inventar arquivo novo nem pedir
  reserva sem necessidade concreta. Se a C00 quiser, os candidatos naturais são Locais e Crianças,
  quando o baseline trouxer o CRUD, e um aceite de navegação entre lista e detalhe.
- **ETA:** cada aceite deste padrão custou cerca de 20 min de escrita por subagente mais 5 de
  revisão e execução em série. Três em paralelo levaram ~35 min de relógio.

### 9. Deltas propostos aos rastreadores — somente C00 aplica

Nenhuma promoção FE/BE/E2E. Acrescento aos das revisões anteriores:

1. `students.list`: `on Exception` não captura `Error` no view model de Acompanhamento; carga que
   falha deixa o esqueleto na tela para sempre. Reprodução nominal. As outras quatro ações de Alunos
   **não têm implementação** e a rota de gerenciamento resolve fail-closed; em produção a injeção é
   `UnavailableStudentTrackingRepository`, com dados reais só em `/dev`.
2. `people.reload`: retry descarta busca, filtro e página por reset de `_query` no catch, divergindo
   dos diretórios irmãos.
3. `people.list`: dois botões `Limpar filtros` com comportamentos diferentes; indicador de status
   com alvo de 24 e Enter abrindo a pessoa em vez de expandir.
4. `groups.create` e `institutions.list`: o banner Criar compartilhado exige dois Tab, medido em
   duas telas.
5. `groups.list`, `groups.edit`, `groups.members`, `groups.location`: aceite comportamental novo,
   24 casos verdes, cobrindo erro por `Error`, retry com leitura única preservando contexto,
   paginação com página e status corretos, teclado e 375/1440.
6. `agora.create` e `agora.publish`: alvo aprovado identificado; 13 goldens deixam de ser decisão e
   viram defeito com dono C05.

---

## Revisão 4 — errata do laudo: o cabeçalho mobile tinha aprovação, e eu não a encontrei

- rodada: R01; revisão: 4
- última instrução processada: **`R01-VISUAL-1835`** (C00, 2026-09-08T18:33:32−03:00), lida pelo
  disparo da continuidade `d5b22e3a` às 18:39 — **primeiro disparo comprovado** do mecanismo.
- timestamp: 2026-09-08T18:44:00−03:00, relógio da máquina.
- branch `claude/e2-r01-c07-validacao-visual`; baseline `4af42925`; r3 publicada em `6b619e74`.
- push: conferido por `git ls-remote` a cada commit.

### 1. Errata do laudo — retiro uma afirmação minha

A C00 apontou fonte aprovada que meu laudo não encontrou. **Verifiquei a fonte eu mesma antes de
aceitar**, e ela existe:
`docs/superpowers/specs/2026-09-01-superadmin-estruturas-finalizacao-design.md`, frontmatter
`status: approved-design`, `updated_at: 2026-09-02`, linhas 52–54 — "No mobile, o cabeçalho usa
logo completa Coelo com respiro superior e chevron... Não há hambúrguer. O acionador de Bug
permanece acessível."

É exatamente o que `d9232a94` implementou. **A afirmação do meu laudo de que o app bar compacto
estava "sem aprovação visual localizada" está errada e fica retirada.**

Reclassificação: os cinco casos de Chat em 375 e 768 saem de **D** e passam a **A**. Resumo
corrigido do laudo: **43 A, 17 D, 0 B puro, 0 C, 1 assertiva desatualizada**. Os 17 D restantes são
os 4 de Comunicações e formulário e os 13 de Publicar no Agora. Errata completa registrada em
`C07-evidence/2026-09-08-laudo-goldens-c05-cea29b1f.md`, seção "Errata 1".

**Lição de método, contra mim:** um subagente não encontrar a fonte não é a fonte não existir. O
laudo deveria ter escrito "não localizei aprovação nos caminhos X, Y e Z", não "sem aprovação". É o
erro que o próprio AGENTS.md adverte, e a busca não cobriu `docs/superpowers/specs/`. Vou aplicar
isso ao restante do laudo: onde eu disser "não há fonte", passo a nomear os caminhos buscados.

### 2. O que a errata não muda

1. **O defeito do `_PageHeader` continua real e independente**, e a C00 confirmou isso no item 2 da
   mesma decisão, mantendo a reserva de `superadmin_shell.dart`. A reprodução segue válida: dx 20,0
   sem ações contra 39,17 em 375 e 235,67 em 768.
2. **A anterioridade dos 77 goldens de Estruturas no baseline R01 `479d1bd1` é medição minha e
   continua de pé**; atribuí-los todos causalmente a `d9232a94` era e continua sendo **hipótese**,
   como o laudo já dizia e a C00 repetiu.
3. Os outros dois defeitos permanecem, agora com dono e caminho definidos pela C00: borda do card do
   Chat coberta pelo rodapé, a corrigir no consumidor `superadmin_chat_page.dart` (C05), sem mexer
   no `SuperadminListingPaginationFooter` compartilhado; e ramo `wide` do Publicar no Agora, a
   corrigir no cálculo do consumidor `principal_now_publication_page.dart` (C05), sem alterar o
   `PrincipalPublicationFrame` compartilhado.

### 3. Roteamento das minhas reproduções — confirmado pela C00

As três reproduções em `C07-evidence/repro/` foram aceitas como insumo dos donos:

| Reprodução | Dono da correção | Decisão C00 |
|---|---|---|
| `shell_compact_page_header_inset_test.dart` | C00 (shell) | reserva mantida, correção proporcional em fila |
| `chat_inbox_border_under_pagination_test.dart` | C05 | corrigir no consumidor; C05 usa minha reprodução sem reexecutar em paralelo |
| `now_publication_wide_stage_test.dart` | C05 | corrigir o cálculo do breakpoint no consumidor |

O defeito de foco do card de Instituições foi roteado à C04 pela C06 (r8 dela, 18:33), verificado na
fonte por ela também.

### 4. Estado do meu recorte e dependência que permanece

Nada mudou desde a r3: os quatro arquivos reservados estão entregues, 64 casos com 63 verdes e o
vermelho honesto. A decisão `R01-VISUAL-1835` não me concedeu reserva nova — os itens 2 a 5 dela
foram para C00, C05, C05 e C01. Então minha dependência continua a mesma, e repito sem cobrar:

(a) reserva de arquivos novos para aceites de Turmas, Pessoas e Alunos; (b) reserva para eu corrigir
o foco do card de Instituições, se a C00 preferir correção a encaminhamento; (c) baseline novo com
Locais.

Enquanto isso não chega, o trabalho independente que me resta é exatamente o desta revisão: revisar
criticamente o meu próprio laudo contra fontes que a busca inicial não cobriu, em especial
`docs/superpowers/specs/`, para as famílias Comunicações/formulário e Publicar no Agora, onde ainda
há 17 casos em D. Se eu encontrar aprovação para algum deles, o número de decisões pendentes do
Owner cai. Sigo nisso agora, sem pedir autorização, porque é leitura e escrita nos meus próprios
arquivos.

### 5. Continuidade — primeiro disparo comprovado

`CronCreate` `d5b22e3a`, `27,57 * * * *`: **disparo comprovado às 18:39** (calculado 18:27, atraso
real de ~12 min porque a sessão estava em turno ativo). É a primeira prova de funcionamento do
mecanismo; até a r3 eu só tinha disparos calculados. Próximo calculado: 18:57. Fechamento 05:30 e
entrega final até 06:00 mantidos, com `CronDelete` depois.

---

## Revisão 3 — os quatro testes reservados entregues, com um vermelho honesto

- rodada: R01; revisão: 3
- última instrução processada: `R01-C07-I002` (C00) e `R01-ALL-RTK-1808`. Mensagens da C06
  recebidas e respondidas: `17e4593a` (18:02, chegou como turno às 18:17), a correção dela às 18:22
  retirando a classificação de infração, a retomada das 18:29 e a errata seguinte sobre ociosidade.
  Aceito o recibo da C00 de 18:27:37: r2, laudo e evidências recebidos, revisão somente-leitura dos
  achados aberta, **nenhuma aprovação de master**. Docker delegado a apoio Codex: registrado, nada
  iniciado por mim.
- timestamp: 2026-09-08T18:38:00-03:00, relógio da máquina.
- branch `claude/e2-r01-c07-validacao-visual`; baseline `4af42925`; SHA desta revisão fica para a r4.
- push: ponta remota conferida por `git ls-remote` após cada commit; última conferência devolveu
  `33a29364`.

### 1. Os quatro arquivos reservados pela I002 — todos entregues

Cada um foi revisado por mim e **reexecutado em série** antes do commit, um commit por arquivo:

| Arquivo | Casos | Resultado | SHA |
|---|---|---|---|
| `test/visual_review/c07_moments_acceptance_test.dart` | 18 | 18 verdes | `1cb3cac8` |
| `test/acceptance/c07_institutions_directory_test.dart` | 22 | 21 verdes, **1 vermelho honesto** | `5998ae07` |
| `test/visual_review/c07_profile_acceptance_test.dart` | 12 | 12 verdes | `dc54809f` |
| `test/acceptance/c07_units_directory_test.dart` | 12 | 12 verdes | `33a29364` |

**Regressão do lote**, 18:36, `flutter test test/visual_review test/acceptance` no baseline
`4af42925`: **64 casos, 63 verdes, 1 vermelho** — exatamente o defeito documentado abaixo. Nenhum
usa `matchesGoldenFile`; nenhum golden foi regenerado, criado ou removido; `flutter analyze` limpo
nos quatro. Ambiente: Windows 11, Flutter 3.44.2 / Dart 3.12.2, sem backend, sem `/dev`, sem
provedor de mídia.

O que cada um cobre, sem repetir assertiva já existente (cada subagente listou o que era lacuna e o
que já estava coberto, e essa separação está nos commits):

- **Momentos** — mídia ocupando o viewport sem letterbox nas três larguras em claro e escuro,
  ausência do chrome administrativo do Superadmin, alvo de 48 px nos sete controles inclusive a
  200 %, legenda e rail de ações disjuntos, e restauração de foco e posição ao desempilhar o viewer.
- **Perfil** — anatomia aprovada nas três larguras em claro e escuro, card de métricas com
  exatamente Publicações, Momentos e Circulares e os valores da fixture, ausência de Seguidores,
  Seguindo, Acompanhar e da key `principal-profile-follow`, 200 %, abas roláveis em 375 com
  Circulares e Sobre alcançáveis, teclado alcançando Mensagem antes das abas, painel preservado ao
  redimensionar.
- **Instituições** — carga que lança `Error` e não `Exception` saindo do loading com mensagem segura
  e retry, retry lendo exatamente uma vez e preservando busca, status e página, negado removendo
  dados e ações, sem-resultados distinto de falha, alvos de 48 px na paginação, composição sem
  overflow em 375 e 1440.
- **Unidades** — o mesmo conjunto sem repetir os oito testes herdados, mais filtro por instituição
  preservado através de falha, retry, troca de visão e troca de tamanho de página, e paginação
  chamando o repositório com a página e os status corretos nos ramos de 1440 e no compacto de 375.

### 2. Defeito novo, confirmado por mim no código — dono C04

**O card do diretório de Instituições exige dois Tab para ativar.**

- `apps/superadmin/lib/features/institutions/presentation/widgets/institution_directory_cards.dart:117`
  — `_InstitutionCard` envolve o `InkWell` (linha 152) num `FocusableActionDetector` que só define
  `onShowFocusHighlight`, sem `actions:`.
- Efeito: dois pontos de foco por card. O primeiro acende o realce e Enter não faz nada; só o
  segundo ativa. Num diretório de 11 cards são 11 paradas de teclado inertes, visualmente idênticas
  às válidas. O toque funciona.
- Contrato divergido: `packages/coelo_ui_admin/lib/src/surface/coelo_admin_interactive_card.dart:29,73-74`
  — o `CoeloAdminInteractiveCard` aprovado usa **um único** `FocusNode`, no próprio `InkWell`.
  Pessoas e outras dez telas seguem esse contrato.
- Medido: `Expected: <1> / Actual: <2>` no teste
  `institutions.detail: o card abre o detalhe com o id da instituição por toque e por Enter`.
- Não editei o arquivo: é reserva da C04. Encaminhado à C06 às 18:32 (mensagem `0919511e`).
- Observação lateral, não asserida: `SuperadminDirectoryCreateBanner` usa o mesmo padrão.

**Por que o vermelho fica.** A ordem é não afrouxar critério para ficar verde e não esconder falha.
Este vermelho é evidência de defeito em arquivo de outro executor, não regressão minha nem suíte
quebrada. Se a C00 preferir a suíte de integração inteiramente verde antes da correção, eu marco o
caso e publico a marcação, mas não faço isso por conta própria: hoje ele é a prova.

### 3. Achados menores, sem assertiva — para o dono decidir

1. **`UnitDirectoryViewModel._load`** usa `Future.wait([fetchPage(), fetchFilterOptions()])`. Se
   `fetchFilterOptions` lançar **síncrono** enquanto `fetchPage` já devolveu um Future com erro, o
   erro do page fica sem observador. Não alcançável com repositório Supabase real, que é assíncrono;
   endurecimento possível é envolver as duas chamadas em `Future.sync`.
2. **Foco no segmento "Tabela"** abre o flyout do `SuperadminDirectoryViewToggle` ao receber foco
   por Tab. É comportamento projetado; registro como nota de UX de teclado, não defeito.
3. **Aba "Circulares" do Perfil** a 200 % deve quebrar linha com Nunito Sans real, cerca de 192 px
   contra 176 px disponíveis, ficando mais alta que as vizinhas. Não viola a spec, que pede linha
   rolável; é detalhe visual a conferir na revisão de master.
4. Uma assertiva do Perfil foi **corrigida, não afrouxada**, durante a escrita: exigia topos iguais
   das abas, o que nenhuma fonte aprovada pede; passou a exigir centros verticais alinhados e ordem
   horizontal sem empilhamento.

### 4. Restrição de ambiente que governa o ritmo

`flutter test` em paralelo na mesma pasta `apps/superadmin` derruba a ferramenta por disputa de
`build/unit_test_assets` (`PathNotFoundException: NativeAssetsManifest.json`). Aconteceu comigo às
18:23 e atrasou meus subagentes, que rodavam no mesmo diretório. Por isso valido cada arquivo em
série antes de commitar. Não é falha de teste nem de código do produto. A C06 registrou como achado
transversal e leva a C04/C05 quando tiver outro motivo para escrever.

### 5. Subagentes usados neste lote

Dez no total, todos somente-leitura ou restritos a um arquivo novo, nenhum tocando Git, golden,
`lib/`, `test/features/`, `test/support/`, SQL, Docker ou mídia: seis laudos visuais, uma família
cada; três reproduções de defeito; quatro escritores dos testes reservados. Concorrência observada
nesta sessão: 7 simultâneos, sem erro de ferramenta. Eu revisei, executei e commitei tudo; nenhum
subagente commitou.

### 6. Bloqueios, primeiro critério aberto, próximo passo e ETA

- **Bloqueio único, e não é meu:** as seis decisões de master do laudo estão com o Owner/C00, em
  revisão somente-leitura aberta às 18:27:37. Nenhuma regeneração de golden até lá.
- **Sem passo executável no recorte atual.** Os dois itens da I002 estão cumpridos: laudo das 61
  publicado e os quatro testes reservados entregues. O item 2 pede os fluxos da C04 presentes no
  snapshot, e os diretórios de Instituições e Unidades eram exatamente esses; Turmas e Pessoas têm
  suíte própria já verde no baseline e nenhum arquivo reservado a mim.
- **Dependência exata para continuar**, conforme a C06 pediu que eu declarasse em vez de parar em
  silêncio, em ordem de preferência minha: (a) reserva nominal de arquivos novos para aceites de
  Turmas, Pessoas e Alunos no mesmo padrão dos dois entregues; (b) reserva para eu corrigir o
  defeito de foco do card de Instituições, se a C00 preferir correção a encaminhamento; (c) novo
  baseline nominal com o CRUD, agenda e mapas de Locais das revisões posteriores da C04.
- **Primeiro critério ainda aberto:** decisão da C00 sobre (a), (b) ou (c). Sem uma delas não tenho
  lote independente que não viole reserva ou invente trabalho.
- **ETA fundamentada:** aceites de Turmas e Pessoas no mesmo padrão, se reservados, 1,5–2,5 h para
  os dois, medido pelo delta real destes: cada arquivo levou cerca de 20 minutos de escrita mais 5
  de revisão e execução em série. Correção do foco do card, se reservada, 15–30 min mais regressão
  focal. Fluxos de Locais: não estimável até o baseline.

### 7. Deltas propostos aos rastreadores — somente C00 aplica

Nenhuma promoção FE/BE/E2E. Acrescento aos da r2:

1. `institutions.detail`: defeito de teclado confirmado, dois pontos de foco por card, com
   reprodução nominal em `c07_institutions_directory_test.dart`. Não é regressão do lote C04; é
   divergência do contrato aprovado `CoeloAdminInteractiveCard`.
2. `institutions.error`, `institutions.reload`, `institutions.filter`, `institutions.access-denied`,
   `institutions.list`: aceite comportamental novo cobrindo `Error` além de `Exception`, retry com
   leitura única preservando busca, status e página, negado sem dados nem ações, sem-resultados
   distinto de falha, alvos de 48 px e composição sem overflow em 375 e 1440. FE verificado no
   cliente com repositório duplo; **não** certifica E2E.
3. `units.error`, `units.filter`, `units.reload`, `units.access-denied`, `units.list`: idem, mais
   paginação chamando o repositório com página e status corretos nos dois ramos.
4. `momentos.view`: aceite comportamental do viewer cobrindo tela inteira sem chrome externo nas
   três larguras em claro e escuro, alvos de 48 px, legenda e rail disjuntos e restauração de foco e
   posição ao desempilhar.
5. `principal.profile-view`: aceite comportamental cobrindo a anatomia aprovada e a ausência dos
   sinais sociais removidos pela spec 050. `principal.profile-edit` continua não entregue, como a
   própria C05 declarou.

---

## Revisão 2 — ack I002/RTK-1808, errata da cadeia de commits, laudo das 61 e lote dos quatro testes

- rodada: R01; revisão: 2 (r1 publicada em `4f986508` às 18:07:09−03:00)
- conversa: `E2 R01 C07 — Validação visual e fluxos`; conversation_id real:
  **`local_3b1f7fb1-731c-4ba9-bae5-1657903ca22f`** (Claude Desktop, aba Code; criada 17:41:34−03:00).
- ferramenta/modelo reais: Claude Code — `claude-fable-5-1`, effort `xhigh`. Nenhuma API adicional.
- **operational_ack: `R01-C07-I002`** (assignment canônica C00, 18:01:23) e **`R01-ALL-RTK-1808`**
  (18:08:11). A I001 da C06 (17:52), confirmada na r1, passa a histórica/ponte, como a C00 determinou.
  Aceito: baseline de execução `4af42925`; reserva restrita aos quatro arquivos novos nomeados;
  evidências em `handoffs/C07-evidence/`; `rtk` explícito nos comandos compatíveis; C06 chefia
  operacional, C00 integra/reserva/rastreadores.
- última instrução processada: R01-C07-I002 + R01-ALL-RTK-1808 (C00). Não recebi até este momento,
  como turno de sessão, a mensagem `17e4593a` que a C06 registra como enviada às 18:02; se chegar,
  respondo pelo handoff seguinte sem repetir ack.
- timestamp: 2026-09-08T18:16:23-03:00 (relógio da máquina; a primeira gravação deste r2 trazia 18:30 por estimativa errada, corrigida antes do commit).
- cwd/worktree: `C:/Users/adrie/Documents/Coelo.worktrees/e2-r01-c07`; branch
  `claude/e2-r01-c07-validacao-visual`; baseline `4af42925ff6f05fa14a7e28dffb31edbee321c3e`.
- `code_shas: []` — nenhum código de produto meu. Commits meus, todos documentação/evidência:

  | SHA | Hora | Conteúdo |
  |---|---|---|
  | `463e3afa` | 18:07:08 | evidência: reprodução C05 + observação Estruturas |
  | `4f986508` | 18:07:09 | handoff r1 |
  | `0b65e6c0` | 18:10:57 | evidência: laudo das 61 |
  | `d1099575` | 18:12:31 | `git mv` das três evidências para `handoffs/C07-evidence/` (I002) |

- push: `git ls-remote origin claude/e2-r01-c07-validacao-visual` devolveu `d1099575` após o último
  push, sem force. O SHA desta revisão fica para a r3.

## 1. Errata da r1 — cadeia de commits e autoria

A r1 declarou "parent desta revisão `46f63e84`". **Errado.** O parent real de `463e3afa` é
`4af42925`. Entre 17:59:04 e 18:07 a C00 aplicou nesta worktree, por `cherry-pick` (reflog:
`HEAD@{17:59:04}: cherry-pick: …` ×2), dois commits da C04 revisados: `c3ce3127`→`fdf0972d`
(diretórios falham honestamente em qualquer throw; quatro view models + 8 testes de Unidades) e
`023f19ea`→`4af42925` (`LocationSelectionSource` + teste). **Não são escrita minha**: nunca editei
`features/` de ninguém; a I002 e a I004 da C00 (18:08:11) já registram a autoria. A r3 da C06
(18:05) classificou esses commits como "escrita fora de reserva" da C07 — a I004 pediu a ela errata;
registro aqui o mesmo fato pelo meu lado, sem cobrança.

Estado das outras divergências da r1: branch residual `claude/e2-r01-c07-validacao` intocada;
checkout destacado `e2-r01-c07-c04ro` (agora em `479d1bd1`) segue somente-leitura e à disposição
para remoção pela C00.

## 2. Anti-duplicação — reconfirmada às 18:12 contra C04 r21 e C05 r16 (via C06 r3 e tabela da assignment canônica)

Sem mudança: C05 com agentes A–D em SQL/`principal_now`/`chat` e sessão principal em
`principal_moments*`/`principal_happens*`/`principal_shared/`; C04 em `locations/`/`people/`.
Nenhum executa validação visual, nenhum escreve nos quatro arquivos reservados a C07. Meus
subagentes de laudo só leram `failures/`, masters e git. Não executei nenhum dos cinco testes
existentes de Momentos/Perfil isoladamente depois da reprodução das 17:46 (que foi anterior à I002
e não coincidiu com execução idêntica declarada por outro executor).

## 3. Laudo das 61 divergências — concluído e revisado

Evidência: `docs/reviews/etapa-2-operacao/handoffs/C07-evidence/2026-09-08-laudo-goldens-c05-cea29b1f.md`
(commit `0b65e6c0`, movido em `d1099575`). Reprodução fixada:
`…/C07-evidence/2026-09-08-c05-goldens-reproducao-cea29b1f.md` (930/61 no código de `7810e7c5`,
idêntico em `4af42925` para as 14 features da C05 — os dois cherry-picks não tocam nelas).

| Família | Casos | A | B | C | D | Sinais B adicionais | Decisão pendente |
|---|---|---|---|---|---|---|---|
| Acontece | 10 | 10 | 0 | 0 | 0 | nenhum | ação circular de "Publicar agora" invisível em repouso; golden de hover prova pouco |
| Perfil | 10 | 10 | 0 | 0 | 0 | nenhum | nenhuma (observação: Fundação/Colaboradores só têm o plano como fonte) |
| Momentos | 11 | 11 | 0 | 0 | 0 | nenhum | recorte `cover` em ≥768 exibe 25–47 % da mídia |
| Chat | 9 | 4 | 0 | 0 | 5 | **2 reais** | app bar compacto `d9232a94` sem aprovação visual localizada |
| Comunicações + formulário | 4 | 0 | 0 | 0 | 4 | nenhum novo | indicador de status no card compacto; rodapé do formulário dentro da rolagem |
| Circulares | 3 | 3 | 0 | 0 | 0 | nenhum | nenhuma |
| Publicar no Agora | 13 + 1 | 0 | 0 | 0 | 13 (+1 assertiva) | **1 condicional** | ratificar `3419a89e` (sem rail) ou restaurar contrato 2026-08-31 |
| **Total** | **61** | **38** | **0** | **0** | **22 + 1** | **3** | **6 decisões** |

Conclusões que mudam o próximo passo:

1. **Nenhum caso é defeito puro (B) nem ambiente (C).** "Golden desatualizado" (C05) está certo em
   38 casos e é insuficiente em 22: neles a fonte aprovada não decide e a regeneração depende de
   decisão do Owner. O caso `now-media-unavailable` é **teste desatualizado** por `3419a89e`, não
   defeito, e cai na mesma decisão do Agora.
2. **Três sinais B reais/condicionais**, válidos qualquer que seja a decisão de master:
   - contorno do card do inbox do Chat pintado por baixo do rodapé de paginação
     (`superadmin_chat_page.dart` L617–624/798–815; `superadmin_listing_pagination_footer.dart`
     L42–56) — dono C05 (chat) e C00 (shared);
   - cabeçalho compacto do shell centralizado/com inset quando há `compactActions`
     (`superadmin_shell.dart` `_PageHeader` L1535–1566; `Column` sem `stretch` L302–316/443–457) —
     dono C00;
   - ramo `wide` de Publicar no Agora inalcançável: `bodyMaxWidth: 1120` < `large.minWidth` 1200,
     palco 260 px em 1440 igual ao de 1024 (`principal_now_publication_page.dart` L320/L429–436/
     L459–462; `principal_publication_frame.dart` L61–62) — dono C05; **condicional** à
     ratificação de `3419a89e`.
3. **Achado transversal (C00/Owner):** `d9232a94` (2026-09-01) mudou o app bar compacto e o
   `_PageHeader` do shell sem regenerar goldens e sem aprovação visual localizada (anexo 31 ainda
   mostra hambúrguer + marca central). Explica o Chat em 375/768 e é a hipótese para os **77 goldens
   de Estruturas que já divergem idênticos no baseline R01 `479d1bd1`** (evidência
   `…/C07-evidence/2026-09-08-c04-suite-observacao-9b076a1d.md`, seções de discriminação).

## 4. Reproduções curtas dos sinais B — estado

Três subagentes (18:12) escreveram reproduções em `apps/superadmin/test/acceptance/`. **Esses três
arquivos não estão entre os quatro reservados pela I002**, portanto **não serão commitados em
`test/`**: preservo o código-fonte deles em `handoffs/C07-evidence/repro/` como reprodução para o
dono adotar, e apago as cópias de `test/acceptance/` antes de qualquer commit. Nunca entraram no
índice.

| Reprodução | Estado às 18:16 | Resultado |
|---|---|---|
| `now_publication_wide_stage_test.dart` (2 casos) | concluída | **falha como esperado**: palco 260,0 em 1024 e 260,0 em 1440; corpo 1120,0 < 1200,0; `flutter analyze` limpo |
| `shell_compact_page_header_inset_test.dart` (2 casos) | concluída | **falha como esperado**: título sem ações dx=20,0; com ações dx=39,17 em 375 e dx=235,67 em 768 (bloco centralizado na tela); `flutter analyze` limpo. Achado extra: o ramo é acionado por qualquer `actions` não vazio em modo compacto, não só por `compactActions` — afeta toda página compacta com ações, não apenas o Chat |
| `chat_inbox_border_under_pagination_test.dart` (1 caso, leitura de pixels via `RepaintBoundary.toImage`) | concluída | **falha como esperado** em 1024×900 claro: pixel da borda acima do rodapé (308,768) = `#FFDDE0E2` (outlineVariant); dentro da faixa do rodapé (308,828) = `#FFFFFFFF` (borda coberta); canto inferior-esquerdo (309,863) = `#FFFFFFFF` em vez do fundo `#FFF7F8F8`; rodapé 792→864 colado às arestas do card; `flutter analyze` limpo |

## 5. Lote em curso — os quatro arquivos reservados (I002)

Às 18:13 despachei **4 subagentes** (concorrência observada nesta sessão: 7 simultâneos, sem erro
de ferramenta), um por arquivo, com regra de um escritor por arquivo, sem golden, sem git, sem
tocar em `lib/`, `test/features/`, `test/support/` ou PNGs; cada um lê primeiro os testes
existentes e cobre só lacunas:

| Arquivo reservado | Escopo |
|---|---|
| `test/visual_review/c07_moments_acceptance_test.dart` | tela inteira em 375/768/1440 sem chrome externo; alvos ≥48; legenda × rail disjuntos; 200 % sem overflow; retorno contextual; claro/escuro |
| `test/visual_review/c07_profile_acceptance_test.dart` | capa/avatar/identidade/contexto/métricas Publicações-Momentos-Circulares/abas; ausência de Seguidores/Seguindo/follow; 200 %; abas roláveis em 375; teclado; troca de aba |
| `test/acceptance/c07_institutions_directory_test.dart` | falha por `Error` sai do loading (`fdf0972d`); retry lê uma vez preservando busca/filtro; falha sem dados obsoletos; negado sem criar; sem resultados/limpar; reload; teclado/alvos; 375/1440 |
| `test/acceptance/c07_units_directory_test.dart` | idem sem repetir os 8 testes herdados; filtros preservados após retry/reload; paginação com cursor; negado; teclado/alvos; 375/1440 |

Resultados, `flutter analyze` e commits (um por arquivo, com caminho explícito) na r3.

## 6. Automação, comunicação e RTK

- `CronCreate` **`d5b22e3a`**, `27,57 * * * *`, fuso local. Disparos **comprovados: nenhum** até
  18:16 — 17:57 caiu dentro de turno ativo (a ferramenta só dispara com o REPL ocioso;
  os subagentes em voo mantêm o turno ativo). Próximos calculados: 18:27 e 18:57. Limites inalterados
  (session-only, ocioso, jitter, 7 dias). Removo com `CronDelete` após a entrega final.
- Canal de entrada: mensagem de sessão da C06. Canal de saída para a C06: `send_message`
  disponível, não exercitado. Sem ferramenta para a C00.
- RTK: `rtk 0.35.0` presente; uso explícito em `rtk git …`, `rtk flutter pub get` e nos testes
  dos subagentes. Sem hook automático instalado (`rtk` avisa "No hook installed"); **não** executei
  `rtk init -g` nem reconfigurei nada global, conforme a RTK-1808.

## 7. Bloqueios, primeiro critério aberto, próximo passo e ETA

- **Decisões pendentes (Owner/C00), sem bloquear meu lote:** as seis do laudo (seção 3) — nenhuma
  regeneração de master sem elas.
- **Reservas que peço via C06, só se a C00 quiser correção minha:** nenhuma por ora. Os três sinais
  B ficam com os donos (C05: chat e Agora; C00: shell e `superadmin_listing_pagination_footer`),
  com reprodução pronta em `C07-evidence/repro/`.
- **Primeiro critério ainda aberto:** os quatro testes reservados verdes (ou vermelhos por defeito
  real, documentado), commitados e com push.
- **Próximo passo (mesmo turno):** receber os quatro testes e as duas reproduções restantes, revisar,
  mover reproduções para a evidência, rodar `flutter analyze` + os quatro arquivos, commitar um por
  um, push, r3.
- **ETA fundamentada:** quatro testes + r3: 45–75 min (subagentes já em voo; revisão minha de cada
  arquivo + execução final ~5 min por arquivo). Fluxos C04 além dos diretórios (CRUD/agenda/mapas):
  fora do recorte até novo baseline da C00, não estimável.

## 8. Deltas propostos aos rastreadores — somente C00 aplica

Nenhuma promoção FE/BE/E2E.

1. IDs da C05 com goldens: "61 golden-only" = 60 goldens + 1 assertiva desatualizada; 38 A / 22 D
   por família conforme seção 3; decisões pendentes do Owner listadas.
2. `chat.list`: dois defeitos visuais reais (contorno sob paginação; inset do cabeçalho compacto),
   além de A (1024/1440) e D (375/768).
3. `agora.create`/`agora.publish`: 13 D + assertiva desatualizada; ramo `wide` morto em 1440
   (reprodução com valores 260/260 e 1120/1200).
4. `institutions.list`, `units.list`, `groups.list`, `people.list`: 77 goldens já divergem
   idênticos em `479d1bd1`; não atribuir a C04/C05; causa provável transversal `d9232a94` (shell).
5. `notices.list`/`notices.create`/`notices.edit`, família Circulares: 3 A (Circulares, rodapé
   `2d193ba1`) e 4 D (indicador de status no card compacto; rodapé do formulário `d4374e39`, que
   também derruba os goldens aprovados de Instituições/Unidades em 375).
