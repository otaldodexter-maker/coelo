---
title: "Handoff R01 C05"
source: "assignments/C05.md (R01-C05-I003 a I009 e aditivo I008); repasses do Coordenador C06 ate 2026-09-08T19:32; revisao da C00 sobre 5f10c0b7; docs/reviews/evidence/etapa-2/principal-visual/2026-08-31-publicar-agora-approved.png (SHA256 e096c855...c315, copia versionada pela C00); docs/superpowers/specs/2026-08-28-coelo-visual-completion-stage-design.md item 31"
status: "active"
generated_at: "2026-09-08T19:49:46-03:00"
timezone: "America/Sao_Paulo"
---

# E2 R01 C05 — Comunicacao e Principal

## Revisao 19 — os dois corretivos da revisao da C00

- rodada: R01; revisao: 19
- ultima instrucao processada: `R01-C05-I009`; repasses do C06 ate 19:32 absorvidos
- timestamp: 2026-09-08T19:49:46-03:00, relogio da maquina
- branch: `claude/e2-r01-c05-comunicacao`; baseline `479d1bd1`
- HEAD do codigo: `edded9bf971900c689e2c981fe9febf5e737ce26`
- push: verificado; `git ls-remote` devolveu `edded9bf`
- arvore limpa; nenhum PNG de `failures/` em commit

| commit | conteudo |
| --- | --- |
| `d6e2aa89` | `chat-media` recusa ticket de leitura em branco |
| `8df05c1a` | envelope de anexo coberto onde a mutacao mostrou buraco |
| `478a805e` | recorte do card do Chat no raio, alem da borda em foreground |
| `edded9bf` | Agora em coluna unica com a previa ao lado, como a referencia |

**A revisao da C00 estava certa: o `5f10c0b7` era parcial nos dois casos.** Corrigi os dois e
registro abaixo o que medi, incluindo onde a medicao muda a leitura do achado e onde eu **parei de
proposito**.

## 1. Chat — o recorte entrou, e o que a medicao mostrou

O `_body()` nao estava recortado, entao o rodape, que e um retangulo, pintava alem do arco. Agora ha
`ClipRRect` do mesmo `CoeloRadius.lg` dentro do `DecoratedBox` foreground. Nenhuma alteracao no
rodape compartilhado.

**Mas o canto nao produz diferenca de pixel nesta composicao, e isso muda a leitura do achado.**
Amostrei o entorno: o fundo do shell atras do card e a superficie do card sao **a mesma cor** nos
dois temas — `#FFFFFFFF` no claro e `#FF181C1F` no escuro, identicos dentro e fora do card. Um filho
vazando alem do raio nao aparece porque nao ha contraste atras dele. Mantive o recorte assim mesmo:
e correcao estrutural que nao custa nada e protege se o fundo mudar. **Nao vou dizer que corrigi um
pixel que ninguem via.**

O teste passou a cobrir claro **e** escuro e ganhou a assercao do arco. Sendo exato sobre o que cada
assercao prova: quem mata o mutante de devolver a borda ao fundo e a assercao da **reta**, nos dois
temas; a do arco fixa um contrato real, mas nao e o discriminante desse caso.

199 verdes na suite de chat; as 9 falhas seguem sendo as golden pre-existentes.

## 2. Agora — abri a referencia versionada e corrigi o que ela determina

Usei a copia que a C00 publicou no repositorio, nao a pasta externa. Comparando com o que eu tinha
feito: **as duas zonas lado a lado eram invencao minha.** Nos tres tamanhos a referencia mostra
**uma coluna** — Midia com seu proprio trilho de ferramentas, Texto, Publico e contexto,
Agendamento, Opcoes — e, no desktop, a previa ao lado dela.

Corrigido: coluna unica em qualquer largura; previa lateral a partir de 840; cabecalho da previa
passa a ser **`Previa da publicacao`** com **`Assim sera exibido no Agora.`**, como na referencia.
Verificado em **375, 768, 1440 e a 200%**, com `Sua publicacao`, o nome da superficie, a previa no
desktop e **sem** barra segmentada.

**O que ainda NAO bate, declarado em vez de dado por fechado.** Na referencia a midia e **dominante**
no desktop: uma caixa larga ocupando a coluna, com o assunto centralizado e o trilho flutuando
dentro dela, a direita. No codigo a midia continua uma caixa vertical de largura fixa com o trilho
ao lado. **Parei aqui de proposito.** A geometria exata — largura, altura e posicao do trilho dentro
do palco — eu so obteria medindo a olho no PNG, e a spec 036 exige reproduzir a geometria canonica
**sem aproximacoes**. Aproximar seria repetir exatamente o erro que me trouxe ate este corretivo.
Fica como pendencia nomeada, com a referencia ja versionada para quem tiver as medidas.

As 14 golden do Agora seguem vermelhas de proposito e **nao foram regeneradas**. Confirmo os tres
limites: rail nao restaurado, nenhum golden autorizado, `PrincipalPublicationFrame` intocado.

## 3. Mutacao nas entregas antigas achou o que eu nao tinha visto

Passei mutacao pelas guardas que ainda nao tinham passado por isso.

- **Gateways de midia:** cinco mutantes mortos — ticket e asset juntos, ticket de espectador usando
  autorizacao de autor, nenhum dos dois passando, `prepare` fingindo sucesso e `prepare` pulando a
  validacao de forma. **Um sobreviveu**: a guarda de ticket vazio no `read` nao era exercida. O teste
  que escrevi para ela achou coisa **pior** que o mutante — um ticket so com espaco passava como
  valido e devolvia 503, reportando erro de cliente como pendencia de servidor. A guarda agora corta
  pelo valor aparado.
- **Envelope de anexo do Chat:** de quatro guardas, so duas eram exercidas. **Arquivo vazio e limite
  de nome sobreviviam** — duas recusas que o codigo prometia e nenhum teste cobrava. 15 casos de
  borda agora, incluindo a ordem das recusas, e os tres mutantes morrem.

## 4. Sem SQL, sem replay

Lease liberada pela `I014` de Forms nao autoriza inicio novo: **nenhuma frente comeca SQL ate
concessao nominal**. Meus dois candidatos corrigidos continuam esperando. Nao acionei recuperacao
paralela, nao subi banco local, nao apliquei nada remoto.

## 5. Pendencias que dependem da C00

1. **Concessao nominal** para o replay dos dois candidatos corrigidos.
2. **Geometria da midia dominante** do Agora no desktop, com medidas — secao 2.
3. **Reserva** dos comandos autorizados que hoje fazem os clientes falharem fechado:
   `remove_happens_post`, remocao de Momentos, expiracao do Agora.
4. **Contrato de revisao do Chat** (RPCs de edicao e revogacao mais os campos da projecao).
5. **Contrato do Perfil institucional** para o restante dos campos.
6. **Remocao do `PrincipalPublicationProgressBar`**, orfao no frame compartilhado.
7. **Onde ficam** o leitor com respostas e o compositor de Circulares, hoje sob `principal.profile-view`.
8. **Master x referencia** de Acontece e Momentos quanto ao trilho: os goldens das duas **passam**
   hoje (56/56 e 79/79), entao os masters incluem o trilho. Nao mexi.
9. **Revisao visual nominal** dos goldens, inclusive os 14 do Agora.

## 6. Retomada exata

```
cd C:\Users\adrie\Documents\Coelo.worktrees\e2-r01-c05
git status --short
git log --oneline -8
cd apps\superadmin
flutter analyze
flutter test test\features\chat\presentation\superadmin_chat_outline_test.dart
flutter test test\features\chat\domain\chat_attachment_policy_test.dart
flutter test test\features\principal_now_publication\presentation\principal_now_publication_page_test.dart
cd ..\..\packages\coelo_database\supabase\functions\chat-media
deno test --allow-all
```

Cron `e9f4cae3` ativo. Fechamento seguro **09/09 05:30**, entrega final ate **06:00**, com WIP
identificado e preservado.
