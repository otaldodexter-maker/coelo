---
title: "Handoff R01 C05"
source: "assignments/C05.md (R01-C05-I003 a I009, aditivo I008 de revisao SQL, ack 17:21:46 e detalhamento 17:34:14); repasse do Coordenador Claude C06 de 2026-09-08T17:30 sobre Supabase/Cloudflare; PROTOCOLO.md R01 (linhas 71 e 128); packages/coelo_database/migrations/20260901185008_superadmin_internal_notices_v2.sql; packages/coelo_database/migrations/20260821112822_moments_publication_mvp.sql; packages/coelo_database/migrations/20260901101500_superadmin_internal_chat_v2.sql"
status: "active"
generated_at: "2026-09-08T19:05:14-03:00"
timezone: "America/Sao_Paulo"
---

# E2 R01 C05 — Comunicacao e Principal

## Revisao 17 — os quatro lotes fechados

- rodada: R01; revisao: 17
- ultima instrucao processada: `R01-C05-I009` mais o repasse do C06 das ~17:30
- timestamp: 2026-09-08T19:05:14-03:00, relogio da maquina
- branch: `claude/e2-r01-c05-comunicacao`; baseline `479d1bd1`
- HEAD do codigo: `5f10c0b70b58b1679f72cf91005d2b15bc18ff65`
- push: verificado; `git ls-remote` devolveu `5f10c0b7`.

Commits desta revisao, todos com caminhos explicitos:

| commit | conteudo |
| --- | --- |
| `1fe36326` | cliente de `agora.expire` |
| `1d77d21f` | clientes de `chat.edit` e `chat.revoke` |
| `71f745d5` | correcao dos dois candidatos SQL conforme o aditivo |
| `5f10c0b7` | correcao dos dois defeitos visuais apontados pela C07 |

## 1. I008 subagentes — o que aconteceu de verdade

Despachei quatro subagentes com caminhos disjuntos, objetivo e criterio de saida
declarados antes do despacho, como a r15 registrou. **A sessao foi encerrada com os quatro ainda
rodando** e eles nao voltaram; o trabalho parcial ficou no disco. Relato a limitacao exata, como a
I008 manda, sem inflar o resultado: **os quatro foram iniciados de verdade e nenhum concluiu por
conta propria**. Eu assumi, revisei linha a linha, corrigi o que estava quebrado, completei o que
faltava e fiz os commits. A responsabilidade pelo lote e minha e continua minha.

- Concorrencia efetivamente observada: **4 simultaneos**.
- Escritor unico por arquivo: conferido por `git status` durante a execucao — nenhum agente
  escreveu fora dos seus caminhos, nenhum tocou o indice Git, rastreadores, protocolo ou handoff,
  nenhum executou SQL remoto e nenhum regenerou golden.
- O que cada um deixou pronto e o que eu tive de fazer depois esta nas secoes 2 a 4.

Tambem restabeleci a continuidade: o cron da sessao anterior morreu com ela. Novo **`e9f4cae3`**,
`7,37 * * * *`, com os mesmos limites ja declarados — session-only, so dispara com o REPL ocioso,
jitter de ate ~3 min. Nao afirmo autonomia alem disso.

Descartei ainda os PNGs de `failures/` que a execucao interrompida da suite deixou sujos. Sao
saida de teste, nao golden; nenhum golden foi tocado.

## 2. `agora.expire` entregue (`1fe36326`)

O agente C deixou o desenho completo e correto; nao precisei corrigir codigo. Fiz a verificacao que
faltava.

- `can_expire` vem da projecao autorizada, default `false`; sem id de publicacao nao ha afordancia.
- O adaptador **falha fechado** (`PrincipalNowExpireUnavailable`). Nenhuma RPC foi inventada.
- Detalhe que o desenho anterior nao tinha e que faz diferenca no visor: a historia **pausa** o
  avanco enquanto o dialogo esta aberto, senao o comando confirmado carregaria uma publicacao que o
  operador nao escolheu.
- Motivo obrigatorio; `requestId` preservado por publicacao para a repeticao replay.

**Evidencia.** `flutter analyze` de `lib/features/principal_now` e `test/features/principal_now`
sem issues; **70 testes verdes** em `principal_now`, dos quais 9 de aceitacao novos e 2 de
adaptador. **Mutacao: 4 de 4 mortos** — permissao sempre concedida, retry com `requestId` novo,
adaptador fingindo sucesso, e mapeamento concedendo `can_expire` sem a projecao.

## 3. `chat.edit` e `chat.revoke` entregues (`1d77d21f`)

O agente D deixou implementacao boa **e dois defeitos reais**, alem de **zero testes** — foi
interrompido antes dessa fase. Achei os dois defeitos escrevendo os testes.

**Defeito 1, que quebraria em producao toda vez.** O dialogo de edicao descartava o
`TextEditingController` em `whenComplete`, ou seja, enquanto a rota ainda animava a saida e ainda
reconstruia o campo: `A TextEditingController was used after being disposed`. Corrigi com o mesmo
padrao ja aceito em `principal_removal_dialog.dart` — o dialogo virou `StatefulWidget` dono do
proprio controller, descartado so quando a rota morre.

**Defeito 2.** O seletor do transporte de revisao nao compilava: promocao de tipo entre interfaces
nao relacionadas nao acontece em Dart. Resolvi alargando o local para `Object`, sem cast.

O desenho que preservei:

- `can_edit` / `can_revoke` vem da projecao, default `false`, e **qualquer valor que nao seja `true`
  nega** — string `'true'` e numero `1` nao concedem nada. Sem nenhuma concedida **nao existe menu**;
  nao e afordancia desabilitada.
- Edicao e **re-projecao do servidor**, nunca reescrita local. O teste prova isso do jeito mais
  direto possivel: o duplo responde com um corpo diferente do que foi digitado, e a tela mostra o do
  servidor. A mensagem fica marcada como editada.
- Revogacao e **lapide**: a linha mantem o lugar, o corpo sai, os anexos ficam marcados e o estado
  novo e provado **relendo a conversa**. Uma lapide nunca volta a ser revisada.
- O contrato de revisao e **interface separada** de `ChatRepository`, de proposito, para a
  superficie distinguir "nao oferecido aqui" de "oferecido e recusado".
- O adaptador de producao falha fechado **sem chegar a rede** — o teste conta as requisicoes e exige
  zero. Nenhum nome de RPC foi adivinhado.
- O limite de 4000 caracteres nao foi inventado: conferi em
  `20260901101500_superadmin_internal_chat_v2.sql` e em `20260812000000_chat_production_contract.sql`.

**Evidencia.** `flutter analyze` de `lib/features/chat` e `test/features/chat` sem issues.
**182 testes verdes** na suite de chat, 19 deles novos (12 de aceitacao, 7 de adaptador). As **9
falhas restantes sao golden pre-existentes**: medi a mesma contagem `-9` com e sem a minha mudanca,
usando `git stash`. Nenhum golden regenerado.

**Mutacao, com um sobrevivente que registro em vez de esconder.**

| mutante | resultado |
| --- | --- |
| menu oferece editar sem a projecao | **morto** |
| menu oferece remover sem a projecao | **morto** |
| lapide nao retira o menu | **morto** |
| retry gera intencao nova | **morto** |
| mapeamento concede `can_edit` sem a projecao | **morto** |
| lapide mantem o corpo vazado | **morto** |
| adaptador finge sucesso | **morto** |
| guarda `!canEdit \|\| isRevoked` dentro de `_editMessage` | **sobreviveu** |

O sobrevivente e inalcancavel pela UI porque o menu ja barra o caminho. **Escolhi manter a guarda**
e declaro que nenhum teste a prova: e defesa em profundidade num **ponto de entrada de comando**, e
autorizacao redundante vale mais mantida do que removida. E uma decisao diferente da que tomei em
`momentos.remove`, e a diferenca importa: la a guarda duplicava outra ja provada e o `PageView`
tornava o caminho impossivel, entao removi; aqui ela e a unica checagem dentro do proprio handler.

## 4. Candidatos SQL corrigidos (`71f745d5`) — sem executar nada

Correcao local nos mesmos arquivos reservados. **Nenhuma execucao de SQL, nenhuma aplicacao remota,
nenhum replay.** A concessao nominal da C00 continua pendente e nao a presumi.

**Notices.** O achado mais grave era **ACL**, nao paginacao — e era regressao minha. O laco agora
nomeia so as tres funcoes que a migration cria, e os cinco contratos sobreviventes sao reafirmados
com as assinaturas **conferidas na fonte** `20260901185008`. O desempate de id passa a seguir a
direcao escolhida, para `ORDER BY` e comparacao de tupla concordarem. `NOTICE_INVALID_CURSOR` entra
na allowlist: ele e levantado em tres pontos da propria funcao e virava `NOTICE_INTERNAL_ERROR`. As
duas funcoes de erro sao reproducoes fieis do original, conferidas lado a lado, com apenas essa
entrada nova. A projecao passa a receber a linha intacta em coluna propria, porque
`superadmin_notice_json` recebe `public.platform_notices` e a CTE carrega colunas extras.

**Momentos.** O `INSERT` de permission saiu: `moments.publications.read` ja e catalogada pela
fundacao `20260821112822` com `module_code` e `action_code` reais, e agora e reutilizada. O resgate
do ticket deixou de ser passe permanente e **reautoriza no uso**: expiracao, vinculo de auth ativo,
**realm separado** do superadmin interno, ativo pronto, publicacao ainda publicada, membership ativa
e nao revogada, papel derivado de dados reais e audiencia ainda compativel — e o ticket e consumido.
Papel desconhecido ou nulo **nao vira mais `school_staff`**: nao recebe nada. O store privado mantem
RLS habilitada e forcada sem policy, e o `revoke` agora alcanca **tambem `service_role`**, ou seja,
ficou mais restrito do que estava, nao menos.

**Testes.** Plano de notices corrigido de **22 para 32**, com guardas de regressao para o defeito de
ACL — um assert por contrato sobrevivente — e para o codigo do cursor. Plano de momentos corrigido
para **47**. Registro que errei a contagem na primeira passada: o total original era 25, como a
revisao da C00 dissera, e nao 24; conferi de novo e o plano publicado bate com os asserts.

**Limite da verificacao, declarado sem suavizar.** So verificacao estatica: conferi que **cada**
tabela, tipo, coluna e assinatura referenciada existe nas migrations, e que delimitadores e blocos
estao balanceados.

Alem disso, parte dos asserts de momentos e **contrato de fonte, nao comportamento**: as guardas do
resgate sao asseridas contra `pg_get_functiondef`, porque executa-las exigiria criar instituicao,
pessoa, membership, publicacao e ativo, e este candidato **nao pode cunhar identidade sintetica em
`public.people`**. Conferi que o precedente citado no cabecalho do teste existe de verdade —
`activity_template_unit_scope_test.sql` usa a mesma tecnica seis vezes — entao nao e invencao. Isso
pega a reintroducao de qualquer defeito removido, mas **nao substitui replay**: o cabecalho do
proprio teste declara que o replay com a fixture de identidade interna continua devido antes de
qualquer declaracao ponta a ponta, e eu repito aqui.

A paginacao com valores empatados de notices atravessa a RPC pelo mesmo motivo e **fica declarada
como nao coberta** em vez de asserida contra uma copia da consulta feita a mao.

## 4b. Execucao SQL — dono unico, e eu nao sou ele

Recebi do C06 as 18:29 a restricao da C00 (recibo 18:27:37): **Docker foi delegado exclusivamente a
um apoio Codex por decisao do Owner, e C04, C05, C06 e C07 nao iniciam recuperacao paralela nem
replay.** Confirmo o recebimento e registro que isso **nao muda nada** no que eu ja vinha fazendo:
nao pedi replay, nao tentei subir banco local e nao vou. A dependencia fica descrita aqui e sigo nos
lotes de cliente, que nao dependem dela.

## 5. Repasse do C06 sobre Supabase/Cloudflare — posicao mantida

Confirmei o recebimento na r16 e a posicao nao mudou: **nao linkei projeto, nao rodei SQL remoto,
nao publiquei Edge Function, nao toquei bucket**. Nenhum ID saiu de `pending-verification`. As
razoes estao na r16 e a principal continua sendo que a instrucao direta do meu usuario para este
lote inclui **sem migrations ou deploys remotos**, e um repasse de sessao par nao substitui a
palavra do proprio usuario. Perguntei a ele e sigo sem resposta ate agora; o lote independente nao
depende disso e nao parou.

## 5b. Laudo visual da C07 — dois defeitos meus, verificados por mim

A C07 fechou o laudo das divergencias golden-only e o resultado confirma o que eu vinha dizendo:
nenhuma e defeito de ambiente, a maioria e master desatualizado e uma parte depende de decisao do
Owner. **Nao regenerei e nao vou regenerar.**

Dois sinais sao de arquivo meu. **Nao aceitei nenhum dos dois de primeira** — conferi cada um antes
de mexer, e as coordenadas do laudo estavam desatualizadas porque meu commit de Chat deslocou o
arquivo em ~475 linhas.

1. **Contorno do card do Chat apagado pelo rodape de paginacao.** Real. Localizei por conteudo e li a
   reproducao da C07: o `DecoratedBox` pinta a borda **atras** do filho, e o rodape, encostado na
   borda do card, borra o proprio fundo — o `BackdropFilter` come a linha de 1 px onde os dois se
   sobrepoem. Corrigi do meu lado pintando o contorno em **foreground**, sem encostar no rodape
   compartilhado, que e da C00. Pinei com teste proprio que amostra a mesma coluna da borda acima e
   dentro da faixa do rodape — asserir qual decoracao carrega a borda passaria mesmo com o borrao por
   cima, entao so o pixel prova.

2. **Ramo `wide` inalcancavel no compositor do Agora.** Real, e **e codigo morto que eu introduzi**.
   Verifiquei estruturalmente em vez de confiar no laudo: `body` -> `ExcludeFocus` -> `AbsorbPointer`
   -> `_composerBody()` -> `LayoutBuilder`, tudo dentro do `ConstrainedBox(maxWidth: 1120)` do frame,
   e `CoeloBreakpoints.large.minWidth` e **1200**. Logo `wide` nunca e verdadeiro.

   **Divirjo do C06 num ponto**, e registro: ele classificou o achado como condicional a C00
   ratificar `3419a89e`. O que depende dessa ratificacao e **qual** correcao e a certa, nao **se**
   existe codigo morto. Removi o ramo: assim nenhum pixel muda, o codigo para de anunciar um terceiro
   layout que nunca renderizou, e eu **nao presumo** a decisao de contrato. Se o `3419a89e` for
   ratificado e quiserem palco mais largo, isso e mudanca de largura do frame, com revisao visual
   propria.

Ambas as correcoes estao em `5f10c0b7`, com teste proprio para cada uma. Provei que a guarda do
contorno **pega** o defeito revertendo a correcao: ela le `#FFDDE0E2` acima e `#FFFFFFFF` dentro da
faixa, exatamente os pixels do laudo. A remocao do ramo do Agora e **neutra em pixel**: 78 verdes e
14 falhas em `principal_now_publication`, contagem identica com e sem a mudanca, medida com
`git stash`.

Registro um limite do meu proprio teste de largura: ele fixa a **precondicao** — o compositor nunca
alcanca `large` — e nao a ausencia de um ramo. Ajustei o comentario para dizer isso, em vez de
prometer que ele impede a reintroducao.

Recebi tambem o repasse unico `R01-ALL-RTK-1808` sobre a skill RTK.

## 6. Evidencia consolidada

**Suite completa do `apps/superadmin`, execucao limpa e sem edicao concorrente:
`+4325 ~5 -224` em 7:45.**

E aqui **corrijo um numero meu**. Eu vinha repetindo "61 divergencias golden-only" e dizendo que
nenhuma vinha do meu trabalho. Medido de verdade, contra o baseline `479d1bd1` numa worktree
descartavel, o quadro e outro:

| | HEAD `5f10c0b7` | baseline `479d1bd1` | de quem |
| --- | --- | --- | --- |
| falhas totais | **224** | — | — |
| **nao-golden** | **59** | **59** nos mesmos 30 arquivos | **nenhuma minha** |
| **golden** | **165** | **149** nos mesmos 35 arquivos | **16 de diferenca**, decomposta abaixo |

As 59 nao-golden sao herdadas: rodei os **mesmos 30 arquivos** no baseline e deu **exatamente 59**.
Sao roteador, `core/config`, `shared`, Atividades, Instituicoes, Seguranca, Perfis de acesso,
Agenda e dev menu — nada de Chat nem `principal_*`. Nao sao minhas e nao vou trata-las como se
fossem.

As 16 golden de diferenca se decompoem, e **14 sao minhas de fato**:

- **`principal_now_publication_golden_test.dart`: 0 no baseline, 14 no HEAD.** E a reformulacao do
  compositor do Agora que o Owner pediu — tirar o wizard lateral. Os masters sao anteriores a
  decisao. Sao **14 divergencias que eu introduzi**, declaradas como minhas, e **nao regenerei**
  nenhuma: elas ficam vermelhas ate revisao visual nominal.
- **`circular_directory_golden` e `notice_directory_golden`: 2 -> 3 cada.** Isso **nao e falha
  nova**: o teste combinado "matriz responsiva aprovada" foi desmembrado por breakpoint, e agora so
  o **375** falha. Em outras palavras, 768, 1024 e 1440 passam separadamente, o que antes ficava
  escondido dentro de um unico teste vermelho. A divergencia de fundo e a mesma, agora localizada.

- `chat-media`: **19/19** no Deno; `moments-media`: **21/21**. Reconferidos nesta revisao.
- `flutter analyze` limpo em `principal_now`, `principal_moments`, `principal_happens` e `chat`.

## 7. Pendencias que dependem da C00

1. **Concessao nominal** para o replay dos dois candidatos, agora com as correcoes do aditivo
   aplicadas e verificadas estaticamente.
2. **Reserva** para os comandos autorizados que hoje fazem os clientes falharem fechado:
   `remove_happens_post`, remocao de Momentos e expiracao do Agora, com as projecoes
   `can_remove` / `can_expire`.
3. **Contrato de revisao do Chat** — edicao e revogacao. O cliente precisa de: RPC de edicao que
   receba conversa, mensagem, corpo e chave de idempotencia e **devolva a mensagem re-projetada**;
   RPC de revogacao que receba conversa, mensagem e chave e grave a lapide; e os campos
   `can_edit`, `can_revoke`, `is_edited`, `edited_at`, `is_revoked`, `revoked_at` na projecao da
   thread. Sem isso o cliente fica indisponivel de forma honesta, como esta.
4. **Contrato do Perfil institucional** — campos e recomendacao por campo estao na r16.
5. **Onde ficam** o leitor com respostas e o compositor de Circulares, hoje subdeclarados dentro de
   `principal.profile-view` (r16). Nao criei IDs.
6. **Composicao produtiva** de `for-you`, `moments` e `profile`, e destino de producao para
   `principal-chat`.
7. **Revisao visual nominal** das divergencias golden-only. Nao regenerei e nao vou regenerar.

## 8. Retomada exata

```
cd C:\Users\adrie\Documents\Coelo.worktrees\e2-r01-c05
git status --short
git log --oneline -5
cd apps\superadmin
flutter analyze
flutter test test\features\chat\presentation\superadmin_chat_revision_test.dart
flutter test test\features\chat\data\supabase_chat_repository_test.dart
flutter test test\features\principal_now
flutter test test\features\principal_moments\presentation\principal_moments_remove_test.dart
cd ..\..\packages\coelo_database\supabase\functions\chat-media
deno test --allow-all
```

Cron `e9f4cae3` ativo. Fechamento seguro **09/09 05:30** e entrega final ate **06:00**.
