---
title: "Handoff R01 C05"
source: "assignments/C05.md (R01-C05-I007); PROTOCOLO.md R01; decisao do Owner 2026-09-08 sobre remocao e sobre trabalho continuo ate o fechamento; decisions/0032-mvp-private-media-r2.md; packages/coelo_database/migrations/20260812000000_chat_production_contract.sql; packages/coelo_database/supabase/functions/_shared/r2_s3.ts; docs/reviews/coelo-flutter-pendencias.md; docs/reviews/coelo-supabase-pendencias.md; docs/reviews/coelo-flutter-integrado-supabase-pendencias.md"
status: "active"
generated_at: "2026-09-08T17:15:03-03:00"
timezone: "America/Sao_Paulo"
---

# E2 R01 C05 — Comunicacao e Principal

## Revisao 14 — slot das 17:30

- rodada: R01; revisao: 14
- ultima instrucao processada: `R01-C05-I007`
- timestamp: 2026-09-08T17:15:03-03:00, relogio da maquina
- branch: `claude/e2-r01-c05-comunicacao`; baseline `479d1bd1`
- HEAD do codigo: `f413c2dc0e6aaf6cbec8a5ebbda7adefa42025a6`; handoff em commit proprio
- push: verificado; `git ls-remote` devolveu `f413c2dc`.

## 1. Janela conferida com a C00 e mecanismo ajustado

O Owner pediu trabalho continuo ate o fechamento. Conferi no PROTOCOLO vivo, nao por memoria:
execucao ate **09/09 05:30**, entregas finais ate **06:00**, novas atribuicoes suspensas a partir das
06:00, feedback e R02 ate **07:40**; handoffs as 12:50, 14:50 e **17:30** e a cada mudanca material.
E o que passo a seguir.

Troquei o cron: o antigo so consultava a assignment e nao fazia nada quando nao havia revisao nova,
o que nao sustenta trabalho continuo. O novo **retoma o proximo passo do lote** alem de checar
instrucao, e faz o fechamento seguro sozinho depois das 05:30.

- **ID `f10b9942`**, expressao `7,37 * * * *`, disparos as :07 e :37 (o antigo `6a2a031f` foi
  cancelado).
- Limites reais, sem promessa: e **session-only**, morre se o app fechar; **so dispara com o REPL
  ocioso**; jitter de ate ~3 min; expira em 7 dias. Nao afirmo trabalho autonomo alem disso.

## 2. `chat-media` entregue sob a reserva (`313c43bb`)

Os seis arquivos reservados existem. O transporte, as guardas e o contrato sao reais; o ramo de
catalogo nao e, e nada finge o contrario.

`media_contract.ts` mantem tres formas separadas de proposito:

- **asset** espelha `chat_attachment_metadata`, cujo `upload_status` ja carrega o tombstone
  `deleted` — entao `discard` marca e nunca apaga linha nem auditoria;
- **attempt** carrega **dois ids de intencao distintos**, para um retry replicar cada etapa em vez de
  criar um segundo ativo;
- **binding** e declarado e **recusado**: `message_id` e `NOT NULL`, nao ha ramo de staging e a I007
  mantem esse SQL sem reserva. Nada aqui relaxa a coluna.

Limites espelham o envelope aprovado de Circulares, **sem video**: a ADR 0032 nao exige Stream no
Chat. O handler valida forma **antes** de responder qualquer coisa, entao chamada malformada recebe
`invalid_request` e nunca e confundida com funcionalidade pendente; cada operacao sem RPC responde
**503 com motivo nomeado**: `prepare`, `finalize` e `discard` dizem `catalog_branch_missing`, `bind`
diz `send_contract_missing`, `read` diz `read_ticket_missing`. Chave de objeto e opaca e nao carrega
nome de arquivo nem pessoa. `_shared/r2_s3.ts` e `media_image_contract.ts` reutilizados **sem
edicao** (`git diff` em `_shared/` volta vazio) e os pins existentes ficaram como estavam.

**19/19 testes Deno passam localmente.**

## 3. Adapter produtivo do feed de Momentos (`f413c2dc`)

`principal_moments` nao tinha camada de dados. Agora tem, contra o `list_visible_moments` do
candidato da r13: no dia em que a migration for aplicada o cliente ja funciona, e ate la **falha
fechado como indisponivel**, nao com feed vazio silencioso.

Um ponto que vale registrar: a projecao **nao devolve contagens sociais**, entao `likes`, `comments`,
`shares` e `saves` viraram opcionais e ausentes por padrao. O cliente **nao renderiza zero que nao
recebeu**: o overlay do Momentos esconde a contagem que nao tem, uma curtida local nao transforma
ausencia em numero, e o Perfil omite a metrica em vez de imprimir zero. As fixtures continuam
fornecendo as suas, entao nada muda ali.

Midia chega como descritor opaco ligado ao espectador, ordenado por `display_order`, e `resolveMedia`
redime um por vez pelo gateway `moments-media`, reusando o guard de HTTPS compartilhado.

`principal_moments` + `principal_profile`: **50 passaram, 21 falharam**, as 21 sendo os goldens ja
vermelhos.

### Dependencia que isso expoe

O `moments-media` de hoje tem `read` ligado a `authorize_moments_media_read`, que e **do autor**. O
feed publico precisa de um ramo que redima o ticket de espectador via
`redeem_moments_media_read_ticket`, que criei no candidato e concedi **so a `service_role`**.
`moments-media` e gateway meu, entao esse ramo entra no proximo lote; registro para nao parecer que
o adapter ja tem leitura publica funcionando.

## 4. Estado das acoes

As 28 continuam `pending-verification`. Nenhuma promocao FE, BE ou E2E.

## 5. Deltas propostos para os rastreadores (por ID)

- `chat.attach`: gateway reservado existe com contrato asset/attempt/binding publicado e 19/19 Deno;
  todas as operacoes ainda indisponiveis por falta de RPC, cada uma com motivo nomeado.
- `momentos.view`: adapter produtivo entregue contra o candidato; sem replay e sem ramo de leitura
  publica no gateway ainda.
- `principal.profile-view`: deixa de imprimir contagem social ausente como zero.
- Demais IDs sem mudanca desde a r13.

## 6. Bloqueios ativos

1. Replay local dos dois candidatos SQL — lease ocupado (`R01-LOCAL-XLSX-02`, C02).
2. Extensao tipada do catalogo de Chat e SQL de staging/send — sem reserva, conforme I007.
3. Reserva de migration para `remove_happens_post` e projecao `post_id`/`can_remove` (r12).
4. Decisao sobre o contrato do Perfil institucional (r7).
5. Composicao produtiva de `for-you`, `moments`, `profile` e destino de `principal-chat` — C00 (r7).
6. Regeneracao de golden negada; revisao visual nominal da C00.

## 7. Primeiro criterio ainda aberto

O replay dos candidatos. Sem ele, `momentos.view` tem cliente e SQL revisavel, mas nenhum contrato
provado.

## 8. Proximo passo

Ramo de leitura por ticket de espectador no gateway `moments-media`, com testes Deno. Depois o
cliente de `momentos.remove` e da expiracao manual do Agora, que reaproveitam o desenho ja entregue
em `acontece.remove`.

## 9. ETA

- ramo de leitura no `moments-media` com testes: 1,5-2,5 h, nao bloqueada.
- cliente de `momentos.remove` e `agora.expire` manual: 2-3 h, nao bloqueada.
- cliente de `chat.revoke` e `chat.edit`: 2-4 h, nao bloqueada.
- replay dos candidatos: depende do lease.

## 10. Continuidade

`CronCreate` ID **`f10b9942`**, `7,37 * * * *`, com o prompt de retomada descrito na secao 1.

---

## Historico das revisoes anteriores

- **r13** (`9b809593`): I007 confirmada; dois candidatos SQL escritos sob reserva exclusiva —
  ordenacao de Comunicacoes com allowlist e cursor de duas metades, e `list_visible_moments`
  espelhando o Acontece; nenhum dos dois testes executado, porque o replay aguarda lease.
- **r12** (`4e9bfa26`): decisao do Owner sobre remocao aplicada; cliente de `acontece.remove`;
  auditoria de guardas por mutacao.
- **r11** (`073224d1`): nucleo de upload incorporado por cherry-pick; consumidor de Chat ligado.
- **r10** (`ef0d583d`): inventario das 28 acoes.
- **r9** (`2bfe364a`): tres acoes sem UI e sem contrato.
- **r8** (`a198c013`): semantica unica de ordenacao; `chat-media` pedido.
- **r7** (`1e5684b4`): mapa de Circulares; rotas do Principal sem composicao.
- **r6** (`c956d3fa`): fluxo de anexo de Chat.
- **r5** (`fdef81db`/`00c52284`): reserva do indicador devolvida sem mudancas.
- **r4** (`545355c4`): cron ativado; 41/41 Deno dos gateways.
- **r3** (`2f322d70`): goldens por viewport; compositor do Agora reconciliado.
- **r2** (`cbbff86a`): causa-raiz corrigida.
- **r1** (`a0927022`): ambiente, recorte, primeira medicao.

Ambiente constante: cwd `C:/Users/adrie/Documents/Coelo.worktrees/e2-r01-c05`, remoto
`origin https://github.com/otaldodexter-maker/coelo.git`, Flutter 3.44.2 / Dart 3.12.2, Deno 2.9.5,
modelo `claude-opus-5`, ID de harness `2a43a349-a639-4be5-aefc-1ff180d1fc7a`.
