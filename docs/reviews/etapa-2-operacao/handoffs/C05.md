---
title: "Handoff R01 C05"
source: "assignments/C05.md (R01-C05-I003 a I009, aditivo I008 de revisao SQL, ack 17:21:46 e detalhamento 17:34:14); repasse do Coordenador Claude C06 de 2026-09-08T17:30 sobre Supabase/Cloudflare; PROTOCOLO.md R01 (linhas 71 e 128); specs/050-principal-ui-ux-closure.md; referencias visuais do Owner em .codex/generated_images/01a0204c-d8d6-76c3-b20e-0e4e8a5ac1cf; decisions/0032-mvp-private-media-r2.md"
status: "active"
generated_at: "2026-09-08T17:48:55-03:00"
timezone: "America/Sao_Paulo"
---

# E2 R01 C05 — Comunicacao e Principal

## Revisao 16 — mudanca material

- rodada: R01; revisao: 16
- ultima instrucao processada: `R01-C05-I009` mais o repasse do C06 das ~17:30
- timestamp: 2026-09-08T17:48:55-03:00, relogio da maquina
- branch: `claude/e2-r01-c05-comunicacao`; baseline `479d1bd1`
- HEAD do codigo: `7810e7c584fa63eb6b7c79f0399ccdae2e582518`
- push: verificado; `git ls-remote` devolveu `7810e7c5`.

Esta revisao existe por duas mudancas materiais: o repasse do C06 (secao W), que preciso responder
antes que ele consolide o handoff dele, e as conferencias visuais/documentais das secoes X, Y e Z.
Os quatro subagentes da I008 **ainda estao rodando**; os resultados deles vao na proxima revisao,
depois que eu ler e revisar. Nao antecipo nada sobre eles.

## Suite completa — rodando, e nao vou citar numero dela

Disparei `flutter test` do `apps/superadmin` inteiro para a evidencia de fechamento. Ela ainda esta
rodando **e esta contaminada**: os subagentes C e D estao editando `principal_now/` e `chat/` durante
a execucao, entao qualquer contagem que eu tirasse dela agora misturaria falha real com arquivo
mudando debaixo do compilador. **Descarto essa execucao como evidencia** e repito a suite depois de
recolher e revisar o trabalho dos agentes. Prefiro nao ter numero a publicar numero sujo.

O que continua valendo como evidencia limpa desta rodada, medida antes do despacho dos agentes:
`flutter analyze` do `apps/superadmin` inteiro sem nenhuma issue; 9 testes de `momentos.remove`
verdes; 39 testes de Acontece verdes; `chat-media` 19/19 e `moments-media` 21/21 no Deno.

## X. `principal.profile-view` — subtelas de Circulares mapeadas, sem criar IDs

A I001 diz que Circulares esta **sob `principal.profile-view`** no inventario atual e manda mapear
as subtelas sem criar IDs em silencio. Mapeei o que existe hoje no codigo. **Nao criei nenhum ID
novo**; proponho o delta e a C00 decide.

| subtela | classe / arquivo | o que faz hoje | acao do usuario |
| --- | --- | --- | --- |
| aba Circulares do perfil | `PrincipalProfileCircularsTab` (`principal_circular_surfaces.dart`) | lista paginada por escopo, com loading, vazio, erro e **nao autorizado distintos**; paginacao single-flight; invalida ao trocar escopo | rolar, paginar, abrir |
| cartao de circular no feed | `PrincipalCircularFeedCard` (mesmo arquivo) | identifica que o item e Circular e mostra o estado de resposta | abrir previa acessivel em dialogo |
| detalhe da circular | `PrincipalCircularDetailPage` | carrega uma circular por id, com retorno contextual e `Tentar novamente` no erro | ler, voltar |
| leitor com respostas | `PrincipalCircularReader` | leitura do corpo e **envio de respostas** (`circular-submit-responses`) | responder |
| compositor | `PrincipalCircularComposerPage` | titulo, corpo, anexos, perguntas, previa, rascunho e publicacao (`circular-save-draft`, `circular-publish`) | criar/publicar |

**Delta que proponho a C00, por ID, sem executar:**

1. `principal.profile-view` cobre com honestidade as tres primeiras linhas (listar, identificar,
   abrir/ler). Sao leitura.
2. As duas ultimas **nao sao leitura de perfil**: `PrincipalCircularReader` grava resposta e
   `PrincipalCircularComposerPage` cria e publica. Hoje elas estao dentro do mesmo ID de leitura, o
   que subdeclara escrita. Peco decisao da C00 sobre onde elas pertencem — se em IDs proprios de
   Circulares, se sob os IDs de Avisos ja existentes, ou se ficam explicitamente fora do MVP.
   **Nao vou criar esses IDs por conta propria** e nao alterei denominador nenhum.

## Y. `principal.profile-edit` — o que falta e a decisao que peco

Situacao honesta: **nao ha nenhuma afordancia de edicao no perfil institucional hoje**. Conferi o
arquivo inteiro (`principal_profile_preview_page.dart`, 1244 linhas): nao existe botao, rota nem
comando de edicao. O ID esta ativo no inventario e continua **nao entregue**.

Nao entreguei um contrato inventado porque a decisao do contrato do Perfil institucional esta
pendente com a C00 e a I007 e explicita: lacuna de politica exige decisao especifica. Para encurtar
essa decisao, ela agora tem os campos reais na mesa, com a minha recomendacao:

| campo em `PrincipalProfilePreviewData` | natureza | recomendacao |
| --- | --- | --- |
| `name`, `typeLabel` | cadastro da instituicao | **nao editavel aqui**; ja pertence ao dominio de Instituicoes. Editar nos dois lugares cria duas fontes de verdade |
| `bio` | editorial | **editavel**, com limite de tamanho e auditoria |
| `highlights` | curadoria editorial | **editavel**, com ordem e limite definidos |
| `metrics` (Publicacoes/Momentos/Circulares) | contagem derivada | **nunca editavel**; tem de ser calculado no servidor, senao vira numero declarado pelo cliente |
| `nextEvent` | derivado da Agenda | **nao editavel aqui**; espelha a Agenda |
| `links` | ambiguo no codigo (hoje sao iniciais, nao URLs) | **precisa de decisao**: sao vinculos de pessoas/unidades ou links externos? A resposta muda politica de privacidade e LGPD |

Com essa decisao eu entrego `principal.profile-edit` no mesmo desenho ja aceito nas remocoes:
permissao vinda da projecao autorizada com default `false`, adaptador falhando fechado enquanto o
comando autorizado nao existir, e auditoria com motivo/versao. Sem a decisao, qualquer contrato que
eu escrevesse seria invencao minha.

## Z. Conferi o wizard do Agora contra as referencias que o Owner apontou

O Owner respondeu que "o estilo de publicacoes e diferente, nao tem wizard lateral" e apontou
`C:\Users\adrie\.codex\generated_images\01a0204c-d8d6-76c3-b20e-0e4e8a5ac1cf`. Fui olhar em vez de
supor. A pasta tem 19 imagens; **abri 3** e registro exatamente quais:

| imagem | o que e | tem trilho lateral de etapas? |
| --- | --- | --- |
| `exec-9cd545d1-...` | **Editar atividade** — Superadmin administrativo, "Etapa 3 de 6" | **sim**, trilho lateral no desktop; rodape `Cancelar / Anterior / Salvar rascunho / Continuar` |
| `exec-bbfe2ee4-...` | **Lancar avaliacoes** — Superadmin administrativo, "Etapa 2 de 4" | **sim**, mesmo trilho |
| `exec-0aa0905a-...` | **Acompanhamento / Visao da familia** — familia **Coelo (Principal)** | **nao**; dock inferior de 5 itens no mobile, sidebar de navegacao no desktop, conteudo em zonas de cartao, breadcrumb `Principal > Acompanhamento` |

A leitura e direta e confirma o Owner: o **trilho lateral de etapas pertence a familia
administrativa**, nao a familia Principal. As duas primeiras imagens nao sao contraexemplo da
decisao — sao justamente o padrao que **nao** pode ser aplicado a publicacao. A terceira e a familia
certa e nao tem trilho nenhum.

Portanto a correcao que fiz no compositor do Agora esta alinhada a referencia do Owner: tirei o
`PrincipalPublicationStepNavigation` lateral e os botoes `Continuar`/`Anterior`, e deixei uma barra
de progresso de 3 segmentos com `Semantics` de "Progresso da publicacao", que **nao** e wizard
lateral. Mantive `bodyMaxWidth: 1120` e o layout em zonas a partir de 600px, empilhado abaixo disso
ou com texto ampliado.

Ressalva honesta: **abri 3 das 19 imagens**, escolhidas pelas mais recentes. Nao afirmo ter
conferido o conjunto inteiro. Se a C00 ou o Owner quiser a conferencia nominal das 19, faco no
proximo lote.

Uma diferenca que **nao** virou correcao, e explico por que. Na terceira imagem o dock inferior e
`Acontece / Para voce / Agora / Momentos / Agenda`, sem acao central. O implementado e
`Home / Para voce / (+) Publicar no Agora / Momentos / Pesquisar`. Fui a fonte antes de mexer:
`specs/050-principal-ui-ux-closure.md` diz textualmente que "o dock canonico preserva Home, Para
voce, a acao central de publicar no Agora, Momentos e Pesquisar", e a referencia de UI confirma que
"navegacao, header, dock e launcher seguem a composicao unica da spec 050". A imagem e de 20-24/08 e
**foi superada pela spec aprovada**. Portanto o codigo esta certo e a imagem esta desatualizada:
**nao alterei o dock**. Ja errei nessa direcao uma vez nesta rodada — tratei divergencia de golden
como regressao antes de ler a spec 050 — e nao vou repetir o erro na direcao oposta.

## W. Repasse do C06 sobre Supabase/Cloudflare — recebido, e o que ele muda de fato

Recebi as ~17:30 o repasse do **Coordenador Claude E2 R01 C06** com a fala do Owner: "o supabase e
cloudflare estao liberados para uso". Confirmo o recebimento. Registro tambem, sem inflar alcance:

**Estado real da ferramenta nesta sessao, conferido agora:**

| ferramenta | estado verificado |
| --- | --- |
| Supabase CLI | `2.117.0`, **autenticado** — `supabase projects list` respondeu; projeto `coelo` `evvbomzejfijozbtgvpt`, `ACTIVE_HEALTHY`, **nao linkado** nesta worktree |
| wrangler | `4.130.0` instalado |
| MCP `cloudflare-api` | **nao autenticado** nesta sessao, que e nao interativa; nao consigo rodar o OAuth daqui |

**O que o repasse NAO destrava, e por que nao vou tratar como destravado:**

1. O meu caminho critico nao era autorizacao do Owner — era **concessao nominal da C00**. O proprio
   repasse do C06 diz que as reservas da I007 seguem valendo ate release por SHA/ack, e o ack
   operacional das 17:21 ja dizia que o replay dos meus candidatos aguarda revisao/concessao, **nao**
   a lease XLSX. O PROTOCOLO (linha 71) e explicito: autores preparam candidato e teste no dominio
   reservado **sem aplicar remoto**; a linha 128 fecha com "nenhuma autorizacao remota nova".
2. A instrucao **direta do meu usuario** para este lote inclui, com todas as letras, **sem migrations
   ou deploys remotos**. Um repasse de sessao par nao substitui a palavra do proprio usuario sobre a
   restricao que ele mesmo me deu. Nao vou tratar mensagem de par como aprovacao pendente do usuario.
3. Portanto: **nao linkei projeto, nao rodei SQL remoto, nao publiquei Edge Function e nao toquei em
   bucket**. Nada mudou de "pending-verification" para verificado por causa deste repasse, e eu nao
   vou declarar que mudou.

**O que eu faria se e quando o proprio usuario confirmar a suspensao daquela restricao**, em ordem,
tudo forward-only e minimizado, sem dado pessoal:

1. Leitura de catalogo em producao para conferir exatamente os dois achados que a revisao da C00
   levantou: os `EXECUTE` reais das funcoes `superadmin_notice_*` (valida a correcao de ACL) e a
   existencia/forma de `moments.publications.read` com `module_code`/`action_code` (valida a
   correcao do INSERT de permission). Sao consultas a catalogo, nao a dado de pessoa.
2. So depois, e so com concessao nominal da C00, o replay dos dois candidatos no runner, com
   perfil/runner/porta/lease registrados aqui para C02/C03/C04 nao colidirem.

Ate la sigo com o lote independente, que nao depende disso.

## V. Pendencias que dependem da C00

Sem cobranca repetida, so o estado:

1. **Concessao nominal** para o replay dos dois candidatos SQL, agora com as correcoes do aditivo
   aplicadas localmente pelos subagentes A e B (resultado na proxima revisao). O repasse do C06 nao
   substitui essa concessao e eu nao a estou presumindo.
2. **Reserva** para `remove_happens_post` e para a projecao `post_id`/`can_remove`; o mesmo para o
   comando de remocao de Momentos (`can_remove`) e para a expiracao manual do Agora.
3. **Extensao tipada do catalogo Chat** e SQL de staging/send, ainda sem reserva.
4. **Contrato do Perfil institucional** — agora com os campos reais e uma recomendacao por campo na
   secao Y, para encurtar a decisao.
5. **Onde ficam** o leitor com respostas e o compositor de Circulares, hoje subdeclarados dentro de
   `principal.profile-view` (secao X). Nao criei IDs.
6. **Composicao produtiva** das rotas `for-you`, `moments` e `profile`, e destino de producao para
   `principal-chat`.
7. **Revisao visual nominal** das divergencias golden-only. Nao regenerei e nao vou regenerar.

## U. Retomada exata

```
cd C:\Users\adrie\Documents\Coelo.worktrees\e2-r01-c05
git status --short
git log --oneline -5
cd apps\superadmin
flutter analyze
flutter test test\features\principal_moments\presentation\principal_moments_remove_test.dart
flutter test test\features\principal_happens\presentation\principal_happens_remove_post_test.dart
cd ..\..\packages\coelo_database\supabase\functions\chat-media
deno test --allow-all
```

Cron `f10b9942` ativo (`7,37 * * * *`), session-only, so dispara com o REPL ocioso, jitter de ate
~3 min. Fechamento seguro **09/09 05:30**, entrega final ate **06:00**.
