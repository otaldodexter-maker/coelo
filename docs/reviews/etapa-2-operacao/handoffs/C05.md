---
title: "Handoff R01 C05"
source: "assignments/C05.md (R01-C05-I001); PROTOCOLO.md R01; docs/reviews/coelo-flutter-pendencias.md; docs/reviews/coelo-supabase-pendencias.md; docs/reviews/coelo-flutter-integrado-supabase-pendencias.md"
status: "active"
generated_at: "2026-09-08T12:35:00-03:00"
timezone: "America/Sao_Paulo"
---

# E2 R01 C05 — Comunicacao e Principal

## Revisao 1

- rodada: R01
- revisao: 1
- ultima instrucao processada: `R01-C05-I001` (assignment revisao 1), lida diretamente na worktree C00.
- timestamp: 2026-09-08T12:35:00-03:00 (America/Sao_Paulo)
- conversa: `E2 R01 C05 - Comunicacao e Principal`; ID real da sessao: **nao exposto por ferramenta
  nesta sessao**. Registro o identificador de runtime disponivel:
  `2a43a349-a639-4be5-aefc-1ff180d1fc7a` (diretorio de sessao do harness). Nao afirmo que seja o ID
  que a C00 usa; se a C00 precisar de outro identificador, precisa nomea-lo.
- ferramenta/modelo real: Claude Code (desktop, aba Code), modelo `claude-opus-5`. Sem cobranca de
  API adicional e sem seletor de esforco exposto na sessao.

## Ambiente confirmado

- cwd de trabalho: `C:/Users/adrie/Documents/Coelo.worktrees/e2-r01-c05`
- branch: `claude/e2-r01-c05-comunicacao`
- baseline HEAD: `479d1bd1771b13e0cd96084d244fc2cdc6b0e556`
  (`docs(e2-r01): establish single-writer protocol and five executor assignments`)
- baseline de codigo herdado citado na assignment: `6cb8ba15bae15f5a6129b0db6e740a66f8f82b3f`, que e
  ancestral do HEAD atual (`6cb8ba15` -> `479d1bd1`).
- upstream: **ainda nao configurado** para esta branch; nenhum push nesta revisao.
- remoto: `origin https://github.com/otaldodexter-maker/coelo.git`
- toolchain local: Flutter 3.44.2 / Dart 3.12.2 (stable), Windows 11. `flutter pub get` executado em
  `apps/superadmin` porque a worktree nao tinha `.dart_tool`.

### Divergencia de caminho na instrucao

A instrucao da sessao aponta `C:\Users\adrie\Documents\Coelo\.worktrees\e2-r01-c05`. Esse caminho
**nao existe**. A worktree realmente registrada em `git worktree list` e
`C:/Users/adrie/Documents/Coelo.worktrees/e2-r01-c05` (diretorio irmao, sem barra). Estou usando a
worktree registrada. A mesma divergencia vale para a C00 (`Coelo.worktrees/e2-c00`), de onde li
`PROTOCOLO.md` e `assignments/C05.md`.

## Recorte desta rodada

- objetivo: fechar as 28 acoes `chat.*`, `notices.*`, `acontece.*`, `agora.*`, `momentos.*` e
  `principal.*` dentro de `apps/superadmin`, com a midia exigida por acao.
- incluido: UI, dominio, adapters e testes focais das features reservadas; correcao das duas
  divergencias apontadas pelo Owner (tabela de Comunicacoes e criacao/publicacao do Agora);
  reaproveitamento do nucleo de midia do C02.
- fora de escopo: `apps/principal`, `apps/admin`, Site, convites (C01), router/shell/barrels/
  migrations historicas (C00), gateway universal concorrente, import/export adiados.
- ordem: (1) inventario de delta por prova executavel; (2) regressoes visuais contra golden
  aprovado; (3) problemas do Owner; (4) lifecycle e negativas por superficie; (5) midia por acao.
- criterio de parada da preparacao: este handoff (revisao 1) com recorte e evidencia medida.

## Evidencia medida nesta revisao (FE local)

Comando unico, worktree C05, dentro de `apps/superadmin`:

```
flutter test test/features/chat test/features/notices test/features/circulars \
  test/features/principal_circulars test/features/principal_for_you \
  test/features/principal_happens test/features/principal_happens_publication \
  test/features/principal_moments test/features/principal_moments_publication \
  test/features/principal_now test/features/principal_now_publication \
  test/features/principal_profile test/features/principal_shared test/features/profile_about \
  --reporter=compact
```

Resultado: **876 passaram, 45 falharam** em 02:30. Ambiente: Flutter 3.44.2 local, sem backend, sem
`/dev`, sem provedor de midia. E medicao de estado, nao certificacao FE.

As 45 falhas sao todas golden (`LocalFileComparator`). Nenhum teste comportamental falhou.

| Arquivo de teste | Falhas | Causa observada no artefato de falha |
|---|---|---|
| `chat/presentation/superadmin_chat_page_golden_test.dart` | 9 | (a) o shell ganhou o no de navegacao `Governanca` depois do golden aprovado; (b) a lista de conversas ganhou rodape `Pagina 1 de 1` que o master aprovado nao tem |
| `principal_happens/presentation/principal_happens_preview_golden_test.dart` | 11 | o card `Publicar agora` da faixa Agora virou card de criacao administrativo (borda tracejada, `+` sem circulo preenchido); o master aprovado tem circulo laranja preenchido e borda solida |
| `principal_moments/presentation/principal_moments_preview_golden_test.dart` | 11 | deslocamento horizontal do viewer inteiro; causa ainda nao isolada |
| `principal_profile/presentation/principal_profile_preview_golden_test.dart` | 10 | botao `Acompanhar` e faixa de estatisticas deslocados; causa ainda nao isolada |
| `notices/notice_directory_golden_test.dart` | 2 | em 375 o titulo do card de comunicacao quebra em duas linhas onde o master mostra uma linha, e o card seguinte fica cortado |
| `circulars/presentation/circular_directory_golden_test.dart` | 2 | mesma familia de diretorio; ainda a confirmar caso a caso |

Os artefatos (`masterImage`, `testImage`, `isolatedDiff`, `maskedDiff`) ficaram em cada `*/failures/`.
**Nao sao referencia aprovada** e nao serao promovidos a golden.

## Leitura dos dois problemas do Owner

### 1. Tabela de Comunicacoes

`apps/superadmin/lib/features/notices/presentation/notice_directory_page.dart` ja usa os componentes
canonicos administrativos (`CoeloAdminListingToolbar`, `CoeloAdminResizableTable`,
`CoeloAdminPagination`, `CoeloAdminInteractiveCard`, `CoeloAdminFlyout`,
`CoeloAdminExpandableStatusIndicator`). A divergencia medida hoje nao e de familia de componente e
sim de composicao em 375: o titulo do card quebra e o card seguinte e cortado. A conferencia item a
item contra a baseline de Instituicoes (anexos 8-18: cabecalho tonal, divisores, linhas continuas,
`space4` da faixa de criacao, linha hovered que nao vira card, toolbar/conteudo/paginacao como
composicao unica) continua e entra na revisao 2.

### 2. Criacao e publicacao do Agora

`principal_now_publication_page.dart` implementa hoje um wizard de **2 etapas** (`Midia` e
`Detalhes`) com rail lateral de 248 px (`PrincipalPublicationStepNavigation`) e rodape com
`Continuar`/`Anterior`.

A `specs/036-principal-now-publication-mvp.md` (status `approved`) descreve para o Agora: 375 com
"midia dominante, ferramentas compactas, **uma rolagem** e acoes em largura total"; 768 com
"midia/ferramentas e dados editoriais em duas zonas"; 1440 com "midia vertical ampla, ferramentas
adjacentes e coluna editorial enxuta". O que a spec compartilha com os demais `Publicar` do
Principal e explicitamente **shell, conteiner direito, insets e rodape responsivo** aprovados em
2026-08-31; o rail de etapas **nao** esta nessa lista. O rail de etapas e aprovado para o
**Acontece** (`docs/superpowers/specs/2026-08-20-coelo-happens-publication-design.md`: "rail,
composer central e previa lateral") e la sera preservado.

Conclusao preliminar: a reconciliacao correta e remover o wizard de 2 etapas **apenas do Agora**,
preservando integralmente as partes funcionais (midia, texto sobre a midia, musica, corte, capa,
publico e contexto, agendar publicacao, salvar rascunho, publicar agora), o rodape aprovado e o rail
do Acontece. Nenhuma etapa aprovada e removida: as etapas aprovadas em `Publicar` pertencem ao
Acontece e continuam.

Impacto conhecido: 15 goldens de `principal_now_publication` e cerca de 18 pontos de teste que hoje
tocam `Continuar`/`Anterior`. Os goldens novos passarao a registrar a composicao da spec 036; isso
sera declarado como mudanca intencional e **fica sujeito a confirmacao visual do Owner**. Nenhum
golden de outra superficie sera regenerado para esconder divergencia.

## Acoes e estado (28 IDs)

As 28 continuam `pending-verification` nos tres rastreadores. Nenhuma promocao nesta revisao.

`chat.list`, `chat.open`, `chat.send`, `chat.edit`, `chat.attach`, `chat.receipts`, `chat.revoke`,
`notices.list`, `notices.create`, `notices.edit`, `notices.schedule`, `notices.publish`,
`notices.archive`, `acontece.feed`, `acontece.create`, `acontece.publish`, `acontece.remove`,
`agora.view`, `agora.create`, `agora.publish`, `agora.expire`, `momentos.view`, `momentos.create`,
`momentos.publish`, `momentos.remove`, `principal.for-you`, `principal.profile-view`,
`principal.profile-edit`.

## Reservas consumidas

Nenhuma. Nenhum arquivo alterado e nenhum commit de codigo nesta revisao.

As correcoes previstas caem em `apps/superadmin/lib/features/{chat,notices,circulars,principal_*}/`
e nos testes focais correspondentes, que ja sao meus. O no `Governanca` do shell
(`apps/superadmin/lib/app/navigation/superadmin_navigation.dart`) **nao e meu**: ver bloqueios.

## Bloqueios

1. **Goldens de Chat contaminados por mudanca de shell alheia.** O master aprovado mostra
   `Comunicacao > Circulares`; o codigo atual mostra `Governanca` no mesmo nivel. Navegacao e arquivo
   compartilhado sob C00. Nao vou editar navegacao nem regenerar goldens de Chat apenas para absorver
   essa mudanca. Preciso de decisao da C00: ou o no `Governanca` e aprovado e os goldens que contem o
   shell sao regenerados sob decisao registrada, ou a navegacao volta ao estado do golden. Enquanto
   isso, `chat.*` nao pode ser certificado FE.
2. **Midia real (R2/Stream) nao exercitavel nesta sessao.** Os conectores MCP `cloudflare-api` e
   `figma` exigem autorizacao OAuth e esta sessao e nao interativa; o MCP `github` falhou ao conectar
   (`400 Authorization header is badly formatted`). Nao ha prova E2E de midia possivel daqui. O
   trabalho independente de FE e dominio continua.
3. **Migrations e deploy remotos pertencem a C00.** Nenhum pacote remoto foi preparado nesta revisao.

## Primeiro criterio ainda aberto

Separar, com prova executavel e nao por suposicao, quais das 45 falhas golden sao regressao de codigo
desta frente e quais sao golden desatualizado por mudanca externa aprovada. Sem essa separacao nao
existe certificacao FE honesta em Chat, Acontece, Momentos, Perfil e diretorios.

## Proximo passo

1. Isolar a causa de cada familia de falha golden (Momentos e Perfil ainda sem causa nomeada).
2. Corrigir o card `Publicar agora` do Acontece para a anatomia Principal aprovada (menor delta com
   causa ja isolada).
3. Reconciliar a criacao/publicacao do Agora com a spec 036.
4. Conferir a tabela de Comunicacoes item a item contra a baseline de Instituicoes.

## ETA

Separada e ainda parcial; so o que ja inspecionei esta estimado.

- isolar as familias golden restantes (Momentos, Perfil, Circulares): 1,5-2,5 h.
- corrigir o card `Publicar agora` do Acontece: 0,5-1 h com teste.
- reconciliar o Agora conforme a spec 036 (codigo, cerca de 18 pontos de teste e 15 goldens): 3-5 h.
- conferir a tabela de Comunicacoes contra a baseline: 1-2 h.
- lifecycle, negativas e midia por acao nas 28 acoes: **nao estimavel ainda**; depende do resultado
  dos itens acima e do desbloqueio de midia.

Nao somo essas faixas em um total de rodada e nao reutilizo a estimativa historica de 36-60 h.

## Deltas propostos para os rastreadores (por ID)

Nenhum delta de estado nesta revisao: nada saiu de `pending-verification` e nada regrediu de estado
formal. Proponho que a C00 registre, ao integrar, os fatos medidos abaixo, sem mudar classe
operacional:

- `chat.list`: existe rodape de paginacao `Pagina X de Y` na lista de conversas, alimentado por
  `total`/`has_more` vindos da RPC (`ChatInboxPage.totalCount`). Ainda nao confirmei a RPC no banco.
  O golden aprovado nao contem esse rodape.
- `acontece.feed`: card `Publicar agora` divergente da anatomia Principal aprovada, com regressao
  visual confirmada contra o golden master.
- `agora.create` e `agora.publish`: a composicao atual (wizard lateral de 2 etapas) diverge da
  spec 036 aprovada.

## Continuidade

Mecanismo nativo desta sessao: **ainda nao configurado**. Esta sessao do Claude Code oferece `/loop`
(retomada temporizada); ainda nao o ativei e por isso nao afirmo ID nem cadencia. Vou consultar
`assignments/C05.md` na C00 no inicio e no fim de cada lote e publicar handoff as 12:50, 14:50 e
17:30, alem de quando houver mudanca material. Se eu ativar `/loop`, registro ID e limites reais na
revisao seguinte. A C00 nao consegue me acordar por arquivo.
