---
title: "Handoff R01 C05"
source: "assignments/C05.md (R01-C05-I003 a I009, aditivo I008, R01-ALL-RTK-1808 e R01-VISUAL-1835); reserva temporaria exclusiva R01-C06-I010, ja liberada; specs/036-principal-now-publication-mvp.md linhas 27-36; docs/reviews/evidence/etapa-2/principal-visual/2026-08-31-publicar-agora-approved.png"
status: "active"
generated_at: "2026-09-08T20:36:19-03:00"
timezone: "America/Sao_Paulo"
---

# E2 R01 C05 — Comunicacao e Principal

## Revisao 21 — evidencia da suite atualizada e adaptadores cobertos

- rodada: R01; revisao: 21
- ultima instrucao processada: `R01-C05-I009`, mais `R01-VISUAL-1835` e a reserva `R01-C06-I010`
- timestamp: 2026-09-08T20:36:19-03:00, relogio da maquina
- branch: `claude/e2-r01-c05-comunicacao`; baseline `479d1bd1`
- HEAD do codigo: `d003365f9880049a130782cb0a0c97d32ea10ebb`
- push: verificado; `git ls-remote` devolveu `d003365f`
- arvore limpa; nenhum PNG de `failures/` em commit — conferi o `--stat` do commit

## 1. Suite completa, medida de novo depois de tudo que mudou hoje

**`+4360 ~5 -224` em 6:44.** Comparando com a medicao da r17, que foi `+4325 ~5 -224`:

| | r17 | agora |
| --- | --- | --- |
| passando | 4325 | **4360** |
| golden | 165 | **165** |
| nao-golden | 59 | **59** |

Ou seja: **35 testes novos e nenhuma falha nova**, apesar da anatomia do Agora ter mudado duas
vezes, do recorte do Chat, da troca de `on Exception` nos tres controladores e do `ExcludeFocus` no
toggle. As 14 golden do Agora continuam 14. As 59 nao-golden continuam sendo as herdadas do
baseline, ja provadas em worktree descartavel na r17.

## 2. `R01-VISUAL-1835` processada, e o que ela nao resolve

Li a decisao. Os itens 3 e 4 sao os corretivos de Chat e Agora que ja entreguei em `478a805e` e
`edded9bf`.

O item 4 aponta a **spec 036 linhas 27-36** como aprovacao da midia ampla. Fui ler em vez de assumir:
as linhas dizem `mobile 375: midia dominante`, `tablet 768: midia/ferramentas e dados editoriais em
duas zonas`, `desktop 1440: midia vertical ampla, ferramentas adjacentes e coluna editorial enxuta`.
**Nenhuma medida.** Entao a pendencia da geometria continua de pe pelo mesmo motivo de antes:
implementar exige numero, e a propria spec exige reproduzir a geometria canonica sem aproximacoes.

Registro tambem um **falso conflito que quase reportei**. A linha do 768 fala em "duas zonas", e por
um momento pareceu contradizer a coluna unica da referencia aprovada. Relendo: as duas zonas sao
midia/ferramentas e dados editoriais, que e exatamente o que a minha coluna entrega, empilhada. Nao
ha conflito, e nao vou inventar um para parecer diligente.

## 3. Adaptadores de feed cobertos (`d003365f`)

Segui passando mutacao pelas entregas antigas e achei duas lacunas.

**O adaptador de Momentos nao tinha teste nenhum.** Ele chama a projecao autorizada, mapeia
descritores opacos e falha fechado na remocao, e nada disso era exercido. Agora sao 17 casos:
`can_remove` so concede com `true` — string "true" e numero 1 negam; linha sem campo obrigatorio
recusada em vez de renderizada pela metade; descritor de midia sem ordem recusado; negacao virando
`Unauthorized` e qualquer outra falha virando `Unavailable`; e a remocao falhando fechada **sem
chegar a rede**, com a contagem de requisicoes exigida em zero.

No adaptador de Acontece faltavam os mesmos dois pontos: o mapeamento de `can_remove` e o
`removePost` fail-closed.

**A mutacao achou um caso que eu nao tinha imaginado.** Recusar a linha quando `moment_id` esta
ausente **nao** cobria `moment_id` em branco: o mutante que trocava `id == null || id.isEmpty` por
`id == null` sobrevivia. Nao e detalhe de teste — a afordancia de remocao no cliente so verifica
`id != null`, entao um id vazio atravessaria e viraria um comando sem alvo. Com os casos de string
vazia e de espacos, o mutante morre.

Cinco mutantes mortos no adaptador de Momentos. 37 verdes nos dois adaptadores, `analyze` limpo.

## 4. O que ja tinha sido conferido e nao tem lacuna

Para a C00 nao gastar tempo repetindo: o guarda de URL assinada
(`parsePrincipalSignedMediaUrl`) esta **completo** — cada uma das quatro clausulas tem caso, e a
lista de recusas cobre ausencia de esquema, `http`, `ftp`, `file`, `data`, `javascript`, host vazio,
credenciais embutidas e tipo nao textual.

## 5. Pendencias que dependem da C00

1. **Concessao nominal** para o replay dos dois candidatos SQL corrigidos.
2. **Geometria da midia ampla** do Agora em 1440, com medidas — secao 2.
3. **Rede de captura global de erro** no `apps/superadmin`: `runZonedGuarded`,
   `FlutterError.onError`, `PlatformDispatcher.instance.onError` e `ErrorWidget.builder` nao existem
   no app. Fora do meu recorte; nao inventei uma.
4. **Reserva** dos comandos autorizados de remocao e expiracao.
5. **Contrato de revisao do Chat** e **contrato do Perfil institucional** para os campos restantes.
6. **Remocao do `PrincipalPublicationProgressBar`**, orfao no frame — reserva ja liberada por mim.
7. **Onde ficam** o leitor com respostas e o compositor de Circulares.
8. **Master x referencia** de Acontece e Momentos quanto ao trilho.
9. **Revisao visual nominal** dos goldens, inclusive os 14 do Agora.

## 6. Retomada exata

```
cd C:\Users\adrie\Documents\Coelo.worktrees\e2-r01-c05
git status --short
git log --oneline -12
cd apps\superadmin
flutter analyze
flutter test test\features\principal_moments\data
flutter test test\features\principal_happens\data
flutter test test\features\principal_shared\presentation\principal_publication_frame_test.dart
flutter test test\features\principal_now_publication\presentation\principal_now_publication_error_recovery_test.dart
```

Cron `e9f4cae3` ativo. Fechamento seguro **09/09 05:30**, entrega final ate **06:00**.
