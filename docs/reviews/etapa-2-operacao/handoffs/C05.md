---
title: "Handoff R01 C05"
source: "assignments/C05.md (R01-C05-I003 a I009, aditivo I008 de revisao SQL, ack 17:21:46 e detalhamento 17:34:14); PROTOCOLO.md R01; decisao do Owner 2026-09-08 sobre remocao e sobre trabalho continuo ate o fechamento; decisions/0032-mvp-private-media-r2.md; packages/coelo_database/migrations/20260821112822*; docs/reviews/coelo-flutter-pendencias.md; docs/reviews/coelo-supabase-pendencias.md; docs/reviews/coelo-flutter-integrado-supabase-pendencias.md"
status: "active"
generated_at: "2026-09-08T17:37:25-03:00"
timezone: "America/Sao_Paulo"
---

# E2 R01 C05 — Comunicacao e Principal

## Revisao 15 — mudanca material

- rodada: R01; revisao: 15
- ultima instrucao processada: `R01-C05-I009` (li tambem I003, I004, I005, I006, I007, I008, o
  aditivo I008 de revisao SQL das 17:24:56, o ack operacional das 17:21:46 e o detalhamento tecnico
  das 17:34:14)
- timestamp: 2026-09-08T17:37:25-03:00, relogio da maquina
- branch: `claude/e2-r01-c05-comunicacao`; baseline `479d1bd1`
- HEAD do codigo: `cea29b1f881d806bdf1e88fa3a5fb416d8f0fb5c`; handoff em commit proprio
- push: verificado; `git ls-remote origin claude/e2-r01-c05-comunicacao` devolveu `cea29b1f`.

## 1. Confirmacao de recebimento

Confirmo a leitura viva, pelo mecanismo local, de todas as instrucoes abaixo. Registro o que cada
uma me obriga e o que ja fiz, sem cobranca repetida.

- **I003 / I004** — catalogo evolui exclusivamente pela C02; nao criei gateway nem catalogo
  paralelo. Nao criei gate obrigatorio de Stream. Preservo tombstone, auditoria e retencao: as
  remocoes que entreguei sao logicas, marcam o ativo e nao presumem delete de metadados.
- **I005** — o handoff nao encerra a execucao. Segui para o lote seguinte no mesmo turno.
- **I006** — nucleo de upload cliente aprovado. **Conferi por diff antes de usar**, como a instrucao
  exige, e **nao fiz o cherry-pick**: ele seria duplicacao.
  `git diff 2980151e HEAD -- packages/coelo_api/lib/src/media packages/coelo_api/lib/coelo_api.dart`
  volta **vazio** — os quatro arquivos (`media_upload_contract.dart`, `media_uploader.dart` e os dois
  testes) e o barrel ja estao identicos ao release da C00 na minha arvore, pelo commit `96706739`,
  equivalente de `0a94f2da`. E dele que vem o `SessionMediaUploader` consumido por `chat.attach`.
  Nao dupliquei patch e nao fiz merge da C00.
- **I007** — reservas confirmadas e em uso: os dois candidatos SQL, os seis arquivos de
  `chat-media`, e a orientacao de que `chat.edit`/`chat.revoke`, `acontece.remove` e
  `momentos.remove` exigem **implementacao**, nao so teste. `acontece.remove` e `momentos.remove`
  estao implementados; `chat.edit`/`chat.revoke` estao em execucao agora (secao 3).
- **I008 subagentes** — confirmado e **efetivamente iniciado**, nao apenas planejado (secao 3).
- **Aditivo I008 (revisao SQL, SHA `5f0e7e29`)** e **detalhamento das 17:34:14** — recebidos. Os
  achados sao concretos e os aceito; estou corrigindo os dois candidatos **localmente**, nos mesmos
  arquivos reservados. Nenhuma execucao SQL, nenhuma aplicacao remota, nenhum replay.
- **I009 / C06** — recebida. Ainda **nao observei** a r1 operacional da C06; ate la trato a C00
  como cobertura, como a instrucao determina. Nao aguardei a C06 para seguir com lote de codigo
  independente.

## 2. `momentos.remove` entregue (`cea29b1f`)

Reaproveita o desenho ja aceito em `acontece.remove`, sem redesenhar.

- A permissao vem da projecao autorizada (`can_remove`), **default `false` quando ausente**. O
  cliente nao deriva permissao; sem id de servidor tambem nao ha afordancia.
- O adaptador de producao **falha fechado** (`PrincipalMomentsRemoveUnavailable`) enquanto o comando
  autorizado nao existir. Nao inventei RPC, receipt nem sucesso.
- Sucesso confirmado **recarrega o feed**: a ausencia e provada pelo servidor, nunca escondendo o
  quadro localmente. Motivo obrigatorio, botao desabilitado sem motivo, `requestId` preservado por
  momento para que a repeticao **replay** em vez de remover duas vezes.
- O dialogo de remocao virou componente compartilhado (`principal_removal_dialog.dart`), consumido
  por Acontece e Momentos. As chaves e o comportamento ja aceitos em `acontece.remove` nao mudaram —
  os 8 testes de Acontece seguem verdes sem alteracao.

**Evidencia.** `flutter analyze` no `apps/superadmin` inteiro: `No issues found`. 9 testes novos de
aceitacao passando; suite de Acontece (39 testes) verde.

**Mutacao, com um resultado negativo que registro em vez de esconder.** Mutei tres guardas:

| mutante | resultado |
| --- | --- |
| `canRemove && id != null` -> `true` | **morto** (2 testes falharam) |
| comando alveja `moments.first` em vez do indice corrente | **morto** |
| pager entrega `onRemove` a todo quadro (remove a guarda de indice) | **sobreviveu** |

O terceiro sobreviveu porque o `PageView` descarta o quadro fora da tela: a guarda era inalcancavel
e nenhum teste podia prova-la. **Removi a guarda** em vez de mante-la sem prova, e mantive a que e
provada — o comando resolve o momento corrente no instante da invocacao. Tambem reescrevi um teste
meu que era vacuo ("apenas o momento em tela tem a afordancia") pelo que de fato se observa: apos
paginar, o comando carrega `moment-2`, nao o momento em que a pagina abriu.

**Golden.** Os 11 goldens de Momentos que falham ja falhavam antes desta mudanca — verifiquei com
`git stash` e a contagem foi identica (`-11`) com e sem o meu trabalho. Fazem parte das 61
divergencias golden-only ja relatadas. **Nao regenerei nenhum golden.**

## 3. I008 — subagentes efetivamente iniciados

Concorrencia observada na minha sessao: **4 simultaneos**, o limite util aqui — os quatro lotes
independentes que existem agora com caminhos disjuntos. Nao criei trabalho artificial nem arvore
recursiva. Todos receberam paths, objetivo e criterio de saida antes do despacho.

| # | objetivo | paths exclusivos (escritor unico) | criterio de saida |
| --- | --- | --- | --- |
| A | corrigir o candidato de notices conforme o aditivo | `migrations/20260908190652_superadmin_notices_sort_v2.sql` + `supabase/tests/superadmin_notices_sort_v2_test.sql` | ACL nominal preservando detail/audience/save/publish/change_status; desempate alinhado ao filtro; `NOTICE_INVALID_CURSOR` na allowlist; `plan` igual a contagem real |
| B | corrigir o candidato de momentos conforme o aditivo | `migrations/20260908190654_superadmin_moments_feed_v2.sql` + `supabase/tests/superadmin_moments_feed_v2_test.sql` | permission com `module_code`/`action_code` reais sem duplicar `moments.publications.read`; resgate reautoriza sessao/realm/membership/capacidade/publicacao/audiencia/vinculo/expiracao; papel desconhecido nao vira `school_staff`; RLS FORCE e ACL do store privado; `plan` correto |
| C | cliente de `agora.expire` | `apps/superadmin/lib/features/principal_now/` + `test/features/principal_now/` | mesmo desenho de `acontece.remove`; `can_expire` default false; adaptador falha fechado; motivo obrigatorio; `requestId` preservado; analyze limpo; mutacao dos guardas |
| D | clientes de `chat.edit` e `chat.revoke` | `apps/superadmin/lib/features/chat/` + `test/features/chat/` | `can_edit`/`can_revoke` default false; adaptador falha fechado com excecoes nomeadas; revogacao logica com lapide, sem purga no cliente; edicao marcada, nunca reescrita em silencio; mutacao dos guardas |

Regras que vinculam todos eles, como a I008 exige: **nenhum** toca o indice Git, faz commit, merge
ou push; **nenhum** escreve rastreadores, protocolo, assignment ou handoff; **nenhum** executa SQL
remoto, migration ou deploy; **nenhum** regenera golden; **nenhum** encosta em `apps/principal`. Um
unico escritor por arquivo — os quatro conjuntos de paths sao disjuntos entre si e disjuntos do que
eu mesmo edito. Eu permaneco responsavel pelo lote: coleto, reviso, testo e faco os commits
pequenos com caminhos explicitos.

**Resultados ainda nao chegaram** quando publico esta revisao. Nao vou afirmar o que eles produziram
antes de ler e revisar. Publico os resultados, com evidencia e com o que eu tiver rejeitado, na
proxima revisao.

## 4. Candidatos SQL — retidos, corrigidos localmente

Os dois candidatos ficam **retidos** no SHA da revisao, como o aditivo determina. Aceito os
achados; estao sendo corrigidos nos mesmos arquivos reservados pelos subagentes A e B. Nenhuma
concessao de execucao foi assumida: **nao rodei SQL, nao apliquei nada remoto e nao pedi replay**.
O replay segue aguardando concessao nominal da C00.

Registro que o achado mais grave nao era de paginacao e sim de **ACL**: revogar EXECUTE de todas as
`superadmin_notice_*` e reconceder so `directory` quebraria contratos ja aprovados. Era uma
regressao real que eu introduzi e que a revisao pegou.

## 5. Pendencias que dependem da C00

Sem novidade de status, so o que continua bloqueando:

1. **Replay/concessao** dos dois candidatos SQL apos as correcoes desta rodada.
2. **Reserva** para `remove_happens_post` e para a projecao `post_id`/`can_remove` — hoje o cliente
   de `acontece.remove` esta pronto e falha fechado por falta dela. O mesmo vale agora para
   `momentos.remove` (`can_remove` + comando de remocao) e, em breve, para `agora.expire`.
3. **Extensao tipada do catalogo Chat** e o SQL de staging/send, ainda sem reserva.
4. **Contrato do Perfil institucional** — decisao pendente.
5. **Composicao produtiva** das rotas `for-you`, `moments` e `profile`, e destino de producao para
   `principal-chat`.
6. **Revisao visual nominal** das 61 divergencias golden-only. Nao regenerei e nao vou regenerar.

## 6. Retomada exata

```
cd C:\Users\adrie\Documents\Coelo.worktrees\e2-r01-c05
git status --short
git log --oneline -5
cd apps\superadmin
flutter analyze
flutter test test\features\principal_moments\presentation\principal_moments_remove_test.dart
flutter test test\features\principal_happens\presentation\principal_happens_remove_post_test.dart
```

Cron `f10b9942` segue ativo (`7,37 * * * *`), com os limites ja declarados: session-only, so dispara
com o REPL ocioso, jitter de ate ~3 min. Fechamento seguro as **05:30** de 09/09 e entrega final ate
**06:00**, como conferi no PROTOCOLO vivo.
