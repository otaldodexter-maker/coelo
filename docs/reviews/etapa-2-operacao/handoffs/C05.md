---
title: "Handoff R01 C05"
source: "assignments/C05.md (R01-C05-I003 a I009, aditivo I008, R01-ALL-RTK-1808 e R01-VISUAL-1835 — sem alteracao desde 08/09 18:33); correcao de afirmacao propria publicada na r23"
status: "active"
generated_at: "2026-09-09T03:00:00-03:00"
timezone: "America/Sao_Paulo"
---

# E2 R01 C05 — Comunicacao e Principal

## Revisao 30 — correcao de uma afirmacao minha da r23

- rodada: R01; revisao: 30
- ultima instrucao processada: `R01-C05-I009` mais `R01-VISUAL-1835`; **nada novo** desde a r29
- timestamp: 2026-09-09T03:00:00-03:00, relogio da maquina
- HEAD do codigo: `b7e3b672280d7a6c474c1442def5f08d5cf8c646`; push verificado
- arvore limpa

## 1. Corrijo o "padrao" que publiquei na r23

Na r23 eu escrevi que tres achados independentes tinham a mesma forma — "verificacao de vazio que so
olha nulo ou comprimento zero, deixando passar espacos em branco" — e recomendei que a C00
considerasse isso ao revisar as demais frentes.

**Dois dos tres estavam caracterizados errado, e a recomendacao vale menos do que eu disse.** Fui
reler o codigo comparando os quatro adaptadores entre si e encontrei o proprio erro:

| caso | guarda real | o que era de fato |
| --- | --- | --- |
| `moment_id`, adaptador de Momentos | `(json[key] as String?)?.trim()` **antes** do teste | espaco **nunca passou**; era lacuna de **cobertura** |
| campo obrigatorio, feed misto | `json[key]?.toString().trim()` **antes** do teste | espaco **nunca passou**; era lacuna de **cobertura** |
| `read_ticket`, `chat-media` | `body.read_ticket.length < 1`, **sem** aparar | espaco **passava**; era **defeito de comportamento**, corrigido com `trim()` |

Ou seja: **um defeito real, nao tres**. Nos outros dois o codigo ja estava correto e o que faltava
era teste — o mutante que eu apliquei removia a checagem de vazio inteira, e nenhum teste a exercia.
Os testes que escrevi continuam validos e continuam necessarios; o que estava errado era a minha
descricao do achado, nao o remedio.

Registro isso com destaque porque a afirmacao errada e do tipo que faz outras frentes procurarem uma
classe de defeito que, nelas, pode nao existir — e porque o C06 encaminha achados meus adiante.

## 2. O que a comparacao entre irmaos confirmou, sem achado

Na mesma releitura dos quatro adaptadores de feed:

- **Tratamento de negacao.** Os quatro mapeiam `42501` e `PGRST301` para o estado nao autorizado.
  Sem divergencia.
- **Captura de falha de forma.** Dois usam `on FormatException` explicito e dois usam `on Object`
  amplo. Os dois padroes acabam no mesmo estado indisponivel, entao **nao e defeito** — e nao vou
  uniformizar estilo no fim do lote.
- **Aparo de texto obrigatorio.** Os quatro aparam antes de testar. Era isso que eu tinha lido
  errado.

## 3. Pendencias que dependem da C00

Sem novidade desde a r29: concessao de replay dos dois candidatos; projecao de recibos na thread do
Chat; contrato de revisao do Chat; geometria da midia ampla do Agora com medidas; rede de captura
global de erro no app; reserva dos comandos autorizados de remocao e expiracao; contrato do Perfil
institucional; remocao do `PrincipalPublicationProgressBar` orfao; destino do leitor e do compositor
de Circulares; master contra referencia de Acontece e Momentos; revisao visual nominal dos goldens.

## 4. Retomada exata

```
cd C:\Users\adrie\Documents\Coelo.worktrees\e2-r01-c05
git status --short
git log --oneline -30
cd apps\superadmin
flutter analyze
flutter test
```

Cron `e9f4cae3` vivo. Fechamento na primeira entrega a partir de ~05:20; entrega final ate **06:00**.
