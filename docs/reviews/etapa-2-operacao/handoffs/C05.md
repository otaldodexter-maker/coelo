---
title: "Handoff R01 C05"
source: "assignments/C05.md (R01-C05-I007); decisao do Owner 2026-09-08 sobre remocao; PROTOCOLO.md R01; decisions/0032-mvp-private-media-r2.md; packages/coelo_database/migrations/20260901185008_superadmin_internal_notices_v2.sql; packages/coelo_database/migrations/20260820182000_happens_publication_mvp.sql; packages/coelo_database/migrations/20260821112822_moments_publication_mvp.sql; docs/reviews/coelo-flutter-pendencias.md; docs/reviews/coelo-supabase-pendencias.md; docs/reviews/coelo-flutter-integrado-supabase-pendencias.md"
status: "active"
generated_at: "2026-09-08T16:51:40-03:00"
timezone: "America/Sao_Paulo"
---

# E2 R01 C05 — Comunicacao e Principal

## Revisao 13

- rodada: R01; revisao: 13
- ultima instrucao processada: **`R01-C05-I007`** (2026-09-08T16:17:15-03:00). Confirmo a leitura
  viva. Tambem li o ack de 16:24:59 sobre a r10.
- timestamp: 2026-09-08T16:51:40-03:00, relogio da maquina
- branch: `claude/e2-r01-c05-comunicacao`; baseline `479d1bd1`
- HEAD do codigo: `5f0e7e294501c3e4033b6c42f64df4aee25b523f`; handoff em commit proprio
- push: verificado; `git ls-remote` devolveu `5f0e7e29`.

Observacao util para a C00: o frontmatter da assignment ainda diz
`instruction_revision: 6; instruction_id: R01-C05-I006`, enquanto o corpo ja traz a I007. Segui o
corpo. Se o cabecalho nao acompanhar, um executor que so leia o frontmatter perde instrucao.

## 1. Reserva consumida: os dois candidatos SQL (`5f0e7e29`)

Conferencias feitas antes de escrever, conforme I007:

- os skeletons existem em `C:/Users/adrie/AppData/Local/Temp/coelo-c00-round-candidates-…/supabase/
  migrations`, os seis com **0 bytes** — sao nomes, nao conteudo;
- os dois nomes que me cabem estavam **ausentes** em
  `packages/coelo_database/migrations` da minha worktree antes de eu criar;
- copiei os nomes exatos; testes em `packages/coelo_database/supabase/tests` com o mesmo sufixo sem
  timestamp e com `_test.sql`;
- **nenhuma aplicacao remota**, nenhum toque em historico ou runner, SQL separado de Flutter.

### `20260908190652_superadmin_notices_sort_v2.sql`

- coluna resolvida por `case` fixo sobre allowlist (`updated_at`, `starts_at`, `title`, `type`,
  `priority`, `status`); **nada de SQL montado a partir de texto do cliente**;
- timestamps viram texto UTC de largura fixa, entao **uma unica coluna de keyset** serve a todas as
  ordenacoes sem malabarismo de tipo; desempate sempre `id desc`;
- cursor com **duas metades md5**: a de escopo precisa bater, senao a chamada e recusada **sem
  linhas**; a de consulta, quando difere, significa apenas que o operador trocou ordem ou filtro, e
  a resposta reinicia na pagina 1 com `cursor_reset: true`;
- cursor malformado e recusado em vez de lido silenciosamente como pagina 1;
- a assinatura antiga e **dropada antes**, para as duas nunca coexistirem como overload ambiguo; o
  cursor legado continua valendo enquanto a ordem for a padrao, entao o cliente atual segue
  funcionando sem mudanca;
- toda pagina chama `superadmin_notice_context('notices.read')`, como antes.

### `20260908190654_superadmin_moments_feed_v2.sql`

`momentos.view` nao tinha leitura autorizada nenhuma. `list_visible_moments` **espelha o feed do
Acontece** em vez de inventar segunda forma:

- capacidade primeiro: sem `moments.publications.read` o chamador nao alcanca linha alguma,
  independentemente do que a UI pediu;
- visivel so quando publicado, com escopo compativel e **audiencia casada por membership ativa**;
- expirados excluidos **pela consulta**, nao pelo cliente;
- midia volta como **descritor opaco, ligado ao espectador e curto**. Bucket, `object_key` e URL
  **nao atravessam** essa fronteira — o conjunto de nomes de argumento e de saida esta coberto por
  teste exatamente para isso;
- `redeem_moments_media_read_ticket` e concedido **so a `service_role`**: o navegador nunca
  transforma ticket em URL sozinho, o mesmo corte que o Acontece ja usa;
- cursor `(published_at, id)`, na mesma ordem da projecao.

### Limite honesto dos testes

Os dois `_test.sql` cobrem o que e deterministico sem fixture autenticada: assinatura, allowlist,
metades do fingerprint, regra de audiencia e o corte de privilegio. **Nao executei nenhum dos dois**:
os caminhos de autorizacao, cross-scope, reset e cursor precisam da fixture de identidade interna e
de replay local, e a propria I007 diz que o replay aguarda lease, hoje com o C02 em
`R01-LOCAL-XLSX-02`. Declaro isso em vez de afirmar cobertura que nao rodei.

## 2. Reserva de `chat-media` aceita, ainda nao iniciada

Aceito a reserva exclusiva dos seis arquivos e as regras: reutilizar `_shared/r2_s3.ts` e
`media_image_contract.ts` **sem editar**, sem catalogo paralelo, sem mensagem falsa, sem `ready`
alegado pelo cliente, sem relaxar `message_id NOT NULL` e sem inventar pessoa global para autoria
interna. Registro que **SQL de staging/send continua sem reserva**, entao o handler nascera com a
operacao de vinculo indisponivel — bridge nao e backend pronto.

Comeco por ele no proximo lote, publicando o contrato asset/attempt/binding junto.

## 3. Sobre nao repetir negativas ja existentes

Anotado. Foi o que a r10 e a r12 ja registraram: as negativas de contexto e resposta tardia de
Perfil, Circulares, Para Voce, Acontece e Agora **ja existiam**, e eu disse isso em vez de produzir
teste redundante. A unica que faltava, no Momentos, entrou em `40306f45` **com prova de que falha
sem a guarda**. Nao vou gerar mais casos desse tipo.

## 4. Estado das acoes

As 28 continuam `pending-verification`. Em relacao ao inventario da r10:

- `momentos.view` deixa de estar sem contrato: existe candidato de leitura autorizada, ainda nao
  aplicado nem replayado;
- `notices.list` ganha candidato de ordenacao;
- `acontece.remove` continua com cliente entregue (r12) e contrato de remocao pendente de reserva.

## 5. Deltas propostos para os rastreadores (por ID)

- `notices.list`: candidato `20260908190652` com allowlist, desempate e cursor vinculado a
  escopo/coluna/direcao/filtros. Sem replay.
- `momentos.view`: candidato `20260908190654` com projecao minima autorizada e descritores de ativo.
  Sem replay. O adapter Flutter continua ausente por depender deste contrato.
- Demais IDs sem mudanca desde a r12.

## 6. Bloqueios ativos

1. Replay local dos dois candidatos — lease ocupado (`R01-LOCAL-XLSX-02`, C02).
2. Reserva de migration para `remove_happens_post` e para a projecao `post_id`/`can_remove` (r12).
3. Extensao tipada do catalogo de Chat e SQL de staging/send — sem reserva, conforme I007.
4. Decisao sobre o contrato do Perfil institucional (r7).
5. Composicao produtiva de `for-you`, `moments`, `profile` e destino de `principal-chat` — C00 (r7).
6. Regeneracao de golden negada; revisao visual nominal da C00.

## 7. Primeiro criterio ainda aberto

O replay dos dois candidatos. Sem ele, o que entreguei e SQL revisavel, nao contrato provado.

## 8. Proximo passo

Handler `chat-media` com os seis arquivos reservados, adapter nominal e testes Deno, mantendo
indisponivel a operacao cujo RPC ou ramo de catalogo falte, e publicando o contrato
asset/attempt/binding.

## 9. ETA

- `chat-media` com testes Deno: 3-5 h, nao bloqueada.
- adapter Flutter de `list_visible_moments`: 2-3 h, nao bloqueada por replay, mas so certificavel
  depois dele.
- replay dos dois candidatos: depende do lease.
- remocao nos quatro dominios: 6-9 h apos a reserva do item 2.

## 10. Continuidade

`CronCreate` ID `6a2a031f`, `7,37 * * * *`. Session-only, so dispara com o REPL ocioso. Retomou
lotes as 14:37, 15:07, 15:37, 16:07 e 16:37.

---

## Historico das revisoes anteriores

- **r12** (`4e9bfa26`): decisao do Owner sobre remocao recebida e aplicada — perfis autorizados por
  hierarquia e RLS, Owner pode tudo; cliente de `acontece.remove` entregue com 7 aceites; auditoria
  de guardas por mutacao, que encontrou um teste meu vazio e o renomeou.
- **r11** (`073224d1`): I006 confirmada; nucleo de upload incorporado por cherry-pick; consumidor de
  Chat ligado ao nucleo com 7 aceites.
- **r10** (`ef0d583d`): inventario das 28 acoes — 16 implementadas, 8 parciais, 4 inexistentes.
- **r9** (`2bfe364a`): tres acoes sem UI e sem contrato; contrato de remocao logica.
- **r8** (`a198c013`): semantica unica de ordenacao; seis arquivos de `chat-media`.
- **r7** (`1e5684b4`): mapa de Circulares; rotas do Principal sem composicao produtiva.
- **r6** (`c956d3fa`): fluxo de anexo de Chat com 9 aceites.
- **r5** (`fdef81db`/`00c52284`): reserva do indicador devolvida sem mudancas.
- **r4** (`545355c4`): cron ativado; 41/41 testes Deno dos gateways.
- **r3** (`2f322d70`): goldens por viewport; compositor do Agora reconciliado.
- **r2** (`cbbff86a`): causa-raiz corrigida.
- **r1** (`a0927022`): ambiente, recorte, primeira medicao.

Ambiente constante: cwd `C:/Users/adrie/Documents/Coelo.worktrees/e2-r01-c05`, remoto
`origin https://github.com/otaldodexter-maker/coelo.git`, Flutter 3.44.2 / Dart 3.12.2, Deno 2.9.5,
modelo `claude-opus-5`, ID de harness `2a43a349-a639-4be5-aefc-1ff180d1fc7a`.
