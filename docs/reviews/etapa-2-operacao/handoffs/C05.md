---
title: "Handoff R01 C05"
source: "assignments/C05.md (R01-C05-I003 a I009 e aditivo I008); repasses do Coordenador C06 de 2026-09-08 17:30, 18:22, 18:29, 19:00 e 19:10; docs/superpowers/specs/2026-08-28-coelo-visual-completion-stage-design.md item 31, linhas 328-343; laudo e varredura da C07 em e2-r01-c07/docs/reviews/etapa-2-operacao/handoffs/C07-evidence/; PROTOCOLO.md R01"
status: "active"
generated_at: "2026-09-08T19:29:03-03:00"
timezone: "America/Sao_Paulo"
---

# E2 R01 C05 — Comunicacao e Principal

## Revisao 18 — anatomia do Agora reposta, foco corrigido, Perfil parcial

- rodada: R01; revisao: 18
- ultima instrucao processada: `R01-C05-I009`; repasses do C06 ate 19:10 lidos e absorvidos
- timestamp: 2026-09-08T19:29:03-03:00, relogio da maquina
- branch: `claude/e2-r01-c05-comunicacao`; baseline `479d1bd1`
- HEAD do codigo: `428b62b04a88cc851ffc9a837bcc49c07df75554`
- push: verificado; `git ls-remote` devolveu `428b62b0`
- arvore limpa: `git status --porcelain` vazio antes de cada commit; **nenhum PNG de `failures/`
  entrou em commit**, e conferi isso a cada vez.

| commit | conteudo |
| --- | --- |
| `8b009901` | `principal.profile-edit` restrito ao texto editorial |
| `2802d7fc` | anatomia de publicacao do Agora reposta conforme a aprovacao de 2026-08-31 |
| `428b62b0` | parada de Tab silenciosa removida nos chips de audiencia do Acontece |

## 1. Agora: eu estava errado, e a correcao e essa (`2802d7fc`)

**Conferi a fonte antes de aceitar o encaminhamento.** O item 31 de
`docs/superpowers/specs/2026-08-28-coelo-visual-completion-stage-design.md`, `status:
approved-design`, linhas 328-343, diz literalmente que Publicar no Agora foi aprovado visualmente
pelo Owner em 2026-08-31 preservando a anatomia comum de Acontece/Momentos, "com midia temporaria
vertical, texto curto, ferramentas proprias, publico/contexto, aviso de 24 horas e **preview lateral
no desktop**", e nomeia **`Sua publicacao`** como parte da anatomia. Li as linhas, nao a parafrase.

**Isso nao contradiz o que o Owner me disse, e o erro foi meu.** Trilho de etapas e previa de
publicacao sao coisas diferentes. Ele rejeitou o **wizard lateral**; eu tratei as duas como a mesma
coisa e removi a previa junto com o trilho. Tambem acrescentei uma barra de progresso que nao esta
na anatomia aprovada.

Reposto: `Sua publicacao`, o nome da superficie e a **previa lateral no desktop**, no mesmo limiar
de 840 e na mesma largura de 320 que o Acontece usa. Removida a barra de progresso e o helper que a
alimentava — o compositor nao tem etapas para contar, entao qualquer indicador de etapa descreveria
um fluxo inexistente. O trilho continua fora. O `_MediaPreview` recupera o modo somente leitura,
para a previa mostrar a publicacao sem oferecer o seletor uma segunda vez.

Os tres limites da C00 foram respeitados: **nao restaurei o rail**, **nao regenerei nenhum golden** e
**nao encostei no `PrincipalPublicationFrame`**.

**Declaro a mudanca de pixel**, como pedido: os 14 goldens do Agora seguem vermelhos e agora
divergem por outro motivo, o correto. A aprovacao do master continua sendo revisao nominal da C00.
Fora deles, 82 verdes e **nenhuma falha nao-golden** — atualizei tres testes meus que fixavam a barra
de progresso, porque eles pinavam um controle que a aprovacao diz nao existir.

**Consequencia que nao resolvo sozinho:** `PrincipalPublicationProgressBar`, que eu havia adicionado
ao `principal_publication_frame.dart`, ficou **sem nenhum consumidor**. Nao removo porque alterar
aquele arquivo esta vedado. **Proponho a remocao** e deixo a decisao com a C00.

## 2. Foco: medi antes de aceitar, e o defeito era real (`428b62b0`)

A varredura da C07 classificou `principal_happens_publication_page.dart:487` como DEFEITO-1. Em vez
de aceitar, construi o instrumento: um teste que conta os nos focalizaveis sobre o **mesmo retangulo**
do chip. Devolveu **2**. O `FocusableActionDetector` era uma parada de Tab por cima do `TextButton`,
com aparencia identica a real e sem ativar nada no Enter — invisivel para quem usa mouse, cansativo
para todo o resto.

Trocado por um `Focus` que nao pede foco. O realce continua funcionando, porque `hasFocus` e
verdadeiro quando um descendente tem o foco primario, e a parada extra sumiu. **O teste falhava com
`Actual: <2>` e passa com 1.** 56 testes verdes, goldens inclusive: o realce nao mudou de aparencia,
entao nenhum pixel se moveu.

Registro um erro meu no caminho: a primeira versao do teste media o **botao errado** do cabecalho e
passava sem provar nada. Refiz percorrendo o fluxo ate a etapa de publico, como um operador faz.

Registro sem alterar: `principal_circular_reader.dart:366` tem o mesmo detector com um `TextButton`
dentro, mas **com** `actions` mapeando `ActivateIntent`, entao as duas paradas ativam. A C07
classificou como correto pelo criterio dela; nao expando o recorte por conta propria.

## 3. Hipotese do C06 sobre Acontece e Momentos — respondida com medida, sem mexer

O C06 levantou, como hipotese, que Acontece e Momentos ainda passam `navigation:` ao frame e que as
referencias de 31/08 dessas duas tambem nao teriam trilho. Conferi:

- as duas passam `navigation:` na linha 192, sim;
- e **os goldens das duas passam hoje**: `principal_happens_publication` 56/56 e
  `principal_moments_publication` 79/79, com os testes visuais dentro.

Ou seja, **os masters aprovados dessas duas correspondem a uma composicao que tem o trilho**. Mexer
por hipotese quebraria golden verde e trocaria uma divergencia conhecida por uma inventada. **Nao
alterei nada.** Se as referencias de 31/08 realmente nao tiverem trilho, entao master e referencia
discordam, e isso e decisao da C00 e do Owner — nao coisa que eu resolva editando.

## 4. `principal.profile-edit` entregue, restrito e declarado (`8b009901`)

Entrega **parcial declarada**, nao o ID inteiro. Na r16 eu disse que qualquer contrato de Perfil
seria invencao minha. Continua verdade para a maioria dos campos, e por isso eles ficaram de fora,
cada um com o motivo escrito no proprio contrato: nome e tipo sao cadastro e criariam segunda fonte
de verdade; metricas sao contagem derivada e virariam numero declarado pelo cliente; o proximo
evento espelha a Agenda; destaques sao editoriais mas ordem e limite nao estao definidos; e vinculos
sao ambiguos no modelo, com efeito direto em privacidade e LGPD.

O que mudou e que a analise separou o que **nao** depende da decisao: a biografia e texto editorial
sem ambiguidade. E so isso que o comando carrega.

Mesmo desenho das outras quatro acoes: `can_edit` da projecao com default `false`; **sem transporte
nao ha afordancia alguma** — indisponivel nao e o mesmo que recusado; adaptador de producao falha
fechado sem inventar RPC; a tela renderiza a **re-projecao do servidor**, nunca o texto digitado; a
**versao** que o operador viu viaja na escrita, para uma alteracao concorrente ser recusada em vez
de sobrescrita; e o `requestId` e preservado para a repeticao replay.

11 testes de aceitacao. **Mutacao matou os cinco guardas**: afordancia ignorando `can_edit`, no-op
virando revisao, retry gerando intencao nova, reescrita local no lugar da projecao e versao esperada
zerada. As 10 falhas de `principal_profile` sao golden pre-existentes, mesma contagem do baseline.

## 5. Repasses recebidos

- **17:30 e correcao das 19:00** — a liberacao de Supabase/Cloudflare **nao** concede replay. Eu nao
  havia assumido concessao; a posicao nao muda.
- **18:22** — laudo visual da C07 e repasse do RTK. Os dois defeitos meus estao resolvidos em
  `5f10c0b7` e agora em `2802d7fc`.
- **18:29** — Docker delegado a dono unico; **nao tentei replay nem subi banco local**.
- **19:00** — decisoes da C00 sobre os dois defeitos; a correcao ja estava no consumidor certo.
- **19:10** — recibo do Owner localizado pela C07 e conferido por mim na fonte.

Sobre a serializacao: a lease atual e `R01-LOCAL-MODELS-AAL1-02`, com a C01. **Nao aciono
recuperacao paralela.** Meus dois candidatos corrigidos continuam esperando concessao nominal.

Sobre o RTK: recebido. Uso onde reduz ruido de verdade; nao reinstalei nem reconfigurei nada.

Sobre paralelismo de teste: confirmo o risco que o C06 apontou. **Rodo `flutter test` em serie** na
`apps/superadmin`; nunca disparei duas execucoes simultaneas na mesma pasta.

## 6. Pendencias que dependem da C00

1. **Concessao nominal** para o replay dos dois candidatos corrigidos.
2. **Reserva** dos comandos autorizados que hoje fazem os clientes falharem fechado:
   `remove_happens_post`, remocao de Momentos, expiracao do Agora, com `can_remove`/`can_expire`.
3. **Contrato de revisao do Chat**: RPC de edicao que devolva a mensagem re-projetada, RPC de
   revogacao que grave a lapide, e os campos `can_edit`, `can_revoke`, `is_edited`, `edited_at`,
   `is_revoked`, `revoked_at` na projecao da thread.
4. **Contrato do Perfil institucional** para o restante dos campos; a biografia ja esta entregue.
5. **Remocao do `PrincipalPublicationProgressBar`** do frame compartilhado, agora orfao.
6. **Onde ficam** o leitor com respostas e o compositor de Circulares, hoje subdeclarados sob
   `principal.profile-view`.
7. **Master x referencia** de Acontece e Momentos quanto ao trilho (secao 3).
8. **Revisao visual nominal** dos goldens, inclusive os 14 do Agora que agora divergem pelo motivo
   correto.

## 7. Retomada exata

```
cd C:\Users\adrie\Documents\Coelo.worktrees\e2-r01-c05
git status --short
git log --oneline -8
cd apps\superadmin
flutter analyze
flutter test test\features\principal_happens_publication
flutter test test\features\principal_now_publication\presentation\principal_now_publication_page_test.dart
flutter test test\features\principal_profile\presentation\principal_profile_edit_test.dart
flutter test test\features\chat\presentation\superadmin_chat_revision_test.dart
```

`flutter analyze` do `apps/superadmin` inteiro: **No issues found**. Cron `e9f4cae3` ativo.
Fechamento seguro **09/09 05:30**, entrega final ate **06:00**, com WIP identificado e preservado.
