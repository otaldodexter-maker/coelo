---
title: "Handoff R01 C05"
source: "assignments/C05.md (R01-C05-I005); PROTOCOLO.md R01; decisions/0032-mvp-private-media-r2.md; packages/coelo_database/migrations/ (chat, happens, moments, now, notices); apps/superadmin/lib/features/ (chat, notices, circulars, principal_*); docs/reviews/coelo-flutter-pendencias.md; docs/reviews/coelo-supabase-pendencias.md; docs/reviews/coelo-flutter-integrado-supabase-pendencias.md"
status: "active"
generated_at: "2026-09-08T15:54:48-03:00"
timezone: "America/Sao_Paulo"
---

# E2 R01 C05 — Comunicacao e Principal

## Revisao 10 — inventario de existencia das 28 acoes

- rodada: R01; revisao: 10
- ultima instrucao processada: `R01-C05-I005`
- timestamp: 2026-09-08T15:54:48-03:00, relogio da maquina
- branch: `claude/e2-r01-c05-comunicacao`; baseline `479d1bd1`
- HEAD do codigo: `40306f45`; handoff em commit proprio; push da r9 verificado em `2bfe364a`

## Por que esta revisao existe

Nove revisoes tratando as 28 acoes como `pending-verification` deram a impressao de que faltava
**verificar**. Ao procurar as negativas do proximo lote descobri que parte delas nao tem o que
verificar: nao existe UI nem contrato. Levantei entao, acao por acao, se **existe afordancia no
cliente** e se **existe contrato no banco**. Isso nao promove nada; separa o que falta testar do que
falta construir, que e a diferenca que a rodada precisa enxergar.

Metodo: listagem completa das funcoes das migrations de Chat, Acontece, Momentos, Agora e
Comunicacoes, cruzada com busca por afordancia nas features correspondentes. Nao inspecionei runtime
remoto.

## Inventario

| action_id | UI | Contrato no banco | Situacao |
|---|---|---|---|
| `chat.list` | sim | `superadmin_chat_inbox_v2` | implementado |
| `chat.open` | sim | `superadmin_chat_thread_v2` | implementado |
| `chat.send` | sim | `superadmin_chat_send_message_v2` | implementado |
| `chat.edit` | **nao** | **nao** | **inexistente** |
| `chat.attach` | sim (entregue nesta rodada) | catalogo `chat_attachment_metadata` existe; **gateway e RPC de vinculo nao** | parcial |
| `chat.receipts` | parcial: nao lidas por conversa | `superadmin_chat_mark_read_v2`; **sem estado de recibo por mensagem** | parcial |
| `chat.revoke` | **nao** | **nao** | **inexistente** |
| `notices.list` | sim | `superadmin_notice_directory` | implementado |
| `notices.create` | sim | sim | implementado |
| `notices.edit` | sim | sim | implementado |
| `notices.schedule` | sim | sim | implementado |
| `notices.publish` | sim | sim | implementado |
| `notices.archive` | sim, com motivo de auditoria | sim | implementado |
| `acontece.feed` | sim | `list_visible_happens_posts` | implementado |
| `acontece.create` | sim | `save_happens_draft` | implementado |
| `acontece.publish` | sim | `publish_happens_post` | implementado |
| `acontece.remove` | **nao** | **nao** (`remove_happens_media` e midia de rascunho) | **inexistente** |
| `agora.view` | sim | `list_visible_now_publications` | implementado |
| `agora.create` | sim | `save_now_draft` | implementado |
| `agora.publish` | sim | `publish_now` | implementado |
| `agora.expire` | **nao**, para expirar manualmente | expiracao automatica de 24 h implementada e filtrada no feed; status `expired` nunca atingido | parcial |
| `momentos.view` | sim | **nao ha** `list_visible_moments`; sem adapter produtivo | parcial |
| `momentos.create` | sim | `save_moments_draft` | implementado |
| `momentos.publish` | sim | `publish_moment` | implementado |
| `momentos.remove` | **nao** | **nao** (`mark_moments_media_deleted` e midia) | **inexistente** |
| `principal.for-you` | sim | projecao de Comunicacoes; rota real nao compoe repositorio | parcial |
| `principal.profile-view` | sim, com dados de preview | **nao ha contrato de perfil**; rota real nao compoe | parcial |
| `principal.profile-edit` | editor `profile_about` existe, mas **consumido por Atividades**, nao pelo Perfil do Principal | `ProfileAboutRepository` existe como contrato | parcial |

Resumo: **16 implementadas**, **8 parciais**, **4 inexistentes** (`chat.edit`, `chat.revoke`,
`acontece.remove`, `momentos.remove`).

Nenhuma delas muda de estado no rastreador por este handoff. `pending-verification` continua correto
como estado formal; o que este inventario acrescenta e **por que** cada uma esta pendente.

## O que isso implica

1. As quatro inexistentes nao sao caso de teste. Precisam de decisao de produto e reserva antes de
   qualquer linha: remover e editar mensagem e publicacao tocam retencao, auditoria e melhor
   interesse da crianca. Ja propus na r9 a forma unica de remocao logica com tombstone; `chat.edit`
   precisa da mesma conversa (janela de edicao, historico de versoes, o que o destinatario ve).
2. `chat.receipts` como esta hoje entrega nao lidas por conversa. O aceite do rastreador fala em
   "estados textualizados" por mensagem, o que exige estado de recibo em `ChatMessage` e projecao na
   RPC de thread. E extensao de contrato, nao ajuste de UI.
3. `agora.expire` esta implementado no unico sentido que hoje existe: o tempo. Se o aceite exige
   retirar do ar antes das 24 h, cai no mesmo contrato da r9.
4. As tres parciais do Principal (`for-you`, `profile-view`, `profile-edit`) e `momentos.view`
   dependem das lacunas de composicao e de contrato ja registradas nas r7 e r9.

## Correcao do proximo passo que anunciei na r9

Escrevi que seguiria com negativas de contexto e resposta tardia em Perfil, Circulares e Para Voce.
Fui conferir e **elas ja existem**: Para Voce tem `loads B and ignores a late result from repository
A` e `old validity timer cannot replace a pending context load`; Circulares tem `late detail cannot
replace the newly selected context`, `old response denial cannot purge the replacement context`,
`late submit cannot change the new response version` e `schedule selected for old controller cannot
schedule the new draft`; Perfil nao tem repositorio, entao nao ha resposta tardia a testar. Acontece
e Agora tambem ja cobrem troca de contexto e resgate obsoleto.

Ou seja: o lote que a I005 confirmou como proximo **ja estava feito**, exceto pela guarda do Momentos
que entreguei em `40306f45`. Prefiro dizer isso a produzir testes redundantes para parecer produtivo.

## Estado das acoes

As 28 continuam `pending-verification`. Nenhuma promocao FE, BE ou E2E.

## Deltas propostos para os rastreadores (por ID)

Um por acao, conforme a tabela acima. Os quatro que mudam a leitura do rastreador de forma
substantiva: `chat.edit` e `chat.revoke` inexistentes; `chat.receipts` sem recibo por mensagem;
`agora.expire` so por tempo.

## Bloqueios ativos

1. Decisao de produto sobre remover e editar publicacao/mensagem, com reserva de migration para
   Chat, Acontece, Momentos e Agora — cobre `chat.edit`, `chat.revoke`, `acontece.remove`,
   `momentos.remove` e a parte manual de `agora.expire`.
2. Extensao de contrato para recibo por mensagem (`chat.receipts`).
3. Reserva dos seis arquivos de `chat-media` e das duas mudancas na RPC de envio (r8).
4. Reserva de migration para `list_visible_moments` (r7).
5. Reserva de migration para a ordenacao de Comunicacoes (r8).
6. Decisao sobre o contrato do Perfil institucional (r7).
7. Composicao produtiva de `for-you`, `moments`, `profile` e destino de `principal-chat` — C00 (r7).
8. Regeneracao de golden negada; revisao visual nominal da C00.

## Primeiro criterio ainda aberto

A decisao 1. Ela define cinco acoes de uma vez.

## Proximo passo

Sem as reservas, o que resta de util no cliente e pequeno e eu nao vou inflar teste para ocupar
espaco. Vou usar o tempo em: (a) conferir se as negativas existentes sao guardas reais, removendo a
guarda e vendo o teste falhar, como fiz no Momentos, comecando por Chat e Agora, que sao as mais
sensiveis; (b) preparar o SQL candidato e os testes das reservas pedidas, sem aplicar nada remoto,
para que a reserva chegue e o trabalho ja esteja pronto para revisao.

## ETA

- auditoria de guardas reais em Chat e Agora: 2-3 h, nao bloqueada.
- SQL candidato e testes de `list_visible_moments` e da ordenacao, sem aplicar: 3-4 h, nao bloqueada.
- as demais permanecem como na r9, dependentes de reserva e decisao.

## Continuidade

`CronCreate` ID `6a2a031f`, `7,37 * * * *`. Session-only, so dispara com o REPL ocioso.

---

## Historico das revisoes anteriores

- **r9** (`2bfe364a`): `acontece.remove`, `momentos.remove` e `agora.expire` sem UI e sem contrato;
  contrato unico de remocao logica com tombstone, auditoria e retencao; guarda de resposta tardia do
  Momentos verificada por remocao da guarda; registro de um teste meu descartado por ser trivialmente
  verdadeiro.
- **r8** (`a198c013`): semantica unica de ordenacao; seis arquivos de `chat-media`;
  `chat_attachment_metadata` ja existente com tombstone; URL redimida validada em quatro consumidores.
  Suite 907/61.
- **r7** (`1e5684b4`): mapa das subtelas de Circulares; rotas do Principal sem composicao produtiva;
  `principal-chat` sem destino em producao; contrato `list_visible_moments`.
- **r6** (`c956d3fa`): fluxo de anexo de Chat com 9 aceites e o picker real.
- **r5** (`fdef81db`/`00c52284`): reserva do indicador devolvida sem mudancas; card de 375 medido.
- **r4** (`545355c4`): cron ativado; 41/41 testes Deno dos gateways.
- **r3** (`2f322d70`): goldens por viewport; compositor do Agora reconciliado.
- **r2** (`cbbff86a`): causa-raiz corrigida.
- **r1** (`a0927022`): ambiente, recorte, primeira medicao.

Ambiente constante: cwd `C:/Users/adrie/Documents/Coelo.worktrees/e2-r01-c05`, remoto
`origin https://github.com/otaldodexter-maker/coelo.git`, Flutter 3.44.2 / Dart 3.12.2, Deno 2.9.5,
modelo `claude-opus-5`, ID de harness `2a43a349-a639-4be5-aefc-1ff180d1fc7a`.
