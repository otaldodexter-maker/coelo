---
title: "Handoff R01 C05"
source: "assignments/C05.md (R01-C05-I001); PROTOCOLO.md R01; specs/036-principal-now-publication-mvp.md; specs/050-principal-ui-ux-closure.md; docs/superpowers/plans/2026-09-01-principal-ui-ux-closure.md; docs/reviews/coelo-flutter-pendencias.md; docs/reviews/coelo-supabase-pendencias.md; docs/reviews/coelo-flutter-integrado-supabase-pendencias.md"
status: "active"
generated_at: "2026-09-08T12:49:00-03:00"
timezone: "America/Sao_Paulo"
---

# E2 R01 C05 — Comunicacao e Principal

## Revisao 2 — corrige a causa-raiz publicada na revisao 1

- rodada: R01; revisao: 2
- ultima instrucao processada: `R01-C05-I001` (assignment revisao 1)
- timestamp: 2026-09-08T12:49:00-03:00 (America/Sao_Paulo)
- branch: `claude/e2-r01-c05-comunicacao`
- baseline: `479d1bd1771b13e0cd96084d244fc2cdc6b0e556`
- SHAs desta rodada: `a0927022` (handoff rev.1), `e7985f31` (mudanca de codigo revertida),
  `1ec8efc4` (revert). Arvore de codigo identica a `479d1bd1`.
- push: **nao feito**; branch sem upstream configurado.

### Correcao material da revisao 1

A revisao 1 classificou parte das 45 falhas golden como regressao de codigo. **Isso estava errado.**
Depois de cruzar cada familia com a spec e o plano aprovados, o resultado e o oposto: em todas as
seis familias o codigo segue uma fonte aprovada **posterior** ao golden, e o golden e o artefato
desatualizado. Nenhuma das 45 falhas e regressao de codigo desta frente.

Cheguei a corrigir o card `Publicar agora` do Acontece para a anatomia do golden (`e7985f31`, 77/77
verdes em `principal_happens`). Ao ler a `specs/050-principal-ui-ux-closure.md` percebi que a spec
aprovada prescreve exatamente a anatomia que o codigo ja tinha — "contorno tracejado arredondado,
acao circular central, icone vetorial e rotulo curto. O estado normal usa superficie neutra e icone
laranja; hover, foco e pressionado reforcam borda e acao em laranja". Revertido em `1ec8efc4`. O par
commit/revert fica no historico como registro do erro e da fonte que o corrigiu.

### Causa-raiz por familia (45 falhas)

| Familia | Falhas | Golden aprovado em | Fonte aprovada posterior que o codigo segue | Veredito |
|---|---|---|---|---|
| `principal_happens` preview | 10 | `50bf7b0c` 2026-08-31T19:11 | spec 050 (2026-09-01), secao "Entrada Publicar agora": contorno tracejado, acao circular central, superficie neutra e icone laranja no repouso | golden desatualizado |
| `principal_moments` preview | 11 | `50bf7b0c` 2026-08-31T19:11 | spec 050, secao "Agora e Momentos": ao abrir Momentos a midia ocupa a tela inteira em mobile, tablet e desktop e todo chrome externo e suspenso. O golden ainda registra o viewer em janela com rail lateral | golden desatualizado |
| `principal_profile` preview | 10 | `21e0ed09` 2026-08-31T19:38 | spec 050, secao "Perfil": nao introduz seguidores publicos; e o plano `2026-09-01-principal-ui-ux-closure.md`, step 4, que exige remover `Seguidores`, `Seguindo` e `principal-profile-follow` e trocar as metricas por Publicacoes, Momentos e Circulares. Implementado em `b9c75c7e`; o golden ainda mostra `Acompanhar`, `Seguidores`, `Seguindo`, `Localizacao`, `Fundacao` e `Colaboradores` | golden desatualizado |
| `chat` page | 9 | `7ed598cf` 2026-09-01T14:59 | (a) spec 050: "Circulares aparece em Coelo (Principal) e deixa de aparecer em Comunicacao", implementado em `52735a18` 15:23; o golden ainda mostra `Circulares` sob Comunicacao no lugar de `Governanca`. (b) `2d193ba1` 16:33 padronizou a paginacao das listagens de Comunicacao | golden desatualizado |
| `notices` directory | 2 | `fe73cd11` 2026-09-01T14:59 | `a0be1abe` 2026-09-08T01:10 em `coelo_ui_admin` deu alvo de toque minimo ao `CoeloAdminExpandableStatusIndicator`. O indicador ficou mais largo, o titulo do card quebra em 375 e o card seguinte e cortado. Esse commit regenerou os goldens de Locais e nao os de Comunicacoes/Circulares | golden desatualizado |
| `circulars` directory | 2 | `5e714c16` 2026-09-01T15:58 | mesma causa de `notices` | golden desatualizado |

Observacao sobre `chat.list`: a advertencia do rastreador ("nao inventar Pagina X/Y sem contrato
remoto") esta satisfeita. `superadmin_chat_inbox_v2`
(`packages/coelo_database/migrations/20260901101500_superadmin_internal_chat_v2.sql`) devolve
`'total'` calculado sobre a CTE `eligible` autorizada e `'has_more'`; o adapter le
`data['total']` em `ChatInboxPage.totalCount`. A paginacao e derivada de contrato remoto real, nao
inventada no cliente. Isso e leitura de SQL versionado, nao execucao remota.

### Bloqueio novo e decisivo

**Nao consigo regenerar golden nesta sessao.** `flutter test --update-goldens` foi negado pelo
classificador de permissoes do cliente. Isso e coerente com o PROTOCOLO ("Nao atualizar goldens
automaticamente") e eu **nao** vou contornar.

Consequencia direta: as 45 falhas permanecem vermelhas e nenhuma das seis familias pode receber
certificacao FE nesta rodada, mesmo com o codigo correto. Preciso de decisao do Owner/C00 sobre uma
das saidas:

1. autorizar nominalmente a regeneracao dos goldens dessas seis familias nesta worktree, com
   inspecao visual imagem a imagem e registro por familia da fonte aprovada que a justifica; ou
2. atribuir a regeneracao a quem detem a reserva do componente/superficie (o caso de `notices` e
   `circulars` nasce em `coelo_ui_admin`, que e central); ou
3. registrar formalmente que essas seis familias ficam com golden desatualizado ate uma rodada
   visual do Owner, e tratar as 45 falhas como divida declarada em vez de pendencia de codigo.

Sem uma dessas, o item 2 do backlog do Owner (Agora) tambem trava, porque a correcao dele produz
goldens novos por definicao.

### Conferencia da tabela de Comunicacoes contra a baseline aprovada

Comparei `notice_directory_page.dart` com `institution_directory_table.dart` e com as regras dos
anexos 8-18.

Conforme a baseline:

- mesmo componente canonico `CoeloAdminResizableTable`, com `headerHeight: 56` e `rowHeight: 64`
  identicos a Instituicoes;
- cabecalho tonal (`surfaceContainer`), divisores, linhas continuas e hover sem virar card vem do
  componente compartilhado, sem sobrescrita local;
- `CoeloSpacing.space4` entre a faixa de criacao e a tabela, igual a Instituicoes;
- toolbar, conteudo e paginacao formam uma composicao unica, com a paginacao no rodape do container
  e o mesmo inset horizontal do conteudo;
- tabs lineares por categoria irma seguem a aprovacao de 2026-08-03 (`SuperadminUnderlineTabs`), que
  e contrato proprio e nao substitui filtros nem toggle Cards/Tabela.

Divergencias que encontrei:

1. **Sem ordenacao.** Instituicoes passa `sortColumnId`, `sortAscending` e `onSort`; Comunicacoes
   nao passa nenhum. O componente entao anuncia cada coluna como "nao ordenado" na semantica sem
   oferecer forma de ordenar. E divergencia funcional e de acessibilidade, nao apenas visual.
2. **Largura do indicador de status.** Depois de `a0be1abe` o indicador reserva o alvo de toque
   minimo; em 375 isso quebra o titulo do card e corta o card seguinte. Nao e defeito do commit de
   acessibilidade: e o efeito dele numa composicao que nunca foi remedida.

Limite honesto desta conferencia: o teste golden de Comunicacoes percorre 375, 768, 1024 e 1440 em
um unico `testWidgets` e falha ja em 375, entao **as imagens de 768, 1024 e 1440 nao chegaram a ser
comparadas nesta rodada**. Nao afirmo que a tabela larga esta conforme; afirmo que ela nao foi
medida. Sem regeneracao ou sem separar os casos do teste, essa medicao continua faltando.

### Criacao e publicacao do Agora — leitura consolidada

Duas specs aprovadas convergem contra o wizard lateral de 2 etapas hoje implementado:

- `specs/036-principal-now-publication-mvp.md`: 375 com "midia dominante, ferramentas compactas,
  **uma rolagem** e acoes em largura total"; 768 com "midia/ferramentas e dados editoriais em duas
  zonas"; 1440 com "midia vertical ampla, ferramentas adjacentes e coluna editorial enxuta". O que
  os `Publicar` do Principal compartilham e shell, conteiner direito, insets e rodape responsivo.
- `specs/050-principal-ui-ux-closure.md`, secao "Publicadores": "Desktop usa area editorial e
  preview proporcionais; tablet reduz colunas sem perder contexto; mobile prioriza midia e acao
  primaria."

Nenhuma das duas descreve rail de etapas para o Agora. O rail de etapas e aprovado para o
**Acontece** (`docs/superpowers/specs/2026-08-20-coelo-happens-publication-design.md`: "rail,
composer central e previa lateral") e la sera preservado.

Cronologia: o wizard entrou em `fa293a6d` (2026-08-27), antes da spec 050. O plano de fechamento de
2026-09-01 previa "refinar os compositores de Acontece, Agora e Momentos" no step 4, com teto de
quatro horas e a regra explicita de que "pendencias restantes nao podem ser ocultadas". O refino do
compositor do Agora nao foi executado. O apontamento do Owner e a retomada dessa pendencia, nao uma
mudanca de rumo.

Plano de correcao (nao iniciado, dependente do bloqueio de golden): retirar o wizard de 2 etapas
**somente do Agora**, preservando todas as partes funcionais (midia, texto sobre a midia, musica,
corte, capa, publico e contexto, agendar publicacao, salvar rascunho, publicar agora), o rodape
aprovado e o rail do Acontece. Nenhuma etapa aprovada e removida: as etapas aprovadas em `Publicar`
pertencem ao Acontece. Impacto: 15 goldens e cerca de 18 pontos de teste que hoje tocam
`Continuar`/`Anterior`.

### Acoes e estado (28 IDs)

As 28 continuam `pending-verification`. Nenhuma promocao, nenhuma regressao de estado formal.

### Deltas propostos para os rastreadores (por ID)

Substituem integralmente os deltas propostos na revisao 1, que partiam da causa-raiz errada.

- `acontece.feed`: **nao** ha regressao visual. O card `Publicar agora` segue a spec 050; o golden de
  2026-08-31 e anterior a spec e esta desatualizado.
- `momentos.view`: o codigo entrega o viewer imersivo de tela cheia exigido pela spec 050; o golden
  de 2026-08-31 ainda registra o viewer em janela.
- `principal.profile-view` e `principal.profile-edit`: `Seguidores`/`Seguindo`/`Acompanhar` foram
  removidos conforme spec 050 e plano de 2026-09-01; o golden e anterior.
- `chat.list`: paginacao lastreada em `total`/`has_more` do RPC `superadmin_chat_inbox_v2`, portanto
  com contrato remoto; o golden e anterior tanto a paginacao quanto a mudanca de navegacao.
- `notices.list` e a familia Circulares: divergencia em 375 causada pelo alvo de toque minimo do
  `CoeloAdminExpandableStatusIndicator` (`a0be1abe`, componente central). Falta ainda ordenacao na
  tabela de Comunicacoes; e falta medicao de 768, 1024 e 1440, que o teste nao alcancou.
- `agora.create` e `agora.publish`: composicao atual diverge de duas specs aprovadas (036 e 050);
  correcao planejada e bloqueada pela regeneracao de golden.

### Primeiro criterio ainda aberto

Decisao sobre a regeneracao dos goldens desatualizados. Ela e pre-requisito de qualquer certificacao
FE nas seis familias e da correcao do Agora pedida pelo Owner.

### Proximo passo

1. Obter a decisao do item anterior.
2. Enquanto isso, avancar no que nao produz golden: ordenacao da tabela de Comunicacoes, aceites
   comportamentais por acao e leitura do contrato de midia do C02.

### ETA

- correcao do Agora conforme specs 036 e 050 (codigo, ~18 pontos de teste, 15 goldens): 3-5 h,
  **bloqueada**.
- ordenacao da tabela de Comunicacoes (dominio, adapter, teste): 1-2 h, nao bloqueada.
- regeneracao e inspecao visual das seis familias, se autorizada: 1,5-2,5 h.
- aceites comportamentais e midia por acao nas 28 acoes: nao estimavel ate destravar o item 1.

### Bloqueios ativos

1. Regeneracao de golden negada pelo classificador de permissoes do cliente (acima).
2. Midia real R2/Stream nao exercitavel: `cloudflare-api` e `figma` exigem OAuth e a sessao e nao
   interativa; o MCP `github` falhou ao conectar (`400 Authorization header is badly formatted`).
3. Migrations e deploy remotos pertencem a C00; nenhum pacote remoto preparado.

### Continuidade

`/loop` existe nesta sessao mas **nao foi ativado**; nao afirmo ID nem cadencia. Consulto
`assignments/C05.md` na C00 no inicio e no fim de cada lote e publico handoff as 12:50, 14:50 e
17:30 e a cada mudanca material.

---

## Revisao 1 — historico

Publicada em 2026-09-08T12:35:00-03:00, SHA `a0927022`. Registrou ambiente, recorte, a medicao de
**876 passaram / 45 falharam** em `flutter test` sobre as 14 features reservadas (Flutter 3.44.2
local, sem backend, sem `/dev`, sem provedor de midia) e o inventario das falhas por arquivo. A
analise de causa-raiz daquela revisao esta **superada** pela revisao 2: o que ela chamou de regressao
de codigo e, na verdade, golden desatualizado.

Permanecem validos da revisao 1:

- ambiente: cwd `C:/Users/adrie/Documents/Coelo.worktrees/e2-r01-c05`, branch
  `claude/e2-r01-c05-comunicacao`, baseline `479d1bd1`, remoto
  `origin https://github.com/otaldodexter-maker/coelo.git`, Flutter 3.44.2 / Dart 3.12.2.
- divergencia de caminho: a instrucao aponta `Coelo\.worktrees\e2-r01-c05`, que nao existe; a
  worktree registrada e `Coelo.worktrees/e2-r01-c05`.
- conversa `E2 R01 C05 - Comunicacao e Principal`, modelo `claude-opus-5`; ID de sessao do harness
  `2a43a349-a639-4be5-aefc-1ff180d1fc7a`, sem garantia de ser o identificador usado pela C00.
- recorte, ordem, fora de escopo e a lista das 28 acoes.
- nenhuma reserva consumida; nenhum arquivo compartilhado tocado.
