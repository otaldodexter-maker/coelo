---
title: "Handoff R01 C05"
source: "assignments/C05.md (R01-C05-I003 a I009, aditivo I008, R01-ALL-RTK-1808 e R01-VISUAL-1835 — sem alteracao desde 08/09 18:33); PROTOCOLO.md R01 sem alteracao desde 18:08"
status: "active"
generated_at: "2026-09-09T00:00:23-03:00"
timezone: "America/Sao_Paulo"
---

# E2 R01 C05 — Comunicacao e Principal

## Revisao 26 — desempate de notices coberto e suite remedida

- rodada: R01; revisao: 26
- ultima instrucao processada: `R01-C05-I009` mais `R01-VISUAL-1835`; **nada novo** desde a r25
- timestamp: 2026-09-09T00:00:23-03:00, relogio da maquina
- HEAD do codigo: `c5916aa6e65740b7274939810a9ea6ef67fb4c49`; push verificado, `git ls-remote`
  devolveu `c5916aa6`
- arvore limpa; commit conferido pelo `--stat`, **sem nenhum PNG de `failures/`**

## 1. Suite completa remedida: `+4404 ~5 -224`

| | r17 | r21 | agora |
| --- | --- | --- | --- |
| passando | 4325 | 4360 | **4404** |
| golden | 165 | 165 | **165** |
| nao-golden | 59 | 59 | **59** |

**79 testes a mais que na primeira medicao e nenhuma falha nova.** As 59 nao-golden continuam sendo
as herdadas do baseline `479d1bd1`, ja provadas em worktree descartavel na r17. As 14 golden do
Agora continuam 14.

## 2. O desempate de notices deixou de ficar sem guarda (`c5916aa6`)

Estava declarado como nao coberto porque atravessa a RPC, que exige fixture de identidade interna e
replay — e o replay segue esperando concessao nominal. Em vez de deixar **o achado mais grave da
revisao da C00** sem nenhuma guarda ate la, asserto a propriedade que tornou o defeito possivel: a
direcao do `ORDER BY` e o operador da comparacao de tupla tem de concordar.

Cinco asserts: ascendente ordena `id asc` e retoma com `>`, descendente ordena `id desc` e retoma
com `<`, e o desempate incondicional `filtered.id desc` que discordava do filtro nao esta mais la.
Reintroduzir qualquer um dos dois lados muda a definicao da funcao e derruba o assert.

Registro um erro que cometi e corrigi antes de commitar: escrevi primeiro com `with ... as`, e um
`with` liga a **um unico comando**. Os quatro asserts seguintes teriam quebrado. O padrao correto ja
estava no candidato de Momentos e em `activity_template_unit_scope_test.sql` — tabela temporaria com
o corpo normalizado. Refiz assim.

**Declaro o que isso nao e:** contrato de fonte pega a reintroducao, **nao substitui o replay**. A
execucao com dados empatados de verdade continua devida antes de qualquer declaracao ponta a ponta.
Nao executei nada; verificacao estatica apenas, plano corrigido de 32 para 37 asserts.

## 3. Conferencias sem achado nesta rodada

- **Paginacao do inbox do Chat.** Dois mutantes sobreviveram — avancar sem cursor e voltar antes da
  primeira pagina — e os dois sao redundantes com a mesma condicao que ja desabilita o botao no
  call-site. Terceiro caso do mesmo padrao; **declarado, nao coberto com cenario construido**.
- **`principal.profile-edit`.** Procurei spec do Perfil institucional em `specs/` e em
  `docs/superpowers/specs/`: **nao existe**. A entrega parcial restrita a biografia era mesmo o teto
  do que dava para fazer sem inventar contrato.
- **Gateways.** `chat-media` 20/20 e `moments-media` 21/21.

## 4. Saldo da varredura de mutacao

18 lacunas reais fechadas — gateways, envelope de anexo, adaptadores de feed de Acontece, Momentos e
Agora, envelope das seis acoes de Avisos, feed misto, politica de rascunho do Agora e recibo do Chat.
Cinco guardas declaradas sem prova de proposito, por nao mudarem resultado observavel.

## 5. Pendencias que dependem da C00

1. **Concessao nominal** para o replay dos dois candidatos SQL.
2. **Projecao de recibos** na thread do Chat.
3. **Contrato de revisao do Chat** (`chat.edit`/`chat.revoke`).
4. **Geometria da midia ampla** do Agora em 1440, com medidas.
5. **Rede de captura global de erro** no `apps/superadmin`.
6. **Reserva** dos comandos autorizados de remocao e expiracao.
7. **Contrato do Perfil institucional** para os campos restantes.
8. **Remocao do `PrincipalPublicationProgressBar`**, orfao no frame.
9. **Onde ficam** o leitor com respostas e o compositor de Circulares.
10. **Master x referencia** de Acontece e Momentos quanto ao trilho.
11. **Revisao visual nominal** dos goldens, inclusive os 14 do Agora.

## 6. Retomada exata

```
cd C:\Users\adrie\Documents\Coelo.worktrees\e2-r01-c05
git status --short
git log --oneline -22
cd apps\superadmin
flutter analyze
flutter test
cd ..\..\packages\coelo_database\supabase\functions\chat-media
deno test --allow-all
```

Cron `e9f4cae3` ativo. Fechamento seguro **hoje 05:30**, entrega final ate **06:00**.
