---
title: "Handoff E2 R01 C03 — Operações"
source: "PROTOCOLO R01 rev. 1; assignments R01-C03-I001 a R01-C03-I010; inspeção e implementação local C03"
status: "in-progress"
generated_at: "2026-09-08T18:11:00-03:00"
timezone: "America/Sao_Paulo"
---

# E2 R01 C03 — Operações

## Revisão 1

- Rodada: `R01`.
- Conversa: `E2 R01 C03 — Operações`, ID real `01a0819b-a12e-7110-88cb-99e51a82f384`.
- Última instrução processada: `R01-C03-I001`; acknowledgement: recebido e em execução.
- Ferramenta/modelo real: Codex; o seletor exato de deployment e esforço não é exposto à sessão, portanto `GPT-5.6 Sol / high` planejado ainda não foi confirmado tecnicamente e não houve API adicional.
- Timestamp desta revisão: `2026-09-08T12:26:05-03:00`.
- Worktree/cwd: `C:/Users/adrie/Documents/Coelo.worktrees/e2-r01-c03`.
- Branch: `codex/e2-r01-c03-operacoes`.
- Baseline operacional conferido no checkout: `479d1bd1771b13e0cd96084d244fc2cdc6b0e556`, limpo. O assignment ainda cita `6cb8ba15bae15f5a6129b0db6e740a66f8f82b3f`; C00 confirmou nominalmente `479d1bd...` ao cadastrar a worktree, então a divergência fica explícita e nenhuma reconstrução/cherry-pick foi feita por suposição.
- Ownership consumido: diretórios de feature e testes focais de `activities`, `assessments`, `attendance`, `daily_routine`, `agenda`, `meal_plans`, `plans`, `support`, `audit`, `catalog`, `imports`; `packages/coelo_domain/lib/src/attendance/`. Nenhum arquivo compartilhado reservado a C00/C01/C02 foi alterado.

## Contrato de execução

- Objetivo: concluir verticalmente as 68 ações atribuídas de Operações no Superadmin, reaproveitando código, contratos e evidências antes de implementar delta.
- Incluído: UI administrativa, domínio, adapters, backend local candidato, testes e evidências por `action_id`; persistência/reload, erro/retry, concorrência pertinente, autorização, tenant A/B e IDs adulterados conforme aplicabilidade.
- Fora: `apps/admin`, `apps/principal`, `apps/site`, deploy, mutação remota, merge, push em `dev`, migrations históricas/novas sem reserva, gateways novos, exportação XLSX de Forms e import/export geral real. Ações adiadas mantêm somente indisponibilidade honesta.
- Família visual: administrativa. Baselines: Instituições para diretório/cards/tabela; Criar/Editar Instituição para formulários; componentes canônicos Coelo UI antes de qualquer componente local.
- Ordem: Atividades A01/runtime → Avaliações v2 → delta visual preservado de Agenda → Cardápios → Assiduidade/Rotina/Auditoria/Suporte → Planos conforme spec 051 → Catálogo e adiados.
- Critério de parada por lote: um fluxo vertical focal com código/testes/evidência revisados e commitável, sem abrir vários formulários parciais.
- Evidência esperada: testes Dart/Flutter focais, análise dos arquivos tocados, pgTAP/replay local aplicável, goldens existentes sem atualização automática, diff/segredos e ponta remota após push.
- ETA: não calculável ainda; depende da inspeção do runtime HTTP/UI já integrado e da diferença entre implementação presente e aceites nominais. A primeira calibração será publicada após o lote A01.

## Delta real inicial

- `activities.list`: adapter v2 `51c02b75`, negativa `225c8867` e evidência histórica A01GREEN55 `97/97` pgTAP locais já constam nos três rastreadores. Delta declarado: runtime HTTP/UI normal, visuais, filtros, paginação e reload; não repetir a correção v1.
- `activities.create/detail/edit/publish/assessment/location`: fundação v2 histórica `32516e63`; provas de listagem não promovem comandos. Requer inspeção e revalidação nominal.
- Dependências abertas: `activities.location` aguarda contrato nominal de Locais C04; mídia de atividade, se realmente exigida pelo fluxo, consumirá o contrato C02 e ativará Cloudflare sem novo gateway.
- Primeiro critério aberto: confirmar o alcance real dos repositories/controllers/testes já presentes e localizar o gap mínimo do runtime normal.

## Ações e medições nesta revisão

- IDs efetivamente auditados em R01: `activities.list` (inspeção documental cruzada iniciada; runtime ainda não executado).
- Front-end `verified`: `0/68` no recorte nesta R01.
- Back-end `done`: `0/68` no recorte nesta R01; 68 é o conjunto atribuído, mas adiadas e aplicabilidade serão reportadas separadamente antes de qualquer percentual final.
- Integração `verified-e2e`: `0/68` no recorte nesta R01; nenhum remoto foi executado.
- Proposta de delta aos rastreadores: nenhuma promoção ainda; manter `activities.list` em `pending-verification` até prova runtime/UI nominal.

## Código, testes e publicação

- SHAs de código C03: nenhum nesta revisão.
- Testes C03: apenas inspeções read-only; nenhum teste de produto executado ainda.
- Push: não realizado; `origin/codex/e2-r01-c03-operacoes` ainda não verificado/publicado nesta revisão.
- Produção: não alterada.

## Continuidade e próximo passo

- Mecanismo real: tarefa Codex ativa `01a0819b-a12e-7110-88cb-99e51a82f384` e coordenação nativa com C00 `01a0818b-2a34-7fa3-a9aa-f191fc91cc8d`. O protocolo reserva heartbeat único ao C00; C03 não criou automação concorrente. Checkpoints conhecidos: 12:50, 14:50, 17:30; fechamento 09/09 05:30 e handoff final até 06:00, condicionados ao app/host ativos.
- Próximo passo exato: ler implementação/testes/evidência A01, executar testes focais existentes, identificar o primeiro RED nominal e aplicar TDD no menor delta de `activities.list`.
- Bloqueios: nenhum para `activities.list`; contratos C04/C02 retêm somente local/mídia quando alcançados.

## Revisão 2 — checkpoint 12:50 preparado às 12:36

- Última instrução processada: `R01-C03-I001`; assignment viva consultada no início do lote.
- SHA de código C03: `445ee6e2` (`test(activities): reach mobile location golden state`).
- Alteração: o golden de seleção de local no wizard agora rola até `activity-form-continue` antes do toque. Antes, o teste tocava em `y=1162` num viewport de 900 px e falhava antes de alcançar o estado visual; depois da correção, alcança a comparação de pixels e expõe o delta visual real.
- Verificação focal: `flutter test` sobre adapter v2, ViewModel, página e ordenação de requisições — `73/73 PASS` em Flutter `3.44.2` / Dart `3.12.2`.
- Verificação ampla de Atividades e rotas — `156 PASS / 9 cenários de teste com falha`. Todos os testes não visuais alcançados passaram; as nove falhas estão no arquivo `activity_golden_test.dart`.
- Repro após correção do harness: cenário `matches internal location dialog on mobile` chega ao comparador e falha por `30.35%`/`102428 px`, sem o aviso de toque fora do viewport.
- Análise/formatação: `dart format --output=none --set-exit-if-changed` em 1 arquivo, `0 changed`; `flutter analyze` focal, `No issues found`; `git diff --check`, limpo.
- Causa visual investigada: os masters de diretório/formulário foram atualizados pela última vez em `8e8ef93b`; mudanças posteriores em `ec31171c` e `2c0dbe84` alteraram o workflow canônico e o wizard. O master mobile ainda mostra 4 etapas, enquanto o runtime/teste atual possui 6. Mudanças posteriores de shell/tabs/componentes compartilhados também explicam deltas amplos no diretório. O commit `b191b727` atualizou apenas o golden de profissionais de Atividades e goldens de Instituições, não os demais masters de Atividades.
- Inspeção visual: o test image atual preserva superfície administrativa branca, navegação/tabs e wizard de 6 etapas; não há autorização para converter essa inspeção em aprovação. Nenhum golden foi atualizado.
- Estado por ID: `activities.list/create/detail/edit/location` efetivamente auditados na camada visual compartilhada; `activities.publish/assessment` ainda não executados nominalmente nesta revisão. Nenhum ID é promovido a FE `verified`, BE `done` ou E2E `verified-e2e`.
- Primeiro critério aberto: decisão visual nominal sobre os masters defasados de Atividades; em paralelo, fechar o runtime HTTP/read-only de `activities.list` e auditar comandos separadamente.
- ETA recalibrada: inspeção funcional/lista local concluível no próximo lote; fechamento visual de Atividades depende de revisão/aprovação dos nove candidatos, sem ETA honesta até o owner visual decidir. A dependência não bloqueia inspeção de backend/comandos nem Avaliações.
- Proposta de delta aos rastreadores: manter os sete IDs de Atividades em `pending-verification`; acrescentar bloqueio visual comum “masters anteriores ao workflow canônico 6 etapas/shell atual; 9 cenários falham, não atualizar automaticamente”; registrar `445ee6e2` como correção de harness, não como conclusão de ação.
- Push nesta revisão: pendente até o commit separado deste handoff; produção permaneceu intocada.

## Revisão 3 — lote I002 de save transacional de Atividades

- Última instrução processada: `R01-C03-I002`; acknowledgement: recebida diretamente da assignment viva e executada dentro da reserva nominal. I001 permanece vigente.
- Reserva consumida: somente `packages/coelo_database/migrations/20260908154257_superadmin_activity_save_v2.sql`, `packages/coelo_database/supabase/tests/superadmin_activity_save_v2_test.sql`, repository/testes focais de Atividades e este handoff. Nenhum router, runner, manifesto, migration histórica, rastreador oficial ou composição compartilhada foi alterado.
- SHAs de código C03: `b9a58002` (`feat(activities): add transactional v2 save gateway`) e `0dbdba5e` (`feat(activities): wire safe aggregate saves`).

### Contrato SQL candidato

- Assinatura pública: `public.superadmin_activity_save_v2(uuid, uuid, bigint, boolean, jsonb) returns jsonb`; argumentos, na ordem: request ID, activity ID nulo na criação, versão esperada (`0` na criação), intenção de publicar e snapshot JSON estritamente allowlisted.
- Envelope: mantém exatamente as chaves top-level `ok`, `data`, `error`. Em sucesso, `data` contém `activity_id`, `management_version`, `status`, `correlation_id` e `replayed`. Erros reutilizam os códigos normalizados das gateways v2, inclusive `SAI_CONCURRENT_CHANGE`, `ACTIVITY_INVALID_REFERENCE`, `ACTIVITY_NOT_FOUND` e negações `SAI_*`.
- Orquestração transacional: reutiliza `create/update`, `set_units`, `set_groups`, `set_participants`, `set_professionals`, `set_permissions` e, quando solicitado, `publish`. Cada capacidade é reautorizada antes do replay e novamente pelo comando filho. Qualquer envelope filho de erro é elevado dentro do sub-bloco; o catch externo reverte definição, vínculos, auditorias e recibos filhos da tentativa antes de devolver a negativa.
- Concorrência/idempotência: advisory lock por request ID agregado; hash inclui activity ID, versão esperada, intenção e payload integral. Repetição idêntica retorna o mesmo resultado com `replayed=true`; mudança apenas de payload ou intenção com o mesmo request retorna `SAI_CONCURRENT_CHANGE`; edição obsoleta é negada.
- Persistência mínima de replay: nova tabela privada `app_private.superadmin_internal_activity_save_receipts`, sem payload, com hash SHA-256 pseudônimo, IDs/resultados/correlação e FKs. RLS habilitada e forçada, sem policy permissiva e com todos os privilégios diretos revogados de `public`, `anon`, `authenticated` e `service_role`. A allowlist histórica de oito tipos de recibo filho permanece inalterada.
- Grants: somente `authenticated` recebe `EXECUTE` no novo wrapper. `public`, `anon` e `service_role` permanecem sem execução. Não houve grant direto em tabela nem ampliação de papel/capacidade.
- Limites: o payload aceita apenas definição v2 (`name`, `description`, `taxonomy_id`, `icon_key`, `initials`) e snapshots de unidades, grupos/participação, participantes, profissionais e permissões. Assessment, local, About, template, mídia/arquivo, configuração pedagógica, governança não opcional, handle customizado e origem por unidade não foram incorporados nem silenciosamente descartados.

### Adapter Flutter e comportamento honesto

- `SupabaseActivityCommandRepository.save` faz uma única chamada HTTP a `superadmin_activity_save_v2`, ordena sets/listas antes do hash e mapeia envelope de concorrência/autorização para exceções de domínio.
- O adapter abre somente o subconjunto equivalente hoje persistido: governança `optional`, identidade por iniciais/cor padrão, sem mídia, template, handle customizado, origem por unidade ou avaliação pedagógica. Qualquer variante fora desse contrato falha antes de HTTP; export, criação de local e cópia de template continuam indisponíveis sem fallback legado.
- Consequência de escopo: `activities.create/edit/publish` ganham candidato BE local e adapter focal, mas não são promovidas a concluídas. O fluxo normal ainda pode incluir About/mídia/governança e depende de composição reservada e dos contratos C02/C04; não há prova de persistência/reload remoto.

### Evidências

- RED SQL no baseline `20260907222911`: função ausente, como esperado, antes da migration.
- Replay descartável derivado exatamente do perfil nominal `A01DirectoryAuditGreen` mais somente I002: novo pgTAP `23/23 PASS`; inclui ACL/RLS, create+publish, edit, snapshots, tenant A/B, ID cross-tenant, ator revogado, replay, mudança de intenção, versão obsoleta e rollback tardio de definição/recibos.
- Regressão v2 com I002: `319` testes, `314 PASS / 5 FAIL`. O contrato de segurança dos oito recibos filhos passa. As cinco falhas remanescentes são idênticas ao baseline sem I002 (`296` testes, `291 PASS / 5 FAIL`) e decorrem de expectativas AAL1 anteriores a `20260901200206_defer_superadmin_internal_mfa_until_mvp_go_live.sql`; I002 não alterou MFA nem esses testes.
- Adapter focal: `5/5 PASS`; `flutter analyze` nos dois arquivos tocados: `No issues found`; `dart format`: zero delta; `git diff --check`: limpo.
- Suíte Flutter Atividades/rotas após o adapter: `158 PASS / 9 FAIL`, todas as falhas ainda em `activity_golden_test.dart`; nenhum master foi atualizado e o conjunto visual bloqueado permanece o mesmo da revisão 2.
- Replay temporário: script fora do repositório removido; `0` containers, volumes ou networks `coelo_c03_*` residuais. Produção e bancos compartilhados permaneceram intocados.

### Proposta de delta aos rastreadores e continuidade

- `activities.create`, `activities.edit`, `activities.publish`: registrar candidato BE local `b9a58002` e adapter focal `0dbdba5e`, sem promover a `done`, `verified` ou `verified-e2e`; anexar limites explícitos de equivalência e ausência de remoto/reload.
- `activities.list`: anexar replay local fresco A01 `97/97 PASS` já reproduzido nesta R01, mantendo bloqueio visual comum.
- Bloqueios comuns: nove goldens aguardam decisão visual; About/composição compartilhada requer reserva C00; mídia depende C02 e deve carregar Cloudflare apenas quando implementada; local depende C04. Nenhum desses bloqueios impede iniciar Avaliações v2.
- Gate de conhecimento: nenhuma projeção em `docs/knowledge/` foi alterada porque este RPC ainda é candidato local não integrado; o conhecimento durável e aprovado continua nas specs/ADRs, e a implementação/limites ficam registrados neste handoff para captura após integração.
- Próximo passo: consultar novamente a assignment viva e iniciar o delta real de Avaliações v2, preservando este lote em commits independentes.

## Revisão 4 — checkpoint 14:50: Avaliações e Assiduidade

- Última instrução processada: `R01-C03-I006`; I001–I005 continuam observadas. Reservas consumidas somente no novo teste `packages/coelo_database/supabase/tests/superadmin_assessment_workflow_regression_test.sql` e, após prova read-only do C00 de ausência no ledger/catálogo remoto, no candidato local `packages/coelo_database/migrations/20260901182838_superadmin_assessments_internal_v2.sql`. Runner, perfis comuns, ledger, remoto e rastreadores oficiais não foram alterados.
- SHA Flutter apto à revisão: `1bfdde2f` (`fix(assessments): guard closing transitions by status`). A UI de fechamento agora só habilita `Devolver` em `submitted|reviewed`, `Revisar` em `submitted` e `Publicar` em `reviewed`, além de bloquear as três durante save. RED reproduzido em `draft return Actual: true`; arquivo focal `14/14 PASS`, suíte de Avaliações `27/27 PASS` e análise focal `No issues found`.
- SHA SQL intencionalmente WIP/RED: `2f3e65f9` (`test(assessments): capture blocked workflow regressions`). A fixture possui `35` aceites sem redução: create/save/replay, versão obsoleta, submit/read, return/reopen, correção, novo submit, review, schedule, publish/read/history, idempotência alterada, transição inválida, rollback tardio, fila, tenant A/B e ator revogado.

### Prova local derivada de Avaliações

- O replay histórico amplo canônico não alcança Avaliações: para antes em `20260812002010`, pela ausência de `app_private.unit_import_source_attestations`, bloqueio histórico já conhecido. O replay nominal `A01DirectoryAuditGreen` com a migration canônica exata de Avaliações também parou antes da fixture com SQLSTATE `42601`: faltava um `)` em `superadmin_assessment_context_options`.
- I004 autorizou apenas derivação TEMP replay-only. Perfil materializado: `55` migrations (`53` canônicas + `2` preflight), `additional=8`, manifest SHA256 `4279e67c9651f4049329591b6e8aad82e3a9052506c1a4e16ba8bbb693249d59`; preflights `20260811151253=718c2de052e9df29abc42642806d9a5e4d98c8964665453de6f14f0f8b61ab75` e `20260811215452=d97e02796fcd5897707b5657b7a1c1df690f831e09ed98df6ed21886f75f9ef3`.
- Bytes canônicos de `20260901182838_superadmin_assessments_internal_v2.sql`: SHA256 `3da039bef47e88fc03cc73ba32b846ddd7e2dbae5b8a00648bbf9a6f02c82d29`. Derivado: SHA256 `034f9cc85591a3629a098137abc4b6dfffb86eb133d1e93b9aae4aeab246f883`; diff validado em memória como exatamente `+ ')'`, `+1 byte`, no fim da expressão anterior a `INTO`. Nenhuma cópia derivada foi persistida no repositório.
- Com essa única pré-condição, o schema chega ao alvo `20260907222911`, mas a execução nominal encontra um segundo defeito canônico: SQLSTATE `42702` em `app_private.assessment_v2_validate_students`, pois `child_context.id = child_context_id` é ambíguo entre a variável PL/pgSQL e a coluna. A gateway pública converte o defeito em `SAI_INTERNAL_ERROR` 500. Essa prova anterior permanece registrada separadamente.
- I005 autorizou uma segunda derivação TEMP estrita: substituir somente `child_context.id = child_context_id` por `child_context.id = (item->>'child_context_id')::uuid`, reutilizando o UUID validado imediatamente antes. O diff adiciona `17 bytes`; SHA256 após I004+I005: `6b05def3082f47c048c637698b703130fedf1c0e4a3394d20ef4cdf28bc8f170`. Com as duas pré-condições explícitas, a fixture executa `35` aceites: `34 PASS / 1 FAIL`. Workflow, replay/idempotência, versão obsoleta, submit/return/reopen/review/schedule/publish/read/history, rollback tardio, tenant A/B e revogação passam.
- A única falha remanescente é de produto e está preservada: `superadmin_assessment_closing_queue` usa `b.status <> 'published'` e inclui drafts; o aceite mantém a regra de fila de fechamento `submitted|reviewed`, excluindo draft, published e tenant B. Eventual correção exige migration forward-only nominal; C03 não criou uma.
- Lint do schema derivado foi executado separadamente e falha por erros históricos/preexistentes. No domínio de Avaliações, revelou ainda `institution_id` ambíguo em `assessment_v2_save_configuration` e em `superadmin_assessment_configuration_read`, além do warning de volatilidade em `assessment_v2_validate_students` (`STABLE` usando expressão `VOLATILE`). Os demais erros de lint pertencem a Imports/Perfis e ficam fora deste lote. Nenhum foi corrigido sem reserva.
- Após consulta C00 somente leitura provar `0` registros da versão `20260901182838` no ledger produtivo e `0` das três funções examinadas, I006 reclassificou o arquivo como candidato local não aplicado e autorizou incorporar os três deltas já provados. Commit `9f97d802` (`fix(assessments): repair local v2 workflow candidate`): exatamente três linhas — parêntese I004, predicado I005 e fila `status in ('submitted', 'reviewed')`; sem grant, capability, helper de autorização, payload, tabela, timestamp ou migration duplicada. SHA256 fonte `3da039bef47e88fc03cc73ba32b846ddd7e2dbae5b8a00648bbf9a6f02c82d29`; novo SHA256 canônico `cc1ce9192fc719fd02e2164ab50480a9e58a81167e213f456b62ad52b347a77f`.
- A prova `34/35` é a evidência RED anterior à correção da fila. O replay de `9f97d802` sem patches TEMP foi solicitado, mas aguarda novo lease nominal porque C03 já havia liberado I003; este checkpoint não promove o candidato antes dessa repetição.
- Proposta de delta: anexar `1bfdde2f` a `assessments.close/reopen` como correção FE focal, sem promoção; anexar `2f3e65f9` e `9f97d802` aos cinco IDs de Avaliações, preservando a prova dos três REDs e mantendo BE/E2E `pending-verification` até o replay canônico. Os dois erros adicionais de lint permanecem abertos sem ampliação da reserva.

### Auditoria focal de Assiduidade

- IDs auditados: `attendance.dashboard/create/mark/correct/finish/export`. Suíte funcional sem goldens: `61/61 PASS`; análise focal: `No issues found`. Suíte completa: `64 PASS / 1 FAIL`, apenas `matches the new attendance call context reference`, delta visual `15.84%`/`53.458 px`.
- Inspeção visual sem regravação: o master mantém cabeçalho compacto com hamburger e footer inteiro no viewport; o test image atual usa o shell de marca `Coelo >` e posiciona o footer abaixo da dobra. Três cenários golden do mesmo arquivo passam. Nenhum master foi atualizado ou aprovado por inferência.
- Backend permanece bloqueado por contrato: OQ-040 e spec 048 `draft-for-review` registram que o adapter produtivo aponta para RPCs existentes apenas na cadeia recovery/local removida e people-based. Capability interna, matriz/AAL, scope/DTO e cutover ainda não estão aprovados; a própria OQ-040 proíbe RED funcional/SQL e restauração histórica. Não houve migration, RPC, RLS ou mutação remota.
- `attendance.export` continua honestamente adiada: testes responsivos do botão informativo passam; nenhum picker, job, arquivo, download ou persistência foi habilitado.
- Proposta de delta: atualizar a contagem funcional histórica de `55` para `61 PASS`, registrar um golden divergente e três cenários em conformidade com os masters existentes, mantendo os cinco IDs ativos `pending-verification` e `attendance.export` em `deferred-post-mvp`.

### Estado seguro e continuidade

- Produção/remoto: intocados. Cada replay descartável encerrou com zero containers, volumes e networks `coelo_safe_*` residuais; wrapper TEMP removido por path literal validado; mutex `Local\CoeloSafeSupabaseReplay` testado disponível e liberado. Lease I003 formalmente liberado ao C00 às `14:40:58-03:00`.
- O gate de conhecimento não gerou projeção: foram encontrados bloqueios e candidatos locais, sem mudança durável aprovada de produto. OQ-040/spec 048 continuam como fontes canônicas; a divergência de fila foi elevada ao C00 em vez de resolvida silenciosamente.
- Próximo passo independente: após publicar este checkpoint, seguir para o delta preservado de Agenda (`f8a04b3a` reader, `4daeafb9` controller, view `ca4c82ab`) sem reconstrução; se C00 conceder novo lease, repetir primeiro os `35` aceites sobre bytes canônicos de `9f97d802`, sem patches TEMP.

## Revisão 5 — replay canônico I006

- Última instrução processada: `R01-C03-I006`; janela `R01-C03-REPLAY-02` recebida após o release anterior e usada exclusivamente para o candidato de Avaliações.
- Replay nominal `A01DirectoryAuditGreen` consumiu a migration canônica de `9f97d802`, SHA256 `cc1ce9192fc719fd02e2164ab50480a9e58a81167e213f456b62ad52b347a77f`, copiada byte a byte para o projeto descartável sem patches de conteúdo.
- Resultado: `35/35 PASS`. Ficam provados localmente create/save/replay, concorrência obsoleta, submit/read, return/reopen, correção, resubmit, review, schedule, publish/read/history, idempotência alterada, transição inválida, rollback tardio sem recibo/evento, fila somente `submitted|reviewed`, tenant A/B e revogação imediata.
- Limite da conclusão: o resultado promove o candidato local para revisão BE, mas não torna o lint global verde, não cura a cadeia histórica ampla, não prova produção/remoto e não fecha os dois erros de configuração/leitura descobertos pelo lint fora da reserva I006. FE/E2E continuam separados.
- Cleanup/release: wrapper TEMP removido; zero containers, volumes ou networks `coelo_safe_*`; mutex conferido disponível e liberado. Janela `R01-C03-REPLAY-02` devolvida explicitamente ao C00.
- Integração informada pelo C00: `1bfdde2f` entrou em `aa960d54`; regressão conjunta Convites/Avaliações `61/61 PASS`. O SHA continua citado como origem C03, sem duplicar integração.

## Revisão 6 — Agenda preservada e bloqueio visual isolado

- Última instrução processada: `R01-C03-I006`; a assignment viva e o protocolo foram consultados no início e no encerramento do lote. Nenhuma reserva compartilhada foi consumida.
- Reconciliação aplicada: o lote recupera exclusivamente a view preservada em `ca4c82ab` — getter do controller, `agenda_read_view.dart`, teste focal e oito masters — sobre o reader `f8a04b3a` e controller `4daeafb9` já integrados. Não restaura documentação/evidência antiga, não reconstrói Agenda e não toca router, composition root, pacote UI central ou rastreadores oficiais.
- SHA WIP C03: `b049b163` (`wip(agenda): recover read view for visual decision`). O prefixo WIP e este handoff proíbem integração até decisão visual nominal do C00/Owner.
- Prova focal: `agenda_read_view_test.dart` executou `27 PASS / 4 FAIL`. Todos os testes comportamentais passaram; quatro goldens de detalhe/negação/erro também passaram. Falharam somente `list-light`, `list-dark` e `hover-light` por `0,29%` (`4180`, `4180`, `4167` pixels) e `status-light` por `0,39%` (`5556` pixels). `list-light` falha igualmente isolado, eliminando contaminação pela ordem da suíte.
- Causa comprovada: o parent `65d8d309` de `ca4c82ab` não contém `a0be1abe`. O baseline atual contém `a0be1abe`, que mudou `CoeloAdminExpandableStatusIndicator` de visual/layout de 24 px para alvo mínimo acessível 48 × 48, teclado/toque e rótulo expandido; a mesma mudança atualizou a fonte canônica de design e os goldens de Locais. O diff de Agenda fica restrito à anatomia do status/card; estados sem card preservam exatamente os masters.
- Decisão de segurança visual: nenhum PNG foi regravado. C03 enviou ao C00 a escolha nominal necessária entre aceitar capturas candidatas coerentes com `a0be1abe` ou fornecer critério de adaptação específico para Agenda. Não foi criado contorno local que reduzisse o alvo de 48 × 48 ou divergisse do componente compartilhado.
- Qualidade estática: `flutter analyze` nos três arquivos Dart, `No issues found`; `dart format --output=none --set-exit-if-changed`, `0 changed`; `git diff --check`, limpo.
- Push: `origin/codex/e2-r01-c03-operacoes` conferido exatamente em `b049b1634fc68532b00ac6edc0852b2a12446a38`. Produção/remoto de dados permanecem intocados.
- Proposta de delta aos três rastreadores para `agenda.calendar/create/detail/edit/requests/permissions/location`: registrar reader/controller integrados e view WIP recuperada com `27 PASS / 4 golden FAIL`; manter todos os sete IDs em `pending-verification`, sem promoção FE/BE/E2E. O bloqueio visual retém somente a certificação da view; comandos, composição normal, persistência/reload, negativas e remoto continuam abertos separadamente.
- Primeiro critério aberto: decisão visual nominal dos quatro masters afetados pelo alvo acessível; depois, reserva C00 para a composição normal se a view for aceita. Próximo passo independente: inspecionar o delta real de Cardápios/Rotina sem tocar arquivos compartilhados.
- ETA: a view pode voltar a lote apto assim que houver decisão visual e replay focal; não há ETA honesta para essa decisão. O próximo domínio independente continua imediatamente.
- Gate de conhecimento: nenhuma regra durável de produto foi alterada; a mudança acessível já está na fonte canônica `docs/design/design-system.md`. Portanto não foi criada projeção nova em `docs/knowledge/`.

## Revisão 7 — Cardápios fecham mídia legada por padrão

- Última instrução processada: `R01-C03-I006`; assignment/protocolo consultados no início e no fechamento do lote. A skill Cloudflare/R2 foi carregada porque Cardápios inclui imagens; nenhum recurso, configuração ou remoto Cloudflare foi alterado.
- SHA apto à revisão: `82b1f4e5` (`fix(meal-plans): keep legacy media picker closed`). O wizard agora usa `imageSelectionEnabled = false` por padrão. O adapter histórico `SupabaseMealPlanImageRepository` pode continuar injetado pela composition root reservada, mas não abre picker, upload, leitura ou exclusão a partir da UI normal sem habilitação explícita futura.
- RED/GREEN nominal: novo teste `image selection is fail-closed until the private R2 gateway is composed` falhou antes (`Actual: true`) e passou depois. A correção altera uma linha produtiva e adiciona um teste; não cria gateway, não usa Supabase Storage como substituto de R2 e não toca router/composition root compartilhados.
- Fundamento: ADR 0032 exige que toda mídia nova do MVP use R2 privado e que Postgres retenha metadados/autorização/auditoria. O adapter de imagem atual ainda executa `SupabaseClient.storage.createSignedUploadUrl/uploadBinaryToSignedUrl/createSignedUrl` e as migrations históricas autorizam por `platform_memberships`/`has_platform_permission`, anteriores ao realm interno da spec 039. Sem gateway R2 nominal comum e sem migração interna aprovada, deixar o picker habilitado produziria uma operação incompatível com as duas fontes vigentes.
- Regressão funcional após a correção: data + wizard + diretório de Cardápios, `93/93 PASS`; focal do novo guard, `1/1 PASS`; analyzer dos dois arquivos tocados, `No issues found`; formatação, zero delta; `git diff --check`, limpo.
- Baseline visual inspecionado antes do patch: suíte completa `109 PASS / 6 FAIL`, todas em `meal_plan_pages_golden_test.dart`. Falhas: cinco diretório/flyout/status entre `0,86%` e `2,66%`, coerentes com deriva de componentes compartilhados, e identificação mobile do wizard `8,70%`. Três cenários golden passam. Nenhum PNG foi atualizado; esse bloqueio permanece independente do guard de mídia.
- Rota produtiva focal: `meal_plan_production_routes_test.dart` tem `3 PASS / 1 FAIL`; a falha histórica espera `SuperadminErrorScreen` sem tenant em criar modelo, mas o contrato já foi reconciliado anteriormente com seleção institucional (`b0c250fc` citado nas evidências). C03 não alterou o teste global/router reservado.
- Backend: os RPCs `meal_plan_*`, RLS e mídia existentes continuam people-based/Storage legado. Não houve nova migration, SQL, RLS ou replay, pois isso exige reserva nominal e reconciliação 039. O worker histórico de limpeza não foi conectado a R2 por inferência.
- Proposta de delta aos três rastreadores: anexar `82b1f4e5` aos seis IDs `meal-plans.list/create/edit/model-create/model-edit/publish` como hardening FE transversal da mídia, sem promoção FE/BE/E2E; registrar `93/93` funcionais, seis goldens divergentes e três conformes. Manter backend e E2E `pending-verification` por realm 039, R2 privado, persistência/reload e negativas reais.
- Push: `origin/codex/e2-r01-c03-operacoes` avançado com sucesso para `82b1f4e5`; produção Supabase/Cloudflare intocada.
- Primeiro critério aberto: C00 integrar o guard FE e decidir se concede pacote nominal para migração interna/R2 de Cardápios; sem isso, mídia fica honestamente indisponível e operações textuais continuam no contrato existente sem certificação E2E.
- Gate de conhecimento: nenhuma política nova foi criada. O código apenas passou a cumprir a ADR 0032 já aprovada; a fonte canônica não precisou de alteração e nenhuma projeção `docs/knowledge/` foi adicionada.

## Revisão 8 — auditoria focal de Rotina diária

- Última instrução processada: `R01-C03-I006`; nenhuma reserva nova consumida e nenhum arquivo de produto alterado neste lote.
- Escopo cruzado: `daily-routine.list/create/edit/apply/publish`, três rastreadores, `specs/021-superadmin-daily-routine-prototype.md`, evidência `2026-09-07-routine-filter-result.md`, composição normal e contratos/testes da feature.
- Resultado funcional: todos os oito arquivos não-golden da feature passaram, `47/47 PASS`; `flutter analyze lib/features/daily_routine test/features/daily_routine`, `No issues found`.
- Resultado visual: suíte completa `47 PASS / 4 SKIP / 6 testes FAIL`. As seis falhas agregam nove divergências de master: create mobile `16,93%`, edit dark e quatro superfícies com o mesmo delta de `837` pixels (`0,06%–0,08%`), cards/hover `3,61%`/`3,86%` e erro de identidade mobile `14,64%`. Os quatro SKIP já declaram reconciliação pendente da shell de diálogo compartilhada. Nenhum PNG foi atualizado.
- Gate produtivo confirmado: a spec 021 tem status `implemented-local-prototype`; READ01 segue proposta, não migration. A composição normal injeta `UnavailableRoutineRepository` e não oferece callbacks de criar/editar/duplicar/aplicar; o protótipo `/dev` usa `DevelopmentRoutineRepository` separado. Assim, um local-green não foi tratado como persistência ou autorização.
- Backend: a cadeia histórica mencionada na spec 048 mistura Rotina/Assiduidade e continua people-based. Sem spec interna 039 aprovada para este domínio, capability/DTO/cutover e migration nominal, C03 não restaurou migrations, não criou RPC/RLS e não executou replay/remoto.
- Proposta de delta aos rastreadores: atualizar a evidência fresca de `daily-routine.list/create/edit/apply/publish` para `47/47` funcionais, `4 SKIP` e nove masters divergentes; manter cinco IDs `pending-verification`, FE sem promoção por visuais/composição mutante ausente, BE/E2E abertos por contrato interno e reload real.
- Primeiro critério aberto: aprovação de uma spec produtiva interna e pacote nominal backend; depois composição normal e replay. Até lá, indisponibilidade fail-closed é o comportamento correto, não um bug a contornar.
- Gate de conhecimento: nenhuma regra nova foi aprovada ou implementada; spec 021 e proposta READ01 permanecem fontes. Nenhuma projeção durável foi criada.

## Revisão 9 — auditoria focal de Suporte

- Última instrução processada: `R01-C03-I006`; nenhuma reserva nova foi consumida e nenhum arquivo de produto foi alterado neste lote.
- Escopo cruzado: `support.create/table/kanban/detail/reply/close`, os três rastreadores, `specs/016-superadmin-support-prototype.md`, OQ-028 e implementação/testes da feature.
- Resultado funcional: a suíte completa executou `56 PASS / 1 teste FAIL`. A única falha é o teste parametrizado de goldens; criação de solicitação, tabela, Kanban, detalhe, resposta, fechamento, filtros, paginação, teclado e responsividade funcional passaram.
- Resultado visual: os 24 masters históricos falharam, com deltas entre `11,51%` e `90,37%` nas larguras 375/768/1024/1440, claro/escuro e superfícies tabela/Kanban/detalhe. A amplitude indica baseline visual incompatível com o shell/composição atuais; nenhum PNG foi atualizado ou aprovado por inferência.
- Qualidade estática: `flutter analyze lib/features/support test/features/support`, `No issues found`.
- Gate produtivo confirmado: a spec 016 aprova somente protótipo local e declara repository, persistência, backend/Supabase, R2, Realtime e auditoria fora do escopo; nenhum dado sobrevive a reload. OQ-028 mantém fluxo/status produtivo sem decisão. O controller atual é síncrono e em memória, compatível com essa fonte e insuficiente para qualquer alegação E2E.
- Backend: não houve migration, RPC, RLS, arquivo/mídia, replay ou remoto. Não foi inventada máquina de status nem persistência antes da decisão nominal.
- Proposta de delta aos rastreadores: registrar evidência fresca `56 PASS`, 24 divergências visuais e analyzer verde para `support.create/table/kanban/detail/reply/close`; manter os seis IDs `pending-verification`, sem promoção FE/BE/E2E, pois composição produtiva, persistência/reload, autorização 039 e estados aprovados continuam abertos.
- Primeiro critério aberto: resolver OQ-028 e aprovar contrato produtivo interno; só então reservar backend/composição e produzir negativas cross-tenant/ID adulterado. Até lá, o protótipo local não pode ser apresentado como operação real.
- Gate de conhecimento: nenhuma regra durável nova foi criada. A lacuna continua corretamente registrada na OQ-028 e na spec 016; nenhuma projeção `docs/knowledge/` foi alterada.

## Revisão 10 — Planos: crosswalk contratual e retry idempotente

- Última instrução processada: `R01-C03-I007`. O C00 confirmou que o SQL de Planos deve permanecer intacto até reconciliação/reserva nominal; catálogo manual e escopo aprovado da spec 051 são preservados, sem interpretar ativação como cobrança ou ação comercial.
- Crosswalk proposto ao C00: `plans.activate` corresponde exclusivamente a restaurar um plano `archived` para `active`; arquivar é a transição inversa já aprovada. `plans.assign` não autoriza escrita: a spec 051 limita vínculos de instituições a consulta somente leitura. O frontend atual confirma essa leitura — oferece editar, arquivar e restaurar, não atribuir — e avisa que subscriptions não são alteradas automaticamente.
- Conflito 039 × 051 elevado nominalmente: a spec 051 ainda exige pessoa global ativa; a migration `20260901183154_superadmin_plans_production.sql` materializa `created_by_person_id`, `updated_by_person_id`, `plan_change_receipts.actor_person_id`, `current_person_id()` e `has_platform_permission()`. A spec 039/ADR 0019 proíbe principal interno ligado a `people`/`platform_memberships` e exige caminho interno por capability. O SQL permaneceu sem alteração. `units_with_override = 0` no reader legado também foi registrado como placeholder, não contagem comprovada.
- SHA apto à revisão: `4c5af8ce` (`fix(plans): reuse save request after transport loss`). O formulário agora conserva o mesmo `request_id` quando uma falha `unavailable` deixa o resultado remoto incerto; se draft, revisão, capabilities, limites ou motivo mudam, uma nova intenção recebe UUID novo. Sucesso e erros de negócio conhecidos limpam a chave pendente; troca de repository/plano também limpa o contexto.
- TDD: o teste novo falhou no baseline porque o segundo save recebeu UUID diferente; após o patch, prova o mesmo UUID em dois retries de transporte e UUID novo depois da mudança de motivo. Focal `1/1 PASS`; suíte funcional sem goldens `34/34 PASS`; analyzer de toda a feature/testes, `No issues found`; formatação e `git diff --check`, limpos.
- Baseline visual, anterior e independente do patch: suíte completa `34 PASS / 1 teste FAIL`; o teste parametrizado agrega oito divergências somente nos cards: claro/escuro em 375 (`0,57%`), 768 (`0,56%`), 1024 (`0,42%`) e 1440 (`0,30%`). Todos os oito masters de tabela e os três cenários de criar/capabilities/revisão passam. Nenhum PNG foi atualizado.
- Proposta de delta aos rastreadores: anexar `4c5af8ce` a `plans.create/edit` como hardening FE de idempotência, sem promoção FE/BE/E2E; registrar o crosswalk acima em `plans.activate`; manter `plans.assign` sem write e pendente de decisão nominal. `plans.list/create/edit/activate` continuam bloqueados para certificação produtiva pelo realm 039, leitor/comando nominal, placeholder de overrides, goldens de cards e ausência de remoto/reload real.
- Produção/remoto: intocados; nenhuma migration, RPC, RLS, grant ou replay executado. O gate de conhecimento não criou projeção: o crosswalk ainda é proposta operacional a reconciliar pelo C00, e as fontes canônicas 039/051/OQ de Planos permanecem inalteradas.

## Revisão 11 — auditoria focal de Auditoria

- Última instrução processada: `R01-C03-I007`; nenhuma reserva nova foi consumida e nenhum arquivo de produto foi alterado neste lote.
- Escopo cruzado: `audit.list/filter/detail`, `audit.export` apenas para conferir adiamento, os três rastreadores, specs 027/039, migrations `20260812000847`/`20260827233000`/`20260901124500`, repository/controller/UI e testes focais.
- Resultado funcional: os quatro arquivos não-golden passaram `50/50`; incluem cursor opaco, filtros server-side, cancelamento de busca obsoleta, negativa/retry, detalhe sob demanda, estados unauthorized/not-found, teclado, 200%, ator `auth_session` sem ID/papel inventado e rejeição de projeções inconsistentes. `flutter analyze lib/features/audit test/features/audit`, `No issues found`.
- Resultado visual: a suíte completa ficou em `50 PASS / 2 testes FAIL`. Os dois testes parametrizados agregam seis masters divergentes: diretório 375 claro `13,29%`, 768 escuro `14,84%`, 1024 claro `5,73%`, 1440 escuro `4,87%`, empty 1440 claro `4,86%` e failure 1440 claro `4,86%`. Os dois goldens `auth_session` em 200% passam. Nenhum PNG foi atualizado.
- Minimização conferida: lista e detalhe internos projetam ator tipado e nunca retornam `session_id_hash`; `auth_session` sai como “Sessão autenticada”, com `id`/`role_code` nulos. `before`/`after` passam por `audit_mask_payload`, allowlist de IDs/status/contagens/booleanos/timestamps; motivo livre vira `[redacted]`. O cliente rejeita ator de sessão com ID ou papel e renderiza os valores já sanitizados como texto.
- Bloqueio produtivo: embora a migration 039 tenha atualizado as projeções v2/v3, `audit_assert_permission`, `audit_authorization_scope` e os wrappers de lista/detalhe ainda derivam `current_person_id()` e autorização people-based. Não houve migration forward-only, RPC/RLS/grant ou replay sem reserva nominal; runtime/reload do principal interno continuam não certificados.
- Exportação geral: a UI mantém CSV/XLSX visíveis e responde “Disponível depois do MVP”; três testes provam zero `startExport`, zero polling e zero abertura de URL, inclusive quando o repository simula job concluído. O Edge Function/repository legado foi apenas inspecionado e permaneceu desconectado da ação visual; nenhum job, arquivo, download ou persistência foi habilitado.
- Proposta de delta aos rastreadores: atualizar `audit.list/filter/detail` com `50/50`, seis divergências de diretório/estado e dois goldens auth-session conformes; manter os três IDs `pending-verification` pelo cutover 039/runtime/remoto. Manter `audit.export` `deferred-post-mvp`, registrando novamente o bloqueio honesto comprovado.
- Gate de conhecimento: nenhuma regra nova foi criada. A implementação observada cumpre a minimização aprovada onde aplicável e preserva a divergência de realm já registrada; nenhuma projeção `docs/knowledge/` foi alterada.

## Revisão 12 — Catálogo: aplicabilidade comprovada

- Última instrução processada: `R01-C03-I007`; assignment viva consultada no início/encerramento. O limite nominal foi preservado: host Superadmin e execução das ferramentas necessárias, sem editar ou expandir `apps/catalog`.
- Provider por ação: `catalog.list` no Superadmin é `CatalogHostPage`, que valida uma origem HTTPS separada, impede mesma origem, hospeda iframe sandboxed quando suportado e oferece fallback/nova aba; não lê catálogo de banco. `catalog.validate` e `catalog.sync` são comandos locais sobre `coelo-ui.index.jsonl`, barrels, registry, fontes e fingerprints. `catalog.publish` é deploy/hosting estático explicitamente fora da entrega atual; nenhum botão do Superadmin o executa.
- Proposta nominal de aplicabilidade: reclassificar backend Supabase como N/A para os quatro IDs. `catalog.list` é FE/host + eventual proteção de origem/edge; `validate` e `sync` são tooling/CI local; `publish` é infraestrutura de deploy. Nenhum deles justifica CRUD, tenant, RPC, RLS, persistência ou auditoria de domínio. Manter FE/E2E próprios e não remover IDs antes da reconciliação do C00.
- Host Superadmin: seis testes focais `6/6 PASS`; quatro testes de rota protegida/local preview `4/4 PASS`; analyzer da feature/testes, `No issues found`. A composição preserva o bundle boundary: não importa `apps/catalog`, registry nem UI Principal; iframe usa `referrerPolicy=no-referrer`, sandbox apenas `allow-same-origin`/`allow-scripts`, sem top-navigation, e nova aba usa `noopener,noreferrer`.
- Ferramentas: índice canônico `zero diagnóstico`; fronteiras de pacote `zero diagnóstico`; contrato visual administrativo `zero diagnóstico`; suíte `test/tool` `59/59 PASS`; analyzer do app catálogo, `No issues found`.
- Sync real não está verde: `validate_catalog_sync` encontrou 14 `source-example-fingerprint-mismatch` (`advanced-color-picker`, `state-panel`, `dialog-shell`, `pagination`, `file-actions`, `interactive-card`, `expandable-status-indicator`, `flyout`, `form-action-footer`, três date pickers/fields e `forms-editor/forms-response`). O comando reescreveu o relatório por design; C03 restaurou imediatamente o artefato fora da reserva e não sincronizou fingerprints em massa.
- Suíte ampla do app separado, somente leitura: `140 PASS / 1 FAIL`. O único RED é harness de `catalog_app_test`: a página pública monta `CatalogHomePage`, mas o teste procura o heading abaixo do banner stale sem rolar o `ListView`. C03 não editou o teste/app fora da reserva; o fato não esconde os 14 diagnósticos reais.
- `catalog.validate`: funcionalmente as ferramentas são testadas, mas o resultado do snapshot permanece RED pelos 14 fingerprints. `catalog.sync`: não concluído, pois requer revisar cada source/example e o bloqueio funcional de Forms, não apenas regenerar hashes. `catalog.publish`: não executado e permanece bloqueado por contrato de hosting/acesso/CSP; nenhuma publicação implícita.
- Produção/remoto: intocados; nenhuma migration, RPC, RLS, grant, deploy ou arquivo persistente foi criado. Gate de conhecimento sem projeção: esta revisão classifica a implementação existente e propõe aplicabilidade ao C00, sem criar decisão canônica nova.

## Revisão 13 — operações de arquivo adiadas e fail-closed

- Última instrução processada: `R01-C03-I008`; protocolo e assignment vivos foram consultados no início e no encerramento. A reserva LOCAL de `apps/superadmin/lib/core/config/superadmin_auth_scope.dart` e do teste nominal novo foi consumida somente nos limites concedidos. O arquivo reservado está liberado ao C00 após o SHA abaixo.
- RED reproduzido: o teste nominal encontrou `SupabaseImportRepository` na composição configurada e uma chamada imediata de job ao montar a superfície adiada (`Actual: SupabaseImportRepository`; `repository.calls = 1`). Isso confirmava que o wiring produtivo podia alcançar RPC/Edge legado apesar da decisão pós-MVP.
- SHA apto à integração: `63828681abf8348b9d81cf15fb9e5e6b809524d7` (`fix(imports): keep production operations deferred`). A auth scope configurada agora injeta `UnavailableImportRepository`; o adapter Supabase e as rotas `/dev` foram preservados, sem remoção ou alteração. O repository indisponível anuncia conjunto executável vazio, e o diretório não consulta jobs, não abre o diálogo/wizard e não alcança picker quando recebe essa capacidade vazia.
- UI normal: “Nova importação” e Arquivos permanecem visíveis; o card e o estado dizem explicitamente “Disponível depois do MVP”/“Importações adiadas” e informam que nenhum arquivo será selecionado ou processado. O clique só mostra aviso informativo; zero callback de criação e zero chamada ao repository foram comprovados.
- GREEN funcional: novo teste e diretório/controller focais `13/13 PASS`; regressão completa de sessão/auth scope `14/14 PASS`; analyzer dos arquivos de auth/imports tocados e testes, `No issues found`; `git diff --check` limpo. O teste de rota `/dev` foi executado separadamente e mantém RED prévio por overflow de `1409 px` no wizard, sem chamada ao repository produtivo; nenhum ajuste fora do recorte foi feito.
- Baseline amplo de imports: execução conjunta de auth scope + toda a feature produziu `44 PASS / 3 testes golden FAIL`. Duas divergências históricas do wizard permanecem em 375 (`18,85%` e `22,73%`). O estado indisponível agora diverge `0,66%` do master antigo porque remove retry temporário e adota a cópia pós-MVP; o candidato 1440/dark foi inspecionado e está coerente, mas nenhum PNG foi atualizado ou aprovado por inferência.
- Proposta de delta FE/BE/E2E para `imports.list/create/upload/preview/confirm/status/download`: anexar `63828681` como correção FE de wiring e indisponibilidade; manter BE/E2E `deferred-post-mvp`. Manter FE `pending-verification` somente até decisão visual nominal do master de `0,66%`; depois pode ser marcado verificado como operação deliberadamente adiada, nunca como importação funcional.
- `profile-files.import/preview/confirm/status/export/download`, leitura/teste apenas: `person_file_actions_test.dart` + `person_directory_states_test.dart` passaram `9/9`; o componente isolado usa callbacks nulos para mostrar indisponibilidade e não executa arquivo/job. Porém a rota produtiva de Pessoas não injeta `onImport`/`onExport`, e `PersonDirectoryPage` só renderiza `PersonFileActions` quando ao menos um callback existe. Logo o requisito “botão visível” não está comprovado na composição normal; os seis IDs permanecem `pending-verification` no FE e `deferred-post-mvp` no BE/E2E. Nenhum arquivo de Pessoas, Conta ou `profile_about` foi alterado.
- Produção/remoto: zero Supabase/Cloudflare/SQL/RLS/Edge/deploy. O legado de imports permanece congelado e desconectado do MVP. Push confirmado em `origin/codex/e2-r01-c03-operacoes` exatamente em `63828681abf8348b9d81cf15fb9e5e6b809524d7`.
- Primeiro critério aberto: decisão do C00 sobre o master atualizado de imports e sobre onde deve viver o menu visível de `profile-files.*`; qualquer correção desse segundo achado requer reserva nominal da composição/feature correspondente, sem inventar fluxo real.
- Gate de conhecimento: nenhuma regra nova foi criada. O patch materializa a política já validada em `docs/knowledge/team/mvp-storage-and-deferred-file-actions.md`; fonte canônica e projeção de conhecimento permanecem sem delta.

## Revisão 14 — Assiduidade: resposta tardia de comando isolada por chamada

- Última instrução processada: `R01-C03-I008`; protocolo e assignment vivos foram consultados no início do lote. Nenhuma reserva nova, arquivo compartilhado, SQL, migration, RPC, RLS, runner ou ambiente remoto foi consumido.
- RED reproduzido: durante uma marcação de participante com transporte pendente na chamada A, a troca do mesmo widget para repository/chamada B carregava corretamente B; quando A concluía depois, `_applyCall` aceitava o snapshot obsoleto e substituía B por A. O teste novo falhou na asserção final porque `Turma Lua` desapareceu após a conclusão tardia.
- SHA apto à integração: `1e67532abb07fbb26cd119678407669ab165b472` (`fix(attendance): ignore stale command responses`). `AttendanceCallPage` agora versiona comandos, captura repository e `callId` de origem e só aplica sucesso, erro ou liberação de in-flight quando o contexto ainda é o mesmo. A invalidação ocorre na troca A→B e no dispose. O mesmo guard cobre comandos simples (`mark`, `correct`, `finish`/reopen/notice) e bulk/undo.
- GREEN focal: o cenário A→B passou `1/1`. Regressão funcional de Assiduidade sem goldens passou `62/62`, abrangendo dashboard, nova chamada, marcação, correção, conclusão, notificações, domínio, adapters, repository e ciclo de vida. `flutter analyze` nos dois arquivos tocados: `No issues found`; `dart format` e `git diff --check`: limpos.
- Evidência visual: nenhum PNG foi alterado. O gate golden anterior de Assiduidade permanece separado e não foi reclassificado por este patch funcional; esta revisão não solicita aprovação visual.
- Proposta de delta FE/BE/E2E: anexar `1e67532a` a `attendance.mark`, `attendance.correct`, `attendance.finish` e ao detalhe compartilhado como hardening FE de isolamento de contexto; não promover automaticamente os cinco IDs de Assiduidade. `attendance.dashboard/create/mark/correct/finish` continuam `pending-verification` até a decisão visual e, sobretudo, até o contrato produtivo interno bloqueado por OQ-040/spec 048 ser aprovado, implementado e provado com autorização/reload. O frontend corrigido não converte o backend people-based em contrato 039 válido.
- Push: `origin/codex/e2-r01-c03-operacoes` confirmado exatamente em `1e67532abb07fbb26cd119678407669ab165b472`. Produção e bancos compartilhados permaneceram intocados.
- Primeiro critério aberto: decisão nominal de OQ-040/contrato interno para Assiduidade; sem ela, não criar migration/RPC/RLS por analogia. Próximo trabalho independente: cruzar o próximo delta funcional não bloqueado do backlog, preservando Agenda WIP e as operações adiadas.
- Gate de conhecimento: nenhuma regra de produto mudou; o patch corrige apenas consistência de estado assíncrono conforme o comportamento já aprovado. Nenhuma fonte canônica ou projeção em `docs/knowledge/` recebeu delta.

## Revisão 15 — Atividades assíncronas e visibilidade de profile-files

- Última instrução processada: `R01-C03-I009`; protocolo e assignment vivos consultados. A reserva LOCAL temporária foi consumida somente em `apps/superadmin/lib/features/people/presentation/person_directory_page.dart` e `apps/superadmin/test/features/people/presentation/person_directory_states_test.dart`. Os dois caminhos estão formalmente liberados ao C00/C04 após os SHAs abaixo.

### Atividades — save tardio após troca A→B

- RED reproduzido: um save de rascunho iniciado na atividade A, seguido pela troca do mesmo widget para atividade B, concluía tentando notificar o `ActivityFormController` de A já descartado. Flutter lançou `A ActivityFormController was used after being disposed` a partir de `setSubmitting`.
- SHA apto: `570343a4b4ad5ea4b9aadddb361c9a21f64b17f3` (`fix(activities): ignore stale save responses`). A página agora versiona comandos de save/submit e só aplica sucesso, falha ou fim de submitting quando o controller ainda é o contexto corrente; troca de activity/repository e dispose invalidam respostas pendentes.
- GREEN: cenário novo `1/1 PASS`; arquivo completo `activity_form_page_test.dart` `17/17 PASS`; analyzer dos dois arquivos tocados `No issues found`; formatação e diff check limpos. Nenhum golden, repository, SQL ou composição compartilhada foi alterado.
- Proposta de delta: anexar `570343a4` a `activities.create/edit/publish` como hardening FE de ciclo de vida, sem promoção automática. Os gates visuais, About/mídia/local e prova E2E remota já registrados continuam intactos.

### Profile files — ações adiadas visíveis na composição normal

- RED reproduzido: com repository de Pessoas carregado e callbacks produtivos `onImport`/`onExport` nulos, a página não renderizava `coelo-admin-files-action`; assim a indisponibilidade honesta do componente nunca ficava acessível na rota normal.
- SHA apto: `da11057aa0c96518dcf06293dd81a9425e616c38` (`fix(people): keep deferred file actions visible`). O único delta de produto remove a dependência de callbacks para renderizar `PersonFileActions` no estado `success`; loading/error/unauthorized continuam sem ações. Nenhum callback, picker, parser, job, RPC, repository, persistência, router ou auth scope foi criado/tocado.
- GREEN: página com callbacks nulos mostra Arquivos e as opções Importar, Exportar CSV e Exportar XLSX; Importar e XLSX apenas exibem `Indisponível nesta etapa`, sem efeito externo. `person_directory_page_test.dart` + estados + componente passaram `24/24`; analyzer dos três arquivos pertinentes `No issues found`; formatação e diff check limpos.
- Proposta de delta FE/BE/E2E: `profile-files.import/preview/confirm/status/export/download` podem receber FE `verified` para o comportamento deliberadamente adiado — controle visível, feedback honesto e ausência de execução — mantendo BE/E2E `deferred-post-mvp`. Isso não certifica importação/exportação real.
- Push: `origin/codex/e2-r01-c03-operacoes` confirmado em `da11057aa0c96518dcf06293dd81a9425e616c38`. Produção, Supabase e Cloudflare intocados.
- Primeiro critério aberto: revisão/integracão C00 dos dois commits; os dois caminhos I009 estão liberados. Próximo lote segue independente sem reutilizar a reserva.
- Gate de conhecimento: nenhuma regra nova. A visibilidade passa a cumprir a política durável já registrada em `docs/knowledge/team/mvp-storage-and-deferred-file-actions.md`; não houve delta na fonte nem na projeção.

## Revisão 16 — configuração avaliativa isolada por atividade

- Última instrução processada: `R01-C03-I009`, já liberada na revisão 15; nenhuma reserva nova foi consumida neste lote. Escopo restrito à página/teste de configuração avaliativa dentro de Avaliações, sem SQL, repository, router, composição compartilhada ou remoto.
- RED 1: a troca in-place de `activity-a` para `activity-b` não iniciava leitura de B; a página stateful preservava a configuração de A porque não implementava `didUpdateWidget` para repository/atividade/instituição/unidade.
- RED 2: um save iniciado em A e concluído após a troca para B podia aplicar a configuração retornada de A no novo contexto, além de encadear ativação e feedback obsoletos.
- SHA apto: `6213f4c45ace41a5f7d3b1010f024be862b4cfa1` (`fix(assessments): isolate configuration context`). A página agora limpa estado local e recarrega em mudanças de escopo; versiona loads e comandos; valida repository + atividade + instituição + unidade; descarta sucesso/erro tardio; e só chama `activateConfiguration` após save se o contexto original continuar vigente.
- GREEN: os dois novos cenários passaram `2/2`; regressão completa de Avaliações, incluindo ambos, passou `29/29`; analyzer da página e do teste novo `No issues found`; formatação e `git diff --check` limpos.
- Proposta de delta: anexar `6213f4c4` a `activities.assessment` e à configuração compartilhada por `assessments.entry` como hardening FE de isolamento de contexto, sem promover BE/E2E. A máquina de fechamento já corrigida, o candidato SQL local e os gates produtivos/remotos anteriores não mudaram.
- Push: `origin/codex/e2-r01-c03-operacoes` confirmado em `6213f4c45ace41a5f7d3b1010f024be862b4cfa1`; produção intocada.
- Primeiro critério aberto: integração C00 e decisões backend já registradas; próximo delta independente seguirá em outro fluxo do backlog.
- Gate de conhecimento: correção técnica de ciclo de vida, sem regra de produto nova e sem delta durável em `docs/knowledge/`.

## Revisão 17 — wizard de Rotina diária isolado por contexto

- Última instrução processada: `R01-C03-I009`, cuja reserva de Pessoas já estava liberada; protocolo e assignment vivos na C00 foram consultados no início e no encerramento. Nenhuma reserva nova, arquivo compartilhado, router, composition root, SQL, migration, RPC, RLS ou remoto foi consumido.
- RED reproduzido: iniciar a leitura de `application-a`, trocar o mesmo `DailyRoutineWizardPage` para `application-b`, concluir B e depois A fazia a resposta tardia de A substituir o horário visível de B (`Expected: 10:00`, `Actual: 08:00`). O wizard não versionava loads nem reagia com isolamento completo a mudanças de repository/ID/kind/origens de duplicação/aplicação.
- SHA apto à revisão: `5bc2973955f1f34b7b917cedc8b985ba9dff2298` (`fix(daily-routine): isolate wizard context`). O widget captura o contexto de cada leitura, invalida cargas e comandos em troca/dispose e descarta sucesso, erro e finalização tardios de salvar modelo, salvar aplicação, mudar herança e reverter customização. O início de uma nova carga também libera o estado `_saving` do contexto anterior.
- Correção funcional adjacente comprovada: `_saveApplicationMode` antes construía o draft antes de aplicar o novo modo e, portanto, persistia o valor anterior. Agora o draft é criado depois da seleção. O teste novo confirma que mudar de `customized` para `inherited` envia `RoutineInheritanceMode.inherited` ao repository.
- GREEN focal: `daily_routine_application_editor_test.dart` passou `5/5`, incluindo os dois novos cenários. Regressão funcional explícita dos oito arquivos não-golden de Rotina diária passou `49/49`; analyzer dos dois arquivos tocados: `No issues found`; formatação e `git diff --check`: limpos.
- Evidência visual preservada: nenhum PNG foi alterado. A execução ampla voltou a encontrar o passivo já documentado de seis testes golden falhos, agregando nove masters divergentes, e quatro skips explícitos; este patch não altera composição visual estável nem solicita regravação/aprovação.
- Limite produtivo: a composição normal continua injetando `UnavailableRoutineRepository`, e a spec 021 continua `implemented-local-prototype`. O lote robustece criar/editar/aplicar/publicar no protótipo existente, mas não cria persistência, autorização interna 039 nem caminho produtivo; não deve ser interpretado como conclusão E2E.
- Proposta de delta FE/BE/E2E: anexar `5bc29739` a `daily-routine.create`, `daily-routine.edit`, `daily-routine.apply` e `daily-routine.publish` como hardening FE de ciclo de vida; manter os cinco IDs, incluindo `daily-routine.list`, `pending-verification`. BE/E2E permanecem abertos até spec produtiva, capability/RLS/RPC, composição normal, persistência/reload e negativas cross-tenant; os visuais permanecem sob decisão separada.
- Push: `origin/codex/e2-r01-c03-operacoes` confirmado exatamente em `5bc2973955f1f34b7b917cedc8b985ba9dff2298`. Produção Supabase/Cloudflare intocada.
- Primeiro critério aberto: contrato produtivo interno de Rotina diária e decisão visual nominal; sem ambos, não promover o protótipo local. Próximo lote independente deve atacar outro delta funcional do backlog sem reutilizar reservas encerradas.
- Gate de conhecimento: nenhuma regra durável foi criada; trata-se de consistência assíncrona e correção da intenção já selecionada. Specs/fontes e `docs/knowledge/` permanecem sem delta.

## Revisão 18 — arquivar/restaurar Plano não atravessa contexto

- Última instrução processada: `R01-C03-I009`, com todas as reservas anteriores liberadas; protocolo/assignment vivos e a spec 051 foram consultados no início e no encerramento. Escopo estrito ao diretório e teste focal de Planos, sem router, composition root, SQL, migration, RLS, RPC ou remoto.
- Contrato preservado: a spec 051 aprova catálogo manual com listar/criar/editar/arquivar/restaurar, motivo, idempotência e concorrência; vínculos institucionais são somente leitura e cobrança/assinatura automática ficam fora. Assim, este lote trata `plans.activate` somente como o crosswalk ainda pendente para arquivar/restaurar. Nenhuma escrita foi criada para `plans.assign`.
- RED reproduzido: o diálogo capturava o plano A, mas consultava `widget.repository` apenas depois da confirmação. Ao trocar a mesma página para o repositório B com o diálogo aberto, o comando de A era enviado a B (`second.commands` recebeu um `PlanSaveCommand`). O mesmo cenário revelou descarte prematuro do `TextEditingController` do motivo durante a animação de saída.
- SHA apto à revisão: `dfe953bc502d1d5afad8914663416b4d1d1bbff5` (`fix(plans): bind status changes to repository`). A ação agora captura repository/versão antes de abrir o diálogo, invalida confirmação e resposta após troca/dispose e recarrega somente o contexto original ainda vigente. Em troca detectada, nenhum repository recebe comando e a UI pede para reabrir a ação.
- O motivo passou para um widget de diálogo stateful que possui e descarta seu controller no ciclo de vida correto; a anatomia, textos, validação obrigatória e estilos de arquivar/restaurar foram preservados.
- GREEN: cenário cross-context `1/1 PASS`; regressão funcional não-golden de Planos (`fake repository`, adapter Supabase, contexto do formulário e páginas) `35/35 PASS`; analyzer dos dois arquivos tocados `No issues found`; formatação e `git diff --check` limpos. Nenhum master visual foi alterado.
- Proposta de delta FE/BE/E2E: anexar `dfe953bc` a `plans.activate` como hardening FE de arquivar/restaurar e registrar explicitamente que o nome do action_id ainda exige crosswalk nominal, não ativação comercial. Manter `plans.list/create/edit/activate` `pending-verification` pelos gates visuais/runtime/realm 039 versus principal People da 051. Manter `plans.assign` bloqueado sem escrita até decisão específica; não inferir autorização de seu simples registro no rastreador.
- Push: `origin/codex/e2-r01-c03-operacoes` confirmado exatamente em `dfe953bc502d1d5afad8914663416b4d1d1bbff5`; produção intocada.
- Primeiro critério aberto: C00/Owner conciliar formalmente identidade principal 039 versus spec 051 e o nome/aceite de `plans.activate`; decidir separadamente `plans.assign`. O lote pode ser integrado sem resolver essas decisões porque apenas impede escrita no contexto errado.
- Gate de conhecimento: nenhuma regra nova foi introduzida. A correção materializa a spec 051 vigente e mantém as divergências no handoff/open questions; não houve delta canônico nem em `docs/knowledge/`.

## Revisão 19 — detalhe de Atividade isolado por ID

- Última instrução processada: `R01-C03-I009`; protocolo e assignment vivos consultados no início/encerramento, sem reserva nova. Escopo somente `ActivityDetailPage` e seu teste, sem escrita, router, composition root, SQL ou remoto.
- RED reproduzido: a página não implementava `didUpdateWidget`; após iniciar leitura de `activity-1` e trocar o mesmo widget para `activity-2`, nenhuma leitura de B era iniciada e a tela não mostrava Dança. A resposta tardia de A também não possuía identidade de carga para ser rejeitada.
- SHA apto: `ba4b325bafe0d476d1dcbf8ce1260157faa2f2b5` (`fix(activities): isolate detail reads`). A página agora reage a mudança de repository/`activityId`, limpa o detalhe anterior em modo loading e valida geração + repository + ID em sucesso, unauthorized e failure; dispose invalida a carga pendente.
- GREEN: cenário A→B e arquivo completo `activity_detail_page_test.dart` `3/3 PASS`; analyzer dos dois arquivos tocados `No issues found`; formatação e `git diff --check` limpos. Nenhum componente visual, golden, adapter ou contrato de save foi alterado.
- Proposta de delta FE/BE/E2E: anexar `ba4b325b` a `activities.detail` como hardening FE de leitura e isolamento de contexto. Manter a ação `pending-verification`: visuais, composição produtiva, reader/backend real, negativas tenant A/B/ID adulterado e reload continuam gates separados; o patch não os promove.
- Push: `origin/codex/e2-r01-c03-operacoes` confirmado exatamente em `ba4b325bafe0d476d1dcbf8ce1260157faa2f2b5`; produção intocada.
- Primeiro critério aberto: integração C00 e os gates já registrados para o detalhe produtivo. Próximo lote independente segue em outro leitor/comando sem reserva; Agenda WIP continua isolada.
- Gate de conhecimento: correção técnica de ciclo de vida sem mudança de regra de produto; nenhuma fonte/projeção recebeu delta.

## Revisão 20 — criação de chamada isolada por contexto

- Última instrução processada: `R01-C03-I010`; protocolo e assignment vivos consultados no início/encerramento. O Owner reconfirmou continuidade até o fechamento de `2026-09-09 06:00-03:00`; o heartbeat local `c03-opera-o-at-06h` foi efetivamente criado com cadência de 30 minutos até esse horário, notificação somente em falha e instrução para não duplicar lote ativo.
- Concorrência efetivamente iniciada por pedido nominal da I010: `/root/assessments_saving_corrigenda` (regressão `_saving` apontada por C00), `/root/attendance_detail_guard` (read/command fora do novo fluxo) e `/root/meal_plans_context_guard` (concorrência/contexto sem mídia nova). O agente principal permaneceu único escritor de commits/push/handoff; subagentes não receberam trackers, protocolo, router, DI ou SQL runtime.
- RED reproduzido: `AttendanceNewCallPage` iniciava `createCall` no repository A, aceitava uma troca do mesmo widget para repository B e, quando A concluía, chamava o callback atual de B. O teste nominal falhou com `Expected: <0>, Actual: <1>`, comprovando navegação/efeito lateral obsoleto.
- SHA apto: `3041f487f12163109483ff02109256cbcace895a` (`fix(attendance): isolate call creation context`). A página agora invalida comandos quando repository, data-base ou IDs iniciais mudam; captura repository, callback e draft imutável; só processa sucesso/erro/finalização se geração e repository ainda forem correntes. A troca de contexto também reinicializa seleção, data, etapa e carregamento de opções.
- GREEN: novo cenário A→B `1/1 PASS`; sete arquivos não-golden de Attendance `63/63 PASS`; analyzer dos dois arquivos tocados `No issues found`; `git diff --check` limpo. Nenhum golden ou arquivo fora de Attendance foi alterado.
- Proposta de delta FE/BE/E2E: anexar `3041f487` a `attendance.create` como hardening FE de concorrência, sem promoção. `attendance.dashboard/create/mark/correct/complete` continuam `pending-verification`: composição produtiva, OQ-040/spec 048, backend interno 039, autorização tenant A/B/ID adulterado, persistência/reload e golden conhecido permanecem gates independentes.
- Push: `origin/codex/e2-r01-c03-operacoes` confirmado em `3041f487f12163109483ff02109256cbcace895a`; produção e remotos de dados intocados.
- Primeiro critério aberto: integração C00 deste guard e decisão/contrato produtivo nominal de Assiduidade. Próximo passo já em execução paralela: corrigenda de Assessments e inspeções focais de Attendance/Meal Plans, preservando Agenda WIP `b049b163` sem integração.
- Gate de conhecimento: o lote corrige ciclo de vida assíncrono sem alterar regra durável de produto; nenhuma fonte canônica ou projeção `docs/knowledge/` mudou.

## Revisão 21 — checkpoint concorrente de ciclo de vida

- Última instrução processada: `R01-C03-I010`; os três subagentes reais da revisão 20 concluíram seus recortes e o agente principal revisou, testou, separou e publicou quatro commits de código. Depois da consolidação, os mesmos slots foram efetivamente reativados em Support, Audit e Daily Routine, sempre sem trackers/handoff/router/DI/SQL e sem commit/push próprios.
- `activities.assessment`/etapa Sobre — SHA `88447ea9f67df246d53ff6df008b17329bcb5843` (`fix(activities): isolate profile reads`). RED: troca controller/repository/atividade não iniciava leitura B (`requestCount` esperado 1, obtido 0) e a conclusão de A podia gravar o perfil antigo no controller B. O widget agora recarrega no novo contexto, invalida leituras tardias, captura controller/repository/IDs e substitui controllers de texto com descarte seguro. GREEN focal `1/1`; suíte não-golden de Atividades `144/144`; analyzer limpo. Sem mídia/gateway.
- Corrigenda C00 de `assessments.*` — SHA `91748191f2762c4b43579ca5144dc8af062afcf8` (`fix(assessments): reenable saved configuration actions`). RED: após sucesso retornar nova configuração, `_configuration = saved` fazia o `finally` rejeitar sua própria finalização por identidade e deixava “Salvar rascunho” desabilitado. O resultado mantém guard integral do draft; a liberação de `_saving` usa o contexto vigente. GREEN focal `3/3`; conjunto não-golden informado e revisado `30/30`; analyzer limpo e guard A→B preservado.
- `attendance.correct` — SHA `6504610abd9489ee6df428f3474b498e0d415262` (`fix(attendance): bind correction to call context`). RED: diálogo aberto em repository A escrevia a correção no repository B após swap e descartava o `TextEditingController` antes do fim da animação. O diálogo agora possui o controller e devolve comando imutável; a página captura generation/repository/call ID, rejeita contexto trocado e nunca escreve em A ou B nesse caso. GREEN focal `1/1`, arquivo da página `41/41`, suíte não-golden Attendance após os dois lotes `64/64`; analyzer limpo.
- `meal-plans.create/edit/model-create/model-edit` — SHA `808a8d0a876ab08b139cde475a02701013d7101a` (`fix(meal-plans): unlock saves after context swap`). RED: trocar apenas `imageRepository` invalidava a resposta antiga, mas mantinha `_saving=true` e o `LinearProgressIndicator`. O wizard invalida o comando e libera o formulário com mensagem de contexto alterado, preservando operation ID para retry idempotente. GREEN focal `1/1`, não-golden Meal Plans `94/94`, analyzer limpo. Nenhum gateway, Cloudflare/R2, picker, upload ou migration foi criado.
- Proposta de delta FE/BE/E2E: anexar os quatro SHAs aos IDs acima como hardening FE e corrigenda, sem promoção automática. Permanecem os gates já declarados: Atividades Sobre/mídia/composição/backend real; Assessments realm 039/backend/reload; Attendance spec 048/OQ-040/backend/reload/golden; Meal Plans realm 039/R2/composição/reload/goldens. Nenhum pacote remoto foi executado.
- Push: `origin/codex/e2-r01-c03-operacoes` conferido exatamente em `808a8d0a876ab08b139cde475a02701013d7101a`, incluindo os quatro commits aptos. Agenda WIP `b049b163` continua explicitamente fora da integração.
- Primeiro critério aberto: integração C00 da corrigenda `91748191` antes do commit retido `6213f4c4`; depois, revisão dos três lotes paralelos reativados. ETA: próximo checkpoint nominal `17:30-03:00`, condicionado aos resultados desses subagentes, sem interromper a execução.
- Gate de conhecimento: todos os deltas corrigem isolamento/ciclo de vida e cumprem regras já aprovadas; nenhuma política durável nova foi criada e nenhuma projeção `docs/knowledge/` mudou.

## Revisão 22 — checkpoint nominal 17:30

- Última instrução processada: `R01-C03-I010`; assignment/protocolo vivos e novos achados do C00 consultados. Este checkpoint foi preparado às `2026-09-08T17:27:52-03:00` para a janela nominal de 17:30; execução continua após a publicação, com dois subagentes ativos em Planos e modelos de Atividades e o slot recém-liberado de Daily Routine pronto para novo lote.
- `activities.list` — SHA `d2ea1f36ea3b2a2eb564c9da7582754c327e25d1` (`fix(activities): reload directory after repository swap`). RED: com carga A pendente, trocar o mesmo diretório para repository B não criava novo view model e nenhum card B aparecia. A página agora descarta o leitor antigo, limpa a busca do contexto anterior, instancia o leitor B e preserva page size coerente com cards/tabela; conclusão tardia de A fica invalidada pelo dispose. GREEN focal `1/1`, arquivo de diretório `21/21`, analyzer limpo. Modelos internos continuam sob inspeção paralela separada.
- `support.*` — SHA `722dd5ed3edf60fa9ba1600ba69d55dcef91ed25` (`fix(support): bind owner selection to controller`). RED: seletor de responsável aberto em controller A alterava o ticket homônimo de B após swap. O fluxo captura controller/geração, rejeita contexto trocado e não restaura foco obsoleto. GREEN focal `1/1`, suíte não-golden `56/56`, analyzer e validador visual limpos. Nenhum status, transição, persistência ou backend foi inventado; spec 016/OQ-028, toolbar, paginação e teclado permanecem intactos.
- `audit.list/filter` — SHA `258376366f25d00d9ddba9dd428ef3827a9a3260` (`fix(audit): preserve page size across context swaps`). RED: após selecionar Cards com page size 11 e trocar controller/repository, a UI permanecia em Cards mas a nova consulta era forçada a 8. A carga inicial agora deriva 11/8 do display ativo e não altera filtros/minimização. GREEN focal `1/1`, arquivo de diretório `16/16`, analyzer limpo; exportações gerais continuam adiadas.
- Corrigenda C00 de `daily-routine.create/edit/apply` — SHA `2a01c785445f2247b8421ff37eab6bd1bbf64788` (`fix(daily-routine): reenable created entry saves`). RED nominal `2/2`: tanto Modelo quanto Aplicação recebiam nova instância/ID no sucesso e o `finally` rejeitava a própria limpeza de `_saving`, mantendo o botão bloqueado. A materialização do ID agora também libera `_saving` no mesmo `setState`; o segundo save prova uso de `created-model` e `created-application`, preservando guards A→B. GREEN arquivo `7/7`, conjunto não-golden `51/51`, analyzer limpo. O gate produtivo `UnavailableRoutineRepository` e a spec 021 local permanecem inalterados.
- Proposta de delta FE/BE/E2E: anexar `d2ea1f36` a `activities.list`, `722dd5ed` aos seis IDs `support.*`, `25837636` a `audit.list/filter` e `2a01c785` a `daily-routine.create/edit/apply` como hardening/corrigenda FE, sem promoção. Gates BE/E2E e visuais previamente registrados continuam; produção, migrations e deploys seguem intocados.
- Push: `origin/codex/e2-r01-c03-operacoes` conferido exatamente em `2a01c785445f2247b8421ff37eab6bd1bbf64788`, contendo os quatro commits aptos. Agenda WIP `b049b163` não foi misturada.
- Primeiro critério aberto: integração C00 das corrigendas retidas `91748191` e `2a01c785`; depois consolidar os resultados ativos de Planos/modelos de Atividades. Continuidade local está garantida pelo heartbeat `c03-opera-o-at-06h`; fechamento começa 09/09 05:30 e handoff final será entregue até 06:00, sem prometer execução se o host for desligado.
- Gate de conhecimento: correções técnicas sem regra durável nova; nenhuma fonte canônica ou projeção `docs/knowledge/` mudou.

## Revisão 23 — corrigenda de Attendance e novos isolamentos

- Última instrução processada: `R01-C03-I010` mais revisão C00 que reteve `6504610a` até preservar o draft de correção em falha. A corrigenda foi priorizada, testada, publicada e enviada nominalmente ao C00; os três slots foram reativados em diretório Daily Routine, dashboard Attendance e diretório Meal Plans.
- `plans.list` — SHA `52f2075feaf8769b3d99647a20f73afe5222792c` (`fix(plans): compare repository contexts by identity`). RED: dois adapters distintos com igualdade customizada eram tratados como o mesmo; B não recebia `list` e a tela mantinha Plano A. Troca e guards tardios agora usam `identical`. GREEN focal `1/1`, não-golden Plans `36/36`, analyzer/validador visual limpos. Catálogo continua manual conforme spec 051, sem billing, `plans.assign` ou SQL.
- `activities.list`/modelos — SHA `7b09750226045c66d5176fd50ddbabeb5cc1a83b` (`fix(activities): isolate template catalog loads`). RED: options A pendentes mantinham `_templatesRequested=true` após swap, impediam carga B e podiam dominar o estado filho. O subfluxo agora reinicia filtros/options e valida geração + repository em sucesso/erro/finalização. GREEN focal `1/1`, arquivo de diretório `22/22`, analyzer limpo; o guard raiz `d2ea1f36` permaneceu intacto.
- Corrigenda de `attendance.correct` — SHA `b592dc4b50537955bedf2db82fc9b65d5c13bf5e` (`fix(attendance): keep correction draft on failure`), a aplicar junto de `6504610a`. RED: a primeira RPC falhava e o diálogo já havia fechado, destruindo motivo/seleção. O diálogo stateful agora mantém o draft, bloqueia duplicata durante envio, reabilita a ação em falha e fecha somente em sucesso; retry no mesmo diálogo executa a segunda chamada e persiste a revisão. A geração de contexto separada preserva A→B sem invalidar a própria tentativa. GREEN `3/3` cenários de correction, Attendance não-golden `65/65`, analyzer pós-format e diff-check limpos.
- NO-DELTA `assessments.closing/reopen/detail`: prova exploratória de transição com nova instância passou e foi removida; controller libera saving por geração, diálogos possuem controllers próprios e os casos A→B/dismiss permanecem cobertos. `assessment_entry_page_test.dart` `14/14 PASS`, zero diff.
- NO-DELTA `imports.*`/`profile-files.*`: replay focal de quatro arquivos `14/14 PASS` comprova controles visíveis, mensagem pós-MVP/indisponível, `UnavailableImportRepository`, callbacks de Pessoas ausentes e zero backend/job/create/navigation. Nenhum picker, parser, arquivo, RPC ou persistência foi reaberto; zero diff.
- Proposta de delta FE/BE/E2E: anexar `52f2075f`, `7b097502` e o par `6504610a+b592dc4b` aos IDs respectivos como hardening FE, sem promoção. Registrar os dois NO-DELTA como evidência fresca dos gates atuais. Todos os bloqueios backend/E2E, visuais, identidade 039 e Agenda WIP permanecem separados.
- Push: `origin/codex/e2-r01-c03-operacoes` conferido exatamente em `b592dc4b50537955bedf2db82fc9b65d5c13bf5e`; produção/remotos intocados. Primeiro critério aberto: C00 revisar o par Attendance completo e integrar as corrigendas; próxima execução já está em andamento nos três lotes paralelos acima.
- Gate de conhecimento: nenhum comportamento de produto novo foi aprovado; somente isolamento, retry e confirmação de adiamentos já canônicos. Nenhuma projeção `docs/knowledge/` mudou.

## Revisão 24 — identidade de About e lifecycle de diretórios

- Última instrução processada: `R01-C03-I010` e continuidade nominal do Owner até 09/09 06:00. Os três resultados paralelos anteriores foram revisados pelo agente principal; commits e push permaneceram exclusivamente no C03. Depois da consolidação, os slots foram reativados em Support tabela/detalhe, Assessments diário/detalhe e Meal Plans formulários/modelos.
- `activities.detail/edit` — SHA `f9e681f82d8f842d0e67dfd359373c144a04132b` (`fix(activities): reject mismatched profile subjects`). RED: o repository devolvia uma página Sobre com instituição correta, mas `activity_id` adulterado, e o cliente a instalava no controller/renderizava. O carregamento agora valida tipo, instituição e, em edição, o ID exato da atividade antes de qualquer mutação; resposta divergente cai em unauthorized e não expõe conteúdo cruzado. GREEN focal `2/2`; conjunto não-golden de Atividades `147/147`; analyzer e diff-check limpos. O fluxo de criação continua aceitando o subject draft sem ID previamente materializado.
- `daily-routine.list` — SHA `36c44dc48a344101d19f1f9fe522e11b05dc072c` (`fix(daily-routine): preserve directory page size`). RED: init em Cards e swap de repository mantendo Tabela chamavam `controller.load()` com default 20, divergindo dos page sizes visuais aprovados 11/8. Ambos agora passam pelo construtor canônico `_load()`. GREEN de produção/diretório `14/14` reexecutado pelo principal; recorte ampliado do subagente `34/34`; analyzer/validador visual/diff-check limpos. Gate produtivo unavailable e importação/exportação honesta permanecem.
- `attendance.dashboard` — SHA `b70826441a09f39c2748e7fcb5244495550fe392` (`fix(attendance): stop dashboard reads after dispose`). RED: filtros/busca após `dispose` ainda iniciavam duas leituras de acesso no repository antigo, embora o guard de geração mascarasse a fase de dashboard. `_reload` e `changeSearch` agora são inertes quando descartados. GREEN controller `7/7` informado e recorte dashboard controller+página `10/10` reexecutado; analyzer/formatação/diff-check limpos.
- NO-DELTA `meal-plans.list`: testes exploratórios temporários confirmaram troca A→B entre repositories distintos mesmo com igualdade customizada, limpeza de filtros/paginação/page size, descarte da leitura tardia e invalidação de preflight de publicação. O código já usa `identical` em load/action; teste original `4/4`, zero diff e instrumentação removida.
- Proposta de delta FE/BE/E2E: anexar `f9e681f8`, `36c44dc4` e `b7082644` aos IDs acima como hardening FE, sem promoção automática; anexar o NO-DELTA de Meal Plans como evidência fresca. Realm 039, contratos produtivos, autorização backend, reload/remoto e gates visuais continuam separados. Nenhuma migration, RPC, RLS, mídia, gateway Cloudflare ou ambiente remoto foi tocado.
- Push: `origin/codex/e2-r01-c03-operacoes` conferido exatamente em `b70826441a09f39c2748e7fcb5244495550fe392`, contendo os três commits aptos. Agenda WIP `b049b163` continua explicitamente fora da integração.
- Primeiro critério aberto: integração C00 destes três SHAs e resultado dos lotes paralelos reativados. ETA operacional permanece fechamento às 05:30 e handoff final até 06:00 BRT, com continuidade via tarefa/heartbeat enquanto o host estiver ativo.
- Gate de conhecimento: os deltas reforçam identidade, paginação e descarte segundo contratos existentes; nenhuma regra durável nova foi criada e nenhuma fonte/projeção `docs/knowledge/` mudou.

## Revisão 25 — respostas de operação vinculadas ao recurso solicitado

- Ack `R01-ALL-RTK-1808`: reforço do Owner/C00 recebido e aplicado sem interromper testes. RTK `0.35.0` segue explícito em todos os comandos (`rtk proxy` quando necessário), sem depender de hook automático; handoff não encerra lote, subagentes úteis continuam e o fechamento seguro permanece 09/09 05:30–06:00.
- `activities.edit/publish` — SHA `fd791a8175e355e6ba8b0c326ae50dfed960ae37` (`fix(activities): reject mismatched save results`). RED: uma edição de `activity-expected` aceitava envelope de sucesso com `activity_id=activity-tampered`; isso podia direcionar o save subsequente de About ao recurso errado. O adapter agora exige ID não vazio e igualdade exata em edição, antes de devolver resultado. Criação continua aceitando o ID novo gerado pelo backend. GREEN do adapter após este delta `6/6`; analyzer/diff-check limpos.
- `activities.list`/modelos — SHA `e76dca9110c5381f8474bbe60ba2395032f3894a` (`fix(activities): validate model response scope`). RED: criação de modelo escopado em `institution-1` aceitava resposta `institution-tampered`. O adapter valida ID não vazio, instituição exata e `unit_id` exato antes de materializar o resultado. GREEN acumulado do adapter `8/8`; analyzer/formatação/diff-check limpos. Nenhum SQL, grant, RLS ou gateway foi alterado.
- `attendance.mark/correct/finish/detail` — SHA `dcd3492405c3500c2d18637b17fd9f7c9aba7bc6` (`fix(attendance): reject mismatched call results`). Dois REDs comprovaram que leitura de `call-progress` e comando nessa chamada aceitavam snapshot de `call-completed`, exibindo o contexto errado sem erro. A página agora valida `call.id` em load, comandos simples, bulk e undo, e também `receipt.callId` no bulk; divergência falha fechada preservando o snapshot vigente. GREEN focal `2/2`; conjunto Attendance não-golden `68/68`; analyzer/formatação/diff-check limpos.
- Proposta de delta FE/BE/E2E: anexar os três SHAs aos IDs indicados como defesa FE/adapter contra resposta adulterada, sem promoção automática. O backend segue responsável por autorização/tenant e os gates OQ-040/realm 039/remoto/visual continuam. Nenhuma ação de import/export, mídia, Cloudflare ou produção foi aberta.
- Push: `origin/codex/e2-r01-c03-operacoes` confirmado em `dcd3492405c3500c2d18637b17fd9f7c9aba7bc6`. Os três subagentes continuam ativos e seus diffs em Assessments, Meal Plans e Support permanecem WIP não commitado, separados do HEAD publicado.
- Primeiro critério aberto: revisar e testar os três WIPs paralelos antes de qualquer commit; depois publicar handoff incremental ao C00. Agenda `b049b163` continua WIP não integrável e não foi tocada.
- Gate de conhecimento: validações materializam invariantes de IDOR/BOLA já canônicas no AGENTS/skills; não há regra nova nem delta em `docs/knowledge/`.
