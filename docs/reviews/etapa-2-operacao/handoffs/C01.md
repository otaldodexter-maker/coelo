---
title: "Handoff E2 R01 C01 — Identidade e acesso"
source: "Owner R01; PROTOCOLO.md C00 revisão 1; assignment C01 R01-C01-I001; ADR 0019; spec 039; três rastreadores oficiais"
status: "in-progress"
generated_at: "2026-09-08T13:20:16-03:00"
timezone: "America/Sao_Paulo"
---

# R01 C01 — revisão 10

- Conversa: E2 R01 C01 — Identidade e acesso; ID `01a08197-7b62-73c1-9673-5fd40fa40452`.
- Última instrução processada: `R01-C01-I002`, confirmada; protocolo revisão 1. Modelo da sessão não exposto por ferramenta verificável; não afirmo a seleção planejada.
- Worktree confirmada: `C:/Users/adrie/Documents/Coelo.worktrees/e2-r01-c01`.
- Branch confirmada: `codex/e2-r01-c01-identidade`; baseline operacional `479d1bd1771b13e0cd96084d244fc2cdc6b0e556`, herda código `6cb8ba15bae15f5a6129b0db6e740a66f8f82b3f`.
- Árvore inicial limpa. Origin aprovado: `https://github.com/otaldodexter-maker/coelo.git`. Código do lote Auth: `1fd7f9ecd8805379ca4d24b09000f4abcff041af`; push executado e `ls-remote` confirmou esse SHA em `refs/heads/codex/e2-r01-c01-identidade` às 12:30 BRT. Handoff em commit posterior separado.

## Contrato de abertura

Objetivo: concluir as 44 ações atribuídas no Superadmin, separando 41 ativas e três gates MFA. Família visual administrativa: Login para Auth, Instituições para diretórios e cards, Perfil/Configurações para Conta e contrato próprio aprovado para erros. Inclui cliente, domínio, adapters e candidatos backend/testes do recorte; exclui Admin, Principal, Site, deploy/migrations remotas, edição de rastreadores/contratos reservados e reativação MFA.

Action IDs: `auth.login`, `auth.recover`, `auth.reset`, `auth.logout`, `auth.mfa`; `shell.load`, `shell.navigate`, `shell.switch-context`, `shell.unauthorized`, `shell.reload`; `account.profile`, `account.settings`, `account.theme`, `account.mfa`, `account.sessions`, `account.logout`; `internal-users.list`, `internal-users.create`, `internal-users.edit`, `internal-users.suspend`, `internal-users.mfa`; `access-profiles.list`, `access-profiles.create`, `access-profiles.detail`, `access-profiles.edit`, `access-profiles.assign`, `access-profiles.delete`; `access-models.list`, `access-models.filter`, `access-models.create`, `access-models.detail`, `access-models.edit`, `access-models.duplicate`; `invites.list`, `invites.create`, `invites.detail`, `invites.resend`, `invites.revoke`; `errors.403`, `errors.404`, `errors.409`, `errors.500`, `errors.503`, `errors.retry`.

Ordem: Auth/sessão e negativas → READ Modelos/Usuários → ACL Perfis → writes/Convites → Conta local → shell/erros. Primeiro lote: revalidar regressões Auth existentes e investigar resultado de reset concorrente com mudança de sessão, explicitamente ainda aberto na evidência R07. Preparar pacote nominal de personas para C00. Reutilizar implementação; não inferir falta por ausência de certificado.

Reserva encerrada: `R01-SHARED-01` para auth scope/shell/`packages/coelo_auth`, devolvida integralmente à C00 conforme ack12:34:18. Auth integrado por C00 em `2dd5a9bc`; nenhum arquivo compartilhado editado depois do release. Router, main, barrels, UI central e migrations/runner continuam C00. Parada por lote: teste focal/regressão proporcionais, diff nominal, commit de código e handoff separado; bloqueio remoto retém apenas prova remota.

## Delta conhecido e evidências consultadas

Cabeçalhos e 44 IDs cruzados nos três rastreadores vigentes, ADR 0019 e spec 039 lidas. Skills Coelo e Supabase oficiais localizadas; consulta Knowledge executada com `-Root` explícito após wrapper falhar no default PSScriptRoot; índice Coelo UI consultado para Login.

| IDs | Implementado/evidência anterior | Delta real ainda aberto | FE / BE / E2E nesta revisão |
|---|---|---|---|
| auth.login/recover/reset/logout | R06 cleanup; R07 SDK 9/9 e consolidado 74/74; login remontado single-flight e consolidado 154/154 | Reproduzir checks; lifecycle concorrente de senha; SMTP/token/redirect e revogação reais | pending-verification / pending-verification / pending-verification |
| shell.* | Sessão, guards e cache por revisão existentes | Navegação/reload, negativas, contexto A/B e ausência residual | pending-verification / N/A / N/A |
| internal-users.list | Users49 48/48 pgTAP; repository/detail READ composto | HTTP real/UI e visuais restantes | pending-verification / pending-verification / pending-verification |
| access-models.* | READ 38/38; envelopes/consumer/época de autorização; writes locais | Revalidar contrato por operação; HTTP, autorização e persistência | pending-verification / pending-verification / pending-verification |
| access-profiles.* | Consumers/lifecycle/continuidade existentes; probe ACL42501 | Permissão wrapper→helper; definição global sem uso e comandos próprios | pending-verification / pending-verification / pending-verification |
| invites.* | Implementação/evidências legadas a conferir no lote | Validade, entrega, auditoria, negativas e card Novo convite | pending-verification / pending-verification / pending-verification |
| account.settings/theme | Persistência browser/SharedPreferences comprovada anteriormente | Visual/acessibilidade/lifecycle; sem endpoints novos | pending-verification / N/A / N/A |
| account.profile/sessions/logout; internal-users.create/edit/suspend; errors.* | Contratos/implementações existentes a rastrear por lote | Conferir delta focal e prova por operação; conta self depende contrato específico | pending-verification / pending-verification / pending-verification |
| auth.mfa/account.mfa/internal-users.mfa | ADR0019 AAL1 vigente | Gate formal preservado; não bloquear demais ações | pending-verification / gate-formal-mvp / gate-formal-mvp |

Nenhuma promoção de estado proposta nesta revisão. Ressalva para deltas centrais: textos legados de convites mencionam AAL2 e conta.logout/erros usam aceite genérico de listagem; avaliar critério nominal coerente com ADR0019, sem alterar aplicabilidade silenciosamente.

## Lote Auth — atualização 12:28 BRT

`auth.login`, `auth.reset`: RED SDK reproduzido no teste `pending recovery password update excludes a new gateway login`: esperado login negado, obtido sucesso. O SDK gotrue 2.26.0 atualiza o usuário da sessão corrente após a resposta de updateUser; o adapter encerrava a sessão corrente em seguida. Chamadas pelo gateway podiam intercalar login e reset.

Correção mínima candidata em `packages/coelo_auth/lib/src/supabase_coelo_auth_gateway.dart`: exclusão compartilhada entre login e updatePassword, obtida antes do primeiro await e liberada em finally, cobrindo persistência, credenciais e cleanup do reset. Guarda de recovery e single-flight do bootstrap original preservados. Sem mudança de interface, router, scope, shell, dependências ou regra AAL.

Prova focal SDK: **10/10 PASS**, HTTP e credenciais sintéticos, nenhum backend remoto. Teste novo exige login concorrente recusado antes de `/token`, reset concluído e login posterior permitido. Pacote coelo_auth: **25/25 PASS**, incluindo duplicação de reset e liberação após falha. Regressão ampla Auth/scope/session/login-remount: **155/155 PASS**, inclui 375/768/1024/1440, light/dark e 200%; sem goldens. Processos `14153` e `19180` concluíram exit0. Analyzer dos três arquivos alterados: **No issues found**, processo `26928` exit0. `git diff --check` e staged nominal sem achados; nenhum manifest/lockfile alterado. Knowledge: 54 artigos válidos; suíte 12 PASS/1 SKIP por symlink não permitido no host; no-op de conhecimento durável.

Comandos reproduzíveis (executar dentro da worktree C01): `rtk proxy flutter test --no-pub --reporter expanded` em `packages/coelo_auth`; em `apps/superadmin`, `rtk proxy flutter test --no-pub test/features/auth/domain test/features/auth/data test/features/auth/presentation/view_models test/features/auth/presentation/widgets test/features/auth/presentation/screens/superadmin_login_screen_test.dart test/features/auth/presentation/screens/superadmin_forgot_password_screen_test.dart test/features/auth/presentation/screens/superadmin_reset_password_screen_test.dart test/features/auth/presentation/screens/superadmin_auth_responsive_matrix_test.dart test/core/config/superadmin_auth_scope_test.dart test/core/guards/superadmin_session_test.dart test/app/router/login_remount_sdk_test.dart --reporter expanded`.

Release proposto de `R01-SHARED-01`: código coelo_auth entregue no SHA acima; auth_scope e shell sem alterações. C00 confirma transferência antes de outro escritor. Próximo lote independente lê READ Usuários/Modelos e corrige card Novo convite conforme pedido explícito do Owner. Nenhuma reserva nova consumida. ETA de integração depende C00; lote Auth efetivamente levou aproximadamente 8 min da inspeção focal ao push, incluindo compilação inicial. Remoto e substituição externa permanecem sem ETA; dependências explícitas, sem certificar 44 ações pelo teste.

Deltas propostos: `auth.login` e `auth.reset` ganham evidência local de exclusão mútua e regressão R01; FE segue pending-verification até aceites visuais/rota completos, BE/E2E seguem pending-verification. Nenhuma outra promoção.

Limite explícito: callback externo que substitui a sessão diretamente no SDK durante updateUser não passa pelo lock do gateway; a implementação do SDK pode anexar usuário antigo à sessão nova. Esse caminho ainda exige reprodução/solução própria; esta correção não o certifica nem declara Auth concluído. Não remover guards para preservar um aparente sucesso.

Documentação primária consultada: Supabase changelog em 08/09 (sem mudança relevante identificada ao lock Dart); `https://supabase.com/docs/reference/dart/auth-updateuser`; `https://supabase.com/docs/reference/dart/auth-signout`; implementação instalada gotrue 2.26.0 linhas 802–828 e 976–1008. Changelog via web recusou content-type; fallback HTTPS somente leitura funcionou.

## Pacote nominal proposto a C00 — C01-AUTH-PERSONAS-v1

Estado: **proposta, sem autorização ou execução remota**. Objetivo inicial é login normal → bootstrap → leitura mínima → reload → logout/revogação, abrindo sessão real às demais frentes. C00 confere catálogo/ledger remoto e concede janela com IDs opacos reais; não criar pessoas/vínculos globais para identidades internas.

| Persona sintética | Provisionamento/alcance mínimo | Cenário |
|---|---|---|
| C01-R01-OPS-A | Conta Auth exclusiva confirmada; identidade/auth_link/membership internos ativos; papel aprovado com platform.read e escopo instituição A | Login AAL1, bootstrap, leitura/reload em A, tentativa B negada |
| C01-R01-OPS-B | Identidade e credencial distintas; mesmo papel aprovado, escopo B | Isolamento recíproco e troca de conta sem dado de A |
| C01-R01-NO-CAP | Conta interna ativa com papel aprovado sem a capacidade do domínio sob teste | Negação por capability; sem inventar overrides |
| C01-R01-REVOKE | Credencial interna separada, vínculo inicialmente ativo em A, apenas sintético e descartável | C00 suspende/revoga sessão/vínculo nominal após primeira leitura; reload/ID/link direto negados |
| C01-R01-GLOBAL | Conta sintética do realm global sem auth_link interno | Login de credencial não concede bootstrap Superadmin |

Não é necessário novo Owner para o primeiro lote. Se o catálogo não tiver papel que atenda, C00 registra a lacuna sem criar papel ad hoc. Conta de teste de recovery/SMTP é pacote separado dependente do contrato aprovado, caixa postal controlada e redirect localhost autorizado; não enviar email a terceiros nem usar endereços inventados como caixas reais. Apenas IDs opacos/correlações/resultados no handoff. Credenciais exclusivamente em secret store local indicado por C00, nunca no Git/conversa. Cleanup C00 revoga sessões sintéticas e preserva audit conforme retenção; nenhuma deleção histórica automática.

## Testes, bloqueios e continuidade

- Resultados atuais de testes locais e push estão nos lotes abaixo/acima; nenhuma prova runtime remota nesta revisão.
- Bloqueio inicial de worktree ausente resolvido pelo C00 às 12:22. Nenhuma segunda worktree criada.
- Remoto: sem cenário/personas/janela de mutação autorizados nesta revisão. C00 é responsável por pacote nominal e infraestrutura. Não copiei credenciais.
- Primeiro critério aberto: cinco goldens de Convites falham igualmente antes/depois; correção do card entregue, sem rebaseline. Auth externo/HTTP real permanece separado.
- ETA atual: lote card concluído localmente em aproximadamente15min, incluindo comparação dos goldens e captura; próximo delta Erros409/retry e Conta local exige inspeção focal, estimativa15–30min. Integração, contratos e espera remota dependem C00, sem ETA assumida; restante dos44 IDs ainda não estimável.
- Continuidade nativa: sessão ativa e mensagens de tarefa C00 disponíveis; consultar assignment no início/fim dos lotes. Nenhum heartbeat adicional criado, conforme protocolo. Checkpoints 12:50/14:50/17:30 em 08/09; fechamento 09/09 05:30 e final até 06:00.
- Knowledge: nenhuma regra durável nova nesta revisão; captura no-op. Propostas canônicas serão encaminhadas ao C00 neste handoff.

## Lote READ/Convites — revisão 4

Protocolo e assignment vivos relidos ao limite do lote, continuam I001. Handoff anterior publicado em `10f893a724c8f525aeaab94631091b532572a371` (ls-remote confirmado). A reserva compartilhada foi devolvida por proposta ao C00; nenhuma edição nova em auth/shell/scope.

READ local: **107/107 PASS**, processo73809 exit0, sem alterações de código. Comando em apps/superadmin: `rtk proxy flutter test --no-pub test/features/platform_users/data/supabase_platform_user_repository_test.dart test/features/platform_users/presentation/platform_user_directory_page_test.dart test/features/platform_users/presentation/platform_user_detail_page_test.dart test/features/access_profiles/data/access_profile_models_read_envelope_test.dart test/features/access_profiles/data/access_profile_model_context_test.dart test/features/access_profiles/data/supabase_access_profile_model_repository_test.dart --reporter expanded`. Acrescenta evidência R01 às ações internal-users.list, access-models.list/filter/detail; não promove FE/BE/E2E. As quatro falhas visuais históricas de Usuários continuam abertas conforme `2026-09-08-users-expanded-regression.md`; nenhum master atualizado.

Convites, action_ids `invites.list`, `invites.create`: baseline Instituições, CoeloAdminCreateAction.tile/CoeloAdminInteractiveCard e contratos/index consultados; golden Instituições1440light e Convites1440dark inspecionados. RED de altura: Novo convite216px versus item242px; texto200%:216 versus446. A primeira tentativa IntrinsicHeight falhou porque status canônico usa LayoutBuilder; descartada. Candidata atual mantém larguras/gaps/superfícies e faz duas passagens reais por linha em RenderFlex privado da feature, sem medição intrínseca, altura fixa ou novo componente público. Dois focais de altura passaram; variante atual também verifica expansão do status.

Arquivos WIP: `apps/superadmin/lib/features/invites/presentation/invite_directory_widgets.dart`, `apps/superadmin/test/features/invites/invite_directory_page_test.dart`. Analyzer focal sem issues. Execução ampliada `flutter test --no-pub test/features/invites`: **63 PASS/5 FAIL**, todos os cinco testes golden (diretório, flyout/confirmação, table-hover, form, detalhe). Master preservado. Log diagnóstico ignorado `.dart_tool/c01-invites-test.log`; investigar comparação com HEAD anterior antes de afirmar regressão ou baseline antiga. O shell de coleta imprimiu tail com exit0, mas resultado do Flutter foi FAIL e é mantido explicitamente aqui.

Gate visual central ainda não concluído: primeira invocação teve cwd incorreto e a segunda revelou dois argumentos obrigatórios (repo-root, allowlist); executar com paths reais. ETA lote visual: 10–25min para origem dos goldens/captura/validação, mais revisão/integração C00. Sem ETA de prova remota.

Perfis: não foi reservado nem escrito SQL. C00 recebeu dependência específica sobre definições globais sem uso pelo ator institucional e necessidade de reserva nominal; preservar platform.read, membership_count e proibição de grant ao helper. Conta self depende da pergunta canônica específica; não usar diretório como reader self sem contrato.

## Lote Convites entregue — revisão 5 / checkpoint 12:50

Action IDs `invites.list`, `invites.create`. Código **7779bbf20373f40a0e59d708b0ff126e2a2510f2**, push origin/codex/e2-r01-c01-identidade concluído e ls-remote confirmou esse SHA às12:47. Commit de handoff separado em seguida. Apenas dois arquivos próprios alterados, nenhum contrato compartilhado/backend/golden/allowlist.

Implementação final simplificada: Table nativa com colunas fixas e TableCellVerticalAlignment.intrinsicHeight (RenderTable faz layout real, compatível com LayoutBuilder); removeu-se o RenderFlex experimental antes do commit. Altura acompanha o maior conteúdo da linha; min216, largura, gap24, ícone e composição canônicos preservados. Sem altura fixa que corte texto200%. Quatro regressões novas cobrem altura100/200%, expansão do status, coluna móvel375 e quebra de linha1100.

Validação final: **65 PASS / 5 FAIL**, comando `rtk proxy flutter test --no-pub test/features/invites --reporter expanded`, processo10642 **exit1 preservado**. Cinco falhas exclusivamente golden já presentes no HEAD anterior: diretório, flyout/revoke, table-hover, form e detalhe. Comparação com fonte original em HEAD restaurada temporariamente por script com finally: golden baseline **0PASS/5FAIL exit1**, mesmas diferenças de pixels nos nove arquivos, inclusive28427/7660 no diretório e71580 no formulário móvel. Fonte candidata restaurada integralmente. Não houve rebaseline nem inferência de aprovação das falhas antigas.

Analyzer dos dois arquivos: **No issues found**. `git diff --check` e staged nominal: PASS. Gate `rtk proxy dart run apps/catalog/tool/validate_admin_visual_contracts.dart C:/Users/adrie/Documents/Coelo.worktrees/e2-r01-c01 C:/Users/adrie/Documents/Coelo.worktrees/e2-r01-c01/apps/catalog/assets/admin-visual-contract-allowlist.json`: **exit0**, sem alteração da allowlist. Revisão focal do diff concluída.

Captura local1440 light/dark, com fontes Nunito Sans/MaterialIcons: **2/2 PASS**; ambas inspecionadas e mostram Novo convite e item com topo/base alinhados, ícone/rótulo centralizados e gap aprovado. Evidências locais ignoradas disponíveis para C00 em `apps/superadmin/.dart_tool/c01-invites-light.png` e `c01-invites-dark.png`; logs `c01-invites-final.log` e `c01-invites-baseline-golden.log`, helper `c01_invite_capture_test.dart`. Captura inicial foi interrompida por espera de toImage no relógio de teste; helper corrigido para runAsync e execução final exit0. Captura usa fixture sintética, não prova E2E ou produção.

Deltas aos rastreadores: registrar correção visual nominal e65 testes funcionais locais, preservando FE **pending-verification** pelos aceites restantes/goldens, BE **pending-verification**, E2E **pending-verification**. Nenhuma ação promovida. Knowledge no-op: implementação aplica contrato visual existente e não cria regra durável.

Assignment relida: I001 e ack12:34:18 confirmados; Auth integrado em C00, lease compartilhada encerrada. C00 continua responsável pelas personas propostas, decisão Perfis e pacote remoto nominal. Próximo lote independente: Erros/Conta local; inspeção encontrou ausência de409 no enum (fromCode converte para404), a confirmar contra contrato antes de correção; router somente leitura. Não retomarei escrita no pacote Auth sem nova reserva.

## Erros / Conta — revisão 6

I002 lida no arquivo e recebida por mensagem: reserva nominal da tela `features/errors/presentation/screens/superadmin_error_screen.dart`, inclusive onAction, e testes próprios. Código **3a4d136f4f80e75405fb41d710be6f03133bace8**, push e ls-remote confirmados. Entrega candidata; **não está integralmente green**, detalhes abaixo. Encerrada a escrita desta reserva após código entregue; C00 confirma release. Router e qualquer wiring produtivo não editados.

Action IDs `errors.403/404/409/500/503/retry`: RED409 demonstrou esperado409/obtido404. Adicionada variante409 com mensagem sanitizada “Esta ação não pode ser concluída no estado atual.” e “Voltar ao início”. RED assíncrono demonstrou ausência de estado pendente; onAction agora aceita FutureOr<void>, mantém uma execução por vez no callback vigente, desabilita o botão durante a espera, trata falha sem detalhes técnicos e devolve foco de teclado sem roubar foco de outro controle. Troca de callback/kind invalida a continuação antiga; fechamento descarta resultado/erro tardio. Não há retry automático, novo endpoint ou alteração de autorização.

Provas: **25/25 focais PASS**, incluindo409 por teclado,100/200%,375/768/1024/1440 light/dark, callback pendente, repetição, erro, callback substituído e dispose. Analyzer dos dois arquivos e gate visual central: **PASS**. Regressores completos Erros + account_routes + composition_root_fail_closed_routes: **38PASS/3FAIL exit1** (log `.dart_tool/c01-errors-regression.log`). Oito goldens antigos Erros PASS; duas falhas são **novos masters409 ausentes**, sem gerar/rebaseline/aprovar. Terceira falha: Forms espera key forms-operations-unavailable em `composition_root_fail_closed_routes_test.dart:59`; repetida com fonte original Erros de HEAD em script finally: **0PASS/1FAIL**, exatamente a mesma key ausente. Fonte candidata restaurada; falha compartilhada enviada ao C00, nenhuma edição Forms/router. Rotas Modelos nesse teste passam antes do trecho Forms.

Capturas candidatas4091440 light/dark: **2/2 PASS**, inspecionadas; composição fullscreen/código/divisor/mensagem/uma ação preservada. Disponíveis localmente em `apps/superadmin/.dart_tool/c01-error-409-light.png` e `c01-error-409-dark.png`. Não são aprovação de golden. Script local de comparação `.dart_tool/c01_compare_error_route.py`, log `c01-error-route-baseline.log`. Primeiro teste de callback teve erro de posição de import, corrigido antes do RED comportamental; não contar falha de compilação como prova do bug.

Conta: inventário focal e suite existentes executados antes das alterações Erros: **82PASS/8FAIL**, processo48273 exit1. As oito falhas são todos os goldens Perfil/Configurações (375/768/1024/1440); nenhum arquivo Conta alterado. Preferências/reload reais já têm evidência2026-09-07-settings-browser-persistence, com limitação de harness/sessão sintética. Não reimplementei SharedPreferences. Profile e sessions continuam dependentes de contrato self e revogação nominal; não foi criado endpoint nem reader por diretório. Delta proposto para account.settings/theme: revalidação funcional local, visual ainda pendente, BE/E2E N/A pelo contrato.

Consumidores seguros para C00: callbacks atuais das chamadas produtivas de SuperadminErrorScreen são navegação/home e devem continuar assim. Não proponho substituir bloqueio de composition root por retry: não há reader naquela página para reexecutar. O primeiro consumidor real de repetição depende de reader autorizado que falhou transitoriamente, captura de identidade/contexto/ID e operação de leitura nominal. Modelos/Usuários já possuem seus próprios painéis/reloads; não deslocar isso para fullscreen nem repetir writes. Portanto `errors.retry` recebe apenas evidência do mecanismo local, **não wiring ou certificado produtivo**.

Delta canônico proposto a C00 (único escritor): acrescentar409 ao documento `docs/superpowers/specs/2026-07-28-superadmin-error-pages-design.md`, tabela correspondente do Design System e referência error-page-contracts, com texto/ação acima e regra de não repetir conflito automaticamente, conforme Owner R01/I002. Tratar as duas referências visuais como candidatas até aprovação, não masters aprovados. Knowledge: extensão autorizada encaminhada primeiro à fonte canônica por este handoff; nenhuma projeção/arquivo extra criado em C01.

Estado FE **pending-verification**, BE **pending-verification**, E2E **pending-verification** de Erros; nenhuma promoção. ETA lote Erros efetivo aproximadamente9min entre inspeção/RED/testes e entrega; aceite visual/integrador depende C00. Próximo lote independente: continuidade do formulário de Perfis, cujo State ainda não trata substituição de repository/domain/profileId e save pendente; reproduzir antes de alterar. Backend Perfis e convites internos permanecem sem reserva/aprovação nominal.

## Perfis / Modelos — revisão 7 em execução

Action IDs `access-profiles.create/edit`, `access-models.create/edit`. Protocolo/assignment relidos no limite, I002 vigente. Fonte visual/formulário existente preservada. Delta real reproduzido: repository B não era consultado após mesma State trocar contexto (esperado1/obtido0); save de A invocava onSaved de B (esperado vazio/obtido[B]); conflito de A abria modal sobre B. REDs em novo teste próprio `access_profile_form_context_test.dart`; teste de conflito inicialmente esperou pumpAndSettle com spinner e atingiu timeout, depois foi corrigido para aguardar frames limitados e comprovou modal indevido.

Candidato em `access_profile_form_page.dart`: revisão por repository/domain/profileId, descarte/reset de rascunho e permissões ao trocar, verificação em load/save/reload/saída, captura do callback de origem e guarda de save antes de qualquer efeito. Resposta de A não modifica B nem libera save pendente de B. Nenhuma alteração de assinatura/contrato backend/shared. Inicial focal+continuação+consumer19/19PASS; incluído quinto teste para lock de B. Analyzer final **No issues found** após explicitar mounted nos quatro call sites que o linter não infere pela função auxiliar. Gate visualPASS.

Suíte ampla Perfis em andamento (processo84989), log `.dart_tool/c01-profiles-final.log`,211 casos concluídos até13:05 e golden diretório ainda ativo; não registrar como PASS final antes de exit. Fonte de código ainda WIP, sem commit deste lote. Aviso de hit-test ao tocar botão explicitamente desabilitado no quinto teste será removido usando interação nominal coerente, sem reduzir asserções de lock. Nenhuma promoção FE/BE/E2E. ETA5–15min para concluir a comparação visual/regressão e publicar o lote; backend permanece dependente C00.

Limite encontrado para próximo follow-up: confirmações já abertas de saída/conflito não têm ownership completo de overlay neste formulário. Guards evitam comando/callback após troca, mas o overlay ainda pode permanecer sobre a nova tela; tratar em lote focal, preservando anatomia canônica. Não declarar lifecycle/ausência residual integralmente concluídos por este candidato.

## Formulário entregue / falha de diretório discriminada — revisão 8

Código **556bedba18e2938b5b2f035c0e0bf89c3356f10c**, push/ls-remote confirmados. Arquivos nominais: formulário próprio e novo teste de contexto. Focais finais **5/5PASS**; regressão anterior de continuação/consumers **19/19PASS**, contemplando quatro desses cinco casos; analyzer/gate visual/diff checkPASS. Fifth test toca intencionalmente a região bloqueada pelo save, com warnIfMissed:false e asserções explícitas de onPressed null, um comandoB e callback somenteB.

Execução ampla NÃO concluiu:211 testes terminaram, mas primeiro golden do diretório encontrou `LayoutBuilder does not support returning intrinsic dimensions` em `access_profile_directory_page.dart:631`, seguido de cascata de layout/semantics. Processo84989 interrompido nominalmente, **exit1**, sem declarar suítePASS. Separação sem goldens também chegou211 e permaneceu na prova de troca de repository do diretório; processo81584 interrompido nominalmente, **exit1**, resultado incompleto. A contagem211 é progresso e NÃO um certificado211/211.

Golden focal do formulário executado separadamente: três imagens divergem (167265/81969/56383px). Comparação com fonte original de HEAD, preservada/restaurada por finally, reproduz **as mesmas três diferenças**, 0PASS/1FAIL. Log `.dart_tool/c01-profile-form-baseline.log`; script `c01_compare_profile_form.py`. Nenhuma referência visual atualizada. Ausência de alteração da composição e comparação before/after sustentam apenas o delta funcional; FE/BE/E2E continuam pending-verification.

Próximo lote independente necessário: corrigir IntrinsicHeight do grid próprio de Perfis/Modelos, preservando colunas/gaps/card canônico, com RED focal sem pumpAndSettle infinito. Esse defeito do diretório já estava no HEAD e explica a cascata; não é causado pelo formulário. Depois executar a suíte de diretório e retomar a prova ampla. Ownership de overlays do formulário continua follow-up separado. ETA do grid5–15min conforme reprodução; nenhuma mudança backend/ACL/assign em curso.

## Grid Perfis / Modelos entregue — revisão 9

Action IDs `access-profiles.list`, `access-models.list/filter`. Código **8146bf682fd8a12fd4f899c6b652bbe9da123e3e**, push/ls-remote confirmados às13:13:45. Apenas `access_profile_directory_page.dart` e `access_profile_pages_test.dart`; nenhuma assinatura, shared ou SQL alterado. RED focal375 comprovou LayoutBuilder sem intrinsics em frames limitados (`.dart_tool/c01-profile-grid-red.log`). Substituído IntrinsicHeight/Row por Table nativa com largura fixa e altura natural da linha, preservando colunas, espaços vazios finais, min216 e gap24; mesmos componentes/status canônicos. Quatro casos novos100/200%,375/1440; teste375200 passou a rolar no ListView nominal para materializar o card, sem pressupor que estaria no viewport.

Validação final COMPLETA: `rtk proxy flutter test --no-pub test/features/access_profiles --reporter expanded`, processo31681 **exit1:223PASS/3FAIL**. Todas as falhas agora são os três testes golden: matriz cards/table, hover da aba e formulário. A cascata de layout não voltou e testes funcionais de troca de repository/contexto terminaram. Log `.dart_tool/c01-profiles-grid-final.log`. Analyzer dos dois arquivos **No issues found**, gate visual central **exit0**, diff/staged checkPASS. Não repetir suite sem nova mudança/achado.

Inspeção visual literal do testImage1440dark, master1440dark e testImage375light: composição de cards está legível, alinhada e sem erro de renderização. Divergências grandes incluem alterações anteriores de shell, nova aba Perfis/Modelos, toolbar Arquivos e ausência de Criar quando o teste não passa callback; não foram introduzidas pelo diff deste lote. Não foi possível obter imagem before do diretório sem falha de layout; logo **não atribuo todos os pixels diferentes ao baseline**. Os três PNGs específicos do formulário permanecem com diferenças exatamente iguais ao HEAD conforme r8. Evidências locais em `test/features/access_profiles/presentation/failures/access_profile_cards_dark_1440_testImage.png` e `access_profile_cards_light_375_testImage.png`. Nenhum master/allowlist atualizado ou aprovado.

Deltas propostos: registrar correção de layout e revalidação223 funcional, mantendo FE/BE/E2E **pending-verification** para Perfis/Modelos. Backend e UI remota não foram executados. Knowledge no-op: ambas as correções aplicam isolamento e composição já canônicos. Lote grid levou aproximadamente4min de reprodução à publicação. Próximo lote: ownership dos diálogos do formulário com descarte após troca/fechamento, estimativa10–20min a partir das duas confirmações existentes; reservas C00 e decisões externas permanecem.

## Diálogos de Perfis / Modelos — revisão 10

Action IDs `access-profiles.create/edit`, `access-models.create/edit`. Código **cf8d7c5b780f95f6512524ea6f02bee2416e4378**, push e ls-remote confirmados. Dependência local: formulário556bedba; grid8146bf68 independente. Reserva de Erros continua devolvida por proposta; nenhuma alteração fora da feature/tests próprios de Perfis.

RED nominal: após dispose do formulário, confirmação “Sair sem salvar?” continuava visível (esperado nenhuma, obtida1). Correção: rotas de saída e conflito pertencem ao formulário e são removidas por referência exata ao trocar contexto ou fechar, no próximo frame para respeitar o lock de construção do Navigator. Fechamento de rota aguarda término da transição; contexto/revisão continua conferido antes de callback/reload. Confirmar saída tem guarda única contra pedidos repetidos. Conteúdo da confirmação de Instituições foi preservado literalmente em CoeloAdminDialogShell local; helper compartilhado não expunha ownership, portanto não foi alterado. Scrim usa o token canônico CoeloOverlayColors. Nenhum pop genérico capaz de fechar diálogo alheio.

Validação: **22/22 PASS** em form_context + form_continuation + model_command_consumer. Depois da ampliação do teste de pedidos duplicados, form_continuation **6/6 PASS**. Contexto substituído remove apenas saída própria e preserva AlertDialog externo; conflito já visível é descartado; dispose não recarrega nem aciona callback; allowed/denied SQL envelopes continuam cobertos localmente. Analyzer dos três arquivos **No issues found**; gate visual central **exit0**; diff/staged checkPASS. Suíte ampla anterior223PASS/3goldenFAIL permanece delimitada a r9; não foi repetida sem necessidade. Nenhum master/allowlist alterado.

FE/BE/E2E **pending-verification**; atualizar evidências dos quatro IDs sem promovê-los. Corrigido o limite de ownership do formulário apontado em r7/r8; isso não certifica diálogos de detalhe/duplicação nem fluxos remotos. Knowledge no-op: aplica regras existentes de isolamento/lifecycle. Lote efetivo aproximadamente6min. Próximo inventário focal: comandos de Usuários Internos (update/status), isolamento e envelopes existentes, seguido de detalhe/duplicação de Perfis se houver RED. ETA de próximo delta após inspeção; C00 continua responsável por contratos de criação/convites, global definitions, self, migrations e janela sintética.
