---
title: "Assignment operacional R01 C07 — Validação visual e fluxos"
source: "Owner 2026-09-08 ~17:40; assignments/C06.md R01-C06-I002 (C00, 17:42:54-03:00); PROTOCOLO.md R01 vivo na C00; handoffs C04 r20 e C05 r15"
status: "active"
generated_at: "2026-09-08T17:52:00-03:00"
timezone: "America/Sao_Paulo"
---

# E2 R01 C07 — Validação visual e fluxos

**Escritor único deste arquivo: C06.** C07 não edita esta assignment; escreve apenas o próprio
handoff. C00 pode substituí-la ou realocá-la para o caminho canônico dela a qualquer momento — se
isso acontecer, a versão da C00 prevalece sobre esta.

## Instrução vinculante R01-C07-I001

- Nome exato da conversa: **`E2 R01 C07 — Validação visual e fluxos`**.
- Chefe operacional: **C06** (`local_eff44bb8-3d3f-402f-a4a9-970f309de5c4`).
  Integração, reservas de arquivo, leases, rastreadores, inventário e decisões técnicas continuam
  **exclusivamente com a C00**. C06 acompanha, destrava e consolida; não concede reserva nem
  autoriza execução remota.
- Worktree exclusiva: `C:/Users/adrie/Documents/Coelo.worktrees/e2-r01-c07`
- Branch: **`claude/e2-r01-c07-validacao-visual`** — é o nome publicado pela C00 na I002. Existe uma
  segunda branch `claude/e2-r01-c07-validacao` em `cea29b1f`, sem commits únicos, resíduo da
  preparação simultânea; **não use e não apague**, a C00 decide o descarte.
- Baseline explícito: **`7810e7c584fa63eb6b7c79f0399ccdae2e582518`**, ponta de
  `claude/e2-r01-c05-comunicacao` às 17:39:17−03:00. Foi escolhido porque contém as telas do
  Principal já implementadas e as divergências visuais que abrem o recorte.
- Entrega exclusiva:
  `C:/Users/adrie/Documents/Coelo.worktrees/e2-r01-c07/docs/reviews/etapa-2-operacao/handoffs/C07.md`
- ID real da sessão: preencher no r1. Nunca inventar UUID.

## Limitação do baseline que você precisa conhecer antes do primeiro lote

A base é a ponta da C05. Ela **não contém o lote da C04** — as duas frentes correm em branches
paralelas e só a C00 integra. Consequência prática, sem rodeio:

- as telas de **Comunicação e Principal** estão na sua árvore e podem ser validadas agora;
- as telas de **Estruturas e pessoas** (Instituições, Unidades, Turmas, Pessoas, Alunos, Locais)
  **não estão**. Você consegue lê-las com `git show claude/e2-r01-c04-estruturas:<caminho>`, mas não
  consegue executá-las;
- a C00 anunciou na I002 que publicará um **snapshot nominal** para os fluxos de diretórios e
  seleção da C04. Enquanto ele não sair, **não tente** cherry-pick, merge ou rebase para trazer o
  código da C04. Isso é integração, e integração é da C00.

Ordem prática: comece pelo que roda hoje (visuais de Momentos e Perfil), e trate a parte C04 como
fila que abre quando a C00 publicar o snapshot. Registre a espera no handoff em vez de simular
cobertura.

## Recorte

Testes visuais e de aceitação das telas **já implementadas**, nesta ordem:

1. **Divergências visuais da C05.** A r15 da C05 registra **61 divergências golden-only** e diz
   explicitamente: "Nao regenerei e nao vou regenerar". Seu trabalho é **diagnosticar**, não
   regenerar: para cada divergência, dizer se é regressão real de UI, mudança aprovada que o master
   não acompanhou, ou defeito do próprio harness (viewport, fonte, timing, scroll, interação que o
   teste não executa). Produza um laudo por família de tela, com o arquivo de teste, o master, a
   captura candidata e a conclusão.
2. **Fluxos disponíveis da C04**, quando o snapshot da C00 chegar: diretórios e seleção, navegação
   normal, estados vazios/erro/negativa, foco/teclado/toque, reload.

Fora do recorte: qualquer coisa que não seja teste visual ou de aceitação de tela já implementada.

## Proibições explícitas

- **Nenhuma frente nova de SQL, Docker ou mídia compartilhada.** Não criar banco local, não rodar
  migration, não pedir lease de replay, não mexer em R2/Stream/Workers, não tocar em
  `packages/coelo_database/`.
- **Não regenerar golden.** Nem um. Masters são preservados; capturas candidatas ficam separadas
  para revisão nominal da C00. Aprovação visual é da C00 e não se obtém por percentual.
- **Não transferir módulo inteiro.** Você não assume `features/` de ninguém.
- **Não criar trabalho artificial.** Se o recorte acabar e o snapshot não tiver chegado, diga isso
  no handoff em vez de inventar teste redundante.
- **Não editar** router, composition root, barrels, tokens, componentes centrais, `test/support/`,
  `test/goldens/`, migrations, funções Deno, `apps/admin`, `apps/principal`, `apps/site`.
- Sem merge, cherry-pick, rebase, force-push, push em `dev`, deploy ou limpeza de worktree.

## Arquivos exclusivamente reservados a C07

Só estes. São caminhos **novos**, sem escritor anterior, escolhidos justamente para não colidir:

- `apps/superadmin/test/acceptance/` — testes de aceitação de fluxo que você criar.
- `apps/superadmin/test/visual_review/` — harness e laudos de análise visual.
- `docs/reviews/etapa-2-operacao/handoffs/C07.md` — seu handoff.
- `docs/reviews/evidence/etapa-2/validacao-visual/` — evidências minimizadas que você produzir.

Correção de defeito pontual **só dentro desses caminhos**. Defeito encontrado em arquivo de outro
executor é **reportado**, não corrigido: descreva no handoff com caminho, linha, reprodução e
proposta, que C06 encaminha ao dono. Se a correção for barata e claramente sua, peça a reserva
nominal à C00 via C06 **antes** de editar; sem reserva, não edite.

## Caminhos ocupados agora — anti-duplicação conferida às 17:47

Confirmei, lendo os handoffs vivos, quem está executando o quê. **Nenhum subagente está fazendo
validação visual**, então seu recorte não duplica ninguém. Mas estes caminhos têm escritor ativo e
você não encosta neles:

| Fonte | Subagente / lote | Caminhos ocupados |
|---|---|---|
| C05 r15, agente A | candidato SQL de notices | `migrations/20260908190652_*` + teste correspondente |
| C05 r15, agente B | candidato SQL de momentos | `migrations/20260908190654_*` + teste correspondente |
| C05 r15, agente C | cliente de `agora.expire` | `apps/superadmin/lib/features/principal_now/` e `test/features/principal_now/` |
| C05 r15, agente D | clientes de `chat.edit`/`chat.revoke` | `apps/superadmin/lib/features/chat/` e `test/features/chat/` |
| C05, sessão principal | `momentos.remove`, feed, gateways | `features/principal_moments*`, `principal_happens*`, `principal_shared/` |
| C04, sessão principal | adapters de Locais, People | `features/locations/`, `features/people/`, `features/institutions/`, `features/units/`, `features/groups/`, `features/children/`, `features/student_tracking/` |
| C00 | 3 subagentes de leitura/revisão | nenhum caminho de teste visual |

Antes do **primeiro** teste, releia esta tabela contra os handoffs vivos e confirme no r1 que a
ausência de duplicação continua valendo. A C00 pediu essa confirmação nominalmente.

## Handoff e continuidade

Campos obrigatórios no `C07.md`: rodada; nome e ID real da conversa; revisão monotônica; última
instrução processada; timestamp com fuso; branch e baseline; SHAs de código; estado e ponta do push;
arquivos tocados; testes, resultado e ambiente; evidências minimizadas; bloqueios; primeiro critério
ainda aberto; próximo passo; ETA fundamentada e separada por dependência. Commit de código separado
do commit de handoff, para não gerar SHA circular. Frontmatter `source`/`status`/`generated_at` em
todo Markdown derivado.

Publique o r1 assim que confirmar identidade e recorte, e depois a cada mudança material. Handoff é
checkpoint, não parada: no mesmo turno, siga para o próximo lote independente. Configure **no máximo
um** mecanismo nativo de continuidade e registre ID, expressão, fuso, próximos disparos calculados
versus comprovados e limites reais — sem prometer o que a ferramenta não faz. Markdown não acorda
sessão; quem fala com você é a C06, pela mensagem de sessão.

Use subagentes onde houver subtarefa concreta com caminhos disjuntos — por exemplo, laudos de
famílias visuais diferentes em paralelo. Reservas desta assignment vinculam todos os descendentes:
um escritor por arquivo, nenhum subagente commita, faz push ou escreve handoff.

## Fechamento

Você entra no mesmo ciclo dos demais: **05:30 de 09/09** prepara a entrega, **até 06:00** faz
commits, push verificado por `git ls-remote` e handoff final, com WIP explicitamente identificado e
preservado. Depois das 06:00 não há atribuição nova. Não declare concluído o que ficou aberto, e não
declare fechamento pelo relógio — a C06 recolhe e consolida para a C00.
