---
title: "Assignment R01 C05"
source: "Owner R01; docs/reviews/coelo-etapa-2-coordenacao.md; docs/reviews/inventario-etapa-2.json; AGENTS.md"
status: "active"
generated_at: "2026-09-08T12:19:18-03:00"
timezone: "America/Sao_Paulo"
---

# E2 R01 C05 — Comunicação e Principal

instruction_revision: 1; instruction_id: R01-C05-I001; acknowledgement: aguardando executor; handoff_received_revision: 0; handoff_integrated_revision: 0.

- Ferramenta/modelo planejados: Claude — Claude Opus 5 / High (seleção a confirmar na sessão). Confirmar seleção real na sessão; não cobrar API adicional.
- Nome: `E2 R01 C05 — Comunicação e Principal`; ID real: **a registrar pelo executor**. Conversa ainda não criada pela C00.
- Worktree: `C:/Users/adrie/Documents/Coelo.worktrees/e2-r01-c05`; branch: `claude/e2-r01-c05-comunicacao`.
- Baseline de código/instruções herdadas: `6cb8ba15bae15f5a6129b0db6e740a66f8f82b3f`. O commit do protocolo R01 aparece em `git log -1 --format=%H -- docs/reviews/etapa-2-operacao/PROTOCOLO.md` da C00; ler a versão viva na C00 antes de cada lote.
- Entrega exclusiva: `C:/Users/adrie/Documents/Coelo.worktrees/e2-r01-c05/docs/reviews/etapa-2-operacao/handoffs/C05.md`.
- Preparação local autorizada. Execução pode iniciar ao abrir a sessão; janela de8dias do Owner começa08/09 12:20. Sem autorização nominal de mutação remota registrada nesta revisão.

## Ordem e primeiro lote

Chat e lifecycle existentes → Avisos N01/audiência → Acontece/Agora/Momentos com mídia → Para Você/Perfil/Circulares existentes → fluxo real e negativas por superfície.

Reutilizar MediaSession/M03, isolamento de contexto, cache/tickets e guards de rotas já integrados. N01 precisa pré-requisitos e audiência comprovados. Circulares está sob principal.profile-view no inventário atual: mapear subtelas, sem criar IDs silenciosamente. Limite de anexos por mensagem Chat segue decisão pendente.

Dependências: C02 fornece núcleo comum de mídia. Preservar famílias visuais: Chat/Avisos administrativos; Principal tem feeds/compositores/viewers próprios no Superadmin. Não criar apps/principal nem mudar componentes centrais sem reserva.

ETA inicial: desconhecida até inspecionar o delta/primeiro lote. Reportar implementação, testes, integração, documentação e espera separadas, por dependências. Primeiro critério aberto: confirmar alcance real dos contratos/testes existentes, sem assumir implementação ausente. Parada da preparação: primeiro handoff com recorte e evidência; continuar implementação independente depois, sem aguardar resposta administrativa.

## Arquivos reservados

- `apps/superadmin/lib/features/chat/` e testes focais correspondentes.
- `apps/superadmin/lib/features/notices/` e testes focais correspondentes.
- `apps/superadmin/lib/features/circulars/` e testes focais correspondentes.
- `apps/superadmin/lib/features/principal_circulars/` e testes focais correspondentes.
- `apps/superadmin/lib/features/principal_for_you/` e testes focais correspondentes.
- `apps/superadmin/lib/features/principal_happens/` e testes focais correspondentes.
- `apps/superadmin/lib/features/principal_happens_publication/` e testes focais correspondentes.
- `apps/superadmin/lib/features/principal_moments/` e testes focais correspondentes.
- `apps/superadmin/lib/features/principal_moments_publication/` e testes focais correspondentes.
- `apps/superadmin/lib/features/principal_now/` e testes focais correspondentes.
- `apps/superadmin/lib/features/principal_now_publication/` e testes focais correspondentes.
- `apps/superadmin/lib/features/principal_profile/` e testes focais correspondentes.
- `apps/superadmin/lib/features/principal_shared/` e testes focais correspondentes.
- `apps/superadmin/lib/features/profile_about/` e testes focais correspondentes.

Reservados packages/coelo_domain/lib/src/profile_about/ e testes focais; handlers server-side happens-media/moments-media/now-media/circular-media exceto r2_s3 comum C02. Convites pertencem a C01. Chat/Principal estão na mesma frente, sem gateways concorrentes. Limite por mensagem é decisão aberta; não escolher ilimitado.

Arquivos compartilhados têm precedência conforme PROTOCOLO. Não tocar router/composition root/barrels/migrations históricas/runner/componentes centrais sem reserva nominal. Arquivo sem dono explícito fica retido somente para edição, sem bloquear leitura/testes independentes.

## Reaproveitamento e recuperação

Branch anterior: `codex/e2e-comunicacao-midia-principal`. Comparar pelo diff e patch-id, pois commits cherry-picked têm SHA diferente; `git log --not` sozinho não prova código faltante. Não fazer merge de branch histórica inteira nem trazer seus rastreadores. Candidatos antigos só entram após revisar dependências e reproduzir checks; origem, novos SHAs e limites no handoff.

## Ações atribuídas (28)

| action_id | Classe operacional | Escopo do inventário | Backend |
|---|---|---|---|
| `chat.list` | ativa | mvp | aplicável |
| `chat.open` | ativa | mvp | aplicável |
| `chat.send` | ativa | mvp | aplicável |
| `chat.edit` | ativa | mvp | aplicável |
| `chat.attach` | ativa | mvp | aplicável |
| `chat.receipts` | ativa | mvp | aplicável |
| `chat.revoke` | ativa | mvp | aplicável |
| `notices.list` | ativa | mvp | aplicável |
| `notices.create` | ativa | mvp | aplicável |
| `notices.edit` | ativa | mvp | aplicável |
| `notices.schedule` | ativa | mvp | aplicável |
| `notices.publish` | ativa | mvp | aplicável |
| `notices.archive` | ativa | mvp | aplicável |
| `acontece.feed` | ativa | mvp | aplicável |
| `acontece.create` | ativa | mvp | aplicável |
| `acontece.publish` | ativa | mvp | aplicável |
| `acontece.remove` | ativa | mvp | aplicável |
| `agora.view` | ativa | mvp | aplicável |
| `agora.create` | ativa | mvp | aplicável |
| `agora.publish` | ativa | mvp | aplicável |
| `agora.expire` | ativa | mvp | aplicável |
| `momentos.view` | ativa | mvp | aplicável |
| `momentos.create` | ativa | mvp | aplicável |
| `momentos.publish` | ativa | mvp | aplicável |
| `momentos.remove` | ativa | mvp | aplicável |
| `principal.for-you` | ativa | mvp | aplicável |
| `principal.profile-view` | ativa | mvp | aplicável |
| `principal.profile-edit` | ativa | mvp | aplicável |

## Instrução vinculante I001

Leia PROTOCOLO diretamente na C00. Implemente suas ações, teste e faça commits pequenos próprios, preservando WIP separado. Push em `origin/claude/e2-r01-c05-comunicacao` permitido, conferir ponta remota. Sem merge/force-push/dev/deploy. Não editar rastreadores: proponha deltas por ID no handoff no mesmo turno da mudança. FE/BE/E2E separados; UI normal, backend real, persistência/reload e negativas para E2E; mídia completa quando exigida. Não inventar aprovação de golden, política clínica ou autorização remota.

Primeiro handoff confirma esta I001, nome/ID real/modelo, cwd/branch/baseline, ownership, reserva consumida, recorte, comandos/checks, bloqueios/ETA e mecanismo de continuidade real. Se não houver mecanismo, declarar lacuna e seguir trabalho independente durante a sessão. Não criar heartbeat de cobrança frequente; consultar assignment no início/fim dos lotes. Claude usa mecanismo nativo local espaçado se disponível, sem afirmar configuração inexistente. C00 não consegue acordar Claude por arquivo.

Horários São Paulo: preparar handoffs às12:50/14:50/17:30 em08/09; fechamento09/09 05:30, final até06:00. Conferir relógio nos limites dos lotes; preservar ponto seguro e pausar loops R01 após entrega final. Não aguardar uma mensagem para encerrar no horário conhecido.

## Acknowledgements C00

Nenhuma revisão do executor recebida. Próximo handoff: revisão1, última instrução R01-C05-I001. Não certificar recebimento ou progresso antes de evidência.
