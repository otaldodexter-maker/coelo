---
title: "R02 L00 — handoff do Coordenador Claude"
source: "prompts/L00.md; CONTRATO.md; PRINCIPAL.md; registro.json; escopo.json; assignments/L00.md (D00 rev.1)"
status: "active"
generated_at: "2026-09-09"
timezone: "America/Sao_Paulo"
---

# Handoff L00 — revisão 1

- **Rodada:** E2-R02-20260909.
- **ID real:** L00 — Coordenador Claude.
- **Revisão:** 1.
- **Última instrução processada:** `assignments/L00.md`, emissor D00, revisão 1,
  emitida 2026-09-09T12:45:04.436456-03:00, na base canônica
  `C:/Users/adrie/Documents/Coelo/docs/reviews/etapa-2-operacao/next-round/R02-20260909/assignments/L00.md`.
- **Leitura confirmada em:** 2026-09-09T12:53-03:00 (America/Sao_Paulo, UTC−03).
- **Escrita desta revisão:** 2026-09-09T12:58-03:00.
- **Modelo realmente ativo:** `claude-opus-5[1m]` (Opus 5, contexto 1M),
  esforço médio, executando no Claude Code dentro da extensão VS Code.
  O nome pedido no prompt (`claude-opus-5`) corresponde ao modelo ativo.

## ACK a D00

Recebida e aceita a assignment D00 revisão 1. **Recebido/aceito não é integrado
e não prova E2E.** Peço a D00 a confirmação de retorno prevista no contrato.

## Worktree, branch e baseline observados

| Campo | Valor observado |
| --- | --- |
| Worktree | `C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l00-coordenacao-claude` |
| Branch | `codex/e2-r02-l00-coordenacao-claude` |
| Baseline/HEAD na abertura | `6c5567a60e37698fcc4d71046b0694fa83ed20a9` |
| `git status` na abertura | limpo (sem modificação, sem untracked) |
| `dev` local | `6c5567a6` (`docs(e2): start R02 D00 and publish execution assignments`) |
| `origin/dev` | `fb2d07d34a74c91ec1f65aff361048393e590729` |

O caminho e a branch **coincidem** com o planejado no meu prompt e em
`registro.json`. Não há divergência de destino a reportar para L00.
Não criei worktree aninhada nem reaproveitei cópia da R01.

**Divergência registrada para D00 (documental, não de destino):** em
`registro.json`, `runtimeBaselineSha` continua `null` e o objeto `L00` mantém
`worktree: null`, `baselineSha: null`, `headSha: null`, `assignmentPath: null`,
`handoffPath: null`, `receivedRevision: null` e
`status: "planned-not-started"`. Os valores reais são os da tabela acima e os
caminhos absolutos publicados abaixo. `registro.json` é escrito por D00; não o
alterei. `dev` local está **um commit à frente** de `origin/dev`: o commit
`6c5567a6` de D00 ainda não aparecia publicado no remoto na hora da leitura.

## Estado observado das worktrees da rodada (12:52-03:00)

`git worktree list` registrava a raiz e:
`e2-r02-d01-autenticacao`, `e2-r02-d02-estrutura`, `e2-r02-d03-acompanhamento`,
`e2-r02-d04-acessos` (locked) e `e2-r02-l00-coordenacao-claude` — todas em `6c5567a6`.

**As worktrees de L01, L02 e L03 ainda não existiam.** Isso é um bloqueio de
partida para as três frentes Claude e é a primeira dependência que levo a D00.
Não criei esses destinos: preparar worktrees/branches é atribuição de D00 no
contrato, e criar isolamento concorrente é exatamente o que os prompts proíbem.
Existe também `C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-preserved-auth-20260909`
com `apps/`, `docs/` e `preservation-manifest.json` — consistente com a
preservação do delta local de Auth descrita em `BASELINE-LOCAL.md`. Não inspecionei
seu conteúdo além da listagem: o delta de Auth é assunto de D00 → D01.

## Entregas desta revisão

Assignments L01–L03 publicadas na MINHA worktree, revisão 1, caminhos absolutos
para o registro de D00:

- `C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l00-coordenacao-claude/docs/reviews/etapa-2-operacao/next-round/R02-20260909/assignments/L01.md`
- `C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l00-coordenacao-claude/docs/reviews/etapa-2-operacao/next-round/R02-20260909/assignments/L02.md`
- `C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l00-coordenacao-claude/docs/reviews/etapa-2-operacao/next-round/R02-20260909/assignments/L03.md`

Meu handoff: este arquivo, em
`C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l00-coordenacao-claude/docs/reviews/etapa-2-operacao/next-round/R02-20260909/handoffs/L00.md`.

Cada assignment traz recorte por `action_id`, prioridades, contratos
compartilhados, reservas, regras de prova e os cortes do dia. Nenhuma delas
declara trabalho feito: as três frentes estão `issued-awaiting-role-start`.

## Recorte das frentes Claude conforme `escopo.json`

| Frente | IDs MVP | E2E aplicáveis | Certificados E2E na fotografia |
| --- | ---: | ---: | ---: |
| L01 — Acontece/Agora/Momentos (+ Circulares sem IDs próprios) | 12 | 12 | 0 |
| L02 — Chat e Comunicações | 13 | 13 | 0 |
| L03 — Perfil e Para Você | 3 | 3 | 0 |
| Total Claude | 28 | 28 | 0 |

Os 28 IDs entram como `pending-verification` em FE, BE e integrado. Zero
certificado **não** significa zero código: significa que o registro ainda não
certifica o circuito completo. Denominador de Circulares permanece **aberto**.

## Lacunas de inventário que carrego para D00

1. **Circulares (dona L01).** Spec037 tem diretório, criação/publicação, detalhe
   e edição sem IDs próprios suficientes; `principal.profile-view` cobre apenas
   a projeção no Perfil e pertence a L03. Encaminharei a D00 a proposta de
   mapeamento de L01 **antes** de qualquer 100% do contexto. Enquanto isso, o
   denominador de L01 fica declarado como incompleto.
2. **Chat por superfície (dono L02).** Os IDs `chat.*` são compartilhados entre a
   superfície administrativa Conversas e a superfície contextual Principal.
   Exijo evidência das duas superfícies por ID, sem contar o ID duas vezes e sem
   esconder a superfície não concluída.
3. **Home.** Sem `action_id` próprio e sem dono nesta rodada; não herda conclusão
   de `shell.load`. Fica como lacuna explícita da Etapa 2, não minha entrega.

## Contratos compartilhados que fiscalizo

- **L01 → L02/L03:** catálogo/gateway de mídia privada R2 desta rodada, e
  projeções de Publicações/Circulares. L01 é dono técnico; publica cedo versão
  compatível. `escopo.json` também lista D02 e D04 como consumidores de mídia,
  com D00 como coordenador — mudança transversal passa por D00.
- **L02 → L03:** `ChatRepository` e `NoticeRepository`.
- Mudança **incompatível** nesses contratos passa por mim antes do commit;
  aditiva compatível segue direto com o delta declarado no handoff do autor.
- Reservas em `superadmin_router.dart`, `superadmin_routes.dart`, `main.dart`,
  `superadmin_auth_scope.dart`, shell/navegação, barrels e migrations: peço a
  reserva curta a D00 por arquivo e alteração exata, com release por SHA.
  Nenhuma reserva foi solicitada ou concedida até esta revisão.

## Mecanismo real de continuidade Claude e seus limites

Registrado conforme o contrato — **Markdown sozinho não acorda uma sessão**.

**Temporização (verificado que existe; primeira ocorrência pendente).**
Ferramenta nativa `CronCreate`/`CronList`/`CronDelete` desta sessão. Jobs
criados nesta rodada, todos one-shot, hora local `America/Sao_Paulo`:

| ID | Cron | Momento | Finalidade |
| --- | --- | --- | --- |
| `4f90ed66` | `54 12 9 9 *` | 12:54 | Disparo de verificação do próprio mecanismo |
| `9c8a34b9` | `53 15 9 9 *` | 15:53 | Preparar snapshot antes do corte das 16:00 |
| `d8291be4` | `3 16 9 9 *` | 16:03 | Abrir a janela de consolidação 16:00–16:45 |
| `f6b70aa1` | `38 16 9 9 *` | 16:38 | Fechar e entregar o consolidado a D00 até 16:45 |

Nenhum job depois de 16:45; nenhuma retomada noturna ou para amanhã.

**Limitações operacionais concretas, declaradas sem esconder:**
- Os jobs são **session-only e em memória**: não são gravados em disco e morrem
  se esta sessão Claude terminar. Não há persistência durável a prometer.
- Só disparam com o REPL **ocioso**; um disparo que caia no meio de uma execução
  longa é adiado, não perdido no relógio.
- Agendamento criado **não prova disparo**. A primeira ocorrência (`4f90ed66`,
  12:54) ainda não tinha acontecido quando escrevi esta revisão; confirmarei o
  horário observado na revisão 2. Até lá, o mecanismo está **configurado, não
  comprovado**.

**Mensageria entre sessões Claude (disponível, uso ainda não exercido).**
`ListAgents` + `SendMessage` alcançam sessões Claude locais desta máquina.
Esta sessão L00 se chama **`coelo-fe`** (ref `d0e41a`). Em 12:47-03:00 havia
três sessões Claude interativas pares (`coelo-61`, `coelo-e2`, `coelo-2f`,
iniciadas ~3 h antes); **nenhuma identificada como L01, L02 ou L03**, e não as
contatei para não gerar ruído em conversa alheia. As assignments instruem
L01–L03 a se anunciarem a `coelo-fe` por `SendMessage` no primeiro SHA apto,
em bloqueio real e no corte das 16:00.

**O que não existe e não inventei:** ponte de teclado, API paga, execução
contínua fora desta sessão, e qualquer canal nativo meu para D00, que roda no
Codex. A comunicação L00 ↔ D00 acontece por `assignments/` e `handoffs/` nos
caminhos absolutos registrados, mais o Owner como portador quando necessário.
Essa limitação **não** bloqueia trabalho independente das frentes.

## Estado do trabalho

- FE concluído / FE aplicável: 0 / 28 — nenhuma frente Claude iniciou execução.
- BE concluído / BE aplicável: 0 / 28.
- E2E certificado / E2E ativo aplicável da rodada (Claude): 0 / 28.
- E2E certificado / E2E ativo aplicável da Etapa 2: 0 / 187 (fotografia inicial).
- Testes únicos: P 0, F 0, B 0, S 0, U 28 (não executados).
- Taxa aprovada P/(P+F): **não calculável** (denominador zero).
- Taxa falha F/(P+F): **não calculável** (denominador zero).
- Execução (P+F)/(P+F+B+S+U): 0/28 = 0%.
- Aprovação do plano P/(P+F+B+S+U): 0/28 = 0%.
- Nenhuma suíte do aplicativo foi executada por L00 nesta revisão. As
  verificações desta revisão foram de Git, arquivos e ferramentas — **isso não
  mede progresso do produto**.
- Partes implementadas mas não certificadas: fora do meu recorte apurar por
  frente antes de L01–L03 iniciarem; a fotografia herda `pending-verification`.

## SHAs, remoto e WIP

- SHAs aptos entregues a D00 nesta revisão: **nenhum ainda**. As assignments e
  este handoff serão commitados na branch `codex/e2-r02-l00-coordenacao-claude`
  e a branch publicada; o SHA entra na revisão 2.
- WIP retido: nenhum além dos arquivos desta revisão.
- Remoto verificado: `origin/dev` em `fb2d07d3`; a branch L00 ainda não existia
  no remoto na hora desta escrita.
- Nenhuma alteração remota Supabase/Cloudflare foi preparada ou aplicada.
  Nenhuma autorização nominal foi pedida ou usada.

## Dependências solicitadas e liberadas

- **Solicitada a D00 (1 intervenção, por causa material):** criação das worktrees
  e branches de L01, L02 e L03 nos caminhos planejados, a partir da base
  conferida, e publicação de `worktree`/`baselineSha`/`assignmentPath` em
  `registro.json`. Sem isso, as três frentes Claude ficam em somente leitura.
- **Liberadas por L00:** nenhuma reserva pendente.

## Primeiro critério aberto

Destinos de L01–L03 preparados e registrados por D00, para que as três frentes
saiam de somente leitura e comecem a fechar aceites do próprio contexto.

## Próximo passo

1. Commitar assignments e handoff nesta branch e publicá-la; informar o SHA a D00.
2. Confirmar o disparo de `4f90ed66` e registrar o horário observado (revisão 2).
3. Receber o anúncio de L01–L03 quando o Owner colar seus prompts; conferir
   `registro.json` e liberar dependências sem cobrança repetida.
4. Encaminhar a D00 a proposta de reconciliação de Circulares assim que L01 a
   registrar, antes de qualquer declaração de 100%.

## Recursos e processos ativos

- Quatro jobs one-shot: `4f90ed66`, `9c8a34b9`, `d8291be4`, `f6b70aa1`
  (session-only). Serão removidos por mim no fechamento, sem afetar tarefas alheias.
- Nenhum subagente aberto: não havia subtarefa independente com escritor e
  arquivo próprios nesta revisão. Abrir filho sem trabalho independente é
  proibido pelo contrato.
- Nenhum build, container, servidor ou suíte em execução.

## Parada

Ativo até 16:45 conforme o contrato. Depois do consolidado final a D00, as
frentes Claude ficam em estado seguro aguardando decisão do Owner; D00 entrega
ao Owner até 17:15. Sem retomada automática à noite ou amanhã.

---

# Handoff L00 — revisão 2

- **Revisão:** 2. Escrita em 2026-09-09T12:56-03:00 (America/Sao_Paulo, UTC−03).
- **Última instrução processada:** disparo do próprio agendamento `4f90ed66`
  (não é nova instrução de D00; `assignments/L00.md` segue na revisão 1).
- **Modelo ativo:** `claude-opus-5[1m]`, esforço médio. Sem mudança.

## Mecanismo de continuidade: primeira ocorrência CONFIRMADA

O job one-shot `4f90ed66` (`54 12 9 9 *`) **disparou de fato**, observado às
**12:54:08-03:00**, dentro do minuto agendado e com o REPL ocioso. O disparo
reentrou nesta sessão com o prompt agendado e produziu esta revisão.

Isso encerra a pendência declarada na revisão 1: o mecanismo passa de
*configurado* para **configurado e comprovado**, com ID, horário agendado e
horário observado. Os limites continuam os mesmos e não mudaram por isso:
os jobs são session-only e em memória, morrem com a sessão e só disparam com o
REPL ocioso. Restam armados `9c8a34b9` (15:53), `d8291be4` (16:03) e
`f6b70aa1` (16:38). Nenhum depois de 16:45.

## Git de L00

- HEAD real: `c98ecd77736f2a6e6d085e1e1d93ee9470e19d77` (revisão 1 commitada).
- Branch `codex/e2-r02-l00-coordenacao-claude` publicada; push **verificado**:
  local e `origin/...` no mesmo SHA `c98ecd77`.
- WIP retido: nenhum além desta revisão.

## Bloqueio da revisão 1: LIBERADO por D00

As três worktrees Claude passaram a existir e `registro.json` mudou para
`status: "active-d00-destinations-prepared"` com
`runtimeBaselineSha: 87390aefa395b7c530d269e4e04dcb10d103e8ff`.
`dev` avançou para `87390aef` (`docs(e2): register verified R02 worktrees and
preservation`). A baseline registrada das worktrees é `56eb3f19`.

D00 leu as assignments que publiquei: os campos `assignmentPath` de L01, L02 e
L03 em `registro.json` apontam exatamente para os três arquivos da minha
worktree. **Não repito a cobrança**; a causa foi resolvida.

## Estado observado das frentes Claude (12:54-03:00)

| Frente | Worktree | HEAD observado | `registro.json` | Handoff próprio |
| --- | --- | --- | --- | --- |
| L01 | `.../e2-r02-l01-publicacoes` | `63db8ffb` | `f7b07724`, `activity-observed-identity-pending` | presente |
| L02 | `.../e2-r02-l02-chat-comunicacoes` | `449b3268` | `449b3268`, `activity-observed-identity-pending` | presente |
| L03 | `.../e2-r02-l03-perfil-para-voce` | `56eb3f19` (baseline) | `56eb3f19`, `prepared-awaiting-owner-start` | ausente |

Commits próprios já observados acima da baseline `56eb3f19`:

- **L01:** `f7b07724` abre o handoff L01; `63db8ffb`
  `docs(knowledge): point Circulars media to private R2 per ADR 0032` — é
  exatamente a correção da projeção histórica superada que a assignment L01 pedia.
- **L02:** `449b3268` abre o handoff L02; `63704da1`
  `feat(chat-be): project internal chat receipts and add edit/revoke gateways` —
  toca backend de Chat e é a primeira entrega funcional a revisar por L00.
  Esse SHA está **acima** do head anotado em `registro.json` para L02.
- **L03:** sem commit próprio e sem handoff. Ainda não iniciada pelo Owner;
  `prepared-awaiting-owner-start` é coerente. Não é atraso do executor.

L01 e L02 estão trabalhando de forma independente e **não** precisam de ACK meu
para continuar. Não intervenho nesta revisão: não há bloqueio, conflito, falha
material nem ociosidade comprovada.

## Divergências documentais para D00 (não são de destino)

1. `registro.json` anota para **L00** `baselineSha` e `headSha` iguais a
   `56eb3f19`. O head real de L00 é `c98ecd77`, já publicado. Peço a correção do
   campo no próximo ciclo de escrita de D00; não altero `registro.json`.
2. Minha worktree foi criada sobre `6c5567a6`, um commit **antes** da baseline
   registrada `56eb3f19`. Verifiquei que `6c5567a6` **é ancestral** de
   `56eb3f19`, portanto a integração do meu delta documental é linear e não
   conflita. Não rebaseei: sincronizar worktree no meio da rodada é o que o
   contrato desaconselha, e D00 é o integrador.
3. O head de L02 em `registro.json` (`449b3268`) já está desatualizado frente ao
   `63704da1` observado. Fato normal em execução; anoto para o consolidado.

## Métricas nesta revisão

Sem alteração de produto atribuível a L00. As frentes Claude começaram a
produzir, mas **eu não certifico o trabalho delas por observar commits**:

- FE concluído / FE aplicável (Claude): 0 / 28 certificados por L00.
- BE concluído / BE aplicável (Claude): 0 / 28 certificados por L00.
- E2E certificado / E2E ativo aplicável da rodada (Claude): 0 / 28.
- Testes únicos executados por L00: P 0, F 0, B 0, S 0, U 28.
- Taxa aprovada e taxa falha: **não calculáveis** (denominador zero).
- Denominador de Circulares: continua **aberto**, aguardando a proposta de
  mapeamento de L01 para eu encaminhar a D00.
- Ver commit de código não é evidência de aceite. Os números por frente virão
  dos handoffs de L01–L03 no corte das 16:00, com denominadores próprios.

## Dependências

- Solicitadas: nenhuma nova. A única aberta na revisão 1 foi atendida.
- Liberadas por L00: nenhuma reserva pendente. Nenhum pedido de reserva em
  rotas, bootstrap, shell, barrels ou migrations chegou até aqui.

## Primeiro critério aberto

Proposta de reconciliação de Circulares (spec037 → telas/subtelas/action_ids)
registrada por L01, para eu encaminhar a D00 antes de qualquer 100% do contexto.

## Próximo passo

Aguardar em silêncio produtivo até `9c8a34b9` (15:53), sem interromper L01/L02
e sem cobrar D00 de novo. Se L01 ou L02 sinalizar bloqueio, conflito de contrato
ou pedido de reserva por `SendMessage`, atendo na hora. Revisão focal do delta
de L02 em `63704da1` entra na consolidação, não em interrupção agora.

## Recursos e processos ativos

Três jobs one-shot armados: `9c8a34b9`, `d8291be4`, `f6b70aa1`. `4f90ed66`
disparou e se auto-removeu. Nenhum subagente aberto. Nenhum build, container,
servidor ou suíte em execução por L00.

---

# Handoff L00 — revisão 3

- **Revisão:** 3. Escrita em 2026-09-09T13:10-03:00 (America/Sao_Paulo, UTC−03).
- **Última instrução processada:** três mensagens por `SendMessage` — duas de L01
  (sessão `coelo-a6`: identidade com duas dependências; reserva em migrations e
  router com contratos e SHAs) e uma de L03 (sessão `coelo-09`: reserva retroativa
  em cinco arquivos compartilhados, seis SHAs aptos e dois bloqueios).
  `assignments/L00.md` de D00 permanece na revisão 1.
- **Modelo ativo:** `claude-opus-5[1m]`, esforço médio. Sem mudança.

## Ponte entre sessões Claude: EXERCIDA e comprovada

A revisão 1 registrou `ListAgents`/`SendMessage` como disponível mas **não
exercido**. Isso mudou: L01 me contatou da sessão `coelo-a6` e minha resposta foi
entregue (`msg_id 5ff034f0-71df-446f-a2e8-753fc3bdbd8c`), seguida de nova mensagem
dele. A ponte Claude↔Claude está **comprovada nos dois sentidos**. Continua sem
existir canal nativo meu para D00, que roda no Codex: aquilo segue por arquivo e
pelo Owner. L03 também se anunciou (sessão `coelo-09`); **só L02 ainda não**,
embora `ListAgents` mostre `coelo-64`, iniciada no mesmo horário das outras duas,
como candidata provável. Não presumo identidade sem confirmação.

## Dependência (1) de L01: improcedente por informação desatualizada — encerrada

L01 relatou que `assignments/L01.md` não existiria e que `assignmentPath` estaria
`null`. Conferi: as duas afirmações eram verdadeiras às 12:46, antes de eu
publicar, e estavam desatualizadas na hora do relato. O arquivo existe desde
12:49-03:00, commitado em `c98ecd77` e publicado; `registro.json` já aponta para
ele. L01 confirmou a correção e passou a seguir pela assignment. Causa resolvida,
sem repetir cobrança.

## Dependência (2): proposta de Circulares ACEITA e ENCAMINHADA a D00

Conferi no remoto, não na palavra do executor: `origin/codex/e2-r02-l01-publicacoes`,
commit `eda75646`, arquivo `.../propostas/L01-circulares-mapeamento.md`, 338 linhas.

### Revisão focal de L00

Aprovo o encaminhamento. A proposta faz o que a reconciliação exige e evita o que
a rodada proíbe: separa **observado** de **proposto** item a item com evidência
`arquivo:linha`; declara que não altera `escopo.json`, `registro.json`,
`handoffs/**`, `assignments/**` nem os rastreadores; **não certifica nada**;
não esconde Circulares em `principal.profile-view`, mantendo a aba do Perfil com
o ID existente de L03 para não contar a mesma superfície duas vezes; e avisa que
os números de linha vêm do working tree, cabendo a D00 reconferir contra o SHA
consolidado. Isso é o oposto de atribuir prova a um HEAD limpo.

Achados materiais que a proposta expõe:

1. `circulars.close` e `circulars.delete` — RPC existe, **affordance de UI ausente**;
   `delete_circular` sem equivalente no gateway `v2` de produção.
2. `circulars.respond` — `PrincipalCircularDetailPage` **não é montado em nenhuma
   rota**; RPCs de resposta ausentes do gateway `v2`.
3. `circulars.attach` — seletor de arquivos **desabilitado em produção**, com aviso
   honesto na UI.
4. `circulars.happens-card` — a rota de produção `/principal-happens` usa o
   construtor **sem** feed misto; a projeção de Circulares não chega à tela real.
5. A migration ainda cria e reforça o bucket Supabase Storage
   `coelo-circulars-private`, enquanto ADR0032 manda R2 privado. L01 já corrigiu a
   fonte spec037 em `efa93d1a`; **ADR0027 permanece histórico** e a migration
   continua apontando para Storage.

### Efeito no denominador — anterior/novo publicado, sem fingir regressão

| Cenário | Denominador L01 | Total global de `escopo.json` |
| --- | ---: | ---: |
| Hoje (fotografia) | 12 | 219 |
| Aceitando os 12 IDs da tabela principal | 24 | 231 |
| Aceitando também os 3 opcionais de estado | 27 | 234 |

Os três opcionais (`circulars.error`, `circulars.access-denied`,
`circulars.reload`) existem em `institutions`/`units` mas não em
`acontece`/`agora`/`momentos`/`notices`; a proposta corretamente **não** os soma.
Se D00 aceitar, o denominador cresce porque o inventário passa a refletir o
produto real — não porque o produto piorou. Publicar anterior/novo é obrigação do
contrato. **L00 não escreve inventário nem rastreadores.**

## CONFLITO MATERIAL 1 — colisão de timestamp entre migrations de L01 e L02

Intervenho por conflito, não por status. Conferi as branches:

- **L02** já commitou `packages/coelo_database/migrations/20260909130000_superadmin_internal_chat_receipts_edit_revoke_v2.sql`.
- **L01** declarou `packages/coelo_database/migrations/20260909130000_moments_feed_and_withdrawal_v1.sql` (ainda WIP).

**Mesmo prefixo `20260909130000` em dois arquivos diferentes.** Isso cria ordem
ambígua quando D00 serializar a aplicação. Resolução de L00, dentro do grupo Claude:

- `20260909130000` fica com **L02**, que chegou primeiro e já commitou.
- **L01 renomeia** o arquivo de Momentos para `20260909135000_moments_feed_and_withdrawal_v1.sql`.
- Reservo a faixa `20260909131000`–`20260909139000` para L01 e
  `20260909130000`–`20260909130900` para L02, evitando nova colisão hoje.

Observação adicional para D00: existem **duas árvores de migration**,
`packages/coelo_database/migrations/` e `packages/coelo_database/supabase/migrations/`,
com o mesmo `20260820182000_happens_publication_mvp.sql` nas duas. Os arquivos
novos de L01 e L02 estão só na primeira. Se as duas precisam ficar em sincronia,
isso é decisão de D00 antes da aplicação remota; não instruí ninguém a duplicar.

## CONFLITO MATERIAL 2 — dois executores no mesmo trecho de `superadmin_router.dart`

L01 pediu reserva para os builders de `/principal-happens` (~l.668) e
`/principal-moments` (~l.834), e declarou não ter tocado no arquivo.
**Mas L03 já alterou esse arquivo e já commitou.** Conferi contra a baseline `56eb3f19`:

- Linhas reais na baseline: `/principal-happens` em **669**, `/principal-for-you` em
  **827**, `/principal-moments` em **835**, `/principal-profile` logo abaixo.
- Hunks de L03 em `superadmin_router.dart`: `@@ -831 +838,26 @@` e `@@ -847 +879,41 @@`,
  além de imports e do parâmetro em `-277`. Ou seja, L03 reescreveu os builders de
  **Para Você** e **Perfil**, que ficam imediatamente **antes e depois** do builder
  de Momentos que L01 quer alterar.
- L03 também alterou `superadmin_routes.dart`, `superadmin_auth_scope.dart`,
  `main.dart` e `superadmin_app.dart`, em seis commits até `f376fc3d`.

Resolução de L00: **ordem de integração L03 → L01** nesse arquivo.
`/principal-happens` (l.669) está longe dos hunks de L03 e é seguro.
`/principal-moments` (l.835) fica encravado entre os dois hunks de L03: L01 deve
escrever esse trecho **sobre o estado publicado de L03**, não sobre `56eb3f19`, e
entregar o hunk exato. Não autorizei nenhum dos dois a reverter o trabalho do outro.

Escalo a D00, que é o dono declarado de `superadmin_router.dart`,
`superadmin_routes.dart`, `main.dart`, `superadmin_auth_scope.dart`, shell e
barrels: a reserva curta desse arquivo e a ordem de integração são dele. Registro
também que **L03 alterou os arquivos compartilhados sem reserva prévia** — não
reverto e não interrompo a frente por isso; a correção é de processo e cabe a D00
ao integrar.

## Reservas: decisão de L00 e o que fica com D00

Concedo, no que é meu (evitar colisão entre L01, L02 e L03), e escalo o resto:

| Item de L01 | Decisão de L00 |
| --- | --- |
| 1. `20260909133000_happens_post_withdrawal_v1.sql` (`8b83784e`) | Liberado no grupo. Sem colisão. Ressalva de quebra abaixo. |
| 2. `20260909131000_now_publication_expiry_transition_v1.sql` | Liberado no grupo. Assinaturas inalteradas. |
| 3. `20260909130000_moments_feed_and_withdrawal_v1.sql` | **Negado no timestamp.** Renomear para `20260909135000`. Conteúdo liberado. |
| 4. `20260909132000_circulars_media_private_r2_v1.sql` | Liberado. Afrouxar o CHECK para aceitar `r2` preservando legado é aditivo e alinhado à ADR0032. |
| 5. `20260909134000` — `create or replace list_visible_happens_feed`, assinatura inalterada, filtro `withdrawn_at is null` e campos aditivos no jsonb | Liberado. Único consumidor é `supabase_principal_mixed_feed_repository.dart:21`, do próprio L01. |
| Router `/principal-happens` (l.669) | Liberado no grupo; reserva formal com D00. Sem colisão com L03. |
| Router `/principal-moments` (l.835) | Liberado **condicionado** a escrever sobre o estado publicado de L03 e entregar o hunk exato; reserva formal com D00. |

Nada disso autoriza aplicação remota. Migrations são forward-only, em arquivo
novo, e a serialização/aplicação é de D00, com autorização nominal válida para o
pacote exato. Todo Supabase/Cloudflare remoto é produção.

### Ressalva de revisão sobre a migration 1 (quebra de contrato existente)

L01 declarou `drop + create` de `public.list_visible_happens_posts` com **tipo de
retorno alterado**. Isso não é `create or replace` aditivo. Conferi o alcance:

- Consumidor Dart único: `apps/superadmin/lib/features/principal_happens/data/supabase_principal_happens_feed_repository.dart:15`. Raio contido.
- **Mas há teste pgTAP acoplado ao contrato antigo:**
  `packages/coelo_database/supabase/tests/happens_publication_test.sql` afirma
  `has_function(...)` na l.17, o resultado da função via `pg_get_function_result`
  na l.46, e a checagem de capacidade, escopo de tenant, `publish_at`,
  `post_audiences` e `happens_media_read_tickets` nas l.35-61.
- A migration original também tem `revoke`/`grant` explícitos para a assinatura
  (l.367-368). L01 declarou reaplicar os grants; registro a declaração como dele.

Pedi a L01 que atualize o teste pgTAP no mesmo delta ou declare explicitamente a
regressão em B/U, e que confirme o resultado de `pg_get_function_result` esperado.
Sem isso, o pacote não pode ser dado como pronto para D00 serializar.

## Achado material de plataforma — ADR0032 muito além desta rodada

L01 declarou, e registro como achado a levar ao consolidado e ao Owner:
**não existe gateway de mídia único.** Há cinco Edge Functions por domínio;
apenas `moments-media` e o caminho de leitura de Forms estão em R2;
`happens-media`, `now-media` e `circular-media` ainda usam Supabase Storage; e
`MediaUploadGateway` **não tem nenhuma implementação em produção**.

Isso é uma lacuna de conformidade com a ADR0032 bem maior que o recorte de hoje.
Concordo com a decisão de L01 de **não** criar plataforma nova nesta rodada:
entregar o delta de Circulares mantendo os contratos compatíveis é o correto.
Não certifico "mídia em R2" para nenhuma ação fora de Momentos. D00 e o Owner
precisam decidir o plano de unificação; não é item que se fecha até 16:45.

## Contratos de mídia para L02/L03 — declarados compatíveis

L01 declarou que os contratos Dart em `packages/coelo_api` (`MediaReader`,
`SessionMediaReader`, `MediaSession`, `MediaUploadGateway`, `SessionMediaUploader`
e DTOs) **não** foram alterados e permanecem estáveis nesta rodada;
`PrincipalHappensPostWithdrawal` é contrato novo e segregado; e
`PrincipalPostPreviewItem` ganhou `postId`, `managementVersion` e `canWithdraw`
como campos opcionais com padrão fechado — aditivo compatível, sem quebrar
construtor existente. Aceito como aditivo compatível: **L02 e L03 não têm ação**.
A verificação do compilador contra os consumidores entra na consolidação.

## SHAs aptos entregues por L01

`origin/codex/e2-r02-l01-publicacoes`: `f7b07724`, `63db8ffb`, `8b83784e`,
`efa93d1a`, `f42129cd`, `eda75646`. Handoff revisão 2 em
`C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l01-publicacoes/docs/reviews/etapa-2-operacao/next-round/R02-20260909/handoffs/L01.md`.

## Estado das frentes nesta revisão

| Frente | Sessão | HEAD observado | Situação |
| --- | --- | --- | --- |
| L01 | `coelo-a6` | `eda75646` (remoto) | Ativa; proposta, migrations e `acontece.remove` local |
| L02 | não confirmada (`coelo-64`?) | `63704da1` (local) | Ativa; recibos e gateways de edit/revoke; não me contatou |
| L03 | `coelo-09` | `f376fc3d` (publicado) | **Ativa** — seis commits, Perfil e Para Você com rotas reais |

Correção da revisão 2: L03 **não** está parada. Em 12:54 estava na baseline; hoje
tem seis commits e handoff próprio. Nenhuma frente configura ociosidade.

## L03 identificou-se e pediu reserva retroativa — concedida no grupo, escalada a D00

L03 é a sessão `coelo-09`. Worktree
`C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l03-perfil-para-voce`,
branch `codex/e2-r02-l03-perfil-para-voce`, baseline `6c5567a6`, HEAD `f376fc3d`,
publicada em `origin` e verificada por ele. Nota: a baseline de L03 é `6c5567a6`,
a mesma da minha worktree, e não `56eb3f19` como `registro.json` anota — mais um
campo a corrigir com D00.

Arquivos compartilhados que L03 já commitou e agora declara:

1. `superadmin_router.dart` — 3 builders (`/principal-profile`,
   `/principal-profile/edit`, `/principal-for-you`, que antes renderizavam
   `_unavailableCompositionRootRoute`), 3 imports e 1 parâmetro opcional
   `profileAboutRepository`.
2. `superadmin_routes.dart` — 2 constantes (`principalProfileEdit`, `principalProfileEditName`).
3. `superadmin_auth_scope.dart` — 1 campo e 1 injeção `SupabaseProfileAboutRepository(client)`.
4. `superadmin_app.dart` e 5. `main.dart` — só a passagem do parâmetro até o router.

Isso bate exatamente com os hunks que eu já havia identificado por conta própria
(`@@ -831 +838,26 @@` e `@@ -847 +879,41 @@`, mais imports e o parâmetro em `-277`).
Sem toque em shell, navegação, barrels ou migrations.

**Decisão de L00:** concedo a reserva no grupo Claude e **não** mando reverter —
o trecho é do recorte de L03, é coerente com a assignment dele e a reversão
destruiria trabalho válido. Mantenho a ordem de integração **L03 → L01** no
`superadmin_router.dart`, porque L03 chegou primeiro e já publicou. A reserva
formal e a ordem final continuam sendo de D00, dono declarado desses arquivos.
Registro o desvio de processo — os cinco arquivos foram alterados antes da
declaração — como correção de processo para D00, não como motivo para desfazer código.

SHAs aptos de L03, em ordem: `8c041ed5`, `3437f25b`, `ea954bf6`, `7d2d09c8`,
`0270af5d`, `f376fc3d`. Handoff r1 em
`C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l03-perfil-para-voce/docs/reviews/etapa-2-operacao/next-round/R02-20260909/handoffs/L03.md`.

### Provas de L03, como ele reportou

`flutter analyze lib` limpo. Lote do recorte: **P 138, F 10, B 0, S 0, U 0**.
As 10 falhas são golden drift **pré-existente** de
`principal_profile_preview_golden_test.dart`, reproduzidas por ele na worktree
`e2-r02-d02-estrutura` intocada, na mesma base — controle correto, é falha
observada na base atual e não regressão de L03. Ele reporta ainda ~40 falhas
pré-existentes em `test/app/router` (assiduidade, segurança, avaliações, rotina,
Forms), igualmente reproduzidas fora do recorte dele.

L03 declara FE 3/3 nesta base, BE 0/3 e **E2E certificado 0/3**, porque não rodou
rota real contra Supabase autenticado. Aceito a autodeclaração como honesta:
FE feito não é E2E, e ele não converteu lote verde em certificação.

### Bloqueio A de L03 — é problema de SEGURANÇA e cruza para L02

`SupabaseNoticeRepository` chama `superadmin_notice_directory_v2`, RPC do domínio
interno do Superadmin: **o servidor devolve o diretório inteiro e a audiência é
decidida no cliente.** O filtro que L03 implementou é defesa em profundidade, não
controle de acesso. Isso contraria diretamente as invariantes do `AGENTS.md`
("regra de negócio, ownership, tenant e autorização são validados no backend/RLS";
"rotas e respostas não entregam dados antes da autorização"; "ocultar botões ou
depender do navegador nunca constitui controle de acesso") e impede certificar
`principal.for-you`.

`NoticeRepository` é contrato canônico de **L02**, não de L03. Roteio o achado
para L02 como dono técnico e para D00 como serializador do pacote remoto.
L03 preparou SQL proposto e **não aplicado** de uma RPC `list_my_principal_for_you`
no padrão de `list_my_principal_contexts`; a aplicação é produção e depende de
autorização nominal. Correto ele não ter aplicado.

### Bloqueio B de L03 — leitura do Sobre sem RPC

A única migration versionada expõe apenas `save_profile_about`; o `load` lê as
tabelas `profile_about_*` via PostgREST. Se estiverem sob RLS deny-by-default com
grants revogados — o padrão do projeto —, o `load` retorna negado em produção até
existir algo como `get_profile_about`. Exige verificação remota, que é de D00.
Retém a certificação de `principal.profile-view`/`profile-edit`, não o desenvolvimento.

### Lacuna de governança levantada por L03 — "Editar perfil" sem contrato visual

L03 registra que **não há contrato visual aprovado** para a tela "Editar perfil"
em nenhuma fonte canônica (campos, quem edita, rodapé, rota), e que o gate por
entregável exige aprovação de imagem do Owner. Ele entregou persistência e rota
reutilizando o editor de Sobre já aprovado, com a afordância de edição opcional,
e **não certifica o visual**. Concordo e levo ao Owner: `principal.profile-edit`
não pode ser fechado visualmente nesta rodada sem decisão. Isso é irmão da
ambiguidade de viewers imersivos já aberta em `PRINCIPAL.md` — some às pendências
do Owner, não às do executor.

## Métricas — o que é de L01 e o que L00 não certifica

L01 declarou `acontece.remove` fechado **localmente**, FE mais contrato de BE,
com 8 testes de widget verdes, lote do Acontece em **74 aprovados** contra **66**
na base sem as mudanças dele, e **as mesmas 10 falhas de golden preexistentes nas
duas execuções**. Registro como ele reportou, sem somar e sem esconder:

- Testes do lote Acontece na base atual: **P 74, F 10**, escopo lote de widget,
  runner Flutter local, base `codex/e2-r02-l01-publicacoes`. As 10 falhas são
  **falha observada na base atual**, preexistentes ao delta de L01 — não são
  falha histórica não revalidada nem cenário nunca executado.
- Delta atribuível a L01 no lote: +8 aprovados (66 → 74). Não somo os 8 testes de
  widget separadamente aos 74: são o mesmo conjunto.
- **Não é E2E.** Nada foi aplicado em remoto; sem provedor real, sem persistência
  verificada em produção. `acontece.remove` fica **FE feito + contrato de BE
  proposto**, não certificado ponta a ponta.

Certificado por L00 nesta revisão: FE 0/28, BE 0/28, E2E 0/28. Taxas de L00 não
calculáveis por denominador zero. Ver commit e ler relato alheio não certifica aceite;
os números por frente vêm dos handoffs no corte das 16:00.

## Dependências

- **Solicitada a D00 (nova):** reconciliar o mapeamento de Circulares a partir de
  `origin/codex/e2-r02-l01-publicacoes:eda75646`, publicando denominador
  anterior/novo e motivo.
- **Solicitada a D00 (nova):** reserva formal e **ordem de integração L03 → L01**
  em `superadmin_router.dart`; e decisão sobre manter as duas árvores de migration
  em sincronia antes de qualquer aplicação remota.
- **Solicitada a D00 (nova):** plano de conformidade ADR0032 do gateway de mídia,
  fora do alcance de hoje.
- **Solicitada a D00 (repetida, sem nova cobrança):** corrigir head de L00 no
  `registro.json`.
- **Liberadas por L00:** reservas do grupo Claude descritas na tabela acima,
  com release por SHA quando L01 publicar os arquivos renomeados.

## Primeiro critério aberto

Reconciliação de Circulares publicada por D00 no inventário e nas três matrizes.

## Próximo passo

Aguardar `9c8a34b9` (15:53) sem interromper as frentes ativas; atender na hora
pedidos de reserva, contrato incompatível ou bloqueio que cheguem por `SendMessage`.
Conferir na consolidação se L01 renomeou a migration de Momentos e se o teste
pgTAP do Acontece foi tratado.

## Recursos e processos ativos

Três jobs one-shot armados: `9c8a34b9`, `d8291be4`, `f6b70aa1`. Nenhum subagente
aberto. Nenhum build, container, servidor ou suíte em execução por L00.

---

# Handoff L00 — revisão 4

- **Revisão:** 4. Escrita em 2026-09-09T13:58-03:00 (America/Sao_Paulo, UTC−03).
- **Última instrução processada:** `assignments/L00.md` de D00 até a **revisão 10**,
  mais o commit dev `eaab2df13`, mais quatro mensagens de L01 (`coelo-a6`), duas
  de L02 (`coelo-64`) e uma de L03 (`coelo-09`).
- **Modelo ativo:** `claude-opus-5[1m]`, esforço médio. Sem mudança.
- **HEAD de L00:** `03969969a`, publicado e verificado em
  `origin/codex/e2-r02-l00-coordenacao-claude`.

## As três frentes Claude estão identificadas e ativas

| Frente | Sessão | Situação |
| --- | --- | --- |
| L01 | `coelo-a6` | Ativa; sete-plus SHAs aptos publicados |
| L02 | `coelo-64` | Ativa; recibos, edição, revogação e agendamento |
| L03 | `coelo-09` | Ativa; Perfil e Para Você com rotas reais |

A ponte `ListAgents`/`SendMessage` está comprovada nos dois sentidos com as três.
Continua sem canal nativo meu para D00, que roda no Codex.

## Decisão do Owner sobre viewers imersivos: procedência CONFERIDA, bloqueio removido

L01 e L02 relataram, corretamente e com desconfiança adequada, uma suposta
"decisão final do Owner" aparecendo como arquivo não commitado. **Verifiquei a
procedência em vez de aceitar ou rejeitar pela aparência.** É legítima:

- Commitada em `dev` no commit `eaab2df13` (`docs(e2): reconcile Circulars scope
  and verified R02 integrations`), publicado e verificado.
- `PRINCIPAL.md` da raiz canônica trocou a seção "Única ambiguidade ainda
  submetida ao Owner" por "Decisão final do Owner — 09/09/2026", dizendo com
  todas as letras que a dúvida está resolvida e que não se deve pedir nova
  confirmação nem manter bloqueio por esse motivo.
- `assignments/L00.md` revisão 6 de D00 me instrui a atualizar as assignments
  vivas de L01 e L03 que ainda marcavam a dúvida como pendente.

Conteúdo: no Superadmin, o shell/menu é preservado **no web e no mobile**,
inclusive em Agora, Momentos e na leitura de Circular; a imersão fica dentro do
contêiner e só elementos internos concorrentes do Principal podem ser suspensos;
foco e contexto são restaurados ao sair; a regra é do hospedeiro, não do app
Principal independente. A confirmação final substitui a resposta intermediária
de que valeria apenas no web.

Atualizei as três assignments para a revisão 2 removendo o bloqueio. L01 já está
implementando. **Não reabri a decisão com o Owner**: pedir de novo autorização
válida é exatamente o que o contrato proíbe.

## Escrita cruzada entre worktrees: explicada, não é incidente

As três frentes relataram o mesmo conjunto de arquivos modificado sem que
nenhuma o tivesse escrito. L01 chegou a verificar que `.agents` não é junction —
verificação útil. A causa é a **revisão 8 de D00**: ele materializou
deliberadamente cinco fontes aprovadas (`PRINCIPAL.md`, `specs/037`, `specs/050`,
a referência `coelo-ui` e a projeção de Circulares) nos working trees de L00,
L01, L02 e L03, após conferir que cada path estava limpo, sem tocar índice, HEAD
ou arquivo de implementação, com hashes antes/depois em `documentary-delivery.json`.
Cumpre a regra do `AGENTS.md` de que skill alterada precisa chegar ao checkout de
destino. Instruí as três a preservar e não restaurar versões antigas.

**Decisão de propriedade que tomei para evitar dois escritores no mesmo arquivo:**
`specs/050` e a referência `coelo-ui` são fontes compartilhadas pelas três
frentes; **eu as commito**, e as três deixam as suas preservadas e intocadas.
Feito em `03969969a`. `specs/037` e a projeção de conhecimento são do recorte de
L01, que já reconciliou e commitou.

## Circulares reconciliada por D00 — denominadores novos repassados

`CIRCULARES-MAPEAMENTO-ACEITO.md` aceita **11 IDs**, não os 12 propostos:
`circulars.list/filter/create/edit/detail/schedule/publish/close/delete/respond/attach`.
Duas correções que repassei: `circulars.happens-card` **não vira ID** e é
subaceite obrigatório de `acontece.feed`; `circulars.error`, `access-denied` e
`reload` viram estados das ações de diretório/filtro/detalhe, sem contagem
autônoma — e **sem sair do plano de testes**.

| Escopo | Antes | Depois |
| --- | ---: | ---: |
| L01 | 12 | 23 |
| Grupo Claude | 28 | 39 |
| Seleção da rodada | 136 IDs / 120 E2E | 147 IDs / 131 E2E |
| Global | 219 IDs / 187 E2E | 230 IDs / 198 E2E |

Certificados: FE 2/230, BE 0/223, E2E 0/198. Os 11 IDs novos nascem
`pending-verification`. Denominador maior mede cobertura, não regressão.

## Reservas concedidas por L00 (formalização e ordem final continuam com D00)

**Migrations.** Faixa `20260909130000`–`20260909130900` para L02;
`20260909131000`–`20260909139000` para L01. Resolvi a colisão real de prefixo
`20260909130000` entre a migration de Momentos de L01 (WIP) e a de recibos de
Chat de L02 (já commitada): o prefixo fica com L02 e L01 renomeia para
`20260909135000`. Liberados os arquivos de retirada do Acontece, expiração do
Agora, Circulares em R2 e o `create or replace` de `list_visible_happens_feed`.

**`superadmin_router.dart` — ordem de integração L03 → L01 → L02**, por ordem de
chegada, cada um escrevendo sobre o estado publicado do anterior:
- **L03**: builders de `/principal-profile`, `/principal-profile/edit` e
  `/principal-for-you`, imports e o parâmetro `profileAboutRepository`, mais
  `superadmin_routes.dart`, `superadmin_auth_scope.dart`, `superadmin_app.dart` e
  `main.dart`. Reserva **retroativa** — as mudanças já estavam commitadas.
  Concedi e **não mandei reverter**: o trecho é do recorte dele e reverter
  destruiria trabalho válido. O desvio de processo fica anotado para D00.
- **L01**: `/principal-happens` (l.669), `/principal-moments` (l.835, encravado
  entre os hunks de L03) e as duas construções de `ProductionCircularComposerHost`
  (~l.4391 e ~l.4431) recebendo `mediaRepository`.
- **L02**: rota nova `/principal-conversations` mais 2 constantes em
  `superadmin_routes.dart` e a troca de destino em 2 pontos que hoje mandam a
  superfície Principal para a página administrativa.

## Ressalvas técnicas que L00 levantou por conta própria

- `drop + create` de `public.list_visible_happens_posts` muda o tipo de retorno.
  Consumidor Dart único, mas há teste pgTAP acoplado ao contrato antigo em
  `packages/coelo_database/supabase/tests/happens_publication_test.sql`
  (l.17, 35-61 e o `pg_get_function_result` da l.46). Exigi atualização no mesmo
  delta ou declaração explícita em B/U.
- Existem **duas árvores de migration** (`packages/coelo_database/migrations/` e
  `packages/coelo_database/supabase/migrations/`) com o mesmo arquivo de agosto.
  Os arquivos novos estão só na primeira. Decisão de sincronia é de D00; proibi
  duplicação por conta própria.

## Decisões de mérito que tomei

- **Assimetria de `withdraw_moment` ACEITA.** `p_expected_version` opcional em
  Momentos, obrigatório em Acontece: a projeção de feed de Momentos não expõe
  versão de gerenciamento; autoria, tenant, escopo e capacidade continuam
  validados no servidor; a retirada é idempotente e soft. Nenhuma fonte aprovada
  impõe simetria, e forçá-la custaria trabalho sem ganho de segurança. Exigi
  documentação da razão no handoff de L01.
- **Regra de goldens endossada.** Regenerar apenas os que mudam pela decisão do
  shell; deixar quebrados os que já falhavam por deriva do host e por sprite
  ausente — regenerar gravaria deriva e asset faltante como referência aprovada.
  Exigi lista nominal do que foi regenerado e por qual causa, e proibi substituir
  imagem aprovada. Mantida a ressalva de D00: atribuir a deriva ao host é
  **hipótese**, não conclusão de ausência de falha de produto.
- **Proposta de L02 endossada:** desdobrar `chat.*` em `.admin` e `.principal`
  mantendo o ID atual como agregador. Muda denominador, não implementação;
  decisão é de D00, único escritor do inventário.
- **Correção de horário de L02 aceita.** Ele carimbou uma entrega como "corte das
  16:00" às 13:31 e corrigiu sozinho na revisão 3. Registro como entrega
  antecipada válida; o corte das 16:00 continua de pé para as três frentes.

## Retenções e bloqueios em aberto

- **D00 reteve o lote de retirada do Acontece de L01** (`8b83784e`/`f42129cd`)
  por quatro causas: `expected_version` comparado por `<>` sem negar NULL, com
  bypass do controle otimista; feed misto sem filtrar `withdrawn_at`;
  `_withdrawPost` conferindo só `mounted` sem invalidar repositório e escopo;
  `can_withdraw` projetando autoria sem conferir a capacidade `remove`. Repassei
  e pedi confirmação de quais estão fechadas — `9fe319159` parece atacar a segunda.
- **Autorização de Para Você (achado de L03, contrato de L02).**
  `superadmin_notice_directory_v2` devolve o diretório inteiro e a audiência é
  decidida no cliente. Contraria as invariantes do `AGENTS.md`. Roteado a L02
  como dono e a D00 como serializador; SQL proposto e **não aplicado**.
  Retém a certificação de `principal.for-you`.
- **Leitura do Sobre sem RPC (L03).** `load` via PostgREST contra tabelas
  provavelmente sob RLS deny-by-default. Exige verificação remota de D00.
- **`chat.attach` (L02) retido por L01:** falta `catalog_kind` de chat no catálogo
  R2 e o contrato de ticket do gateway.
- **Conformidade ADR0032 do gateway de mídia.** Não existe gateway único: cinco
  Edge Functions por domínio, `MediaUploadGateway` sem implementação em produção,
  e só Momentos e a leitura de Forms em R2. Lacuna estrutural bem maior que esta
  rodada; **não se fecha até 16:45**. Para D00 e o Owner.
- **`principal.profile-edit` sem contrato visual aprovado.** Nenhuma fonte
  canônica define campos, ator, rodapé e rota, e o gate por entregável exige
  aprovação de imagem do Owner. L03 entregou persistência e rota reutilizando o
  editor de Sobre aprovado e **não certificou o visual** — correto. D00 registra
  que "editor Principal próprio já aprovado"; se cobrir esta composição, resolve.
  **Pendência do Owner, não falha do executor.**

## Métricas

Certificado por **L00**: FE 0, BE 0, E2E 0. Testes executados por L00: P 0, F 0,
B 0, S 0, U 0 — não rodei suíte de produto nesta rodada; meu trabalho é
coordenação, revisão e integração de contratos. Taxas de L00 **não calculáveis**
por denominador zero. Os números por frente vêm dos handoffs no corte das 16:00,
com os denominadores novos (grupo Claude 39). Ler relato alheio e revisar
proposta documental **não** move progresso do produto.

## Próximo passo

Aguardar `9c8a34b9` (15:53) sem interromper as frentes; atender por evento
pedidos de reserva, contrato incompatível ou bloqueio. Conferir na consolidação:
renomeação da migration de Momentos por L01, tratamento do teste pgTAP do
Acontece, quais das quatro causas de D00 foram fechadas, e o alinhamento
L02↔L03 sobre `list_my_principal_for_you` e `/principal-conversations`.

## Recursos e processos ativos

Três jobs one-shot armados: `9c8a34b9`, `d8291be4`, `f6b70aa1`. Nenhum subagente
aberto. Nenhum build, container, servidor ou suíte em execução por L00.

---

# Handoff L00 — revisão 5

- **Revisão:** 5. Escrita em 2026-09-09T14:15-03:00 (America/Sao_Paulo, UTC−03).
- **Causa desta revisão:** reinício do notebook por volta das 14:03, confirmado
  pelo Owner, e as entregas cruzadas que ele destravou.
- **Modelo ativo:** `claude-opus-5[1m]`, esforço médio. Sem mudança.
- **HEAD de L00:** `088f8c586`, publicado e verificado.

## O reinício quebrou o canal entre sessões — corrigido

O host reiniciou e **todas as sessões Claude ganharam nomes novos**. Esta sessão
deixou de se chamar `coelo-fe` e passou a `coelo-ed`. As assignments revisão 1
mandavam enviar `SendMessage` para `coelo-fe`, nome que deixou de existir:
descobri isso quando um envio a `coelo-64` voltou com "No agent named ... is
reachable". O canal entre L00 e as três frentes ficou **inválido** por alguns
minutos.

Publiquei a **revisão 3** das três assignments em `088f8c586`. A correção não é
só trocar o nome — nome fixo volta a quebrar no próximo reinício. O procedimento
agora é: rodar `ListAgents`; havendo dúvida, mandar **uma** mensagem curta a cada
candidata perguntando o ID da rodada e se identificando; anotar o nome vivo e
repetir a busca quando um envio falhar por destinatário inexistente. E, se o
canal não voltar, **não parar**: o handoff continua sendo a evidência durável, o
impedimento vira fato registrado, e eu leio os três handoffs pelos caminhos
absolutos na consolidação.

**Limitação registrada, sem inventar solução:** a identidade de sessão Claude não
sobrevive a reinício do host. Os agendamentos, por outro lado, sobreviveram —
`CronList` confirma `9c8a34b9` (15:53), `d8291be4` (16:03) e `f6b70aa1` (16:38)
ainda armados. Continua valendo que eles são session-only e morrem com a sessão.

Reanunciei-me às quatro sessões pares. `coelo-fb` respondeu que **não** é frente
da rodada: está no checkout raiz em `dev`, iniciou sem prompt e sem assignment, e
recusou assumir um ID que não é seu — comportamento correto, e registro para que
ninguém a conte como executor ausente. As candidatas restantes a L01, L02 e L03
são `coelo-b7`, `coelo-03` e `coelo-0f`, ainda sem confirmação de identidade.

## Entregas cruzadas repassadas na hora, sem esperar o corte

**`chat.attach` de L02 está DESBLOQUEADO.** L01 publicou `251a7ec95`:
- `packages/coelo_database/migrations/20260909135000_private_media_catalog_chat_kind_v1.sql`
  (204 linhas) com o `catalog_kind` de chat que faltava — o validador de chave
  opaca ganhou ramo `chat/message` com o de Forms intocado, e o asset guard foi
  recriado com o ramo de chat depois do de Forms;
- `packages/coelo_database/supabase/tests/private_media_catalog_chat_kind_v1_test.sql`
  (223 linhas de pgTAP);
- `propostas/L01-contrato-midia-chat.md` (232 linhas), com
  `chat_attachment_metadata` mantendo o cliente fora da escrita de metadados e o
  registro explícito de que Chat não usa Stream no MVP, coerente com a ADR0032.

Ressalvas que carrego: a migration **não foi aplicada em lugar nenhum**, a
aplicação é serializada por D00 e exige autorização nominal; e o pgTAP **não tem
runner nesta máquina**, então fica **não executado**, nem aprovado nem bloqueado.

**A superfície Principal de Chat existe.** L02 publicou `65e5c4b5b`,
`principal_chat_page.dart` com 300 linhas de teste — é o que faltava para a ação
Mensagem de L03. Não vi ainda a fiação no router usando a reserva concedida.

## L03 varreu delta alheio e reverteu corretamente — nada se perdeu

O HEAD de L03 é `9c6042732`, `revert(e2): return the foreign delta this branch
swept in`. Ele havia commitado por engano as duas fontes documentais que D00
materializou — a referência `coelo-ui` e a projeção de Circulares — e reverteu,
**preservando o conteúdo** em
`handoffs/L03-preserved-foreign-delta.patch`. Verifiquei as duas pontas: o patch
preservado tem o delta íntegro, e o mesmo conteúdo está commitado em `dev` no
`eaab2df13`. **Nada foi perdido**, e a regra de que ninguém apaga código para
deixar Git limpo foi respeitada. É exatamente o desfecho que a decisão de
propriedade da revisão 4 pretendia evitar; a decisão continua valendo — `specs/050`
e a referência `coelo-ui` são commit meu, já feito em `03969969a`.

## Correção factual que devo a L01

Cobrei dele o teste pgTAP acoplado ao tipo de retorno de
`list_visible_happens_posts`. **O erro era meu:** li a linha 46 na baseline
`6c5567a6` e não na ponta da branch dele. Verifiquei em
`origin/codex/e2-r02-l01-publicacoes` e a assertiva já é a nova, com
`management_version` e `can_withdraw` na assinatura, atualizada no mesmo commit
`8b83784e` que mudou o tipo de retorno. **Não é pendência, não é B nem U**: foi
executada, com 53 ok e 0 not ok em Postgres 17.6 descartável. Retirei do que eu
levaria a D00 e avisei L01.

## Retenção do lote de retirada do Acontece: causas fechadas no código

L01 publicou `9cf2d908a` e eu verifiquei no remoto, não pelo relato:
- guarda otimista agora é `p_expected_version is null or ... is distinct from ...`
  (`20260909133000`, l.94) — versão nula virou conflito explícito;
- `and post.withdrawn_at is null` no feed misto (`20260909134000`, l.38), com a
  assinatura da RPC inalterada;
- `can_withdraw` exige autoria **e** `has_institution_permission(...,'happens.posts.remove',...)`
  (l.217-219).
A quarta causa, a guarda de contexto no `_withdrawPost`, não é verificável por
leitura de SQL: fica registrada como **declarada e coberta por teste dele**, não
verificada por mim.

**A retenção continua de pé**, e isso é deliberado: o que existe é defeito
corrigido, não defeito **provado** corrigido. A exigência de D00 era negativa
comportamental com fixtures reais no lugar de `pg_get_functiondef` e substring, e
essa suíte pgTAP ainda está sendo escrita. Pedi o número real de asserts quando
fechar e proibi antecipar número.

## Decisões de mérito adicionais desta revisão

- **Corrigir dentro do `20260909133000` está certo.** O arquivo nunca foi
  aplicado, então não há estado remoto a conciliar, e empilhar migration de
  conserto entregaria a D00 um pacote que nasce com defeito conhecido mais
  remendo. Forward-only vale para o que já foi aplicado, não para arquivo inédito.
  Se D00 exigir arquivo separado por causa do preflight, ele decide.
- **Timestamps atualizados:** L01 renomeou Momentos para `20260909136000`, e não
  `135000` como eu havia indicado, porque `135000` ficou com o delta de
  `catalog_kind` de chat. Conferi a árvore da branch dele: `131000`, `132000`,
  `133000`, `134000`, `135000` e `136000`, **sem nenhum `130000`**. A colisão com
  L02 está desfeita.
- **Goldens do leitor de Circular** regenerados em `dcda0be15`
  (`circular_reader_contextual_light_375`, `..._dark_375`, `..._light_375_text_200`),
  com causa material declarada: o tile de anexo deixou de ser retângulo genérico
  por índice e passou a mostrar identidade real e estado de leitura autorizada.
  L01 **não** regenerou os goldens do diretório, que falham por deriva e cujo
  arquivo não tocou. Essa distinção é o que impede gravar deriva como referência
  aprovada, e é o critério que aplico nas três frentes.

## Métricas

Certificado por **L00**: FE 0, BE 0, E2E 0. Testes executados por L00: P 0, F 0,
B 0, S 0, U 0 — não rodei suíte de produto; meu trabalho é coordenação, revisão
de delta e desbloqueio. Taxas de L00 **não calculáveis** por denominador zero.
Verificar SQL alheio no remoto e conferir procedência de arquivo **não** move
progresso do produto. Os números por frente vêm dos handoffs às 16:00, sobre o
denominador novo de 39 IDs do grupo Claude.

## Primeiro critério aberto

Identificação das três frentes no canal reconstruído, e o número de asserts do
pgTAP comportamental de L01 que solta a retenção do lote do Acontece.

## Próximo passo

Aguardar `9c8a34b9` (15:53) sem interromper as frentes. Atender por evento
pedidos de reserva, contrato incompatível e bloqueio. Conferir na consolidação a
fiação do router pelas três frentes na ordem L03 → L01 → L02, o alinhamento
L02↔L03 sobre `list_my_principal_for_you` e `/principal-conversations`, e a lista
nominal de goldens do shell que L01 ainda deve.

## Recursos e processos ativos

Três jobs one-shot armados e confirmados por `CronList` após o reinício:
`9c8a34b9`, `d8291be4`, `f6b70aa1`. Nenhum subagente aberto. Nenhum build,
container, servidor ou suíte em execução por L00.

## Adendo à revisão 5 — achado estrutural de L03 sobre o shell (14:20-03:00)

L03 identificou-se (sessão `coelo-03`) e trouxe o achado mais importante do dia
sobre a decisão do Owner: **ela tem duas metades e só uma estava sendo tratada.**

A metade que L01 já implementa é a suspensão de chrome dentro dos viewers.
A metade estrutural, verificada por L03 no router: `/principal-profile` (l.875)
e `/principal-for-you` são **GoRoutes de topo declaradas ANTES do `ShellRoute`**
(l.1012). Rota de topo abre em tela cheia com cabeçalho e dock próprios do
Principal, **fora** do contêiner do hospedeiro — e nenhuma regra aplicada dentro
do viewer conserta isso. Não é regressão de L03: é estado herdado da base, que
ele manteve ao tirar as rotas do fail-closed.

Duas evidências de que hospedar é a estrutura pretendida, não invenção:
`_destinationForLocation` já mapeia `principal-profile` e `principal-for-you`, e
as páginas já aceitam `embedded`, que suprime cabeçalho e dock próprios.

**Decisão de L00:** autorizei L03 a mover **somente** as três rotas dele para
dentro do `ShellRoute` com composição embutida. Ele parou na fronteira certa e
não tocou nas rotas de L01 — mover aquelas é de quem as mantém. Instruí L01 a
conferir `/principal-happens`, `/principal-now`, `/principal-moments`, as
publicações e a leitura de Circular, e a **declarar** se move hoje ou registra
como pendência: pendência declarada é aceitável, pendência silenciosa não.

**Escalo a D00:** a hospedagem do router é mudança transversal que ele precisa
ordenar. Hoje o Principal fica em **estado misto** — parte das rotas hospedada,
parte não — e isso precisa aparecer no consolidado, não ser lido como "Principal
hospedado". Se D00 preferir um movimento único coordenado, L03 reverte o trecho,
que é localizado.

**Correção minha, registrada:** chamei as entregas de L01 e L02 de "desbloqueio"
para L03. L03 apontou, com razão, que `principal_chat_page.dart` está em
`65e5c4b5b` na branch de L02 e o repositório de Momentos na branch de L01, e que
`features/principal_moments/data/` não existe no checkout dele. **Enquanto D00
não integrar, ele não pode consumir.** Aba Momentos em pendência honesta e ação
Mensagem apontando para a rota de Comunicação existente é o comportamento certo.
SHA publicado em branch alheia não é dependência satisfeita — anoto a distinção.

L03 também registrou por conta própria que um `git add -A` varreu dois arquivos
alheios, que ele restaurou em `9c6042732` preservando o delta em patch. Verifiquei
as duas pontas: patch íntegro e mesmo conteúdo em `dev` no `eaab2df13`. Nada
perdido, sem reset, clean ou force push. Não contabilizo aquele conteúdo como
entrega da branch dele. HEAD de L03 agora é `83d1c8a2f`.

`coelo-fb` respondeu que **não** é frente da rodada — está no checkout raiz em
`dev`, iniciou sem prompt e sem assignment, e recusou assumir ID alheio.
Identificadas até aqui: L03 = `coelo-03`. L01 e L02 ainda entre `coelo-0f` e
`coelo-b7`.

---

# Handoff L00 — revisão 6 (nota de consolidação)

Escrita em 2026-09-09, entre o checkpoint antecipado de L01 e o corte das 16:00.
Registro aqui o que **não pode se perder** quando eu montar o consolidado.

## L01 fechou o checkpoint antes do horário

Ponta da branch `09e2fad56`; conteúdo técnico do snapshot fechado em `aec0c4afe`.
Verifiquei por conta própria: HEAD igual ao remoto; o diff desde `6c5567a6` **não
toca** nenhum arquivo em `apps/superadmin/lib/app/router/` nem em
`apps/superadmin/test/app/router/`, confirmando que ele interrompeu o movimento
de rotas sem deixar resíduo; handoff publicado com status `checkpoint-1600`.

Números sobre o denominador novo, depois da correção que pedi:
implementação fechada **6/23**, contrato de servidor local **6/23**,
**E2E certificado 0/23**. Por família: `acontece` 1 de 4, `agora` 1 de 4 no eixo
de backend, `momentos` 4 de 4, `circulars` 1 de 11.
`circulars.happens-card` **não é contado**, por ser subaceite de `acontece.feed`.

## A distinção que o consolidado NÃO pode apagar

Dos 17 IDs restantes de L01, **seis já tinham implementação antes desta rodada**:
`acontece.feed`, `acontece.create`, `acontece.publish`, `agora.view`,
`agora.create` e `agora.publish`. A pendência deles é de **verificação**, não de
implementação. L01 não os declarou concluídos porque não foi ele quem os
verificou — postura correta.

Se essa distinção se perder, "6 de 23" é lido como "17 ações por construir", o
que é **falso**. No consolidado, separar **implementado e não certificado** de
**não implementado**, como o contrato exige ao proibir usar percentual de
auditoria como pronto.

## O item que decide o E2E da rodada

L01 está em E2E 0/23 e a causa **não é falta de código**: é a ausência de
autorização nominal para o pacote remoto. O pacote está pronto e revisável em
`propostas/L01-pacote-remoto-nominal.md`, com ordem de aplicação, superfície de
risco por item, provas locais, limitação honesta do harness e recuperação por
compensação. Nada foi aplicado. A decisão é do Owner; a serialização é de D00.
Levei o ponto ao Owner antes do corte, para não queimar a janela.

## Correção de forma que vale como regra

L01 corrigiu o próprio handoff, que citava um HEAD fixo ultrapassado no instante
do commit. **Um handoff não consegue registrar o SHA que o carrega**: o certo é
declarar em que SHA o conteúdo técnico foi fechado e mandar verificar a ponta da
branch. Aplico a mesma regra aos meus.

## Correções que EU recebi hoje e que vão declaradas no consolidado

1. Afirmei que não havia runner de pgTAP nesta máquina. **Falso.** L01 sobe
   Postgres 17.6 descartável e executou 50/0, 16/0, 23/0, 46/0, 60/0, 53/0 e
   32/0. A ressalva verdadeira é que o replay local é **parcial** (93 de 170
   migrations, falhas em domínios fora do recorte): prova contrato, não banco de
   produção. Propaguei a L02, que podia ter classificado um teste como não
   executado com base no meu erro.
2. Chamei `251a7ec95` de desbloqueio de `chat.attach`. **Errado.** A seção 6 do
   próprio contrato de L01 diz que faltam a RPC de staging/commit e a Edge
   Function `chat-media`, e a migration não concede grant. O que existe é
   contrato revisável e forma de chave — progresso real, não desbloqueio.
3. Autorizei L03 a mover só as três rotas dele para dentro do `ShellRoute`.
   **Revertido**: L01 mostrou que o movimento parcial faz o menu sumir ao navegar
   entre telas irmãs, e são 17 asserções em 6 arquivos de teste codificando a
   regra antiga. Vira movimento único coordenado por D00.
4. Estimei horários em prosa mais adiantados que o relógio real, mais de uma vez.
   Passo a reportar hora lida, não estimada.

---

# Handoff L00 — revisão 7: achado próprio de L00 no arquivo compartilhado

Levantamento feito por L00 em 2026-09-09, hora lida 14:2x, sobre a base da raiz
canônica. Não é relato de executor: é evidência que produzi e que qualquer um
reproduz com `grep` nos números de linha abaixo, em
`apps/superadmin/lib/app/router/superadmin_router.dart`.

## 1. Uma capacidade plumbada e NUNCA usada — só uma, e é a de Circulares

Contei as ocorrências de cada repositório Principal declarado como parâmetro do
router. Nove dos dez têm o parâmetro **mais um** local de uso. Um tem só o
parâmetro:

| Repositório | Ocorrências | Situação |
| --- | ---: | --- |
| `principalRuntimeContextRepository` | 9 | usado |
| `principalHappensFeedRepository` | 2 | l.280 declara, l.676 usa |
| `happensPublicationRepository` | 2 | l.282 declara, l.704 usa |
| `principalNowFeedRepository` | 2 | l.283 declara, l.739 usa |
| `nowPublicationRepository` | 2 | l.285 declara, l.761 usa |
| `momentsPublicationRepository` | 2 | l.284 declara, l.800 usa |
| `chatRepository` | 3 | usado |
| `circularRepository` | 6 | usado |
| `noticeRepository` | 8 | usado |
| **`principalMixedFeedRepository`** | **1** | **l.281 declara e NADA usa** |

`SupabasePrincipalMixedFeedRepository(client)` é instanciado em
`superadmin_auth_scope.dart:372` e o parâmetro existe em `superadmin_router.dart:281`,
mas **nenhum builder o consome**. A projeção de Circulares no Acontece não chega
a nenhuma rota real. Isso confirma, com evidência independente, o que L01 havia
relatado — e acrescenta que é o **único** caso entre os dez.

**Consequência para o denominador:** D00 decidiu que `circulars.happens-card` não
vira ID e é **subaceite obrigatório de `acontece.feed`**. Enquanto o repositório
não chegar à rota, `acontece.feed` **não pode ser certificado**, mesmo que o feed
de posts funcione. Registrar assim, e não como "Acontece pronto".

## 2. Três rotas Principal de produção resolvem para a tela de indisponível

Na base, e sem condição alguma — não é fail-closed por repositório ausente, é
destino fixo:

- `SuperadminRoutes.principalForYou` — builder em l.833
- `SuperadminRoutes.principalMoments` — builder em l.841
- `SuperadminRoutes.principalProfile` — builder em l.849

L03 já corrigiu **duas** na branch dele (`principalForYou` e `principalProfile`).
`/principal-moments` continua, é de **L01**, e está entre as injeções que ele
declarou pendentes de reserva.

Compare com os builders de l.677, 714, 740, 771 e 810, que caem no indisponível
**condicionalmente**, quando o repositório é nulo. Esses são fail-closed legítimo.
Os três de l.833/841/849 não são: são composição que nunca foi ligada.

## 3. As previews /dev têm composição real que a rota normal não tem

`devPrincipalNow` monta `PrincipalNowPreviewPage`, `devPrincipalMoments` monta
`PrincipalMomentsPreviewPage`, `devPrincipalHappens` monta
`PrincipalHappensPreviewPage.demo` e `devPrincipalForYou` monta
`PrincipalForYouPreviewPage` — enquanto as rotas de produção correspondentes caem
no indisponível. É exatamente o padrão que o contrato manda não aceitar:
**existir preview não remove a pendência**, e rota existente não prova acesso
normal. Nenhuma ação pode ser certificada por `/dev`.

## Por que isto importa para o movimento único de D00

O movimento coordenado de hospedagem no `ShellRoute` vai tocar exatamente esses
builders. Fazer os dois trabalhos no mesmo passo — ligar a composição e hospedar
a rota — evita reescrever o mesmo trecho duas vezes e evita que D00 hospede uma
rota que continua resolvendo para a tela de indisponível, o que pareceria
progresso sem ser.

## Revisão 7b — a varredura que mandei fazer produziu achado em L02

Mandei as três frentes caçarem a classe de defeito "capacidade instanciada,
plumbada e nunca usada", em vez de ficarem em estado seguro. L02 achou no
recorte dele, e verifiquei antes de conceder reserva.

**Contador de não lidas do Chat: vertical completa, nunca ligada.**
`chatUnreadCountLoader` aparece em exatamente quatro lugares, **todos dentro de**
`apps/superadmin/lib/app/shell/superadmin_shell.dart` — declarações em l.42 e
l.58, campo em l.80, consumo pelo launcher em l.498 — e **nenhum caller em
`lib/`** fornece o parâmetro. A RPC `superadmin_chat_unread_total_v2` existe com
grant a `authenticated`, `fetchUnreadTotal` está no domínio
(`chat_repository.dart:213`) e implementado em `supabase_chat_repository.dart:18`.
Efeito na rota normal: **badge sempre zero**, em produção e em `/dev`, mesmo com
conversas não lidas. Nenhum teste pega porque `superadmin_shell_test.dart` injeta
o loader direto.

Os três pontos que constroem o shell e omitem o parâmetro:
`superadmin_router.dart` l.450 (`operationalPage`), l.465
(`productionOperationalPage`) e l.966 (`SuperadminShell.host`).

**Reserva concedida por L00, e concedida AGORA, não empurrada para o movimento
de D00:** são 3 linhas em construções do shell; não tocam rota, path nem
`ShellRoute`; e o movimento único mexe nas declarações de rota entre l.669 e
l.849 e no `ShellRoute` da l.1012. Sem sobreposição. Segurar correção de defeito
real por duas horas esperando um movimento que não a inclui seria desperdício.

**A guarda proposta por L02 é o ponto mais importante do patch** e eu a exigi
preservada: passar `chatRepository.fetchUnreadTotal` sem checar o
`UnavailableChatRepository` produziria um zero silencioso — e o `Unavailable`
devolve `0` em `chat_repository.dart:225`, confirmado por mim. Zero silencioso é
pior que ausência, porque **afirma um fato falso** ao usuário. Passar `null` faz
o launcher não afirmar contagem nenhuma: indisponibilidade honesta.

Estendi a reserva ao defeito 2 no mesmo commit: `/dev/conversations` não passa
`mediaReader` nem `mediaSession` à `SuperadminChatPage`, enquanto a rota de
produção passa — preview que não exerce a capacidade vira evidência falsa.

**Pergunta encaminhada a D00, não defeito:** `NoticeDirectoryPage` recebe
`enableInlinePreview: true` só em `/dev/notices`. Não há fonte aprovada exigindo
a prévia lateral em produção, então L02 **não** deve implementar. Inventar
requisito a partir do que a preview faz é o inverso do que a rodada manda.

**Decisão declarada, não lacuna:** a composição Principal do Chat não oferece
editar nem revogar, porque o ator do Principal não é o autor interno e
`can_manage` viria falso do servidor. Oferecer a afordância seria prometer o que
o servidor nega — o mesmo defeito que L01 corrigiu no `can_withdraw`.

**Cobertura negativa registrada**, que também é informação: L02 varreu e NÃO
achou defeito em `chatRepository` chegando às duas superfícies com falha fechada,
no recibo projetado chegando à renderização, em `edit`/`revoke` gated por
`can_manage`, e em `noticeRepository` chegando ao diretório e ao formulário com
`onCreate`/`onEdit` anulados quando indisponível.

**Mapa de reservas no `superadmin_router.dart`, sem sobreposição:**
L03 nos builders de Perfil e Para Você (l.827 e adiante); L01 em `/principal-happens`
(l.669), `/principal-moments` (l.835) e as duas construções de
`ProductionCircularComposerHost` (~l.4391 e ~l.4431); L02 nas construções do
shell (l.450, l.465, l.966) e na rota nova `/principal-conversations`.

---

# Handoff L00 — revisão 8: o que a varredura dirigida produziu

Instrução do Owner às 14:2x: as frentes param demais e ele não deveria precisar
vigiar. Tirei as três do estado seguro e dirigi a varredura a uma classe de
defeito concreta — **capacidade instanciada, plumbada e nunca usada** — que L01
já havia exposto duas vezes. Em pouco mais de uma hora, as três acharam defeito
real. Nenhum deles apareceria por teste de lote: os três sobrevivem a suíte verde.

## Achado de L02 — contador de não lidas, e a autocorreção que salvou o relatório

Defeito: a vertical do badge de não lidas existe inteira — RPC
`superadmin_chat_unread_total_v2` com grant, `fetchUnreadTotal` no domínio e nas
implementações, `chatUnreadCountLoader` declarado e consumido pelo launcher — e
**nenhum caller em `lib/` fornecia o parâmetro**. Badge sempre zero na rota real.

**L02 corrigiu o próprio achado para MAIOR antes que ele entrasse no consolidado.**
Relatou 3 linhas; ao testar ancorado em `/institutions`, o teste continuou
falhando depois do patch. Contei por conta própria: **58 construções de
`SuperadminShell` em 46 arquivos** de `apps/superadmin/lib/`, a maioria dentro
das próprias páginas de feature. O que foi corrigido são os **três** shells que o
router monta; **onde a página monta o próprio shell, o badge continua zero**.

Se isso tivesse passado, D00 e o Owner leriam "badge corrigido" com o badge
zerado na maior parte do app. Registro corrigido-parcial, não resolvido.

**Evidência empírica, medida e não inferida:** antes do patch, repositório
determinístico reportando 13 não lidas, router montando 2 shells, **ambos** com
`chatUnreadCountLoader == null`. Depois, `chat_unread_badge_wiring_test.dart`
com 3 casos verdes. SHA `df932396`.

**Pendência arquitetural para D00 e o Owner:** a correção geral exige um escopo
herdado que forneça o loader a todas as construções, atinge 46 arquivos e as
sete frentes, inclusive D01–D04. **Proibi L02 de tentar** a uma hora e vinte do
corte: é o lote incompatível com o tempo restante que o contrato manda não
iniciar. Mesma categoria do movimento único de hospedagem.

**Achado de plataforma no mesmo pacote:** `refreshAfterRealtime` não tem caller,
e o diagnóstico de L02 é que assinar `postgres_changes` para o realm interno
**nunca entregaria evento**, porque as policies de chat resolvem por
`current_person_id()` e a identidade interna não tem linha em `people`. Qualquer
certificação futura de Realtime no Chat interno estaria assente num caminho que
não pode funcionar. Broadcast por trigger ou reconciliação periódica é decisão de
plataforma. L02 não implementou nenhum dos dois, corretamente.

**Cobertura negativa medida:** dos 14 métodos de `ChatRepository` e
`NoticeRepository`, exatamente **dois** não têm caller — os dois acima. Os seis
de notices têm caller.

## Achado de L03 — inversão de AUTORIZAÇÃO no Perfil

O mais grave do dia, porque não é composição, é autorização. A aba Circulares do
Perfil recebia o repositório **administrativo**, cujo `listProfile` (l.142)
delega ao diretório do Superadmin, filtrado só por `institutionId` e guardado por
permissão administrativa. **O servidor respondia por permissão administrativa em
vez de por visibilidade do ator no Principal** — a mesma inversão que L03 já
havia achado em Para Você.

Verifiquei: o repositório correto, `SupabaseCircularRepository` em
`features/principal_circulars/data/`, chama `list_visible_profile_circulars`
(l.89) e `get_visible_circular` (l.116), e um grep por `SupabaseCircularRepository(`
em todo `apps/superadmin/lib/` devolve **só a declaração do construtor** — nunca
era instanciado. Corrigido em `bb8a1b546`.

**O ponto que D00 precisa ler:** os testes de L03 passavam nas DUAS
configurações, porque injetam o repositório direto. Defeito de autorização que
sobrevive a teste verde só aparece auditando o caminho de composição. É o
argumento concreto de por que lote verde não é certificação.

L03 introduziu a metade errada ao tirar a rota do fail-closed **e** foi quem
achou e corrigiu antes de qualquer cobrança. As duas coisas ficam registradas.

**Endurecimento de API na mesma frente:** o escopo de audiência de Para Você era
opcional "por compatibilidade" — um argumento esquecido **desligava
silenciosamente o gate de autorização**. Agora é obrigatório, e o teste que
afirmava o caminho sem escopo passou a afirmar o contrário. O teste protegia o buraco.

## Números de L03, com a classificação preservada

Recorte 186 P / 10 F, as 10 sendo golden drift já controlado. Suíte completa do
app, rodada porque mexeu em bootstrap compartilhado: **5104 P / 201 F / 5 S**.
As ~191 falhas fora do recorte ficam **NÃO REVALIDADAS** — nem pré-existentes,
nem dele. Aprovei a classificação: é exatamente o que o contrato manda, e não vou
pressioná-lo a converter em "pré-existente" o que ele não mediu.

## Beco sem paridade confirmado por dois lados

O dock do Perfil manda Momentos para `/principal-moments`, que resolve
incondicionalmente para a tela de indisponível (router l.841). Eu levantei na
varredura própria; L03 chegou pela navegação real. A rota é de L01.
