---
title: "Handoff L01 — Publicações do Principal (E2 R02)"
source: "prompts/L01.md; CONTRATO.md; PRINCIPAL.md; CIRCULARES-PENDENCIAS.md; registro.json; escopo.json"
status: "in-progress"
generated_at: "2026-09-09"
timezone: "America/Sao_Paulo"
---

# L01 — Publicações do Principal

## Identificação

- Rodada: E2-R02-20260909. ID real: L01. Revisão: **r2**.
- Última instrução processada: prompt do Owner em `prompts/L01.md`, incluindo o
  parágrafo acrescentado depois da abertura que manda ler `CIRCULARES-PENDENCIAS.md`.
- Executor: Claude. Modelo ativo registrado: `claude-opus-5[1m]` (Opus 5, contexto 1M),
  esforço médio. Coordenador: L00.
- Data/hora desta revisão: 09/09/2026, tarde, America/Sao_Paulo.

## Worktree, branch, base e remoto (observado)

- Worktree REAL: `C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l01-publicacoes` (existia; criada por D00).
- Branch: `codex/e2-r02-l01-publicacoes`. Baseline na abertura: `6c5567a6`.
- Divergência de registro: `registro.json` traz `observedPreparationHead = fb2d07d3`
  e `baselineSha = null` para L01; a base real das worktrees R02 é `6c5567a6`.
  Não alterei `registro.json` (arquivo de D00).
- Remoto **verificado**: branch publicada em `origin/codex/e2-r02-l01-publicacoes`
  (primeiro push desta rodada, com upstream configurado).
- Publicação de branch **não é** aplicação remota em Supabase/Cloudflare.

### SHAs aptos (nesta ordem)

| SHA | Conteúdo |
| --- | --- |
| `f7b07724` | handoff L01 r1 |
| `63db8ffb` | projeção de conhecimento de Circulares apontada para R2 privado (ADR 0032) |
| `8b83784e` | **`acontece.remove`**: migration, capacidade, projeção do feed e cliente |
| `efa93d1a` | spec037: seção de mídia migrada para a plataforma R2 comum |
| `f42129cd` | 8 testes de widget da retirada do Acontece |

### WIP separado, ainda não commitado

Trabalho de subagentes em andamento na mesma worktree, sob minha responsabilidade,
ainda não apto: Momentos (feed + retirada), Circulares (anexos no cliente),
Circulares (gateway R2), Agora (migration de expiração e teste de repositório).

## Assignment viva

`registro.json` traz `assignmentPath = null` para L01, e o caminho planejado em
`.../e2-r02-l00-coordenacao-claude/.../assignments/L01.md` **não existe** — o diretório
de assignments da worktree de L00 contém apenas D01–D04 e L00. A instrução viva em
vigor é o prompt colado pelo Owner. Não usei cópia antiga de outra branch.

**Delta pedido a L00/D00:** publicar `assignments/L01.md` e preencher `assignmentPath`,
`worktree`, `baselineSha` e `headSha` de L01 em `registro.json`.

## Recorte e denominador

Etapa 2 → apps/superadmin → menu Coelo (Principal) → Acontece, Agora, Momentos e
Circulares, mais o núcleo comum de mídia. 12 action_ids atribuídos em `escopo.json`:
`acontece.feed|create|publish|remove`, `agora.view|create|publish|expire`,
`momentos.view|create|publish|remove`.

Circulares continua **sem IDs próprios** no inventário. O denominador do recorte
permanece **aberto** até D00 reconciliar. Proposta entregue (ver abaixo).

## Feito nesta revisão, por tela/subtela

### Acontece / Feed → Retirar publicação — `acontece.remove` — aceite fechado localmente

Pendência herdada (rastreador integrado, linha 265): "Falta implementação do
comando/affordance dentro da política aprovada"; "Falta RPC correspondente";
"Cliente entregue em fail-closed".

Entregue em `8b83784e` + `f42129cd`:

- **Servidor** (`packages/coelo_database/migrations/20260909133000_happens_post_withdrawal_v1.sql`,
  forward-only): colunas `withdrawn_at`, `withdrawn_by_person_id`, `withdrawal_reason`
  com constraints de coerência; capacidade nova `happens.posts.remove` catalogada em
  `institution_permissions`; RPC `public.withdraw_happens_post(uuid,uuid,bigint,text)`
  `security definer set search_path=''`, só para o autor, sob a capacidade nova e a
  versão otimista, idempotente na segunda chamada, com evento de auditoria
  `post_withdrawn`. A retirada é **soft**: não apaga linha, não apaga mídia, não muda
  retenção — coerente com "não inventar hard delete" e com o fora de escopo da spec050
  sobre alterar regras de retenção.
  `list_visible_happens_posts` foi recriada (mudança de tipo de retorno exige drop+create)
  passando a excluir retiradas e a projetar `post_id`, `management_version` e
  `can_withdraw`. Grants originais reaplicados; nada ganhou grant mais amplo.
- **Cliente**: contrato segregado `PrincipalHappensPostWithdrawal` (quem só lê o feed
  não precisa implementá-lo, e a ausência do contrato mantém a ação fechada em vez de
  aparente); implementação no repositório Supabase com mapeamento de negação, conflito
  de versão e indisponibilidade; o controle de opções da publicação vira menu com
  "Retirar publicação" **apenas** quando o servidor marcou o post como retirável.
  Confirmação, envio, sucesso, negado, conflito e falha são estados distintos; o
  sucesso só aparece depois da resposta autorizada e o feed é **relido**, não corrigido
  em memória.
- Composição: nenhuma alteração no router foi necessária. O repositório produtivo já
  injetado passa a satisfazer os dois contratos, então a ação chega à rota real sem
  consumir reserva de arquivo compartilhado.

**Limite honesto:** isto fecha FE e o contrato de BE **localmente**. Não é E2E: a
migration não foi aplicada em Supabase remoto (produção, sem autorização nominal).

### Circulares — fontes corrigidas

`63db8ffb` e `efa93d1a` movem a projeção de conhecimento e a seção de mídia da spec037
do bucket privado Supabase da ADR 0027 para o R2 privado da ADR 0032, preservando a
legibilidade dos ativos legados. Conforme `CIRCULARES-PENDENCIAS.md`, a escolha do
provedor já estava decidida e não voltou ao Owner.

### Circulares — proposta de mapeamento para D00

`docs/reviews/etapa-2-operacao/next-round/R02-20260909/propostas/L01-circulares-mapeamento.md`
(WIP, não commitado ainda). Propõe 12 ações (`circulars.list`, `.filter`, `.create`,
`.edit`, `.detail`, `.schedule`, `.publish`, `.close`, `.delete`, `.respond`, `.attach`,
`.happens-card`), mais 3 candidatos de estado apresentados à parte e não somados.
Efeito no denominador: L01 de 12 para 24; global de 219 para 231. **Isso é cobertura de
inventário que faltava, não regressão de implementação.** A aba Circulares do Perfil
continua em `principal.profile-view` (L03), sem contagem duplicada.

## Testes

Escopo: `apps/superadmin`, runner `flutter test` 3.44.2 / Dart 3.12.2, base = working
tree desta worktree (há WIP de subagentes; não atribuo prova a um HEAD limpo).

| Lote | P | F | B | S | U |
| --- | --- | --- | --- | --- | --- |
| `test/features/principal_happens/` (com as mudanças) | 74 | 10 | 0 | 0 | 0 |
| `test/features/principal_happens/presentation/principal_happens_withdrawal_test.dart` | 8 | 0 | 0 | 0 | 0 |

Taxa aprovada do lote do Acontece: 74/84 = 88,10%. Taxa falha: 10/84 = 11,90%.
Execução: 84/84 = 100%. Não somo reruns nem lotes sobrepostos: o lote menor está
contido no maior e é reportado só para identificar a cobertura nova.

**Classificação das 10 falhas: preexistentes, não introduzidas por L01.** Base
comparável obtida por acidente útil — um subagente executou `git stash` e um lote
rodou contra o HEAD sem minhas mudanças, resultando em **66 P / 10 F**, exatamente as
mesmas 10 falhas de golden. Com as mudanças o lote vai a 74 P / 10 F: mais 8
aprovados, zero regressão. São falhas de golden por deriva de renderização do host,
coerentes com o histórico de goldens da R01; não são falha de produto desta rodada e
não foram revalidadas contra o ambiente que gerou as imagens aprovadas.

`dart analyze lib/features/principal_happens`: sem problemas.

Lotes de Momentos, Circulares e Agora ficam para a próxima revisão, quando os
subagentes fecharem seus arquivos.

## FE / BE / E2E

- FE concluído nesta revisão: **1** (`acontece.remove`) / 12 aplicáveis do recorte atual.
- BE concluído nesta revisão: **1 contrato local** (`acontece.remove`) / 12 aplicáveis.
- E2E certificado: **0**. Nenhuma ação atravessou aplicação remota, persistência real e
  releitura autenticada. Ponta a ponta permanece **não calculável** para o recorte
  enquanto não houver autorização nominal para o pacote remoto.
- Circulares: **não calculável** até a reconciliação do inventário por D00.

## Incidente de worktree (registrado, resolvido)

Às ~13:04 um subagente executou `git stash` na worktree compartilhada, recolhendo o
trabalho não commitado de todas as frentes. Recuperado integralmente com `git stash pop`;
nada foi perdido. Em seguida, o arquivo compartilhado
`apps/superadmin/lib/features/principal_circulars/domain/circular_repository.dart`
apareceu com zero bytes, o que apagava `CircularSummary`, `CircularUnauthorized`,
`CircularScope`, `CircularCursor` e `PrincipalCursorPage` e quebrava a compilação de
todo o app. Restaurei a versão com as alterações do subagente a partir do commit de
stash (`6cce040c`), preservando o trabalho dele em vez de voltar ao HEAD. Os subagentes
foram instruídos nominalmente a não executar comando git nesta worktree.

## Dependências e reservas

- Solicitada a L00/D00: publicar `assignments/L01.md` e completar o registro de L01.
- Solicitada a D00: reconciliar o mapeamento de Circulares no inventário e nas três
  matrizes, publicando denominador anterior e novo com o motivo.
- **Nenhuma reserva de arquivo compartilhado foi consumida até aqui.** Não alterei
  `superadmin_router.dart`, `superadmin_routes.dart`, `main.dart`,
  `superadmin_auth_scope.dart`, shell, navegação ou barrels.
- **Reserva que será pedida:** a rota produtiva `/principal-moments` hoje resolve
  sempre para a composição indisponível; ligar o feed de Momentos exige uma alteração
  pontual no router. O repositório será entregue pronto para injeção.
- Migrations e aplicação remota permanecem serializadas por D00.

## Contratos publicados para consumidores

- `PrincipalHappensPostWithdrawal` é aditivo e segregado: L02, L03 e demais
  consumidores do feed do Acontece não precisam mudar nada.
- `PrincipalPostPreviewItem` ganhou `postId`, `managementVersion` e `canWithdraw`, todos
  opcionais com padrão fechado; nenhum construtor existente quebrou.
- Núcleo comum de mídia: os contratos Dart de leitura e upload em `packages/coelo_api`
  (`MediaReader`, `SessionMediaReader`, `MediaSession`, `MediaUploadGateway`,
  `SessionMediaUploader` e seus DTOs) **não foram alterados** e permanecem estáveis
  para L02/L03/D02/D04.

## Primeiro critério aberto

Persistência e releitura reais das publicações do Acontece em ambiente autenticado —
depende do pacote remoto nominal e da autorização correspondente, que é decisão do
Owner mediada por D00.

## Próximo passo

Consolidar os quatro pacotes de migration da rodada num pacote nominal revisável para
D00 serializar; fechar os aceites de Momentos e Circulares assim que os subagentes
devolverem os arquivos; repetir apenas os lotes afetados.

## Recursos e processos ativos

Três subagentes ativos no fim desta revisão: Momentos (feed e retirada), Circulares
(gateway R2) e verificação local da migration do Acontece em Postgres descartável.
Nenhum processo remoto, nenhum deploy, nenhum agendamento criado.

## Limites de continuidade

Mecanismo real: turno ativo desta sessão, com notificação de conclusão de subagente e
mensagens diretas para eles. Não há agendador nativo confirmado que acorde a sessão
depois do fim do turno; Markdown não acorda sessão. Nada foi agendado. Os cortes de
16:00, 16:45 e 17:15 são respeitados dentro do turno.
