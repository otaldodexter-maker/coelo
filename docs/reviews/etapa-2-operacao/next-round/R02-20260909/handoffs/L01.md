---
title: "Handoff L01 — Publicações do Principal (E2 R02)"
source: "prompts/L01.md; CONTRATO.md; PRINCIPAL.md; registro.json; escopo.json"
status: "in-progress"
generated_at: "2026-09-09"
timezone: "America/Sao_Paulo"
---

# L01 — Publicações do Principal

## Identificação

- Rodada: E2-R02-20260909.
- ID real: L01. Revisão: r1.
- Última instrução processada: prompt colado pelo Owner (prompts/L01.md), 09/09/2026 12:46 (America/Sao_Paulo).
- Executor: Claude. Modelo ativo registrado: `claude-opus-5[1m]` (Opus 5, contexto 1M). Esforço médio.
- Coordenador: L00.

## Worktree, branch e base (observado)

- Worktree REAL confirmada: `C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l01-publicacoes` (existe; criada por D00).
- Branch: `codex/e2-r02-l01-publicacoes`.
- Baseline/HEAD observado na abertura: `6c5567a6` ("docs(e2): start R02 D00 and publish execution assignments").
- Divergência registrada: `registro.json` traz `observedPreparationHead = fb2d07d34a74c91ec1f65aff361048393e590729`
  e `baselineSha = null` para L01. A base real das worktrees R02 é `6c5567a6`, um commit à frente.
  Não alterei `registro.json` (arquivo de D00). Delta proposto abaixo.
- Working tree limpa na abertura (`git status --short` sem saída).
- Remoto: branch ainda não publicada nesta revisão.

## Assignment viva

- `registro.json` traz `assignmentPath = null` para L01 e o caminho planejado
  `.../e2-r02-l00-coordenacao-claude/.../assignments/L01.md` **não existe** (o diretório de assignments
  da worktree L00 contém apenas D01–D04 e L00).
- Portanto a instrução viva em vigor é o prompt colado pelo Owner. Não usei cópia antiga de outra branch.
- Delta solicitado a L00/D00: publicar `assignments/L01.md` e preencher `assignmentPath`,
  `worktree`, `baselineSha` e `headSha` de L01 em `registro.json`.

## Recorte

Etapa 2 → apps/superadmin → menu Coelo (Principal) → telas Acontece, Agora, Momentos e Circulares,
com feeds/diretórios, publicadores, estados, detalhes/viewers e ações aprovadas, mais o núcleo comum
de mídia (catálogo/gateway R2, Stream nos limites da ADR 0032) do qual L01 é responsável técnico.

IDs atribuídos em `escopo.json` (12, todos `mvp`, todos `e2eActiveApplicable`, todos
`pending-verification` nos três eixos na fotografia inicial):

`acontece.feed`, `acontece.create`, `acontece.publish`, `acontece.remove`,
`agora.view`, `agora.create`, `agora.publish`, `agora.expire`,
`momentos.view`, `momentos.create`, `momentos.publish`, `momentos.remove`.

Circulares não possui IDs próprios no inventário (`coverageGaps` de `escopo.json`).
O denominador do recorte permanece **aberto** até D00 reconciliar a proposta de mapeamento
da spec037. Não vou certificar o contexto inteiro pelos 12 IDs existentes.

## Pendências conhecidas herdadas (fonte: coelo-flutter-integrado-supabase-pendencias.md, linhas 262–273)

- `acontece.remove`: falta implementação do comando/affordance de retirada dentro da política aprovada;
  falta RPC correspondente. Cliente entregue em fail-closed.
- `momentos.remove`: mesma lacuna (comando/affordance + RPC); remover mídia de rascunho não equivale a
  remover publicação; não inventar hard delete.
- `agora.expire`: expiração automática de 24 h existe como filtro temporal em SQL, mas o enum `expired`
  não transiciona; falta separar filtro temporal de mudança material de estado e decidir a retirada manual.
- `momentos.view`: RPC de feed não localizada; candidato `list_visible_moments` com cursor e negativas;
  adapter de feed pendente.
- `momentos.create`/`publish`: consumidor pode reter cinco originais de 25 MB; purga/buffer é dívida da frente.
- Todos: gateway/catálogo de mídia, sessão/audiência reais, R2 master e negativas/reload por provar.

## Estado atual

Reconhecimento em curso com subagentes de leitura por contexto (Acontece, Agora, Momentos,
Circulares/mapeamento, núcleo de mídia). Nenhuma alteração de código ainda nesta revisão.

## Testes

P/F/B/S/U: 0/0/0/0/0 nesta revisão. Nenhum lote executado ainda.

## FE/BE/E2E

Não calculável nesta revisão: base de verificação própria ainda não produzida;
denominador de Circulares aberto.

## Dependências

- Solicitada a L00/D00: publicação da assignment L01 e preenchimento do registro (acima).
- Solicitada a D00: reconciliação do mapeamento de Circulares no inventário e nas três matrizes.
- Reserva pontual será solicitada antes de qualquer alteração em rotas/bootstrap/contratos comuns.

## Primeiro critério aberto

`acontece.remove` — comando de retirada de publicação com contrato de servidor, negativas e feed
atualizado após releitura.

## Próximo passo

Consolidar o reconhecimento, fixar o aceite de `acontece.remove` e iniciar a implementação
cliente + contrato de servidor local, sem aplicação remota.

## Recursos e processos ativos

Cinco subagentes de leitura (Acontece, Agora, Momentos, Circulares, mídia). Nenhum build,
teste pesado, container ou processo remoto ativo.

## Limites de continuidade

Mecanismo real: turno ativo desta sessão. Sem agendador nativo confirmado que acorde a sessão
após o fim do turno. Markdown não acorda sessão. Cortes de 16:00 / 16:45 respeitados dentro do turno.
