---
title: "Handoff L01 — Publicações do Principal (E2 R02)"
source: "assignments/L01.md (L00 rev.1 e rev.3); CONTRATO.md; PRINCIPAL.md; CIRCULARES-PENDENCIAS.md; prompts/L01.md; escopo.json"
status: "checkpoint-1600"
generated_at: "2026-09-09"
timezone: "America/Sao_Paulo"
---

# L01 — Publicações do Principal

## Identificação

- Rodada: E2-R02-20260909. ID real: L01. Revisão: **r3**, snapshot do corte das 16:00.
- Última instrução processada: assignment L00 revisão 3 (procedimento de descoberta de
  identidade após reinício) e a mensagem de L00 que transforma a hospedagem de rotas em
  movimento único coordenado por D00.
- Executor: Claude. Modelo ativo registrado: `claude-opus-5[1m]` (Opus 5, contexto 1M),
  esforço médio. Coordenador: L00, sessão `coelo-ed`; esta sessão é `coelo-0f`.

## Worktree, branch, base e remoto

- Worktree REAL: `C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l01-publicacoes`.
- Branch: `codex/e2-r02-l01-publicacoes`. Base observada na abertura: `6c5567a6`.
- HEAD no instante em que o conteúdo técnico deste snapshot foi fechado: `aec0c4afe`,
  com `origin/codex/e2-r02-l01-publicacoes` no mesmo SHA. Os commits que publicam e
  corrigem este próprio handoff vêm depois dele, então a ponta da branch está à frente
  por eles — um handoff não consegue registrar o SHA que o carrega. Nada apto ficou sem
  publicar: verifique a ponta da branch, não este número.
- 21 commits meus sobre a base até este ponto, mais `56eb3f19d`, que veio na base
  compartilhada.
- Árvore limpa, exceto os três arquivos de fonte compartilhada abaixo.

### WIP separado, deliberado

`specs/050-principal-ui-ux-closure.md`,
`.agents/skills/coelo-ui/references/principal-visual-surfaces.md` e
`docs/reviews/etapa-2-operacao/next-round/R02-20260909/PRINCIPAL.md` estão modificados e
**não commitados de propósito**: são fontes compartilhadas pelas três frentes Claude e
L00 determinou nominalmente que é ele quem as commita, já feito em `03969969a` na worktree
dele. Preservados e intocados, sem commit e sem descarte.

## Incidentes de ambiente, registrados e resolvidos

1. **`git stash` em worktree compartilhada.** Um subagente meu executou `git stash` uma
   vez para obter linha de base de goldens, recolhendo o trabalho não commitado das
   quatro frentes que eu tinha em paralelo. Recuperei tudo com `git stash pop`; nada se
   perdeu. Proibi nominalmente comandos git a todos os subagentes seguintes.
   Efeito colateral útil: virou base de comparação real e provou que as falhas de golden
   do Acontece são preexistentes.
2. **Arquivo de domínio esvaziado.** `principal_circulars/domain/circular_repository.dart`
   apareceu com zero bytes, apagando `CircularSummary`, `CircularUnauthorized`,
   `CircularScope`, `CircularCursor` e `PrincipalCursorPage`, o que derrubou a compilação
   de todo o app. Restaurei a versão COM as alterações do subagente a partir do commit de
   stash `6cce040c`, preservando o trabalho dele em vez de voltar ao HEAD.
3. **Reinício do notebook às 14:03.** Corrompeu o índice git desta worktree
   (`bad signature 0x00000000`). Preservei o índice corrompido e reconstruí apenas o
   índice a partir do HEAD com `git reset`, sem tocar na árvore de trabalho. Nenhum
   commit e nenhuma alteração se perderam. Também removi um container Postgres órfão meu
   (`coelo-l01-pgtap`). Dois subagentes foram interrompidos pelo reinício; ambos tinham
   deixado trabalho completo em disco, que verifiquei e integrei.
4. **Escrita cruzada entre worktrees.** `specs/037`, `specs/050` e a referência da skill
   apareceram como modificação não commitada nas quatro worktrees Claude ao mesmo tempo e
   limpas nas Codex e na raiz. Confirmei que `.agents` e `.agents/skills` NÃO são junctions
   em nenhuma delas, então não é diretório compartilhado. Depois esclarecido: era
   materialização declarada do commit `eaab2df13` de D00.

## Recorte e denominador

O recorte nasceu com 12 action_ids MVP atribuídos e Circulares sem IDs próprios.
D00 aceitou a proposta de decomposição, então o denominador **fechou em 23 IDs MVP**:
os 12 originais mais 11 de `circulars`. `circulars.happens-card` não ganhou ID próprio;
é subaceite obrigatório de `acontece.feed`. Todos os números deste handoff usam 23.

## Feito, por tela e subtela

### Acontece / Remover — `acontece.remove`

Contrato de servidor inexistente até esta rodada. Entregue: colunas de retirada,
capacidade `happens.posts.remove`, RPC `withdraw_happens_post` e projeção do feed com
`post_id`, `management_version` e `can_withdraw`. Retirada **soft**: não apaga linha,
não apaga mídia, não muda retenção. Cliente com contrato segregado, afordância só quando
o servidor autoriza, e releitura do feed em vez de correção em memória.

**Três defeitos reais encontrados pelo coordenador e corrigidos** (`9cf2d908a`):
bypass do lock otimista, porque `<>` com versão nula devolvia NULL e escapava da guarda;
afordância falsa, porque `can_withdraw` projetava autoria sem conferir a capacidade;
e decisão obsoleta, porque o cliente só reconferia `mounted` depois do diálogo e uma
troca de contexto podia enviar identidade e versão de outro escopo.

**A recusa do coordenador estava certa e vale registrar por quê:** a suíte que eu tinha
escrito era quase toda `pg_get_functiondef` e substring, e teria passado verde com os três
defeitos intactos. Assertiva de catálogo não prova comportamento.

### Agora / Expirar — `agora.expire`

O enum já tinha `expired`, mas nada nunca chegava lá: a expiração existia só como filtro
de leitura, então o valor era inalcançável. Entregue a transição material com varredura
`for update skip locked`, auditoria por publicação, e as duas funções de leitura com
assinatura preservada. Sem apagar linha, sem tocar mídia, sem mudar retenção.

### Momentos / Visualizar e Remover — `momentos.view`, `momentos.remove`

Momentos não tinha RPC de feed nem repositório em `lib/`: o viewer só conseguia mostrar
fixture. Entregues `list_visible_moments` com cursor e identificadores opacos de mídia,
`withdraw_moment` espelhando o contrato do Acontece, e o repositório Supabase cobrindo
leitura e retirada na mesma instância.

### Momentos / Criar e Publicar — porta de mídia

A rota produtiva não fornecia picker nenhum, e o controller exige ao menos uma mídia:
`/principal-moments/publish` **não conseguia publicar nada**. Entregue a porta com estados
distintos. Capa e vídeo seguem indisponíveis **de propósito**, com mensagem honesta: não
existe editor de crop, e o gateway exige duração medida que um seletor de arquivo não dá.

### Circulares

Anexos passaram a ser selecionáveis e legíveis nas rotas reais: `onPickFiles` era um aviso
de indisponibilidade e o tile de anexo era um retângulo por índice, sem identidade nem
abertura autorizada. Mídia nova migrada para R2 privado conforme ADR 0032, com o gateway
reescrito sobre o transporte compartilhado, presigned PUT de 300 s e GET de 120 s, e MIME
real conferido pelos bytes no finalize. Ativos legados continuam legíveis pelo ramo antigo,
sem backfill e sem remoção. Fontes corrigidas: spec037 e a projeção de conhecimento.

### Núcleo comum de mídia

Contratos Dart de `packages/coelo_api` **inalterados** e estáveis para L02, L03, D02 e D04.
Entregue a L02 a forma de `chat-attachment` no catálogo privado e o contrato de ticket
descrito a partir do que já existe. Corrigi, junto com L02 e L00, a afirmação de que isso
desbloqueava `chat.attach`: **não desbloqueia**, porque faltam a RPC de staging/commit e a
Edge Function `chat-media`, e a migration não concede grant nenhum. É contrato revisável,
que é progresso real, não desbloqueio.

### Decisão do Owner sobre o shell

Implementada a metade da página (`421ff4e84`): os viewers deixam de reaplicar os insets
que o host já consumiu, o que era bug real no desktop. A metade estrutural **não é minha
para fazer sozinho**: as rotas do Principal são `GoRoute` de topo, irmãs da `ShellRoute`,
então o shell nunca é construído para elas. Mover só as minhas fragmentaria o bloco e o
menu sumiria ao navegar entre telas irmãs. Cheguei a despachar o movimento e o
**interrompi** quando L00 decidiu tratá-lo como movimento único de D00; o router e seus
testes ficaram **intactos**, confirmado por diff vazio. Hunks exatos entregues.

## Verificação do eixo FE dos IDs já implementados

L00 apontou, com razão, que os seis IDs implementados antes desta rodada podiam ser
verificados sem depender de nenhuma decisão pendente. A regra que segui: **leitura de
código não é verificação**. Só conta o que a rota real exerce.

O método veio do defeito que eu mesmo tinha deixado passar hoje — o repasse ausente do
repositório de mídia na leitura de Circular, que fazia todo anexo cair fechado na tela
enquanto o código e os testes existiam. Trabalho commitado e reportado não é trabalho que
funciona na tela.

### Varredura da classe de defeito

Comparei o que a composição autorizada instancia com o que os builders do router
realmente usam, para as minhas dez superfícies. Resultado: dos parâmetros de repositório
do Principal, **todos têm parâmetro mais ao menos um uso, exceto `principalMixedFeedRepository`,
que aparece exatamente uma vez — só a declaração, nenhum uso.** L00 chegou ao mesmo número
de forma independente. E `PrincipalMomentsFeedRepository` **não existe em `app/` nem em
`core/`**: o feed de Momentos que entreguei não está sequer plumbado.

### Veredito por ação

| action_id | Veredito | Evidência |
| --- | --- | --- |
| `acontece.feed` | **Parcial, com defeito registrado** | O feed de publicações é verificado pela rota real em `principal_real_route_test.dart`: contexto autenticado, escopo derivado do runtime, sem fixture demo. Mas a projeção de Circulares nunca chega — e D00 tornou `circulars.happens-card` subaceite obrigatório deste ID, então ele **não fecha**. |
| `acontece.create` | **Verificado para escopo com turma** | `principal_happens_preview_route_test.dart` compõe o publicador na superfície Principal. Divergência registrada para escopo institucional. |
| `acontece.publish` | **Verificado para escopo com turma** | Mesma evidência. Divergência e defeito de tela registrados. |
| `agora.view` | **Dois defeitos registrados** | A rota funciona e é **inalcançável pela navegação real**: a Acontece real é montada com `PrincipalHappensPreviewData.empty`, cujo `nowItems` é vazio, então nenhum card que abre o Agora é construído — e esse callback é a única referência à rota no app. Segundo defeito: o retorno contextual usa `go` em vez de `pop`, remontando a origem. |
| `agora.create` | **Verificado com evidência** | `principal_now_real_route_test.dart`: compositor montado com contexto derivado do runtime, sem contexto demo, fail-closed sem repositório e sem turma. |
| `agora.publish` | **Verificado com evidência** | Publicação pelo compositor real envia o escopo do servidor, não re-envia ativo já remoto, e retorna ao viewer com o feed relido. |

### Achados fixados em teste executável

`apps/superadmin/test/app/router/principal_happens_composition_gaps_test.dart`,
commits `167cbd584` e `02072307c`. Três casos, todos exercendo a rota real. Os três
**documentam o comportamento de hoje e devem ser invertidos, não apagados**, quando a
composição for corrigida — a correção vive no router, que pertence ao movimento único.

1. **Defeito.** `principalMixedFeedRepository` é instanciado na composição autorizada,
   chega ao parâmetro do router e o builder de `/principal-happens` nunca o repassa. O
   teste injeta um repositório que grava as chamadas e prova que a página recebe nulo e
   que ele nunca é consultado. Deixa o aceite da spec050 de que Circulares aparece em
   Coelo (Principal) por cumprir nesta superfície.
2. **Divergência, não defeito.** Publicar exige unidade e turma não nulas, então um ator
   de escopo institucional não compõe o publicador, mesmo com o repositório fornecido,
   enquanto o esquema aceita publicação institucional com os dois nulos. Se o
   estreitamento é intencional é decisão de produto; não a tomei.
3. **Defeito, independente da decisão acima.** Recusa de escopo e falha de configuração
   devolvem a MESMA `SuperadminErrorScreen` de indisponibilidade. O ator não distingue
   "você não publica neste escopo" de "o aplicativo está quebrado", e o produto afirma
   uma causa falsa. O teste dirige os dois caminhos pela rota real e prova que hoje o
   resultado é idêntico.

### Por que isso não estava verificado antes

Todos os testes de rota do Agora que existiam exercitavam `/dev/`. O único que tocava a
rota real de publicação **só afirmava a URL**, rodando com repositório nulo — passava
verde enquanto a tela renderizava indisponível. Era teste de path, não de composição.

### Observação de backend, calibrada

`save_now_draft` insere qualquer `audience_kind` enviado pelo cliente sem conferir se o
autor pode endereçar aquele público; `now_audience_matches_role` só é usada na leitura.
Conferi antes de classificar: o autor já é autorizado no escopo pela capacidade e as
linhas carregam instituição, unidade e turma da publicação, então **não há vazamento
cross-tenant e não consigo apontar regra aprovada violada**. Não é defeito de segurança.
O que afirmo é mais estreito: o estreitamento no cliente **não é o controle**, e se existir
política sobre quais públicos cada papel endereça, ela precisa ser imposta na escrita, no
servidor, antes de as audiências serem abertas na interface. Não inventei a regra.

### Rota sem saída, confirmada por três lados

`/principal-moments` resolve **incondicionalmente** para a composição indisponível: não é
fail-closed por repositório nulo, é destino fixo no builder. Levantado por mim, por L00 na
varredura dele e por L03 pela navegação real do dock do Perfil. Corrigir exige mover a
rota **e** a cadeia completa por auth scope, app e main — o que aumenta o tamanho do
movimento único, e D00 precisa saber disso antes de montá-lo.

## Testes

Runner `flutter test` 3.44.2 / Dart 3.12.2 e pgTAP em Postgres 17.6 descartável.
Base: working tree desta worktree em `aec0c4afe`.

| Lote | P | F | B | S | U |
| --- | --- | --- | --- | --- | --- |
| `principal_now` + `principal_moments` + `principal_circulars` | 216 | 11 | 0 | 0 | 0 |
| `principal_happens` | 74 | 10 | 0 | 0 | 0 |
| `principal_moments_publication` | 88 | 0 | 0 | 0 | 0 |
| `principal_circulars` + `circulars` | 151 | 2 | 0 | 0 | 0 |
| pgTAP `happens_post_withdrawal` (comportamental) | 32 | 0 | 0 | 0 | 0 |
| pgTAP `happens_publication` (contrato existente) | 53 | 0 | 0 | 0 | 0 |
| pgTAP `now_publication_expiry_transition` | 16 | 0 | 0 | 0 | 0 |
| pgTAP `moments_feed_and_withdrawal` | 23 | 0 | 0 | 0 | 0 |
| pgTAP `circulars_media_private_r2_v1` | 46 | 0 | 0 | 0 | 0 |
| pgTAP `circulars_production` (regressão) | 60 | 0 | 0 | 0 | 0 |
| pgTAP `private_media_catalog_chat_kind_v1` | 50 | 0 | 0 | 0 | 0 |
| Deno `circular-media` | 27 | 0 | 0 | 0 | 0 |

`dart analyze lib` em `apps/superadmin`: limpo.

### Lote `test/app/router/`

279 aprovados, 1 ignorado, 30 falhos. **Nenhuma das 30 é minha e nenhuma é de rota do
Principal.** Estão em 15 arquivos de outros domínios: `persistent_shell_routes` (9, sobre
o rodapé e a navegação de formulário de Atividades), `institution_directory_routes` (6),
`unit_routes` (2), `forms_composition_sanitization_source` (2), e mais nove com uma cada,
em Pessoas, Segurança, Refeições, Importação, Rotina diária, Assiduidade e Avaliações.

Classificação com evidência, não por suposição: o meu diff desta rodada em
`apps/superadmin/lib` toca **22 arquivos, todos dentro de cinco diretórios de feature do
Principal e de Circulares**, e **zero arquivos em `apps/superadmin/lib/app/`** — nenhum
router, shell, navegação ou barrel. Nenhuma dessas telas está no meu conjunto alterado.
Conferi ainda a mensagem de falha de duas delas e são de Atividades e de Assiduidade.

**Não somo lotes sobrepostos.** Os lotes de widget se cruzam entre si; cada linha é uma
execução distinta e o total não é uma soma.

### Classificação das falhas — todas preexistentes, nenhuma regressão

23 falhas únicas, todas de golden, nenhuma introduzida por L01:

- 11 em `principal_moments_preview_golden_test.dart`: diff de 99,53% a 100,00% dos
  pixels, medido antes de qualquer edição e confirmado duas vezes com as alterações fora
  da árvore. **Causa não estabelecida** — ver o estreitamento 2 acima; o sprite existe e
  está declarado, e nenhum erro de carregamento de asset é levantado.
- 10 em `principal_happens_preview_golden_test.dart`: deriva de renderização deste host.
  Provado por acaso útil — o `git stash` de um subagente fez um lote rodar contra o HEAD
  sem minhas mudanças, dando 66 P / 10 F, exatamente as mesmas 10 falhas; com as mudanças
  vai a 74 P / 10 F.
- 2 em `circular_directory_golden_test.dart`: deriva do host, em arquivo que não toquei.

L02 chegou à mesma conclusão independentemente para notices e chat, revertendo para a base
limpa `6c5567a6`.

### Goldens, lista nominal

Regenerados, três, todos com causa material declarada, em `dcda0be15`:
`circular_reader_contextual_light_375`, `circular_reader_contextual_dark_375` e
`circular_reader_contextual_light_375_text_200`. Causa: o tile de anexo deixou de ser
retângulo genérico por índice e passou a mostrar identidade real e estado de leitura
autorizada.

Pela decisão do shell, **zero regenerados**, e nenhum precisava: a mudança é gated por
`embedded` com default `false`, que todos os goldens existentes usam, então os pixels são
idênticos — confirmado por execução. Nenhuma imagem aprovada foi substituída sem causa.

## Proveniência das provas, e duas afirmações minhas que estreitei

Trabalhei com subagentes e reportei números vindos deles. Antes do corte auditei quais
afirmações eu havia verificado **eu mesmo** e quais eu repassei. Achei duas que precisaram
ser estreitadas. Registro as duas, porque estreitar afirmação vale mais que somar achado.

### Estreitamento 1 — alcançabilidade da rota do Agora

Reportei que `/principal-now` era inalcançável pela navegação real e que `onOpenNow` era a
única referência à rota no app. **Falso.** O menu do hospedeiro expõe o destino
`principal-now` em `app/navigation/superadmin_navigation.dart:176` e o router tem dois
despachantes que o convertem em navegação. O subagente concluiu isso por busca que não
pegou os despachantes, e eu repassei sem reconferir. Corrigido dentro do próprio arquivo
de teste em `36ba21db5`, para ninguém reconstruir a afirmação larga a partir dele.
O que sobrevive é real: a Acontece nunca lê o repositório do Agora, então o carrossel
exigido pela spec050 aparece vazio e a entrada em contexto não existe.

### Estreitamento 2 — causa das 11 falhas de golden de Momentos

Reportei que falhavam porque o sprite `assets/principal_moments/moments-strip.png` não
resolve no ambiente de teste. **A causa não está estabelecida.** Conferi: o arquivo
existe, tem 2,4 MB e está declarado em `pubspec.yaml:58`. A execução não levanta erro de
carregamento de asset; o que há é diferença de 99,53% a 100,00% dos pixels, ou seja, a
imagem renderizada é praticamente outra. Isso é compatível com a mídia não pintar no
harness, mas **é hipótese, não fato provado**, e como estava escrito levava o leitor a
concluir que o arquivo está faltando, o que é falso.

O que permanece verificado sobre elas: **são preexistentes**, medidas duas vezes de forma
independente com as nossas alterações fora da árvore, dando 0 aprovados e 11 falhos; e
regenerá-las gravaria o que quer que este ambiente renderize como referência aprovada,
que é justamente o que não se deve fazer.

### O que eu executei, e o que um subagente executou

| Prova | Quem executou |
| --- | --- |
| `principal_happens` 74/10, e a base 66/10 sem as mudanças | eu |
| retirada do Acontece, 9 testes de widget | eu |
| `principal_now` + `principal_moments` + `principal_circulars` 216/11 | eu |
| `principal_circulars` + `circulars` 156/2 | eu |
| `principal_moments_publication` 88/0 | eu, reexecutado nesta auditoria |
| `test/app/router/` 288/1/30 e a classificação das 30 | eu |
| rota real do Agora, 9 testes | eu, reexecutado após revisar o arquivo |
| composição do Acontece, 3 testes | eu |
| **pgTAP 32/32 da retirada do Acontece** | **eu**, reexecutado nesta auditoria em Postgres 17.6 descartável, container removido; `1..32`, 32 `ok`, 0 `not ok`, sem linha `# Looks like`. O replay deu **93 aplicadas e 77 falhas**, o mesmo número do subagente, e nenhuma migration do Acontece está entre as falhas |
| **pgTAP 53/53, 16/16, 23/23, 46/46, 60/60, 50/50** | **subagentes desta sessão**, em Postgres descartável; **não reexecutados por mim** |
| **Deno `circular-media` 27/0** | **subagente desta sessão**; não reexecutado por mim |
| **replay parcial de migrations, 122 de 168 e 93 de 170** | **subagentes**; não reexecutado por mim |

O 32/32 é o número que solta a retenção do lote, então foi o que escolhi reexecutar
eu mesmo: reconstruí os shims de `auth` e `storage`, repliquei as 170 migrations uma por
transação, confirmei que `withdraw_happens_post`, `list_visible_happens_posts` e a
capacidade `happens.posts.remove` existem depois do replay, e rodei o teste. Confere com
o relatado.

Os demais números de banco e de Deno foram produzidos nesta sessão, sobre esta base, com relato
item a item — não são resultado histórico. Mas não são meus, e o contrato pede distinguir.
Quem for certificar deve reexecutá-los no perfil nominal de replay, como o pacote remoto
já pede.

## FE / BE / E2E

### Denominador vigente

D00 aceitou a proposta de mapeamento de Circulares, então o denominador do contexto L01
**deixou de ser 12 e passou a ser 23 IDs MVP**: os 12 originais das famílias `acontece`,
`agora` e `momentos`, mais 11 novos de `circulars`. `circulars.happens-card` NÃO entra como
ID próprio: virou subaceite obrigatório de `acontece.feed`. Pelo meu registro os 11 são
`circulars.list`, `.filter`, `.create`, `.edit`, `.detail`, `.schedule`, `.publish`,
`.close`, `.delete`, `.respond` e `.attach`; a lista que governa é a de D00.

Denominador anterior 12, novo 23, motivo: cobertura de inventário que faltava. **Isto não
é regressão de implementação** — nenhuma ação foi perdida; ações que existiam sem ID
passaram a ser contáveis.

### Números, com numerador sobre denominador

| Eixo | Numerador | Denominador | Leitura |
| --- | --- | --- | --- |
| Implementação fechada por L01 nesta rodada | **6** | 23 | trabalho novo desta rodada |
| Contrato de servidor entregue localmente | **6** | 23 | local, sem aplicação remota |
| **E2E certificado** | **0** | **23** | nenhuma ação atravessou o circuito completo |

Os 6 IDs com implementação fechada nesta rodada: `acontece.remove`, `momentos.view`,
`momentos.remove`, `momentos.create`, `momentos.publish` e `circulars.attach`.

Os 6 contratos de servidor entregues: `withdraw_happens_post` mais a projeção do feed
direto e do misto (`acontece.remove`), a expiração material do Agora (`agora.expire`),
`list_visible_moments` (`momentos.view`), `withdraw_moment` (`momentos.remove`),
Circulares em R2 (`circulars.attach`) e a forma de `chat-attachment` no catálogo comum,
que é do núcleo de mídia e não tem ID nesta frente.

### O que NÃO estou contando, de propósito

Os 17 IDs restantes continuam exatamente como na fotografia inicial:
`pending-verification` nos três eixos. Entre eles estão `acontece.feed`, `acontece.create`,
`acontece.publish`, `agora.view`, `agora.create` e `agora.publish`, que **já tinham
implementação antes desta rodada**. Não os declaro concluídos porque eu não os verifiquei:
a pendência deles é de verificação, não de implementação, e essa distinção precisa
sobreviver ao consolidado.

Para a leitura por família, dentro do denominador de 23: `acontece` 1 de 4 fechado nesta
rodada, `agora` 1 de 4 no eixo de backend, `momentos` 4 de 4, `circulars` 1 de 11.

**E2E é 0 em todas as leituras, por família ou no total.** Nenhuma ação atravessou
aplicação remota, persistência real e releitura autenticada. Não é falta de código: é a
autorização nominal do pacote remoto, que é decisão do Owner mediada por D00.

## Pendências declaradas, não silenciosas

- Hospedagem das rotas no shell: movimento único de D00. Não movi nada.
- Injeção do repositório de mídia de Circulares e do feed de Momentos: exige a cadeia
  completa por auth scope, app e main, não só o router. Hunks entregues.
- Viewer do Agora não reproduz vídeo: publicar vídeo gera story permanentemente
  indisponível. Há teste que congela isso, à espera de decisão sobre o player canônico.
- Publicar no Agora só oferece a audiência `families` enquanto o banco suporta quatro:
  exigiria mexer em `list_my_principal_contexts`, compartilhada com as outras frentes.
  Não abri perto do corte.
- Varredura de expiração do Agora sem agendador implantado.
- `chat.attach`: faltam a RPC de staging/commit e a Edge Function `chat-media`, minhas.
- Retirada manual pelo autor no Agora: sem decisão canônica; não inventei.

## Primeiro critério aberto

Persistência e releitura reais em ambiente autenticado, para qualquer uma das quatro
ações fechadas localmente. Depende do pacote remoto nominal, publicado em
`propostas/L01-pacote-remoto-nominal.md`, e da autorização correspondente.

## Próximo passo

Aguardar a decisão de D00 sobre soltar a retenção do lote e sobre o movimento único de
hospedagem. Nenhum lote novo será iniciado que não caiba antes de 16:45.

## Recursos e processos ativos

Nenhum. Todos os subagentes encerrados, container descartável removido, nenhum processo
remoto, nenhum deploy, nenhum agendamento criado.

## Limites de continuidade

Mecanismo real: turno ativo desta sessão, com `ListAgents` e `SendMessage` entre sessões
Claude locais. Não há agendador nativo que acorde a sessão depois do fim do turno;
Markdown não acorda sessão. Nada foi agendado. O reinício do notebook às 14:03 confirmou
na prática que os nomes de sessão não sobrevivem, e a revisão 3 da assignment já substitui
nome fixo por descoberta via `ListAgents`.
