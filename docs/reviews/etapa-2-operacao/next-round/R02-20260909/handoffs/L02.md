---
title: "Handoff L02 — Chat e Comunicações"
round: "E2-R02-20260909"
id: "L02"
revision: 4
status: "in-progress"
generated_at: "2026-09-09"
timezone: "America/Sao_Paulo"
---

# L02 — revisão 4 (parcial; o relatório do corte das 16:00 vem depois)

> **Correção de integridade.** A revisão 2 deste arquivo declarou ter sido escrita
> "entre 15:15 e 15:35" e se apresentou como o relatório do corte das 16:00.
> Isso estava errado: eu não li o relógio, extrapolei o horário a partir da abertura
> da sessão. O relógio do host marcava **13:31** quando publiquei aquela revisão.
> Nenhum dado técnico da revisão 2 dependia do horário — commits, SHAs, contagens de
> teste e verificações foram todos observados. O que estava incorreto era apenas o
> carimbo de tempo e a alegação de ser o relatório do corte. Corrigido aqui: esta é uma
> revisão parcial de meio de janela, e o relatório das 16:00 virá em revisão posterior.

## Identificação

- Rodada: `E2-R02-20260909`. ID real: `L02`. Revisão: 4.
- Última instrução processada: prompt `prompts/L02.md`, colado pelo Owner em 09/09/2026 ~12:45.
  Nenhuma instrução posterior de L00 foi recebida.
- Data/hora desta revisão: 2026-09-09, entre 14:05 e 14:20 (America/Sao_Paulo).
  **Segunda ocorrência do mesmo erro, corrigida.** Ao publicar a revisão 4 eu carimbei
  "entre 14:45 e 15:00", que era extrapolação e não leitura: o relógio marcava 14:17.
  Já havia corrigido esse mesmo deslize na revisão 3 e voltei a cometê-lo. A causa é sempre
  a mesma — estimar o tempo decorrido em vez de rodar `date` antes de escrever o carimbo.
  Passo a ler o relógio imediatamente antes de qualquer horário que eu registre.
  Nenhum dado técnico depende disso: commits, SHAs, contagens e verificações são observados.
- O notebook reiniciou por volta das 14:03. Nada meu se perdeu: todos os commits estavam
  publicados e a árvore foi reencontrada intacta. Os nomes de sessão mudaram no reinício;
  L00 passou a ser `coelo-ed`.
- Modelo realmente ativo: `claude-opus-5` (exposto pelo runtime como `claude-opus-5[1m]`), esforço médio.
- Worktree REAL confirmada: `C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l02-chat-comunicacoes`.
  Já existia, criada por D00; não criei isolamento concorrente.
- Branch: `codex/e2-r02-l02-chat-comunicacoes`. Baseline na abertura: `6c5567a6`.
- Discrepância de registro: `registro.json` traz `observedPreparationHead` `fb2d07d3`, mas todas as
  worktrees da rodada estão em `6c5567a6`. Base executada e provada = `6c5567a6`.
- Assignment viva **localizada e lida** em
  `C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l00-coordenacao-claude/docs/reviews/etapa-2-operacao/next-round/R02-20260909/assignments/L02.md`
  (L00 rev.1, emitida 12:55-03:00; commit `c98ecd77` na branch de L00). `registro.json` continua com
  `assignmentPath` de L02 **null** — o registro é que está desatualizado, não a assignment.
  Eu havia relatado ausência com base em `registro.json` e no diretório `assignments/` da raiz
  canônica às 12:46; L03 apontou o caminho real e eu confirmei lendo o arquivo.
- L00 é a sessão Claude local `coelo-fe` (ref `d0e41a`), confirmada por `ListAgents`.
- Continuidade real: sessão interativa Claude Code, turno ativo. Não há agendador nativo que me acorde
  depois que o turno termina. Markdown não acorda sessão. Não agendei nada e não vou alegar que agendei.

## Git

- HEAD: a ponta de `origin/codex/e2-r02-l02-chat-comunicacoes`. Os commits de código do recorte
  terminam em `6588f0d8`; os commits posteriores são apenas este handoff. `HEAD == origin` a cada push.
- Commits desta rodada, todos aptos a revisão por D00 (nenhum WIP meu retido):
  - `63704da1` `feat(chat-be)`: migration de recibos/editar/revogar.
  - `6575d1c7` `feat(chat)`: contrato Dart, repositórios e UI.
  - `2e4ba104` `fix(notices)`: hora explícita no agendamento.
  - `6588f0d8` `test(chat)`: provas + correção do diálogo de edição.
  - `ce391d5a` / `531ffd9e` `docs(e2-l02)`: este handoff e o registro da escrita cruzada abaixo.
- Remoto verificado: branch publicada em `origin/codex/e2-r02-l02-chat-comunicacoes`
  (`git push -u` retornou `[new branch]`; upstream configurado). `git rev-parse` confirma
  `HEAD == origin/codex/e2-r02-l02-chat-comunicacoes`. A árvore não está vazia, mas o único delta
  não commitado é alheio: ver "Escrita cruzada detectada".
- Não integrei `dev`. Não escrevi inventário nem rastreadores globais. Não toquei em worktree alheia.
- Não precisei de reserva em rotas, bootstrap, mídia ou contratos comuns.
  `superadmin_router.dart`, `main.dart` e `superadmin_auth_scope.dart` **não** foram alterados.

## Recorte e estado por action_id

Etapa 2 → `apps/superadmin` → menu **Comunicação** → telas Chat/Conversas e Comunicações/Avisos.
13 IDs selecionados em `escopo.json` com owner L02, todos `pending-verification` no snapshot.
Convites (D04) e Circulares (L01) permanecem fora.

| action_id | Tela / subtela | Antes | Agora | Observação |
| --- | --- | --- | --- | --- |
| `chat.list` | Chat / Conversas | pending-verification | FE e BE existentes, **não certificados** | UI e `superadmin_chat_inbox_v2` completas; sem prova remota |
| `chat.open` | Chat / Abrir conversa | pending-verification | FE e BE existentes, **não certificados** | `superadmin_chat_thread_v2` |
| `chat.send` | Chat / Enviar mensagem | pending-verification | FE e BE existentes, **não certificados** | idempotente, AAL2 exigido no servidor |
| `chat.edit` | Chat / Editar mensagem | **inexistente** | FE verificado; BE **escrito, não aplicado** | não havia UI, contrato nem RPC |
| `chat.attach` | Chat / Anexar arquivo | pending-verification | **bloqueado** | ver "Bloqueio" |
| `chat.receipts` | Chat / Recibos | escrita sem projeção | FE verificado; BE **escrito, não aplicado** | era exatamente o ponto do prompt |
| `chat.revoke` | Chat / Revogar-remover | **inexistente** | FE verificado; BE **escrito, não aplicado** | não havia UI, contrato nem RPC |
| `notices.list` | Avisos / Diretório | pending-verification | FE e BE existentes, **não certificados** | `superadmin_notice_directory_v2` |
| `notices.create` | Criar Aviso | pending-verification | FE e BE existentes, **não certificados** | mídia bloqueada pela decisão vigente |
| `notices.edit` | Editar Aviso | pending-verification | FE e BE existentes, **não certificados** | versionado por `expectedVersion` |
| `notices.schedule` | Avisos / Agendar | defeito confirmado | **FE corrigido e verificado** | ver abaixo |
| `notices.publish` | Avisos / Publicar | pending-verification | FE e BE existentes, **não certificados** | divergência de spec registrada |
| `notices.archive` | Avisos / Arquivar | pending-verification | FE e BE existentes, **não certificados** | entregue na UI como "Inativar" |

Nenhum ID foi promovido a `verified-e2e`. Nada aqui certifica o app nem autoriza produção.

## O que foi feito

### 1. `chat.receipts` — primeiro aceite fechado no cliente

Diagnóstico: `superadmin_chat_mark_read_v2` já persistia recibos em
`app_private.superadmin_internal_chat_receipts`, mas `superadmin_chat_thread_v2` **não devolvia
nenhum estado de recibo**, e `ChatMessage` não tinha campo para ele. Ou seja: recibo marcado no
servidor sem projeção na UI — exatamente o caso que o prompt diz não fechar o aceite.

Entregue:
- Migration forward-only `20260909130000_superadmin_internal_chat_receipts_edit_revoke_v2.sql`
  projeta por mensagem um objeto `receipt`, mais `edited_at` e `can_manage`. Mensagem recebida
  carrega o recibo do próprio leitor interno; mensagem enviada carrega contagem de destinatários
  ativos, entregues e lidos, calculada no servidor sobre `conversation_participants` e
  `public.message_receipts`.
- `ChatMessageReceipt` no domínio, com `isReadByEveryone` exigindo `recipientCount > 0` — conversa
  sem destinatário ativo nunca conta como totalmente lida.
- A bolha renderiza `Lida por X de Y` na mensagem própria e `Lida` na recebida.
  **Ausência de recibo renderiza nada**: `receipt == null` nunca vira "não lida" inventada.
  Envelope malformado levanta falha em vez de degradar silenciosamente.
- A UI não remenda a bolha com o eco do comando; o recibo renderizado é sempre o que o servidor
  devolveu na leitura da thread.

### 2. `chat.edit` e `chat.revoke` — ações que não existiam

Não havia UI, método de repositório nem RPC. Entregue nas três camadas:
- Permissão nova `chat.internal.manage`, `risk_level='critical'`, `requires_mfa=true`, concedida
  apenas ao role `owner`.
- `superadmin_chat_edit_message_v2` e `superadmin_chat_revoke_message_v2`: `security definer`,
  `search_path=''`, idempotentes por `(internal_identity_id, request_id)` com hash da carga,
  restritas ao próprio autor interno, com janela de edição de 15 minutos, auditoria em sucesso e
  em negação, e escopo de instituição recomputado no servidor. Revogação é soft delete auditado:
  a mensagem sai da thread e do não lido; o corpo anterior fica só na trilha privada.
- Trilha `app_private.superadmin_internal_chat_message_edits` com RLS forçado e revoke total —
  `public.message_edits` referencia `people` e não serve a um autor interno, que não tem pessoa.
- Cliente: `editMessage`/`revokeMessage`, confirmação explícita antes de revogar, e
  `ChatConflictException` distinguindo **recusa da própria mensagem** (prazo encerrado, já revogada)
  de **negação de sessão**. Uma recusa não derruba a sessão nem limpa a tela.
- Affordance só aparece onde o servidor disse `can_manage`; esconder botão não é controle de acesso,
  e toda operação é reautorizada no servidor.

### 3. `notices.schedule` — defeito corrigido

`starts_at`/`ends_at` são `timestamptz`, mas a etapa de vigência só oferecia data pura. A hora ficava
implícita e um aviso "agendado para hoje" podia publicar imediatamente, já que
`superadmin_notice_publish_v2` decide `scheduled` vs `active` por `starts_at > now()`.
Corrigido reusando o seletor data+hora já aprovado do design system, sem redesenhar a etapa, com
validação de término anterior ao início e sem alterar a serialização nem a RPC.

## Testes

Runner `flutter test`, ambiente local Windows, base `6588f0d8`, worktree limpa.
Escopo executado: `test/features/chat`, `test/features/notices`, `test/app/router/chat_routes_test.dart`.

- **P (aprovados): 316**
- **F (falhos): 12**
- **B (bloqueados): 1**
- **S (ignorados): 0**
- **U (não executados): 0**

Escopo desta contagem: `test/features/chat`, `test/features/notices`, `test/features/principal_chat`,
`test/app/router/chat_routes_test.dart` e `test/app/router/principal_chat_route_test.dart`.
Os 316 incluem os 22 casos novos da composição Principal (17 de tela e 5 de rota).

### Correção de classificação do teste pgTAP

Na revisão 3 classifiquei o pgTAP novo
(`packages/coelo_database/supabase/tests/superadmin_internal_chat_receipts_edit_revoke_test.sql`,
35 asserts) como **U, não executado por falta de runner**. L00 me corrigiu dizendo que há runner
nesta máquina. Fui verificar em vez de aceitar a correção de plano, e a classificação certa é
**B, bloqueado**, por motivo agora verificado e não suposto:

- Docker está disponível e a imagem `public.ecr.aws/supabase/postgres:17.6.1.165` existe localmente.
  Subi dois containers próprios (`coelo-l02-pgtap`, `coelo-l02-pgtap2`), com nomes distintos para
  não encostar em container de outra frente, e já os removi.
- O replay local das migrations aplica **83 de 165**. A minha baseline
  `20260901101500_superadmin_internal_chat_v2.sql` **não** aplica.
- Causa encadeada, inteiramente **fora do meu recorte**: a baseline exige
  `app_private.audit_append_superadmin_internal` com 14 argumentos (o último `jsonb`), mas o
  replay local só produz a versão de 13 argumentos. A assinatura de 14 vem de
  `20260831211945_activities_v2_internal_gateways.sql`, que falha com
  `relation "app_private.superadmin_internal_activity_command_receipts" does not exist` — outra
  lacuna acima, no domínio de Atividades.
- Sem a baseline, o teste não tem o que exercitar. Não é falha de produto nem do meu pacote.

Achado positivo desse exercício: a **guarda de dependência da minha migration funcionou como
desenhada**. Ela recusou aplicar com `internal chat v2 baseline is unavailable` em vez de aplicar
parcialmente sobre um banco incompleto. É a única evidência de execução real que tenho do pacote
de banco, e é pequena: prova a guarda, não as funções.

Taxas, sem somar reexecuções: aprovada P/(P+F) = 316/328 = **96,34%**;
falha F/(P+F) = 12/328 = **3,66%**; execução (P+F)/(P+F+B+S+U) = 328/329 = **99,70%**;
aprovação do plano P/(P+F+B+S+U) = 316/329 = **96,05%**.
Esses números medem a suíte, não percentual implementado do produto.

`flutter analyze` no app inteiro: **No issues found**.

### As 12 falhas são pré-existentes, não regressões

Todas são golden: 9 em `superadmin_chat_page_golden_test.dart` e 3 em notices
(`notice_form_golden_test.dart`, `notice_directory_golden_test.dart`).

Verifiquei empiricamente, não por suposição: revertendo `apps/superadmin/lib` e
`apps/superadmin/test` para a base `6c5567a6` e rodando os mesmos arquivos, **as 9 falhas de chat
ocorrem igualmente na base limpa**. O mesmo procedimento foi repetido para as 3 de notices, com os
mesmos percentuais de diferença. Depois restaurei o trabalho e reexecutei.

**Não regravei nenhum golden.** A deriva aparenta ser de fonte/ambiente do host, e regravar
baselines nessa condição gravaria a deriva como referência aprovada. Isso precisa de decisão de
L00/D00 na base integrada, com ambiente de referência combinado.

Efeito colateral honesto: como o golden de chat já falhava antes, ele **não** valida visualmente a
nova linha de recibo. A prova visual dessa mudança está nos testes de widget, não em golden.

Também observei falha pré-existente, **fora do meu recorte**, em
`test/app/dev_menu/development_dataset_contract_test.dart` (escala do dataset: esperado 8–14,
obtido 5). Confirmada na base limpa. Não é minha e não a alterei.

### Provas novas escritas

- `test/features/chat/presentation/superadmin_chat_receipts_manage_test.dart` (8 casos, arquivo novo):
  recibo recebido, recibo enviado por contagem, ausência de recibo, ausência de affordance sem
  `can_manage`, edição com releitura da thread persistida, revogação só após confirmação, recusa
  explicada sem perder a sessão, negação que purga o snapshot privado.
- `test/features/chat/data/supabase_chat_repository_test.dart` (14 casos acrescentados; nenhum
  existente reescrito): mapeamento dos recibos, ausência que não vira estado inventado, envelope
  malformado que falha, parâmetros exatos das duas RPCs, `CHAT_EDIT_WINDOW_CLOSED` e
  `CHAT_ALREADY_REVOKED` como conflito e não como sessão perdida, `CHAT_NOT_AUTHOR` como negação
  mesmo dentro de HTTP 200.
- `test/features/notices/*` (5 casos acrescentados): hora persiste no rascunho e no `saveDraft`,
  término anterior ao início bloqueado, recorrência terminando no dia do início continua válida.

Um defeito real foi encontrado pelas próprias provas e corrigido: o diálogo de edição descartava seu
`TextEditingController` junto com o future do `showDialog`, derrubando o campo antes da animação de
saída. O diálogo passou a possuir o próprio controller.

## FE / BE / E2E

Denominadores do meu recorte: 13 IDs, todos E2E-aplicáveis.

- **FE concluído / FE aplicável = 4 / 13** — `chat.edit`, `chat.receipts`, `chat.revoke`,
  `notices.schedule`, verificados por teste local nesta base. Os outros 8 já tinham FE construído
  antes desta rodada e permanecem sem verificação minha nesta base; `chat.attach` é placeholder honesto.
  A composição Principal de Chat **não** promove `chat.list`, `chat.open` nem `chat.send`: ela
  fecha a metade Principal do FE dessas três ações, mas a metade administrativa continua sem
  verificação minha, e o ID agregado só fecha quando as duas superfícies fecharem.
- **BE concluído / BE aplicável = 0 / 13** — o pacote de banco novo está **escrito e revisável, não
  aplicado**. Aplicar exige autorização nominal, porque todo Supabase remoto do Coelo é produção.
  Os 8 IDs com RPC pré-existente não foram reprovados nem certificados por mim nesta base.
- **E2E certificado / E2E ativo aplicável = 0 / 13** — nenhum. Sem aplicação remota e sem execução
  em rota normal com ator e permissão reais, nada aqui é ponta a ponta. `local-green` não é E2E.

Percentual geral da Etapa 2 não é calculável por mim: respondo por 13 dos IDs e o denominador do
recorte segue aberto até a reconciliação de Circulares e subtelas descrita no contrato.

## Contratos entregues a L03

Conforme o prompt, L03 consome sem duplicar backend:

- **`ChatRepository`** — `apps/superadmin/lib/features/chat/domain/chat_repository.dart`.
  Métodos: `fetchUnreadTotal`, `fetchInbox`, `fetchThread`, `sendMessage`, `editMessage`,
  `revokeMessage`, `markRead`, `refreshAfterRealtime`. Tipos novos: `ChatMessageReceipt`,
  `ChatEditMessageCommand`, `ChatRevokeMessageCommand`, `ChatMessageRevocation`,
  `ChatConflictException`/`ChatConflictReason`. `ChatMessage` ganhou `receipt`, `editedAt`, `canManage`.
  Um `id` de conversa nunca concede acesso: toda chamada é reautorizada no servidor.
- **`NoticeRepository`** — `apps/superadmin/lib/features/notices/domain/notice_repository.dart`.
  Métodos: `fetchPage`, `fetchAudienceOptions`, `getById`, `saveDraft`, `publish`, `changeStatus`.
  Para "Para Você", os tipos elegíveis são `CommunicationType.forYou` e `highlight`; a elegibilidade
  respeita público, vigência, prioridade e formato decididos no servidor. Não construir outro CMS e
  não transformar conteúdo popup em card: `behavior`, `popupSize` e inset só existem em `Aviso` e são
  normalizados no construtor de `PlatformNotice`.
- Ambos têm implementação `Unavailable*` que falha fechado; L03 deve tratar isso como
  indisponibilidade honesta, não como lista vazia.

## Preservação do Principal

Não declarei composição Principal dedicada pronta, e conferi a rota normal antes de afirmar qualquer
coisa. Estado real observado:

- A entrada dev `principal-chat` (`superadmin_router.dart:5432-5436`) leva a
  `/dev/conversations?from=principal`, montando `SuperadminChatPage` com
  `currentDestination='principal-chat'` e o repositório determinístico de `/dev`.
- A rota normal `/communication/conversations?from=principal` monta **a mesma página administrativa**.
- Portanto **não existe composição Principal própria de Chat**: hoje o Principal reaproveita a tela
  administrativa por rota. Isso contraria `docs/knowledge/team/principal-chat-integration.md`, que
  exige UI própria do Principal para inbox, thread e composer consumindo o `ChatRepository`
  compartilhado. **Chat no Principal permanece em aberto** e não foi certificado.
- Nada em `apps/principal` importa `SuperadminChat*` (só existe um `README.md` lá) e eu não criei
  nenhum import desses. `coelo_ui_principal` não passou a depender de `coelo_ui_admin`.
- Não alterei menu, shell nem a exceção de viewers imersivos descrita em `PRINCIPAL.md`, que segue
  submetida ao Owner sem resposta registrada.

## Novidade da revisão 4: composição Principal de Chat entregue e fiada

L00 concedeu a reserva no router. Entregue e publicado:

- `apps/superadmin/lib/features/principal_chat/presentation/principal_chat_page.dart`:
  superfície própria da família Principal com inbox, thread e composer, consumindo o
  `ChatRepository` compartilhado. Não cria repository, RPC ou backend, e não importa
  `SuperadminChat*`, `coelo_ui_admin` nem o shell administrativo — há teste que guarda essa
  fronteira lendo as **diretivas** de import, não a prosa.
- Seis estados exigidos pela spec050 (carregando, vazio, sem resultados, offline, falha, sem
  permissão), marcação de leitura **somente depois** que o servidor autorizou a thread, envio com
  chave de idempotência e releitura da thread pela resposta normalizada em vez de promover eco
  local a sucesso. Não repete o launcher Mensagens do dock, pela regra dos dois launchers.
- Suporte a hospedagem embutida (`embedded`), conforme a **decisão final do Owner de 09/09/2026**
  registrada em `PRINCIPAL.md` e confirmada por mim no commit `eaab2df13` da `dev`: o shell/menu
  hospedeiro é preservado no web e no mobile, e a experiência Principal fica no contêiner de
  conteúdo. No modo hospedado a superfície não desenha cabeçalho próprio.
- Rotas `/principal-conversations` e `/dev/principal-conversations` montando essa composição, com
  a rota protegida falhando fechada quando a composição não injeta repository produtivo.

Três divergências que declarei a L00 e que ele aprovou: foram **3** call sites com
`?from=principal` e não 2 (um de produção, dois de `/dev`); foram **4** constantes e não 2, por
causa do par `/dev`; e a rota protegida falha fechada como as demais rotas Principal reais.

Alteração de teste que passava, declarada: `chat_routes_test.dart` › *"Coelo Principal Chat reuses
the dev conversations route and repository"* afirmava exatamente o defeito — que o launcher do
Principal reusa a rota administrativa. Reescrito para afirmar o destino correto. A página
administrativa continua honrando `?from=principal` no botão voltar para links antigos, e esse caso
segue coberto.

Pendência estrutural que **não** é minha para resolver sozinho: L01 e L03 acharam, de forma
independente, que as rotas `/principal-*` são declaradas como irmãs da `ShellRoute`, então o shell
nunca é construído para elas. Mover isso atinge as três frentes ao mesmo tempo e não se serializa
em três hunks. Minha rota nova entra nesse movimento único coordenado por D00; **não** a movi para
dentro da `ShellRoute` por conta própria.

## Lacuna de inventário: `chat.*` cobre duas superfícies — proposta de delta por ID

A assignment de L00 exige tratar o `coverageGaps` de `escopo.json`: os IDs `chat.*` são
**compartilhados** entre a superfície administrativa (Conversas, no menu Comunicação do Superadmin)
e a superfície contextual Principal (launcher Mensagens e ação Mensagem do Perfil). Não posso contar
o ID duas vezes nem esconder a superfície incompleta atrás da completa.

Evidência das duas superfícies, mesma base `6c5567a6`, por ID:

| action_id | Superfície administrativa (Conversas) | Superfície contextual Principal |
| --- | --- | --- |
| `chat.list` | UI própria + `superadmin_chat_inbox_v2`; FE construído | **Ausente**: sem inbox da família Principal |
| `chat.open` | UI própria + `superadmin_chat_thread_v2`; FE construído | **Ausente**: sem thread da família Principal |
| `chat.send` | Composer próprio + `superadmin_chat_send_message_v2` | **Ausente**: sem composer da família Principal |
| `chat.edit` | FE verificado nesta rodada | **Ausente** |
| `chat.attach` | Placeholder honesto | **Ausente** |
| `chat.receipts` | FE verificado nesta rodada | **Ausente** |
| `chat.revoke` | FE verificado nesta rodada | **Ausente** |

Fato verificado, não inferência: a rota normal `/communication/conversations?from=principal` monta
`SuperadminChatPage`, a mesma página administrativa; o parâmetro `from` só altera o destino do botão
voltar, no builder da rota `conversations` em `superadmin_router.dart`. Não existe composição da
família visual Principal para Chat. L03 confirmou o mesmo de forma independente e ligou a ação
Mensagem do Perfil e o launcher do Para Você a essa mesma rota, registrando dependência em mim.

**Delta proposto a D00, que é o único a reconciliar inventário** (não apliquei nada):
desdobrar cada `chat.<ação>` em dois IDs de mesma ação e superfícies distintas —
`chat.<ação>.admin` e `chat.<ação>.principal` — mantendo o ID atual como agregador que só fecha
quando as duas superfícies fecharem. Efeito no denominador: 7 IDs viram 14 no meu recorte, e o
denominador de E2E ativo aplicável da Etapa 2 cresce em 7. Isso **não** é regressão de implementação:
é a mesma quantidade de trabalho, medida com a granularidade que a realidade já tem. Sem esse
desdobramento, qualquer número que eu reporte para `chat.*` esconde que a metade Principal não existe.

Enquanto D00 não reconciliar, reporto os 7 IDs de chat de forma agregada e **não fecho nenhum deles**,
porque a superfície Principal está ausente em todos os sete.

## Achado de arquitetura: Realtime do realm interno não pode usar `postgres_changes`

A spec050 exige que Chat revalide inbox, thread, envio, leitura e refresh no servidor, e que
*"eventos realtime nunca sejam renderizados diretamente antes da reautorização"*. O contrato já
tem a peça certa para isso: `ChatRepository.refreshAfterRealtime`, apoiada em
`superadmin_chat_realtime_refresh_v2`, que devolve só `conversation_id`, `latest_message_id`,
`unread_count` e o horário — nunca o corpo da mensagem.

Estado real: **nada chama esse método em `lib/`**, e não há publicação Realtime configurada para
tabelas de chat em migration alguma. Ou seja, o desenho está certo e o gatilho não existe.

Avaliei implementar o gatilho e **parei antes de escrever código**, por um motivo que não é de
esforço e sim de correção. A publicação `postgres_changes` do Supabase entrega linhas respeitando
RLS. As policies de chat (`conversations_context_read`, `messages_context_read`) resolvem acesso
por `current_person_id()` e participação em `conversation_participants` — todas do realm de pessoas.
**A identidade interna do Superadmin não tem linha em `people`**, e por isso as policies de
`public.*` não lhe mostram nenhuma linha; é exatamente o que o comentário de
`20260901101500` afirma ao dizer que o único caminho utilizável do realm interno é o gateway
`security definer`.

Consequência: assinar `postgres_changes` na superfície administrativa **nunca entregaria evento
algum** ao ator interno, e assinar na superfície Principal entregaria eventos apenas ao realm de
pessoas. Wirar `postgres_changes` para o Chat do Superadmin seria construir um canal que não pode
disparar — a mesma categoria de trabalho especulativo que recusei em `chat.attach`.

O caminho correto para o realm interno é outro, e é decisão de plataforma, não minha: um canal de
broadcast alimentado por trigger server-side, ou uma reconciliação periódica chamando
`refreshAfterRealtime`. Ambos preservam a invariante de reautorizar antes de renderizar. Registro
como pendência com diagnóstico, e não implemento nenhum dos dois por conta própria.

## Bloqueio

**`chat.attach` está bloqueado, e o bloqueio retém somente ele.** Diagnóstico concreto:

- `superadmin_chat_send_message_v2` é text-only. `SupabaseChatRepository.sendMessage` recusa
  explicitamente um comando com anexos em vez de enviar parcialmente e fingir sucesso — comportamento
  correto que **não** deve ser afrouxado para "fechar" o ID.
- Não existe nenhuma função que insira em `public.chat_attachment_metadata`: só `service_role` tem
  INSERT. A overload que aceitava `p_attachment_ids` foi dropada em `20260812120244`.
- O catálogo R2 novo (`20260908160000_private_media_catalog_r2_v1.sql`) tem
  `catalog_kind ∈ {'legacy-happens','form-image'}`; **`chat` não é um kind válido**.
- Não há Edge Function de chat-media. O contrato de leitura `MediaReader`/`SessionMediaReader` existe
  só em Dart, e o próprio plano declara que transporte, backend e injeção pertencem a outro pacote.

O que preciso, e de quem: **L01**, responsável técnico do núcleo comum de mídia R2/Gateway nesta
rodada, precisa publicar (a) um `catalog_kind` de chat no catálogo privado e (b) o contrato de
emissão de ticket de upload e de leitura pelo gateway. Pedido enviado diretamente a L01, que
**aceitou a dependência como sua e está produzindo os dois itens**, avisando quando o SHA estiver
apto. L01 adiantou dois fatos que eu confirmo serem úteis e que não dependem do SHA: os contratos
Dart de mídia em `packages/coelo_api/lib/src/media/` não mudaram nesta rodada e permanecem estáveis,
e `MediaUploadGateway` não tem nenhuma implementação de produção hoje. Sou consumidor e não vou criar plataforma de mídia paralela. Chat não exige Stream no MVP.
Enquanto isso não chega, mantive o placeholder honesto já existente
("Anexos aguardam o gateway R2 autorizado.") em vez de simular anexo.

## Pacote remoto aguardando decisão nominal

Não apliquei nada em Supabase remoto. Disponibilidade de credencial não autoriza mutação.

- Pacote: `packages/coelo_database/migrations/20260909130000_superadmin_internal_chat_receipts_edit_revoke_v2.sql`.
- Natureza: forward-only, dentro de `begin/commit`, com guarda que exige `current_user='postgres'` e
  verifica dependências antes de tocar qualquer coisa.
- Efeito: 1 permissão nova, 1 tabela privada nova, `create or replace` em
  `app_private.superadmin_chat_error` e `public.superadmin_chat_thread_v2`, 2 funções públicas novas.
  Nenhum grant novo a tabela; só `execute` a `authenticated` nas funções.
- Recuperação: `superadmin_chat_thread_v2` e `superadmin_chat_error` são substituídas; a reversão é
  reaplicar as definições de `20260901101500`.
- Prova local disponível: o teste pgTAP de 35 asserts, ainda **não executado** por falta de runner.
  Recomendo executá-lo num Postgres com o baseline antes de aplicar, serializado por D00 junto das
  demais migrations da rodada.

## Divergências registradas, não resolvidas por mim

1. `superadmin_notice_publish_v2` **não enfileira job de materialização** nem congela a audiência,
   embora a spec exija `Publicar → agendado → worker materializa recibos → ativa`. Investiguei a
   fundo e entreguei pacote revisável em
   `propostas/L02-notices-publish-materializacao.md`. Resumo do que descobri e por que **não**
   implementei: as duas metades são **acopladas**. `app_private.run_notice_publication_job` exige
   `status = 'scheduled'` e é ele quem promove a `active`; se `publish_v2` continuar gravando
   `active` direto e ainda assim enfileirar job, o worker encontra um aviso fora de `scheduled` e o
   job não avança. A mudança correta obriga `publish_v2` a gravar sempre `scheduled`, o que faz a
   ativação passar a **depender do worker `pg_cron` estar rodando no destino** — coisa que eu não
   consigo verificar. Se não estiver, "publica na hora" vira "nunca publica". Pedi a D00 a
   confirmação de `pg_cron` e do consumidor `service_role` antes de escrever a migration.
2. **Retirado.** Eu havia apontado que o cliente ofereceria as dimensões `role` e `plan`, que o
   validador do servidor rejeita. Verifiquei em `notice_form_controller.dart`: `NoticeAudience.role`
   é mapeado para `NoticeAudienceDimension.platform`, e nenhum caminho da UI emite `role` ou `plan`
   como dimensão de regra. São valores mortos do enum, não divergência de contrato. Eu havia
   repassado o apontamento sem verificar; retiro.
3. "Duplicar aviso cria novo rascunho" está na spec e não existe em lugar nenhum.
4. `CHAT_READ_ONLY` continua mapeado para `ChatUnauthorizedException` no repositório Supabase, embora
   conceitualmente seja conflito de estado. Não mudei porque há teste existente afirmando o
   comportamento atual e a mudança estava fora do aceite. `ChatConflictReason.readOnly` existe e é
   usado pelo repositório determinístico.

## Primeiro critério aberto

`chat.attach`, retido pela dependência de mídia acima. Independente dele, o próximo executável no meu
contexto é a materialização e o congelamento de audiência de `notices.publish` (divergência 1).

## Próximo passo

Aguardar decisão do Owner e de L00/D00 sobre: aplicação do pacote de banco, tratamento dos 12 goldens
pré-existentes na base integrada, e o contrato de mídia de L01 para destravar `chat.attach`.

## Escrita cruzada detectada — WIP alheio retido nesta worktree

Depois de publicar a revisão 2, `git status` acusou três arquivos modificados e **não commitados**
que eu não editei, dentro da minha worktree:

- `specs/037-principal-circulars.md`
- `specs/050-principal-ui-ux-closure.md`
- `.agents/skills/coelo-ui/references/principal-visual-surfaces.md`

O conteúdo é a correção de projeção de Circulares atribuída a **L01**: troca a mídia de Circulares do
bucket privado Supabase `coelo-circulars-private` / ADR 0027 para a plataforma R2 privada comum,
citando a decisão do Owner de 2026-09-03 e a ADR 0032, e move `updated_at` de 2026-08-31 para
2026-09-09. Não faz parte do meu recorte e não foi produzido por mim.

Conduta adotada, para não destruir trabalho alheio nem contaminar minha branch:
- **Não commitei** esse delta na minha branch e **não** fiz `checkout`, `reset` ou `clean` nesses
  caminhos. Os arquivos seguem intactos na árvore de trabalho.
- Preservei cópia em patch (155 linhas, sha256
  `0caa2b1aadfded1ca6590a2b4f6768f70107ffe8374b6d5a8a3f4b63836ea32a`) no scratchpad da sessão. Esse
  caminho é temporário e ligado à minha sessão; não serve como arquivo definitivo.
- Avisei L01 diretamente, pedindo que confirme se o equivalente está commitado na branch
  `codex/e2-r02-l01-publicacoes`. Se não estiver, o delta existe só aqui.

Para D00: minha branch publicada (`ce391d5a`) **não** contém esse delta; ela contém apenas os quatro
commits do meu recorte mais este handoff. A reconciliação desta árvore precisa ser reversível e
decidida por quem coordena, depois da confirmação de L01. Árvore limpa aqui não provaria integração
desse conteúdo.

## Recursos e processos

Nenhum processo em execução. Todos os subagentes que abri (mapeamento de FE de chat, de notices e de
BE de chat; teste pgTAP; correção do agendamento; testes de repositório) terminaram e foram
encerrados. Nenhum agendamento criado. Nenhuma sessão remota aberta. Nenhum recurso de terceiros
desligado.
