---
title: "R02 L01 — assignment L00 revisão 1"
source: "CONTRATO.md; PRINCIPAL.md; prompts/L01.md; escopo.json; assignments/L00.md (D00 rev.1)"
status: "issued-awaiting-role-start"
generated_at: "2026-09-09"
timezone: "America/Sao_Paulo"
---

Rodada E2-R02-20260909; emissor L00; destinatário L01; revisão 1;
emissão 2026-09-09T12:55-03:00.

## Identidade e destino reais

- Branch planejada: `codex/e2-r02-l01-publicacoes`.
- Worktree planejada: `C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l01-publicacoes`.
- Estado observado por L00 em 12:52-03:00: essa worktree **ainda não existe**.
  Em `git worktree list` constavam apenas a raiz e
  `e2-r02-d01-autenticacao`, `e2-r02-d02-estrutura`, `e2-r02-d03-acompanhamento`,
  `e2-r02-d04-acessos` (locked) e `e2-r02-l00-coordenacao-claude`.
- Baseline observada nas worktrees já criadas: `6c5567a6` (`dev` local),
  à frente de `origin/dev` `fb2d07d3`. `runtimeBaselineSha` em `registro.json`
  continua `null`.
- Antes de editar, confirme em `registro.json` o `worktree` e o `baselineSha`
  reais publicados por D00. Se o destino ainda não existir, trabalhe em
  somente leitura, avise L00 uma vez e **não** crie isolamento concorrente
  nem escreva na raiz ou em checkout alheio. L00 cobra D00; não repita a cobrança.
- Assignment viva: este arquivo, em
  `C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l00-coordenacao-claude/docs/reviews/etapa-2-operacao/next-round/R02-20260909/assignments/L01.md`.
- Seu handoff: `handoffs/L01.md` dentro da SUA worktree, no mesmo caminho
  relativo do pacote. Só você escreve esse arquivo.

## Recorte (12 action_ids MVP, todos E2E aplicáveis)

Etapa 2 → `apps/superadmin` → menu Principal → tela → subtela/estado → action_id.
Todos entram na fotografia como `pending-verification` em FE, BE e integrado.

| Família | action_id | Tela |
| --- | --- | --- |
| acontece | `acontece.feed` | Acontece / Feed |
| acontece | `acontece.create` | Acontece / Criar |
| acontece | `acontece.publish` | Acontece / Publicar |
| acontece | `acontece.remove` | Acontece / Remover |
| agora | `agora.view` | Agora / Visualizar |
| agora | `agora.create` | Agora / Criar |
| agora | `agora.publish` | Agora / Publicar |
| agora | `agora.expire` | Agora / Expirar |
| momentos | `momentos.view` | Momentos / Visualizar |
| momentos | `momentos.create` | Momentos / Criar |
| momentos | `momentos.publish` | Momentos / Publicar |
| momentos | `momentos.remove` | Momentos / Remover |

Circulares pertence a você mas **não tem IDs próprios suficientes** no
inventário (`coverageGaps` de `escopo.json`): a spec037 tem diretório,
criação/publicação, detalhe e edição, e `principal.profile-view` cobre apenas a
projeção no Perfil — e esse ID é de L03. O denominador de Circulares fica
aberto até a reconciliação. Não esconder Circulares em `principal.profile-view`,
não inventar contagem e não excluir ação para atingir 100%.

## Prioridade desta rodada

1. **Proposta de mapeamento de Circulares** (spec037 → telas/subtelas/action_ids),
   registrada no seu handoff como delta por ID. É a reconciliação que D00 precisa
   antes de qualquer percentual completo do contexto. Encaminho a D00 assim que
   estiver no handoff; não espere ACK para seguir com o resto.
2. **Núcleo comum de mídia R2/Gateway** desta rodada, com contrato publicado
   cedo. Você é o dono técnico; L02 (anexos de Chat) e L03 (avatar/capa) são
   consumidores, e `escopo.json` também lista D02 e D04 como consumidores.
   Publique versão compatível antes de refatorar o que já existe; reutilize
   catálogo/gateway atuais e não crie plataforma de mídia por feature.
3. Fechar aceites do seu recorte em ordem executável, sempre pelo circuito
   completo: UI normal → backend/mídia → persistência → releitura na UI, com
   autorização no servidor e negações pertinentes.

## Contratos que você entrega e L00 fiscaliza

- Projeções de Publicações e de Circulares consumidas por L03 (abas do Perfil e
  Acontece). Você mantém o CRUD canônico; L03 apenas consome. Sem duplicar
  registros nem criar repositório paralelo.
- Catálogo/gateway de mídia privada consumido por L02 e L03.
- Mudança **incompatível** nesses contratos passa por L00 antes do commit;
  alteração aditiva compatível segue direto, com o delta declarado no handoff.

## Regras materiais (já decididas, não reabrir)

- ADR0032: R2 privado é o master da mídia nova do MVP; Supabase guarda
  metadados, permissão, vínculo, ownership e auditoria. Agora pode usar cópia
  HOT em Stream por até 24 h; Acontece/Momentos só por necessidade medida;
  PDF nunca usa Stream. Chat não exige Stream.
- A projeção histórica em `docs/knowledge/team/principal-circulars.md` que manda
  mídia nova para Supabase Storage está **superada**. Corrija-a no seu trabalho
  documental, atualizando primeiro a fonte canônica. Não peça decisão nova:
  ela já está tomada em ADR0032, AGENTS.md e CONTRATO.md.
- `PRINCIPAL.md`: o shell/menu do Superadmin permanece como navegação do
  hospedeiro e a composição Principal ocupa o contêiner adjacente.
  `PrincipalPublicationFrame` preserva a geometria externa aprovada, incluindo
  insets e rodapé. Publicar não vira formulário administrativo.
  `coelo_ui_principal` não importa `coelo_ui_admin`; neutros vêm de `coelo_ui_core`.
- **Exceção pendente:** a ambiguidade sobre viewers imersivos de Agora/Momentos
  ocultarem temporariamente o chrome está com o Owner e sem resposta registrada.
  Não altere esse comportamento nem o certifique nesta rodada; silêncio não é
  aprovação. O restante do seu contexto continua normalmente.
- Anexos aprovados: `publicar-acontece-responsive-reference.png`,
  `publicar-momentos-responsive-v2.png` (v1 é histórica),
  `momentos-responsive-reference.png` e, para Agora,
  `docs/reviews/evidence/etapa-2/principal-visual/2026-08-31-publicar-agora-approved.png`.
  As duas imagens de Estrutura chamadas "Publicar agora" pertencem a Atividades e
  **não** são referência do Agora. Captura que documenta defeito não aprova o defeito.
- Rotas existentes como ponto de partida: `/principal-happens`,
  `/principal-happens/publish`, `/principal-now`, `/principal-now/publication`,
  `/principal-moments`, `/principal-moments/publish`, `/circulars`,
  `/circulars/new`, `/circulars/:circularId/read`, `/circulars/:circularId/edit`.
  Rota existente não prova acesso normal, persistência nem E2E.
- Fontes por ação: `specs/050-principal-ui-ux-closure.md`,
  `specs/036-principal-now-publication-mvp.md`, `specs/037-principal-circulars.md`,
  `docs/superpowers/specs/2026-08-20-coelo-happens-publication-design.md`,
  `docs/superpowers/specs/2026-08-20-coelo-moments-publication-design.md`,
  `decisions/0032-mvp-private-media-r2.md`.

## Reservas e limites

- Alteração em `superadmin_router.dart`, `superadmin_routes.dart`, `main.dart`,
  `superadmin_auth_scope.dart`, shell/navegação, barrels ou migrations: peça a
  reserva curta a L00 declarando arquivo e alteração exata; libero por SHA.
  A falta de reserva retém só aquele trecho — o resto do contexto continua.
- Migrations e qualquer aplicação remota são serializadas por D00. Todo
  Supabase/Cloudflare remoto é produção: prepare pacote nominal revisado, provas
  locais e recuperação; não aplique sem autorização válida para o pacote exato.
  Credencial ou MCP disponível não autoriza mutação.
- Builds Flutter, Docker e suítes pesadas: uma execução por lote. Isso limita o
  recurso de teste, não a escrita e a leitura independentes.
- Não integre `dev`, não escreva inventário nem os três rastreadores globais
  (D00 é o único escritor) e não toque na worktree de outro agente.

## Prova e reporte

- Defina os aceites antes do lote e reutilize provas válidas. Depois de verde,
  siga ao próximo critério; não reexecute lote sem causa material.
- FE verified, BE done e E2E verified-e2e são independentes. Mock, `/dev`,
  preview, local-green e golden isolado não equivalem a E2E.
- Testes únicos P/F/B/S/U com escopo, runner, ambiente e base. Sem somar reruns
  ou suítes sobrepostas. PASS não é percentual de implementação.
- Distinguir falha de produto, falha de runner, resultado histórico não
  revalidado, bloqueado e nunca executado.
- Commits pequenos, branch publicada, SHAs aptos entregues a L00 continuamente,
  WIP separado. Não espere 16:00 para me passar um SHA que já desbloqueia L02/L03.

## Ponte real entre sessões Claude (Markdown não acorda ninguém)

Escrever `handoffs/<ID>.md` é obrigatório e é a evidência durável, mas **não
notifica** L00: arquivo sozinho não acorda uma sessão. O mecanismo nativo real
disponível e observado nesta máquina é `ListAgents` + `SendMessage` entre sessões Claude
locais desta máquina.

- L00 é a sessão Claude local chamada **`coelo-fe`** (ref `d0e41a`).
- Rode `ListAgents` para confirmar o nome vivo antes do primeiro envio; se dois
  nomes colidirem, use o `[ref]` mostrado na listagem.
- Envie a L00 uma mensagem curta (`SendMessage`, destinatário `coelo-fe`) quando:
  publicar um SHA apto que desbloqueia outra frente; precisar de reserva em
  arquivo compartilhado; encontrar bloqueio/dependência real; e no corte das 16:00,
  apontando o caminho absoluto do seu handoff com a revisão.
- Não usar `SendMessage` como relatório de status por patch, nem pedir ACK para
  seguir trabalho independente já atribuído. Uma mensagem por causa.
- Se `ListAgents` não mostrar `coelo-fe`, registre o impedimento concreto no seu
  handoff e continue o trabalho independente; não invente entrega ou ACK.

## Horários firmes — 09/09/2026, America/Sao_Paulo

- **16:00** — reporte a L00 no seu handoff: commits/branch/HEAD/remoto, feito
  por tela/subtela, evidências, P/F/B/S/U, FE/BE/E2E com numeradores e
  denominadores, pendências, bloqueios, primeiro critério aberto, próximo passo
  e processos/recursos ainda ativos. Prepare o snapshot antes; não aguarde tudo
  fechar para reportar.
- **16:00–16:45** — somente correções concretas da consolidação. Sem novas frentes.
- **16:45** — código e evidências preservados, handoff final, reservas liberadas,
  subagentes e recursos próprios encerrados, estado seguro aguardando decisão do Owner.
- Sem retomada noturna ou no dia seguinte por conta própria.
