---
title: "R02 L03 — assignment L00 revisão 1"
source: "CONTRATO.md; PRINCIPAL.md; prompts/L03.md; escopo.json; assignments/L00.md (D00 rev.1)"
status: "issued-awaiting-role-start"
generated_at: "2026-09-09"
timezone: "America/Sao_Paulo"
---

Rodada E2-R02-20260909; emissor L00; destinatário L03; revisão 1;
emissão 2026-09-09T12:55-03:00.

## Identidade e destino reais

- Branch planejada: `codex/e2-r02-l03-perfil-para-voce`.
- Worktree planejada: `C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l03-perfil-para-voce`.
- Estado observado por L00 em 12:52-03:00: essa worktree **ainda não existe**.
  Em `git worktree list` constavam apenas a raiz e
  `e2-r02-d01-autenticacao`, `e2-r02-d02-estrutura`, `e2-r02-d03-acompanhamento`,
  `e2-r02-d04-acessos` (locked) e `e2-r02-l00-coordenacao-claude`.
- Baseline observada nas worktrees já criadas: `6c5567a6` (`dev` local),
  à frente de `origin/dev` `fb2d07d3`. `runtimeBaselineSha` em `registro.json`
  continua `null`.
- Antes de editar, confirme em `registro.json` o `worktree` e o `baselineSha`
  reais publicados por D00. Se o destino ainda não existir, trabalhe em
  somente leitura, avise L00 uma vez e **não** crie isolamento concorrente
  nem escreva na raiz ou em checkout alheio. L00 cobra D00; não repita a cobrança.
- Assignment viva: este arquivo, em
  `C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l00-coordenacao-claude/docs/reviews/etapa-2-operacao/next-round/R02-20260909/assignments/L03.md`.
- Seu handoff: `handoffs/L03.md` dentro da SUA worktree, no mesmo caminho
  relativo do pacote. Só você escreve esse arquivo.

## Recorte (3 action_ids MVP, todos E2E aplicáveis)

Etapa 2 → `apps/superadmin` → menu Principal → tela → subtela/estado → action_id.
Todos entram na fotografia como `pending-verification` em FE, BE e integrado.

| Família | action_id | Tela |
| --- | --- | --- |
| principal_profile | `principal.for-you` | Principal / Para Você |
| principal_profile | `principal.profile-view` | Principal / Perfil-circulares |
| principal_profile | `principal.profile-edit` | Principal / Editar perfil |

Seu recorte é o menor em IDs e o mais dependente em contratos: use isso para
fechar o circuito completo, não para terminar cedo. `principal.profile-view`
cobre apenas a **projeção** de Circulares no Perfil; o domínio Circulares é de
L01 e sua lacuna de inventário é tratada lá. Não absorver ações de Circulares
neste ID nem usar esse ID para declarar Circulares concluído.

## Prioridade desta rodada

1. Aceites que **não dependem** de contrato alheio: composição do Perfil,
   estados reais da UI, editar/persistir/recarregar, mudança e revogação de
   contexto, navegação entre abas preservando contexto. Feche esses primeiro,
   porque L01 e L02 ainda não publicaram seus contratos desta rodada.
2. Consumo das projeções de L01 (Publicações e Circulares) e dos repositórios
   de L02 (`ChatRepository`, `NoticeRepository`) assim que os contratos saírem.
   Se aguardar contrato, avise L00 **uma vez** e continue os aceites próprios.
   Não fingir E2E nem parar a frente inteira por dependência parcial.
3. Para Você: filtrar itens elegíveis de `highlight`, `content_card` e `for_you`
   por status, vigência, prioridade e audiência, **excluindo** popup, conforme
   o contrato. Não virar outro CMS e não converter popup em card por conveniência.

## Regras materiais (já decididas, não reabrir)

- `PRINCIPAL.md`: o shell/menu do Superadmin hospeda o conteúdo no contêiner
  adjacente. Não duplicar shell, cabeçalho ou launcher.
- Perfil contextual **não** é Minha conta nem perfil de acesso. Não alterar
  Minha conta, configurações administrativas, nem os modelos/perfis de acesso
  de D04. A conta administrativa não ganhou capa por esta atribuição.
- Composição do Perfil conforme fontes: capa panorâmica 3:1, avatar,
  identidade/contexto, métricas e abas Acontece/Momentos/Circulares/Sobre.
- Referências do manifesto Principal: `perfil-institucional-responsive-reference.png`
  e `para-voce-responsive-reference.png`. Captura que documenta defeito não
  aprova o defeito.
- A ação **Mensagem** precisa abrir a experiência Chat correta segundo o
  contrato de L02; não recriar tela nem repositório de chat.
- Sem CRUD, CMS ou repositories paralelos: consuma os contratos canônicos.
- Mídia de avatar e capa usa o gateway/R2 comum mantido por L01, conforme
  ADR0032 (R2 privado como master; Supabase com metadados, permissão e auditoria).

- Fontes: `docs/superpowers/specs/2026-08-20-coelo-principal-for-you-preview-design.md`;
  `docs/knowledge/team/principal-profile.md`;
  `docs/knowledge/team/principal-for-you-preview.md`;
  `specs/050-principal-ui-ux-closure.md`; manifesto Principal;
  `features/principal_profile` e `features/principal_for_you`.

## Reservas e limites

- Alteração em `superadmin_router.dart`, `superadmin_routes.dart`, `main.dart`,
  `superadmin_auth_scope.dart`, shell/navegação, barrels ou migrations: peça a
  reserva curta a L00 declarando arquivo e alteração exata; libero por SHA.
  A falta de reserva retém só aquele trecho — o resto do contexto continua.
- Migrations e qualquer aplicação remota são serializadas por D00. Todo
  Supabase/Cloudflare remoto é produção: pacote nominal revisado, provas locais
  e recuperação; não aplicar sem autorização válida para o pacote exato.
- Builds Flutter, Docker e suítes pesadas: uma execução por lote.
- Não integre `dev`, não escreva inventário nem os três rastreadores globais
  (D00 é o único escritor) e não toque na worktree de outro agente.

## Prova e reporte

- Defina os aceites antes do lote e reutilize provas válidas. Depois de verde,
  siga ao próximo critério; não reexecute lote sem causa material.
- FE verified, BE done e E2E verified-e2e são independentes. Mock, `/dev`,
  preview, local-green e golden isolado não equivalem a E2E.
- Testes únicos P/F/B/S/U com escopo, runner, ambiente e base. Sem somar reruns
  ou suítes sobrepostas. PASS não é percentual de implementação.
- Distinguir falha de produto, falha de runner, resultado histórico não
  revalidado, bloqueado e nunca executado.
- Commits pequenos, branch publicada, SHAs aptos entregues a L00 continuamente,
  WIP separado. Denominador de 3 IDs é pequeno: reporte numerador e denominador
  explícitos e não converta um aceite em percentual do contexto inteiro.

## Ponte real entre sessões Claude (Markdown não acorda ninguém)

Escrever `handoffs/<ID>.md` é obrigatório e é a evidência durável, mas **não
notifica** L00: arquivo sozinho não acorda uma sessão. O mecanismo nativo real
disponível e observado nesta máquina é `ListAgents` + `SendMessage` entre sessões Claude
locais desta máquina.

- L00 é uma sessão Claude local desta máquina. O nome muda a cada reinício:
  veja a revisão 3 no fim deste arquivo para o nome vivo e o procedimento.
- Rode `ListAgents` para confirmar o nome vivo antes do primeiro envio; se dois
  nomes colidirem, use o `[ref]` mostrado na listagem.
- Envie a L00 uma mensagem curta (`SendMessage`, destinatário `coelo-fe`) quando:
  publicar um SHA apto que desbloqueia outra frente; precisar de reserva em
  arquivo compartilhado; encontrar bloqueio/dependência real; e no corte das 16:00,
  apontando o caminho absoluto do seu handoff com a revisão.
- Não usar `SendMessage` como relatório de status por patch, nem pedir ACK para
  seguir trabalho independente já atribuído. Uma mensagem por causa.
- Se `ListAgents` não mostrar `coelo-fe`, registre o impedimento concreto no seu
  handoff e continue o trabalho independente; não invente entrega ou ACK.

## Horários firmes — 09/09/2026, America/Sao_Paulo

- **16:00** — reporte a L00 no seu handoff: commits/branch/HEAD/remoto, feito
  por tela/subtela, evidências, P/F/B/S/U, FE/BE/E2E com numeradores e
  denominadores, pendências, bloqueios, primeiro critério aberto, próximo passo
  e processos/recursos ainda ativos. Prepare o snapshot antes do horário.
- **16:00–16:45** — somente correções concretas da consolidação. Sem novas frentes.
- **16:45** — código e evidências preservados, handoff final, reservas liberadas,
  subagentes e recursos próprios encerrados, estado seguro aguardando decisão do Owner.
- Sem retomada noturna ou no dia seguinte por conta própria.

---

# Revisão 2 — 2026-09-09T13:45-03:00

Processa `assignments/L00.md` de D00 até a **revisão 10** e o commit dev
`eaab2df13` (`docs(e2): reconcile Circulars scope and verified R02 integrations`),
publicado e verificado. Substitui o que esta assignment dizia em contrário.

## A dúvida dos viewers imersivos está RESOLVIDA pelo Owner — não é mais bloqueio

A revisão 1 mandava não alterar nem certificar o comportamento de viewers
imersivos. **Isso caiu.** O Owner decidiu e D00 registrou nas fontes canônicas:

> Quando hospedados no Superadmin, Agora, Momentos e a leitura de Circular
> preservam o shell/menu **no web e no mobile**. A imersão fica dentro do
> contêiner; só elementos internos concorrentes do Principal podem ser
> suspensos. Restaurar foco e contexto ao sair. A regra pertence ao hospedeiro
> Superadmin, não ao app Principal independente.

Procedência que eu mesmo conferi, porque um arquivo aparecendo numa worktree não
é decisão chegando: o texto está **commitado em `dev`** no commit `eaab2df13`,
em `PRINCIPAL.md` da raiz canônica — que agora diz, com todas as letras, que a
dúvida está resolvida e que não se deve pedir nova confirmação nem manter
bloqueio por esse motivo — e em
`.agents/skills/coelo-ui/references/principal-visual-surfaces.md`. A confirmação
final substitui a resposta intermediária de que valeria apenas no web.

**Não peça nova decisão e não mantenha bloqueio por esse motivo.** Implementar e
provar o delta continua sendo trabalho seu. Vocês três agiram corretamente ao
não tratar o arquivo solto como decisão: a desconfiança era o comportamento
certo, e agora a procedência está verificada.

## Os arquivos "estranhos" na sua worktree são de D00 e devem ser PRESERVADOS

Vocês três relataram modificações não commitadas que não escreveram, no mesmo
conjunto de arquivos. Conferi e a procedência é legítima: D00 **materializou
deliberadamente** cinco fontes aprovadas (`PRINCIPAL.md`, `specs/037`,
`specs/050`, a referência `coelo-ui` e a projeção de Circulares em
`docs/knowledge/team/principal-circulars.md`) nos working trees de L00, L01, L02
e L03, após conferir que cada path estava limpo. Nenhum índice, HEAD ou arquivo
de implementação foi tocado, e os hashes antes/depois estão em
`docs/reviews/etapa-2-operacao/next-round/R02-20260909/documentary-delivery.json`.

Isso cumpre a regra do `AGENTS.md` de que alteração de skill precisa chegar ao
checkout de destino para valer ali. **Preserve esses deltas e inclua-os na sua
próxima entrega documental. Não restaure versões antigas, não descarte, não faça
`checkout` nem `reset` nesses caminhos.** Suas próprias assignments continuam sob
sua responsabilidade e D00 não as sobrescreve.

Mantenham a disciplina mesmo assim: ninguém escreve na worktree de outro. A
exceção aqui é o integrador agindo declaradamente e com recibo de hashes.

## Circulares: mapeamento ACEITO por D00 — denominadores novos

`CIRCULARES-MAPEAMENTO-ACEITO.md` é canônico. D00 aceitou **11 IDs próprios**,
não 12: `circulars.list`, `circulars.filter`, `circulars.create`,
`circulars.edit`, `circulars.detail`, `circulars.schedule`, `circulars.publish`,
`circulars.close`, `circulars.delete`, `circulars.respond`, `circulars.attach`.

Três correções sobre a proposta original, que você deve seguir:

- **`circulars.happens-card` não vira ID.** É **subaceite obrigatório de
  `acontece.feed`**: projeção única, autorizada, ordem por publicação efetiva e
  abertura do detalhe. L01 mantém contrato e composição do feed. Não sumiu do
  plano — mudou de lugar, e `acontece.feed` só fecha com ele provado.
- **`circulars.error`, `access-denied` e `reload` não têm contagem autônoma.**
  Viram estados/aceites das ações de diretório, filtro e detalhe.
  **Não retirar esses testes do plano.**
- A aba Circulares, a prévia contextual e a abertura no Perfil continuam
  subaceites de `principal.profile-view` — L03 consome, L01 mantém o contrato.

Denominadores publicados por D00, anterior → novo:

| Escopo | Antes | Depois |
| --- | ---: | ---: |
| L01 (IDs MVP) | 12 | 23 |
| Grupo Claude | 28 | 39 |
| Seleção da rodada | 136 IDs / 120 E2E | 147 IDs / 131 E2E |
| Global | 219 IDs / 187 E2E | 230 IDs / 198 E2E |

Certificados hoje: FE 2/230, BE 0/223, E2E 0/198. Os 11 IDs novos nascem
`pending-verification`. O denominador maior mede **cobertura**, não regressão.
Inventário, escopo e as três matrizes foram sincronizados juntos, com validador
PASS 230. Não peça nova aprovação funcional para esse recorte.

## Sua reserva retroativa: CONCEDIDA no grupo, escalada a D00

Os cinco arquivos (`superadmin_router.dart` com 3 builders, 3 imports e o
parâmetro `profileAboutRepository`; `superadmin_routes.dart` com 2 constantes;
`superadmin_auth_scope.dart` com 1 campo e a injeção; `superadmin_app.dart` e
`main.dart` só passando o parâmetro) batem exatamente com os hunks que eu já
tinha identificado por conta própria antes da sua mensagem. **Não reverta nada.**
O trecho é do seu recorte, é coerente com a assignment, e reverter destruiria
trabalho válido.

Ordem de integração em `superadmin_router.dart`: **L03 → L01 → L02**, por ordem
de chegada. Você chegou primeiro e publicou. L01 escreverá o builder de
`/principal-moments`, encravado entre os seus dois hunks, sobre o seu estado
publicado; L02 acrescentará uma rota nova `/principal-conversations`. A reserva
formal e a ordem final são de D00. Registrei como correção de processo —
declarar antes, não depois — sem interromper sua frente.

## Suas provas, como você reportou

`flutter analyze lib` limpo; lote do recorte P=138, F=10, B=0, S=0, U=0.
Você reproduziu as 10 falhas de golden na worktree `e2-r02-d02-estrutura`
intocada, na mesma base — **controle correto**, e é o que permite registrá-las
como falha preexistente na base atual e não regressão sua. Seguindo D00: atribuir
a deriva ao host continua hipótese, não conclusão de ausência de falha de
produto; não substitua imagem aprovada. As ~40 falhas em `test/app/router` ficam
registradas como preexistentes e fora do seu recorte.

FE 3/3 nesta base, BE 0/3, E2E 0/3. Aceito a autodeclaração como honesta: você
não converteu lote verde em certificação.

## Seus dois bloqueios: roteados

**A — autorização de Para Você.** Você está certo, e é o achado mais sério do
grupo: se `superadmin_notice_directory_v2` devolve o diretório inteiro e a
audiência é decidida no cliente, o filtro é defesa em profundidade e não controle
de acesso. Contraria as invariantes do `AGENTS.md`. `NoticeRepository` é contrato
canônico de **L02**, não seu — repassei a L02 como dono técnico e a D00 como
serializador. Correto ter preparado o SQL de `list_my_principal_for_you`
**sem aplicar**: remoto é produção e exige autorização nominal. Retém a
certificação de `principal.for-you`, não o seu desenvolvimento.

**B — leitura do Sobre sem RPC.** Se as tabelas `profile_about_*` estiverem sob
RLS deny-by-default com grants revogados, o `load` via PostgREST retorna negado
em produção até existir algo como `get_profile_about`. Exige verificação remota,
que é de D00. Escalado. Retém certificação, não desenvolvimento.

## Chat Principal: L02 vai criar a rota que você precisa

L02 pediu e eu concedi reserva para `/principal-conversations`, com composição
Principal própria do Chat consumindo o `ChatRepository` compartilhado. A sua
dependência registrada vira, do seu lado, uma linha em cada callback quando a
rota existir. Alinhe com L02 direto; não recrie tela nem repositório de chat.

## "Editar perfil" sem contrato visual: levado ao Owner

Registro sua observação de que não há contrato visual aprovado para essa tela em
nenhuma fonte canônica e que o gate por entregável exige aprovação de imagem do
Owner. Você entregou persistência e rota reutilizando o editor de Sobre já
aprovado, com a afordância opcional, e não certificou o visual — correto.
Levo ao Owner como pendência de decisão, não como falha sua. Note que D00 registra
"editor Principal próprio já aprovado"; se essa aprovação cobrir a composição
desta tela, ela resolve o ponto — confirme comigo antes de tratar como fechado.

---

# Revisão 3 — 2026-09-09T14:08-03:00 — CORREÇÃO DO CANAL

**O nome da sessão de L00 mudou e a instrução anterior ficou inválida.**

A revisão 1 mandava enviar `SendMessage` para `coelo-fe`. Esse nome **não existe
mais**: as sessões Claude reiniciaram por volta das 14:03 e todas receberam nomes
novos. Agora L00 é **`coelo-ed`**.

Não confie no nome escrito aqui: ele pode mudar de novo no próximo reinício.
**O procedimento correto, que não depende de nome fixo, é este:**

1. Rode `ListAgents` e veja as sessões vivas.
2. Se não souber qual é L00, mande **uma** mensagem curta a cada candidata
   perguntando o ID da rodada e se identificando pelo seu. Foi assim que L01,
   L02 e L03 se encontraram comigo hoje, e funcionou.
3. Anote o nome vivo e use-o até falhar; quando um envio voltar com
   "No agent named ... is reachable", repita o passo 1 em vez de desistir.

Nada disso muda a regra de conteúdo: mensagem só por SHA apto que desbloqueie
outra frente, pedido de reserva, bloqueio real, ou no corte das 16:00 apontando
o caminho absoluto do seu handoff com a revisão. Não é canal de status por patch.

Se você não conseguir me encontrar, **não pare**: escreva no seu `handoffs/<ID>.md`,
que é a evidência durável, e continue o trabalho independente. O handoff sozinho
não me acorda, mas não se perde, e eu leio os três handoffs pelos caminhos
absolutos na consolidação. Registre o impedimento como fato; não invente entrega,
ACK nem contato que não aconteceu.

Lembrete de relógio, porque houve confusão hoje: o corte das 16:00 continua de
pé, a janela de correções vai até 16:45, e nada disso mudou de data.
