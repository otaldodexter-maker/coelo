---
title: "R02 L02 — assignment L00 revisão 1"
source: "CONTRATO.md; PRINCIPAL.md; prompts/L02.md; escopo.json; assignments/L00.md (D00 rev.1)"
status: "issued-awaiting-role-start"
generated_at: "2026-09-09"
timezone: "America/Sao_Paulo"
---

Rodada E2-R02-20260909; emissor L00; destinatário L02; revisão 1;
emissão 2026-09-09T12:55-03:00.

## Identidade e destino reais

- Branch planejada: `codex/e2-r02-l02-chat-comunicacoes`.
- Worktree planejada: `C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l02-chat-comunicacoes`.
- Estado observado por L00 em 12:52-03:00: essa worktree **ainda não existe**.
  Em `git worktree list` constavam apenas a raiz e
  `e2-r02-d01-autenticacao`, `e2-r02-d02-estrutura`, `e2-r02-d03-acompanhamento`,
  `e2-r02-d04-acessos` (locked) e `e2-r02-l00-coordenacao-claude`.
- Baseline observada nas worktrees já criadas: `6c5567a6` (`dev` local),
  à frente de `origin/dev` `fb2d07d3`. `runtimeBaselineSha` em `registro.json`
  continua `null`.
- Antes de editar, confirme em `registro.json` o `worktree` e o `baselineSha`
  reais publicados por D00. Se o destino ainda não existir, trabalhe em
  somente leitura, avise L00 uma vez e **não** crie isolamento concorrente
  nem escreva na raiz ou em checkout alheio. L00 cobra D00; não repita a cobrança.
- Assignment viva: este arquivo, em
  `C:/Users/adrie/Documents/Coelo.worktrees/e2-r02-l00-coordenacao-claude/docs/reviews/etapa-2-operacao/next-round/R02-20260909/assignments/L02.md`.
- Seu handoff: `handoffs/L02.md` dentro da SUA worktree, no mesmo caminho
  relativo do pacote. Só você escreve esse arquivo.

## Recorte (13 action_ids MVP, todos E2E aplicáveis)

Etapa 2 → `apps/superadmin` → menu → tela → subtela/estado → action_id.
Todos entram na fotografia como `pending-verification` em FE, BE e integrado.

| Família | action_id | Tela |
| --- | --- | --- |
| chat | `chat.list` | Chat / Conversas |
| chat | `chat.open` | Chat / Abrir conversa |
| chat | `chat.send` | Chat / Enviar mensagem |
| chat | `chat.edit` | Chat / Editar mensagem |
| chat | `chat.attach` | Chat / Anexar arquivo |
| chat | `chat.receipts` | Chat / Recibos |
| chat | `chat.revoke` | Chat / Revogar-remover |
| notices | `notices.list` | Avisos / Diretório |
| notices | `notices.create` | Criar Aviso |
| notices | `notices.edit` | Editar Aviso |
| notices | `notices.schedule` | Avisos / Agendar |
| notices | `notices.publish` | Avisos / Publicar |
| notices | `notices.archive` | Avisos / Arquivar |

Convites é de D04; Circulares é de L01 e **não** é sinônimo de notices.

## Lacuna de inventário que você precisa tratar

`escopo.json` registra em `coverageGaps`: os IDs `chat.*` são **compartilhados**
entre a superfície administrativa (Conversas) e a superfície contextual
Principal. Registre evidência das duas superfícies para o mesmo ID, sem contar
o ID duas vezes e sem esconder uma superfície não concluída atrás da outra.
Se propuser granularidade por superfície, entregue a proposta como delta por ID
no seu handoff; encaminho a D00, que é o único a reconciliar inventário e matrizes.

## Prioridade desta rodada

1. **Contratos `ChatRepository` e `NoticeRepository` publicados cedo**, porque
   L03 consome ambos em Perfil e Para Você. Uma versão compatível antes de
   qualquer refatoração maior evita bloquear L03. Mudança incompatível passa
   por L00 antes do commit; alteração aditiva compatível segue direto.
2. Conferir a **entrada normal do Chat Principal**. Ponto conhecido: a entrada
   dev `principal-chat` aponta para `/dev/conversations?from=principal` e
   `SuperadminChatPage`. Existir essa preview **não** comprova composição
   Principal pronta. Não declare o aceite sem a rota normal.
3. Fechar os aceites do recorte pelo circuito completo: UI normal →
   backend/Realtime → persistência → releitura na UI, com autorização no
   servidor e negações pertinentes.

## Regras materiais (já decididas, não reabrir)

- `PRINCIPAL.md`: menu/shell do Superadmin é o hospedeiro; o conteúdo Principal
  fica no contêiner adjacente. Não renderizar a página administrativa dentro do
  Principal por conveniência, nem duplicar chrome.
- Reutilizar domínio e `ChatRepository` compartilhado é adequado. Reutilizar a
  **tela** administrativa como composição Principal exige atender às fontes
  aprovadas; não importar widgets `SuperadminChat*` para o pacote visual
  Principal e não copiar backend/RPCs.
- **Recibos** precisam ser persistidos **e** renderizados. Marcar recibo no
  servidor sem projeção na UI não fecha `chat.receipts`.
- **Realtime** exige reautorização no servidor antes de renderizar dados.
  Assinatura aberta não substitui verificação de ator, tenant e escopo.
- **Anexos** usam o Media Gateway/R2 comum mantido por L01. Chat não exige
  Stream no MVP; PDF nunca usa Stream. Se o contrato de mídia de L01 ainda não
  estiver publicado, avise L00 uma vez e feche os aceites que não dependem dele.
- Para Você (L03) projeta Comunicações elegíveis; não é outro CMS. Conteúdo
  popup não vira card por conveniência. Você mantém a fonte canônica de notices.
- Fontes: `specs/050-principal-ui-ux-closure.md`;
  `docs/superpowers/specs/2026-07-27-shared-chat-visual-system-design.md`;
  `docs/superpowers/specs/2026-07-28-superadmin-chat-local-redesign-design.md`;
  `docs/superpowers/specs/2026-08-05-superadmin-notices-mvp-design.md`;
  `docs/knowledge/team/principal-chat-integration.md`; `features/chat` e `features/notices`.

## Reservas e limites

- Alteração em `superadmin_router.dart`, `superadmin_routes.dart`, `main.dart`,
  `superadmin_auth_scope.dart`, shell/navegação, barrels ou migrations: peça a
  reserva curta a L00 declarando arquivo e alteração exata; libero por SHA.
  A falta de reserva retém só aquele trecho — o resto do contexto continua.
- Migrations e qualquer aplicação remota são serializadas por D00. Todo
  Supabase/Cloudflare remoto é produção: prepare pacote nominal revisado, provas
  locais e recuperação; não aplique sem autorização válida para o pacote exato.
- Builds Flutter, Docker e suítes pesadas: uma execução por lote.
- Não integre `dev`, não escreva inventário nem os três rastreadores globais
  (D00 é o único escritor) e não toque na worktree de outro agente.

## Prova e reporte

- Defina os aceites antes do lote e reutilize provas válidas. Depois de verde,
  siga ao próximo critério; não reexecute lote sem causa material.
- FE verified, BE done e E2E verified-e2e são independentes. Mock, `/dev`,
  preview, local-green e golden isolado não equivalem a E2E.
- Testes únicos P/F/B/S/U com escopo, runner, ambiente e base. Sem somar reruns
  ou suítes sobrepostas. PASS não é percentual de implementação.
- Distinguir falha de produto, falha de runner, resultado histórico não
  revalidado, bloqueado e nunca executado.
- Commits pequenos, branch publicada, SHAs aptos entregues a L00 continuamente,
  WIP separado. Entregue o SHA do contrato de repositórios assim que existir.

## Ponte real entre sessões Claude (Markdown não acorda ninguém)

Escrever `handoffs/<ID>.md` é obrigatório e é a evidência durável, mas **não
notifica** L00: arquivo sozinho não acorda uma sessão. O mecanismo nativo real
disponível e observado nesta máquina é `ListAgents` + `SendMessage` entre sessões Claude
locais desta máquina.

- L00 é uma sessão Claude local desta máquina. O nome muda a cada reinício:
  veja a revisão 3 no fim deste arquivo para o nome vivo e o procedimento.
- Rode `ListAgents` para confirmar o nome vivo antes do primeiro envio; se dois
  nomes colidirem, use o `[ref]` mostrado na listagem.
- Envie a L00 uma mensagem curta (`SendMessage`, destinatário `coelo-fe`) quando:
  publicar um SHA apto que desbloqueia outra frente; precisar de reserva em
  arquivo compartilhado; encontrar bloqueio/dependência real; e no corte das 16:00,
  apontando o caminho absoluto do seu handoff com a revisão.
- Não usar `SendMessage` como relatório de status por patch, nem pedir ACK para
  seguir trabalho independente já atribuído. Uma mensagem por causa.
- Se `ListAgents` não mostrar `coelo-fe`, registre o impedimento concreto no seu
  handoff e continue o trabalho independente; não invente entrega ou ACK.

## Horários firmes — 09/09/2026, America/Sao_Paulo

- **16:00** — reporte a L00 no seu handoff: commits/branch/HEAD/remoto, feito
  por tela/subtela, evidências, P/F/B/S/U, FE/BE/E2E com numeradores e
  denominadores, pendências, bloqueios, primeiro critério aberto, próximo passo
  e processos/recursos ainda ativos. Prepare o snapshot antes do horário.
- **16:00–16:45** — somente correções concretas da consolidação. Sem novas frentes.
- **16:45** — código e evidências preservados, handoff final, reservas liberadas,
  subagentes e recursos próprios encerrados, estado seguro aguardando decisão do Owner.
- Sem retomada noturna ou no dia seguinte por conta própria.

---

# Revisão 2 — 2026-09-09T13:45-03:00

Processa `assignments/L00.md` de D00 até a **revisão 10** e o commit dev
`eaab2df13` (`docs(e2): reconcile Circulars scope and verified R02 integrations`),
publicado e verificado. Substitui o que esta assignment dizia em contrário.

## A dúvida dos viewers imersivos está RESOLVIDA pelo Owner — não é mais bloqueio

A revisão 1 mandava não alterar nem certificar o comportamento de viewers
imersivos. **Isso caiu.** O Owner decidiu e D00 registrou nas fontes canônicas:

> Quando hospedados no Superadmin, Agora, Momentos e a leitura de Circular
> preservam o shell/menu **no web e no mobile**. A imersão fica dentro do
> contêiner; só elementos internos concorrentes do Principal podem ser
> suspensos. Restaurar foco e contexto ao sair. A regra pertence ao hospedeiro
> Superadmin, não ao app Principal independente.

Procedência que eu mesmo conferi, porque um arquivo aparecendo numa worktree não
é decisão chegando: o texto está **commitado em `dev`** no commit `eaab2df13`,
em `PRINCIPAL.md` da raiz canônica — que agora diz, com todas as letras, que a
dúvida está resolvida e que não se deve pedir nova confirmação nem manter
bloqueio por esse motivo — e em
`.agents/skills/coelo-ui/references/principal-visual-surfaces.md`. A confirmação
final substitui a resposta intermediária de que valeria apenas no web.

**Não peça nova decisão e não mantenha bloqueio por esse motivo.** Implementar e
provar o delta continua sendo trabalho seu. Vocês três agiram corretamente ao
não tratar o arquivo solto como decisão: a desconfiança era o comportamento
certo, e agora a procedência está verificada.

## Os arquivos "estranhos" na sua worktree são de D00 e devem ser PRESERVADOS

Vocês três relataram modificações não commitadas que não escreveram, no mesmo
conjunto de arquivos. Conferi e a procedência é legítima: D00 **materializou
deliberadamente** cinco fontes aprovadas (`PRINCIPAL.md`, `specs/037`,
`specs/050`, a referência `coelo-ui` e a projeção de Circulares em
`docs/knowledge/team/principal-circulars.md`) nos working trees de L00, L01, L02
e L03, após conferir que cada path estava limpo. Nenhum índice, HEAD ou arquivo
de implementação foi tocado, e os hashes antes/depois estão em
`docs/reviews/etapa-2-operacao/next-round/R02-20260909/documentary-delivery.json`.

Isso cumpre a regra do `AGENTS.md` de que alteração de skill precisa chegar ao
checkout de destino para valer ali. **Preserve esses deltas e inclua-os na sua
próxima entrega documental. Não restaure versões antigas, não descarte, não faça
`checkout` nem `reset` nesses caminhos.** Suas próprias assignments continuam sob
sua responsabilidade e D00 não as sobrescreve.

Mantenham a disciplina mesmo assim: ninguém escreve na worktree de outro. A
exceção aqui é o integrador agindo declaradamente e com recibo de hashes.

## Reserva em `superadmin_router.dart` e `superadmin_routes.dart`: CONCEDIDA no grupo

Alteração exata reservada para você, e nada além dela:

- `superadmin_routes.dart`: 2 constantes novas, `principalConversations`
  (`/principal-conversations`) e `principalConversationsName`.
- `superadmin_router.dart`: 1 `GoRoute` novo montando a composição Principal,
  mais a troca de destino nos 2 pontos que hoje mandam a superfície Principal
  para a página administrativa via `?from=principal`.

Motivo material aceito: na base `6c5567a6`, `/communication/conversations?from=principal`
monta `SuperadminChatPage`, e `from` só muda o destino do botão voltar — não
existe composição da família visual Principal para Chat. `PRINCIPAL.md`, a
spec050 e `docs/knowledge/team/principal-chat-integration.md` exigem inbox,
thread e composer próprios consumindo o `ChatRepository` compartilhado. L03
confirmou o mesmo de forma independente.

Sem colisão dentro do grupo: L03 alterou os builders de Perfil e Para Você
(l.827 e adiante na baseline) e L01 vai alterar `/principal-happens` (l.669) e
`/principal-moments` (l.835). Seu `GoRoute` é novo. **Ordem de integração no
arquivo: L03 → L01 → L02**, por ordem de chegada; escreva sobre o estado
publicado das frentes anteriores, não sobre a baseline, e entregue o hunk exato.
A reserva formal e a ordem final são de D00, dono declarado desses arquivos.

Certo em seguir construindo os widgets enquanto isso: eles não tocam arquivo
compartilhado, e a falta de reserva retinha só a fiação.

## Correção de horário: aceita, e obrigado por corrigir sozinho

Sua revisão 2 carimbou o relatório como o das 16:00 quando o relógio marcava
13:31, e você mesmo corrigiu na revisão 3 marcando-a como parcial. Registro
assim no consolidado. Recebi sua entrega às **13:33-03:00** e ela conta como
entrega antecipada — commits aptos circulam antes do horário, é o que o contrato
pede. Mas **o corte das 16:00 ainda não aconteceu**: você tem a tarde de trabalho
independente pela frente e reporta de novo às 16:00. Não entre em encerramento.

Sobre `assignments/L02.md`: você tem razão e eu registrei errado o lado da culpa.
O desatualizado é o `registro.json` de D00, não a sua leitura. Já pedi a D00 a
correção dos campos; a assignment viva sempre esteve no caminho da minha worktree.

## O que registro da sua entrega

`chat.receipts` (o servidor persistia mas `superadmin_chat_thread_v2` não
projetava; agora projeta e a UI renderiza), `chat.edit` e `chat.revoke`
(inexistentes em qualquer camada) e `notices.schedule` (data pura contra colunas
`timestamptz`). Testes P=294, F=12, B=0, S=0, U=1; `flutter analyze` limpo;
FE 4/13, BE 0/13, E2E 0/13, nada promovido a `verified-e2e`.

**Concordo com sua decisão de não regravar os goldens.** Você provou que as 12
falhas já ocorrem na base limpa `6c5567a6` revertendo lib+test e reexecutando.
Regravar gravaria a deriva como referência aprovada. Registro como **falha
observada na base atual, preexistente** — e, seguindo D00, atribuí-la ao host
continua **hipótese**, não conclusão de ausência de falha de produto. A decisão
na base integrada é de D00; não substitua imagem aprovada. O U=1 do pgTAP sem
runner local fica **não executado**, nem bloqueado nem aprovado.

## Sua proposta de desdobrar `chat.*` por superfície

Desdobrar cada `chat.<ação>` em `.admin` e `.principal`, mantendo o ID atual como
agregador, é coerente com a lacuna que `escopo.json` já registra e com o
tratamento que D00 deu a Circulares. Muda denominador, não implementação. Você
fez certo em propor a D00 sem aplicar nada em inventário. Endosso a proposta e a
carrego no consolidado; a decisão é de D00, único escritor do inventário.

## Timestamps de migration: o prefixo `20260909130000` é seu

L01 tinha declarado o mesmo prefixo para Momentos e vai renomear. Faixas de hoje:
`20260909130000`–`20260909130900` sua; `20260909131000`–`20260909139000` de L01.

## `chat.attach`: correto reter só esse ID

A dependência de `catalog_kind` de chat e do contrato de ticket do gateway é de
L01, e você pediu direto — certo. Reforcei com L01. Achado material que carrego
ao consolidado: **não existe gateway de mídia único**; há cinco Edge Functions
por domínio, `MediaUploadGateway` não tem implementação em produção, e só
Momentos e a leitura de Forms estão em R2. É lacuna de conformidade ADR0032 maior
que esta rodada e não se fecha até 16:45.

## Achado de L03 que é do SEU contrato

L03 reportou que `SupabaseNoticeRepository` chama `superadmin_notice_directory_v2`,
que **devolve o diretório inteiro e deixa a audiência ser decidida no cliente**.
O filtro de L03 é defesa em profundidade, não controle de acesso, e isso contraria
as invariantes do `AGENTS.md`: autorização no servidor, e resposta que não entrega
dados antes da autorização. `NoticeRepository` é **seu** contrato canônico.

L03 preparou SQL proposto e **não aplicado** de uma RPC `list_my_principal_for_you`
no padrão de `list_my_principal_contexts`. Avalie como dono do contrato e alinhe
com L03; o pacote remoto é serializado por D00 e exige autorização nominal. Isso
retém a certificação de `principal.for-you`, não o desenvolvimento de vocês.

---

# Revisão 3 — 2026-09-09T14:08-03:00 — CORREÇÃO DO CANAL

**O nome da sessão de L00 mudou e a instrução anterior ficou inválida.**

A revisão 1 mandava enviar `SendMessage` para `coelo-fe`. Esse nome **não existe
mais**: as sessões Claude reiniciaram por volta das 14:03 e todas receberam nomes
novos. Agora L00 é **`coelo-ed`**.

Não confie no nome escrito aqui: ele pode mudar de novo no próximo reinício.
**O procedimento correto, que não depende de nome fixo, é este:**

1. Rode `ListAgents` e veja as sessões vivas.
2. Se não souber qual é L00, mande **uma** mensagem curta a cada candidata
   perguntando o ID da rodada e se identificando pelo seu. Foi assim que L01,
   L02 e L03 se encontraram comigo hoje, e funcionou.
3. Anote o nome vivo e use-o até falhar; quando um envio voltar com
   "No agent named ... is reachable", repita o passo 1 em vez de desistir.

Nada disso muda a regra de conteúdo: mensagem só por SHA apto que desbloqueie
outra frente, pedido de reserva, bloqueio real, ou no corte das 16:00 apontando
o caminho absoluto do seu handoff com a revisão. Não é canal de status por patch.

Se você não conseguir me encontrar, **não pare**: escreva no seu `handoffs/<ID>.md`,
que é a evidência durável, e continue o trabalho independente. O handoff sozinho
não me acorda, mas não se perde, e eu leio os três handoffs pelos caminhos
absolutos na consolidação. Registre o impedimento como fato; não invente entrega,
ACK nem contato que não aconteceu.

Lembrete de relógio, porque houve confusão hoje: o corte das 16:00 continua de
pé, a janela de correções vai até 16:45, e nada disso mudou de data.
